---
phase: 05-image-pipeline-seo-fixes
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - src/assets/scss/style.scss
  - src/templates/blog-post.js
autonomous: false
requirements:
  - IMAG-01
  - IMAG-02
  - IMAG-03
  - SEOS-01
  - SEOS-02
  - SEOS-03
user_setup: []

must_haves:
  truths:
    - "Full-site verification passes: yarn install && yarn build && yarn test exit 0, and every phase-5 success-criterion grep holds on the fresh public/ output (no gatsby-image-outer-wrapper, no [object Object], lang=it + zero hreflang, zero Stackrole/English starter strings, privacy page clean)"
    - "IF the held-out visual check shows a blog-post 50vh banner delta (landscape gap or portrait bottom clip under constrained flow), style.scss gains an ADDITIVE absolute-fill rule (position absolute + inset 0 + object-fit cover on the banner img) that restores legacy Img rendering WITHOUT touching any existing selector (conditional — gated by the human checkpoint)"
    - "The conditional CSS change (if applied) is additive only — zero existing selectors rewritten"
  artifacts:
    - "src/assets/scss/style.scss — optional additive absolute-fill block under the .blog-post .featured-image selector (only if the visual check shows a delta)"
    - "src/templates/blog-post.js — unchanged unless the CSS approach requires a component tweak (expected: no change)"
    - "05-04-SUMMARY.md with the recorded outcome of the held-out visual checks (banner landscape/portrait, /blog cards, /privacy)"
  key_links:
    - "yarn build → public/ → grep verification suite (every success criterion is a rendered-source check)"
    - "visual check on a landscape and a portrait post → conditional style.scss fix → rebuild → re-verify"
  prohibitions:
    - "style.scss must NOT be modified preemptively — the absolute-fill rule lands only on a human-reported delta"
    - "Existing selectors (.featured-image, .post-card .featured-image, .home-banner .featured-image) must NOT be rewritten — additive rules only"
    - "gatsby-remark-images options must NOT be further changed (only tracedSVG removal in 05-01)"
---

<objective>
Verification wave: run the full phase-gate loop (install, build, full jest suite), execute the RESEARCH Code Example 7 grep suite against the rendered output, then run the held-out visual checks (blog-post 50vh banner on landscape + portrait posts, blog cards, privacy page) with a blocking human checkpoint, and apply the conditional absolute-fill SCSS fix if — and only if — the banner check shows a real delta.

Purpose: this phase's success criteria are rendered-source checks, not unit tests. The automated grep suite proves the machine-verifiable criteria; the human visual gate proves the one genuinely risky parity question (IMAG-01 banner fill under constrained layout — RESEARCH Pitfall 3) plus the privacy page. The CSS fix is held out so it lands only on evidence, and the plan ends with a full-suite green and recorded visual outcomes.

Output: verified built output with zero legacy-image/English/[object Object] markers, a recorded visual-check outcome, a conditional additive SCSS fix only if the check demands it, and the plan SUMMARY.

Environment notes (apply to every verify step in this plan): node is NOT on PATH — prefix commands with `source ~/.nvm/nvm.sh && nvm use 24` (nvm has v20.20.2, v24.18.0, v24.19.0; the project enforces Node 24 via engines + engine-strict + scripts/check-node-version.js; .nvmrc is 24). /tmp is a tmpfs that fills with JIT artifacts — set TMPDIR=/home/simos/tmp for yarn/build commands. yarn 1.22 only, never npm. To inspect the built site: `TMPDIR=/home/simos/tmp yarn serve` (gatsby serve on port 9000) or open public/ files directly.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-image-pipeline-seo-fixes/05-CONTEXT.md
@.planning/phases/05-image-pipeline-seo-fixes/05-RESEARCH.md
@.planning/phases/05-image-pipeline-seo-fixes/05-PATTERNS.md
@.planning/phases/05-image-pipeline-seo-fixes/05-UI-SPEC.md
@.planning/phases/05-image-pipeline-seo-fixes/05-VALIDATION.md
@src/assets/scss/style.scss
@src/templates/blog-post.js
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Phase-gate verification: install, build, jest suite, and the rendered-source grep suite (all requirements)</name>
  <files>none — verification only; runs over the built public/ directory from waves 1-3</files>
  <read_first>
    - .planning/phases/05-image-pipeline-seo-fixes/05-RESEARCH.md Code Example 7 (the full built-HTML verification command suite — every success criterion maps to a grep)
    - .planning/phases/05-image-pipeline-seo-fixes/05-VALIDATION.md (Per-Task Verification Map — the expected values each grep asserts)
    - .planning/phases/05-image-pipeline-seo-fixes/05-UI-SPEC.md (the contract's Visual Delta Register — the expected after-state)
    - No source files — this task only reads and greps the rendered output.
  </read_first>
  <action>
    <!-- planner-discipline-allow: object Object -->
    <!-- planner-discipline-allow: chiocciola -->
    Run the full phase gate with the environment prefixes:

    1. `source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn install && TMPDIR=/home/simos/tmp yarn build && TMPDIR=/home/simos/tmp yarn test` — the whole loop must exit 0.

    2. Run the RESEARCH Code Example 7 grep suite and record every line of output:
    - `grep -rl "gatsby-image-outer-wrapper" public/ | wc -l` → 0 (IMAG-01 legacy classes gone)
    - `grep -rl "gatsby-image-wrapper" public/ | head -3` → non-empty (plugin wrappers present)
    - `grep -o 'property="og:image" content="[^"]*"' public/*/index.html | grep -c "object Object"` → 0 (IMAG-03)
    - `grep -o 'property="og:image" content="[^"]*"' public/*/index.html | head -3` → starts with https://laryart.it/static/ (IMAG-03 positive proof)
    - `grep -o '<html lang="[^"]*"' public/index.html` → `<html lang="it">` (SEOS-01)
    - `grep -rl 'rel="alternate"' public/ | wc -l` → 0 (SEOS-01 hrefl gone)
    - `grep -rl "Stackrole base blog page" public/ | wc -l` → 0 (SEOS-02 English meta gone)
    - `grep -o '<title[^>]*>[^<]*</title>' public/blog/index.html` → `Blog` and `public/blog/2/index.html` → `Blog — Pagina 2` (SEOS-02 pagination-aware)
    - `grep -c '</p>' public/privacy/index.html` → balanced tag count (SEOS-03; compare with the count of '<p')
    - `grep -c 'chiocciola' public/privacy/index.html` → 0 (SEOS-03 email clean)
    - `grep -c "object Object" public/*/index.html` → 0 site-wide (IMAG-03 belt)

    3. Record all results in the plan summary with exact command output. If ANY assertion fails, fix the root cause in the respective source file (the wave-1/2/3 commits are the revert points), re-run the loop, and record the corrected output.

    Commit atomically if any fix was needed: `git commit -m "fix(05-04): phase-gate verification fixes (IMAG-01, IMAG-03, SEOS-01, SEOS-02, SEOS-03)"` staging exactly the fixed files. If everything was green on the first run, make NO commit.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn install && TMPDIR=/home/simos/tmp yarn build && TMPDIR=/home/simos/tmp yarn test && test "$(grep -rl 'gatsby-image-outer-wrapper' public/ | wc -l)" -eq 0 && test "$(grep -o 'property="og:image" content="[^"]*"' public/*/index.html | grep -c 'object Object')" -eq 0 && grep -q '<html lang="it"' public/index.html && test "$(grep -rl 'rel="alternate"' public/ | wc -l)" -eq 0 && test "$(grep -rl 'Stackrole base blog page' public/ | wc -l)" -eq 0 && test "$(grep -c 'chiocciola' public/privacy/index.html)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn install && yarn build && yarn test` exits 0
    - `grep -rl "gatsby-image-outer-wrapper" public/ | wc -l` → 0
    - `grep -rl "gatsby-image-wrapper" public/ | head -3` → non-empty output
    - `grep -o 'property="og:image" content="[^"]*"' public/*/index.html | grep -c "object Object"` → 0
    - `grep -o '<html lang="[^"]*"' public/index.html` → `<html lang="it">`
    - `grep -rl 'rel="alternate"' public/ | wc -l` → 0
    - `grep -rl "Stackrole base blog page" public/ | wc -l` → 0
    - `grep -o '<title[^>]*>[^<]*</title>' public/blog/2/index.html` → `Blog — Pagina 2`
    - `grep -c 'chiocciola' public/privacy/index.html` → 0
    - Every command result is recorded in the plan SUMMARY — the SUMMARY contains the full command output (or an explicit "all green" note)
  </acceptance_criteria>
  <done>All automated phase-gate checks are green and their output recorded; the full install, build, jest loop exits 0.</done>
  <reversibility rating="reversible">verification only — no code changed on a green run; fixes commit atomically if a failure occurred.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Held-out visual parity check: blog-post banner (landscape/portrait), blog cards, privacy page (UI-SPEC backstops E1/E3/E6)</name>
  <what-built>The image migration + SEO wave output (waves 1-3). The site is built in public/ — this checkpoint holds the held-out visual parity checks (UI-SPEC backstops E1/E3/E6) that greps cannot prove: the blog-post 50vh banner on both a landscape and a portrait post, the blog cards' 540x360 center crop, the index hero, and the rebuilt privacy page.</what-built>
  <how-to-verify>
    1. Serve the built site: `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn serve` (gatsby serve, default http://localhost:9000). Alternatively open the public/ files directly in a browser.
    2. Open a blog post with a LANDSCAPE featured image (check src/content/posts/ frontmatter featuredImage files to find one with a wide image) — the image must FILL the 50vh banner exactly as before: no white gap under the image, no bottom clipping. Then open a post with a PORTRAIT image — the photo's top must be fully visible with no bottom clipping.
    3. Open / — the hero renders at natural ratio in the grid column; home cards look identical to the legacy crop (subject centered, not shifted by saliency).
    4. Open /blog — cards crop centered at 540x360 proportions; thumbnails look as before.
    5. Open /privacy — clean page: no stray tags rendering as text, no literal leading-space heading text, all 8 links clickable, email shows as s.foschi@protonmail.com.
    6. For the banner check: if (and only if) you see a gap or a clipped bottom on landscape or portrait posts, reply with a description of what you see. If everything looks as before, reply "approved".
  </how-to-verify>
  <resume-signal>Type "approved" or describe the banner delta observed (e.g. "landscape post leaves a white gap under the image")</resume-signal>
</task>

<task type="auto">
  <name>Conditional: apply absolute-fill banner CSS only if the visual check reported a delta (IMAG-01 parity)</name>
  <files>src/assets/scss/style.scss, src/templates/blog-post.js</files>
  <read_first>
    - src/assets/scss/style.scss lines 326-339 (the .blog-post block with .featured-image { min-height: 50vh; border-radius: 12px; margin: 0 auto })
    - src/templates/blog-post.js (the GatsbyImage block this rule styles — read-only reference; no change expected)
    - .planning/phases/05-image-pipeline-seo-fixes/05-RESEARCH.md Pattern 3 (absolute-fill conversion — the ONLY sanctioned CSS approach) and Pitfall 3
    - .planning/phases/05-image-pipeline-seo-fixes/05-UI-SPEC.md Image Surface Contract parity rule 4 (class hooks on the wrapper; do NOT rewrite SCSS selectors) and backstop E1
  </read_first>
  <action>
    ONLY if the human checkpoint reported a visible banner delta (gap under landscape images or bottom clipping on portrait images):

    Append an additive rule inside the existing `.blog-post` block of style.scss — do not touch the `.featured-image { min-height: 50vh ... }` rule or any other selector:

    .blog-post .featured-image img { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; object-position: 50% 50%; }

    This restores the legacy absolute-fill behavior (the image fills the wrapper; the wrapper's min-height 50vh governs the box) — the plugin's .gatsby-image-wrapper already provides position: relative, so no wrapper change is needed.

    If the landscape gap persists with the fill applied (the img is absolutely positioned but the wrapper height collapses), verify the wrapper rule `.blog-post .featured-image { min-height: 50vh }` is still present and untouched — do NOT rewrite it.

    Then rebuild and re-run the phase-gate greps to confirm nothing regressed.

    IF the checkpoint reported no delta: make NO change, and record in the SUMMARY that the absolute-fill was held out (no delta observed).

    Commit atomically only if the CSS fix was applied: `git commit -m "fix(05-04): absolute-fill blog-post banner for constrained parity (IMAG-01)"` staging src/assets/scss/style.scss (and blog-post.js ONLY if it needed a change — expected no).
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && test "$(grep -rl 'gatsby-image-outer-wrapper' public/ | wc -l)" -eq 0 && test "$(grep -o 'property="og:image" content="[^"]*"' public/*/index.html | grep -c 'object Object')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - If the human checkpoint reported a delta: `grep -q 'inset: 0' src/assets/scss/style.scss` succeeds AND `grep -q 'position: absolute' src/assets/scss/style.scss` succeeds AND the pre-existing `min-height: 50vh` rule still exists unchanged
    - If the human checkpoint reported no delta: `! grep -q 'inset: 0' src/assets/scss/style.scss` succeeds (nothing was added)
    - `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn build` exits 0 in either case
    - `grep -c 'object Object' public/*/index.html` → 0 (no regression from the rebuild)
    - the SUMMARY records which branch was taken
  </acceptance_criteria>
  <done>The banner renders at parity (either because the fill rule was applied on evidence, or the check showed no delta); the build re-passes; the summary documents the decision.</done>
  <reversibility rating="reversible">additive CSS rule; deleting it restores the natural-ratio behavior instantly.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built output → verification | the rendered HTML crosses here — every success criterion is checked on this boundary |
| visual check → CSS fix | the human observation crosses into a decision here; an over-applied fix would rewrite SCSS |
| SCSS source → dart-sass compile | the conditional fill rule crosses here; a syntax error breaks the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-17 | Tampering | rendered output silently regresses between waves (a wave-2/3 commit broke a marker) | high | mitigate | this plan runs the full grep suite AFTER all waves; any failure is fixed at its root before the SUMMARY is written |
| T-05-18 | DoS | CSS fill rule applied preemptively without the human gate — unexpected layout change | medium | mitigate | blocking checkpoint:human-verify gates the rule; acceptance greps assert the rule exists only when the delta was reported |
| T-05-19 | Info disclosure | visual check skipped/auto-advanced — banner delta ships unseen | medium | mitigate | the plan's checkpoint is blocking; auto-advance does not apply to human-verify gates |
| T-05-SC | Tampering | no package installs in this plan | n/a | n/a | zero install commands |
</threat_model>

<verification>
- Full loop: `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn install && yarn build && yarn test` exits 0
- `grep -rl "gatsby-image-outer-wrapper" public/ | wc -l` → 0
- `grep -o 'property="og:image" content="[^"]*"' public/*/index.html | grep -c "object Object"` → 0
- `grep -o '<html lang="[^"]*"' public/index.html` → `<html lang="it">`; `grep -rl 'rel="alternate"' public/ | wc -l` → 0
- `grep -rl "Stackrole base blog page" public/ | wc -l` → 0; `grep -o '<title[^>]*>[^<]*</title>' public/blog/2/index.html` → `Blog — Pagina 2`
- `grep -c 'chiocciola' public/privacy/index.html` → 0
- Human checkpoint: banner parity visually confirmed or a recorded delta + conditional fix
</verification>

<success_criteria>
- All automated checks exit 0 and their outputs are recorded
- The human visual gate passes (or the delta is fixed with the additive rule)
- The privacy page renders clean in a browser
- The site is ready for the phase-6 comparison — no marker regressions
</success_criteria>

## Artifacts this phase produces

- `src/assets/scss/style.scss` — conditional additive absolute-fill rule ONLY when the visual delta was observed (expected: no change on a clean check)
- `src/templates/blog-post.js` — changed only if the CSS approach needs a component tweak (expected: no change)
- The verification record: grep outputs + the human checkpoint outcome recorded in the SUMMARY
- `.planning/phases/05-image-pipeline-seo-fixes/05-04-SUMMARY.md` (created at plan completion)

<output>
Create `.planning/phases/05-image-pipeline-seo-fixes/05-04-SUMMARY.md` when done
</output>
</content>
