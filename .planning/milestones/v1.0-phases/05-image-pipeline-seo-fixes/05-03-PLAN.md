---
phase: 05-image-pipeline-seo-fixes
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/components/seo.js
  - src/templates/blog-list.js
  - src/templates/blog-list.test.js
  - src/pages/404.js
  - src/pages/thanks.js
  - src/content/pages/privacy.md
autonomous: true
requirements:
  - IMAG-03
  - SEOS-01
  - SEOS-02
  - SEOS-03
user_setup: []

must_haves:
  truths:
    - "rendered HTML carries <html lang=\"it\"> and zero <link rel=\"alternate\"> hreflang tags — the three redundant alternates (it-it/it/x-default) are deleted from seo.js (SEOS-01, D-13/D-14)"
    - "Seo's image prop is string-only with a belt-and-suspenders guard: non-string or empty image values are ignored and seo.image falls back to defaultImage (/assets/heart.png from site.json) — the [object Object] interpolation cannot recur on any page (IMAG-03 belt, D-11)"
    - "blog-list page 1 renders <title>Blog</title>; pages >= 2 render <title>Blog — Pagina N</title> with the Italian meta description \"I post del blog di LaryArt: decoupage, oggetti d'arte e creazioni fatti a mano.\" (+ \" Pagina N\" on later pages) — no \"Stackrole base blog page\" and no \"Blog — Page X of Y\" anywhere in the rendered source (SEOS-02, D-15)"
    - "pagination links render \"Precedente\" (rel=prev) and \"Successivo\" (rel=next); the six blog-list.test.js assertions (lines 53, 55, 68, 73, 85, 90) co-change to the Italian labels in the same commit and the dead jest.mock(\"gatsby-image\") block (lines 7-10) is deleted (SEOS-02, D-16; test-update discipline)"
    - "404 and thanks pages render <title>Pagina non trovata</title> and <title>Grazie</title> respectively (SEOS-02, D-17); visible h1 and body copy unchanged"
    - "privacy page frontmatter title is \"Privacy e Cookie\" and the markdown body is clean markdown: raw <a>/<p> HTML blocks converted to markdown links, all eight links preserved (Firefox, Chrome, Internet Explorer, Safari, Opera, Google, cookiechoices, Garante), the leading-space \" ####\" heading fixed to a valid H4, email deobfuscated to s.foschi@protonmail.com — rendered HTML has no stray </p> tags, no literal \" ####\" text, no \"chiocciola\" (SEOS-03, D-19 + SEOS-02 title leak)"
    - "When a single blog page exists, pagination renders no Previous/Next links (existing isFirst/isLast guards) and the title stays \"Blog\" — ordering and page count derive from the existing pagination math, untouched"
    - { statement: "A malformed pagination pageContext (bad currentPage/numPages math in gatsby-node.js) is outside this phase changes — the existing pagination-math tests in blog-list.test.js keep covering it; held-out check: pagination renders correctly on /blog and /blog/2", verification: backstop }
    - { statement: "The rebuilt privacy markdown renders as valid HTML — a held-out visual check on /privacy after build catches dangling HTML, heading-as-text, and orphan links", verification: backstop }
  artifacts:
    - "src/components/seo.js — lang=\"it\", hreflang tags deleted, imageUrl guard added"
    - "src/templates/blog-list.js — Italian meta + Precedente/Successivo labels"
    - "src/templates/blog-list.test.js — Italian assertions, dead gatsby-image mock removed"
    - "src/pages/404.js — <Seo title=\"Pagina non trovata\" />"
    - "src/pages/thanks.js — <Seo title=\"Grazie\" />"
    - "src/content/pages/privacy.md — title + markdown rebuild"
  key_links:
    - "seo.js → react-helmet → html lang + meta/OG/twitter tags in every public/*/index.html"
    - "blog-list.js → Seo + Pagination labels → /blog and /blog/2/... rendered source"
    - "privacy.md → gatsby-transformer-remark → /privacy/index.html"
  prohibitions:
    - "No English starter strings in rendered output: \"Stackrole base blog page\", \"Blog — Page\", \"Page not found\", \"Thank you\", \"Previous\", \"Next\" must not appear in the built HTML of their pages"
    - "hreflang alternates must NOT be re-added — no <link rel=\"alternate\"> remains in seo.js or any rendered page"
    - "lang must NOT be \"en-US\" anywhere in seo.js output"
    - "privacy.md must NOT retain raw <a> or <p> HTML blocks, must NOT retain [chiocciola], must NOT retain the leading-space \" ####\" heading"
    - "The Seo image prop contract must NOT regress to a non-string (object/undefined) — the guard covers it"
---

<objective>
SEO wave: harden Seo (string-only image guard + lang="it" + hreflang removal), replace all hardcoded English starter meta/UI copy with natural Italian (blog-list meta + pagination labels + 404/thanks titles), co-change the pagination tests, and rebuild the malformed privacy page as clean markdown.

Purpose: complete IMAG-03's belt-and-suspenders (the string guard in seo.js — the getSrc fix already landed in 05-01), deliver SEOS-01 (lang + hreflang) and SEOS-02 (zero English in rendered meta/UI) across the whole site, and SEOS-03 (valid privacy page). This is the SEO/cleanup wave; it runs after the image wave because both plans touch src/templates/blog-list.js (query swap there, copy swap here) — the execute-phase orchestrator serializes overlapping files automatically.

Output: Italian meta and labels rendered site-wide, guard in place, privacy page clean; yarn build + the updated jest suite green; one commit per concern.

Environment notes (apply to every verify step in this plan): node is NOT on PATH — prefix commands with `source ~/.nvm/nvm.sh && nvm use 24` (nvm has v20.20.2, v24.18.0, v24.19.0; the project enforces Node 24 via engines + engine-strict + scripts/check-node-version.js; .nvmrc is 24). /tmp is a tmpfs that fills with JIT artifacts — set TMPDIR=/home/simos/tmp for yarn/build commands. yarn 1.22 only, never npm.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-image-pipeline-seo-fixes/05-CONTEXT.md
@.planning/phases/05-image-pipeline-seo-fixes/05-RESEARCH.md
@.planning/phases/05-image-pipeline-seo-fixes/05-PATTERNS.md
@.planning/phases/05-image-pipeline-seo-fixes/05-UI-SPEC.md
@.planning/phases/05-image-pipeline-seo-fixes/05-VALIDATION.md
@src/components/seo.js
@src/templates/blog-list.js
@src/templates/blog-list.test.js
@src/pages/404.js
@src/pages/thanks.js
@src/content/pages/privacy.md
@src/util/site.json
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Harden seo.js: lang=&quot;it&quot;, remove hreflang, imageUrl string guard (SEOS-01, IMAG-03 D-11/D-13/D-14)</name>
  <files>src/components/seo.js</files>
  <read_first>
    - src/components/seo.js (the whole file — 98 lines: destructure block lines 11-18, seo object lines 20-25, Helmet block lines 28-65 with the hreflang tags at 30-32 and the lang at line 29, propTypes lines 71-83)
    - src/util/site.json (meta.image: /assets/heart.png — the defaultImage fallback)
    - .planning/phases/05-image-pipeline-seo-fixes/05-PATTERNS.md §seo.js (string-guard + interpolation fix, lang + hreflang edits)
    - .planning/phases/05-image-pipeline-seo-fixes/05-RESEARCH.md Code Example 4 (target seo.js state) and Pitfall 4 (getSrc string contract)
    - .planning/phases/05-image-pipeline-seo-fixes/05-CONTEXT.md D-09 (URL-string only contract), D-11 (guard), D-13 (lang), D-14 (hreflang removal), D-18 (titleTemplate stays)
  </read_first>
  <action>
    Edit src/components/seo.js with three surgical changes:

    1. lang fix (D-13, line 29): change `<html lang="en-US" />` to `<html lang="it" />`.

    2. hreflang removal (D-14, lines 30-32): DELETE all three `<link rel="alternate" ...>` lines (hrefLang="it-it", hrefLang="it", hrefLang="x-default"). Zero hreflang alternates remain anywhere in the file.

    3. imageUrl guard (D-11, lines 20-25): before the `const seo = {` object, add `const imageUrl = typeof image === "string" && image.length > 0 ? image : null` and change line 23 from `` image: `${siteUrl}${image || defaultImage}`, `` to `` image: `${siteUrl}${imageUrl || defaultImage}`, `` — a non-string or empty image prop now falls back to defaultImage (/assets/heart.png from site.json) instead of being stringified into "[object Object]" (the belt behind 05-01's getSrc call-site fix).

    Do NOT touch: propTypes (image: PropTypes.string already declares the string-only contract), defaultProps, titleTemplate usage, the google-site-verification meta, og/twitter blocks, the query — all verbatim.

    Prettier conventions (no semicolons, double quotes, 2-space indent) + run `yarn format` on the file.

    Commit atomically: `git commit -m "feat(05-03): set lang=it, drop hreflang alternates, harden Seo image guard (SEOS-01, IMAG-03, D-11)"` staging exactly src/components/seo.js.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && grep -o '<html lang="[^"]*"' public/index.html && ! grep -q 'rel="alternate"' public/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '<html lang="it" />' src/components/seo.js` succeeds
    - `! grep -q 'hrefLang' src/components/seo.js` succeeds (all three alternates deleted)
    - `grep -q 'const imageUrl = typeof image === "string"' src/components/seo.js` succeeds (guard present)
    - `grep -q 'image: `${siteUrl}${imageUrl || defaultImage}`,' src/components/seo.js` succeeds (guard wired into the interpolation)
    - `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn build` exits 0
    - `grep -o '<html lang="[^"]*"' public/index.html` → `<html lang="it">`
    - `grep -c 'rel="alternate"' public/index.html` → 0
    - `git log -1 --format=%s` contains `05-03`
  </acceptance_criteria>
  <done>Every page renders `<html lang="it">`, no hreflang alternate remains in source or built output, and a non-string image prop falls back to defaultImage — the og:image leak cannot recur.</done>
  <reversibility rating="reversible">git revert restores the three edits; a self-contained component with no external state.</reversibility>
</task>

<task type="auto">
  <name>Italian copy: blog-list meta + pagination labels + test co-change (SEOS-02, D-15/D-16)</name>
  <files>src/templates/blog-list.js, src/templates/blog-list.test.js</files>
  <read_first>
    - src/templates/blog-list.js (the Pagination labels at lines 44-49 and 62-71 — the literal "Previous" / "Next" inside the Link children; the Seo block lines 101-106 — the English title/description to replace; the currentPage/numPages destructure at line 78)
    - src/templates/blog-list.test.js (the dead jest.mock("gatsby-image") lines 7-10 with the stale comment at line 12, the six assertions referencing "Previous"/"Next" at lines 53, 55, 68, 73, 85, 90, the comments at 48/60/80 mentioning the old labels)
    - .planning/phases/05-image-pipeline-seo-fixes/05-UI-SPEC.md Copywriting Contract (exact strings: "Blog" / `Blog — Pagina ${currentPage}` / "I post del blog di LaryArt: decoupage, oggetti d'arte e creazioni fatti a mano." / "Precedente" / "Successivo")
    - .planning/phases/05-image-pipeline-seo-fixes/05-PATTERNS.md §blog-list.js (target Seo block, label swap with rel attrs preserved) and §blog-list.test.js (the six-line assertion table with href/class assertions preserved)
    - .planning/phases/05-image-pipeline-seo-fixes/05-CONTEXT.md D-15 (Italian copy; wording is agent discretion, must be natural Italian, no English), D-16 (labels)
  </read_first>
  <action>
    Edit src/templates/blog-list.js and its test in ONE commit (test-update discipline — the behavior change and the asserting test change land together):

    blog-list.js meta (lines 101-106): replace the Seo block with the UI-SPEC Copywriting Contract strings, keeping the currentPage/numPages pagination-awareness:
    - title: `{currentPage === 1 ? "Blog" : \`Blog — Pagina ${currentPage}\`}`
    - description: `` `I post del blog di LaryArt: decoupage, oggetti d'arte e creazioni fatti a mano.` + (currentPage > 1 ? ` Pagina ${currentPage}` : "") `` (both template literals inside the JSX expression)
    - no "Page"/"of" — every word Italian.

    Pagination labels (lines 48 and 65): "Previous" → "Precedente", "Next" → "Successivo". The surrounding markup stays byte-identical: `rel="prev"`/`rel="next"`, the icon spans, the `{" "}` spacing.

    blog-list.test.js — same commit:
    - Delete the jest.mock("gatsby-image") block (lines 7-10) and update the stale comment at line 12.
    - Swap the six assertions: line 53 queryByText("Previous") → queryByText("Precedente") (still toBeNull), line 55 getByText("Next") → getByText("Successivo"), line 68 getByText("Previous") → getByText("Precedente"), line 73 getByText("Next") → getByText("Successivo"), line 85 getByText("Previous") → getByText("Precedente"), line 90 queryByText("Next") → queryByText("Successivo") (still toBeNull). Keep the href/class assertions (/blog/2, /blog/, /blog/3, is-active) untouched. Update the inline comments.
    - Do NOT add a gatsby-plugin-image mock — post-card is mocked entirely at lines 13-15, so no gatsby-plugin-image module reaches Jest.

    Prettier conventions + `yarn format` on both files.

    Commit atomically: `git commit -m "feat(05-03): Italian blog-list meta and pagination labels with test co-change (SEOS-02, D-15, D-16)"` staging exactly src/templates/blog-list.js and src/templates/blog-list.test.js.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && yarn test src/templates/blog-list.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Precedente' src/templates/blog-list.js && grep -q 'Successivo' src/templates/blog-list.js` succeeds
    - `! grep -q '"Previous"\|"Next"' src/templates/blog-list.js` succeeds (English labels gone)
    - `grep -q 'Blog — Pagina \${currentPage}' src/templates/blog-list.js` succeeds
    - `grep -q "I post del blog di LaryArt: decoupage, oggetti d'arte e creazioni fatti a mano." src/templates/blog-list.js` succeeds
    - `! grep -q 'Stackrole base blog' src/templates/blog-list.js` succeeds
    - `! grep -q 'Page .* of' src/templates/blog-list.js` succeeds (no "Page X of Y" template remains)
    - `! grep -q 'gatsby-image' src/templates/blog-list.test.js` succeeds (dead mock + stale comment removed)
    - `grep -q 'queryByText("Precedente")' src/templates/blog-list.test.js && grep -q 'getByText("Successivo")' src/templates/blog-list.test.js` succeeds
    - `! grep -q '"Previous"\|"Next"' src/templates/blog-list.test.js` succeeds (all six assertions re-labeled)
    - `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn test src/templates/blog-list.test.js` exits 0
    - `git log -1 --format=%s` contains `05-03`
  </acceptance_criteria>
  <done>Blog pages render Italian title/description and Precedente/Successivo labels; the test suite asserts the new strings and the dead mock is gone; targeted tests green.</done>
  <reversibility rating="reversible">git revert restores the English copy + old assertions together — the co-change makes the revert atomic.</reversibility>
</task>

<task type="auto">
  <name>Translate 404/thanks titles (SEOS-02, D-17)</name>
  <files>src/pages/404.js, src/pages/thanks.js</files>
  <read_first>
    - src/pages/404.js (line 10: `<Seo title="Page not found" />` — the ONLY change in this file)
    - src/pages/thanks.js (line 10: `<Seo title="Thank you" />` — the ONLY change in this file)
    - .planning/phases/05-image-pipeline-seo-fixes/05-PATTERNS.md §404/thanks (the swap lines)
    - .planning/phases/05-image-pipeline-seo-fixes/05-CONTEXT.md D-17 (translate the two titles), D-12 (no image prop — none added)
  </read_first>
  <action>
    Edit the two pages, one line each:
    - 404.js line 10: `<Seo title="Page not found" />` → `<Seo title="Pagina non trovata" />`
    - thanks.js line 10: `<Seo title="Thank you" />` → `<Seo title="Grazie" />`

    No image prop added (D-12), no other markup touched — the visible h1/body copy stays verbatim.

    Commit atomically: `git commit -m "feat(05-03): Italian titles on 404 and thanks pages (SEOS-02, D-17)"` staging exactly src/pages/404.js and src/pages/thanks.js.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && grep -q 'Pagina non trovata' public/404/index.html && grep -q 'Grazie' public/thanks/index.html && ! grep -q 'Page not found\|Thank you' public/404/index.html public/thanks/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Pagina non trovata' src/pages/404.js` succeeds
    - `grep -q 'Grazie' src/pages/thanks.js` succeeds
    - `! grep -q 'Page not found\|Thank you' src/pages/404.js src/pages/thanks.js` succeeds
    - `grep -q 'Pagina non trovata' public/404/index.html` and `grep -q 'Grazie' public/thanks/index.html` succeed after `yarn build`
    - `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn build` exits 0
    - `git log -1 --format=%s` contains `05-03`
  </acceptance_criteria>
  <done>404 and thanks pages expose Italian SEO titles in the rendered source; no English title strings remain; build green.</done>
  <reversibility rating="reversible">one-line string swap; git revert restores instantly.</reversibility>
</task>

<task type="auto">
  <name>Rebuild privacy.md as clean markdown (SEOS-03, D-19; title SEOS-02)</name>
  <files>src/content/pages/privacy.md</files>
  <read_first>
    - src/content/pages/privacy.md (the whole file — 137 lines: frontmatter lines 1-5, intro prose lines 7-32, the raw-HTML <p>/<a> block lines 34-82, the malformed " ####" heading at line 83, the trailing raw <a> blocks lines 110-135, the obfuscated email at line 137)
    - src/content/pages/laryart.md (the clean markdown analog — target shape for the rebuild)
    - .planning/phases/05-image-pipeline-seo-fixes/05-PATTERNS.md §privacy.md (frontmatter title swap, keep-lines list, rebuild blocks with the preserved hrefs)
    - .planning/phases/05-image-pipeline-seo-fixes/05-UI-SPEC.md Privacy page target (frontmatter title "Privacy e Cookie", prose untouched, all links preserved, " ####" fix, email deobfuscation)
    - .planning/phases/05-image-pipeline-seo-fixes/05-CONTEXT.md D-19 (rebuild lines ~35-82 + trailing blocks; links preserved; clean markdown)
  </read_first>
  <action>
    Rebuild src/content/pages/privacy.md:

    1. Frontmatter (line 4): `title: Privacy Policy` → `title: Privacy e Cookie` (leaks into the rendered <title>, h1, and meta description — English title contradicts SEOS-02).

    2. Keep verbatim: lines 7-32 (the `# Privacy, Cookie e GDPR` H1, GA4/Garante prose, `### Opt out`, `#### Blocca i cookie di terze parti` and its paragraph) and lines 84-108 (Do Not Track prose, navigazione anonima prose, cookie deletion prose).

    3. Convert the raw-HTML <a>/<p> block (lines 34-82) to five markdown paragraphs preserving the EXACT link text + hrefs:
       - `Ulteriori informazioni sulla disabilitazione dei cookie su [Firefox, in inglese.](https://support.mozilla.org/en-US/kb/enable-and-disable-cookies-website-preferences)`
       - `Ulteriori informazioni sulla disabilitazione dei cookie su [Chrome, in inglese](https://support.google.com/chrome/answer/95647?hl=en)`
       - `Ulteriori informazioni sulla disabilitazione dei cookie su [Internet Explorer, in inglese](http://windows.microsoft.com/it-it/internet-explorer/delete-manage-cookies#ie=ie-11)`
       - `Ulteriori informazioni sulla disabilitazione dei cookie su [Safari, in inglese](http://support.apple.com/kb/HT1677?viewlocale=it_IT&locale=it_IT)`
       - `Ulteriori informazioni sulla disabilitazione dei cookie su [Opera, in inglese.](http://help.opera.com/Mac/12.10/it/cookies.html)`

    4. Fix the broken H4 (line 83): ` #### Attiva l'opzione Do Not Track` → `#### Attiva l'opzione Do Not Track` (drop the leading space).

    5. Convert the trailing raw <a> blocks (lines 110-135) to markdown links:
       - `Maggiori informazioni sulla privacy che garantisce Google all'indirizzo [https://policies.google.com/privacy](https://policies.google.com/privacy)`
       - `Altre informazioni sui cookie [https://www.cookiechoices.org/](https://www.cookiechoices.org/)`
       - `Pagina informativa sui cookie del Garante della Privacy [http://www.garanteprivacy.it/cookie](http://www.garanteprivacy.it/cookie)`

    6. Deobfuscate the email (line 137): `s.foschi [chiocciola] protonmail.com` → `s.foschi@protonmail.com`.

    Result: NO raw <a> or <p> HTML anywhere, exactly 8 preserved links, valid heading depth, one deobfuscated address. Prettier markdown formatting.

    Commit atomically: `git commit -m "feat(05-03): rebuild privacy page as clean markdown (SEOS-03, D-19)"` staging exactly src/content/pages/privacy.md.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && grep -c '</p>' public/privacy/index.html && grep -c 'chiocciola' public/privacy/index.html && grep -o '<title[^>]*>[^<]*</title>' public/privacy/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q '<a ' src/content/pages/privacy.md` succeeds (no raw HTML anchors)
    - `! grep -q '</p>\|<p>' src/content/pages/privacy.md` succeeds (no raw HTML paragraphs)
    - `grep -q '^#### Attiva' src/content/pages/privacy.md` succeeds (leading space gone)
    - `grep -q 's.foschi@protonmail.com' src/content/pages/privacy.md` succeeds (deobfuscated)
    - `! grep -q 'Privacy Policy' src/content/pages/privacy.md` succeeds (Italian title)
    - `grep -q 'support.mozilla.org/en-US/kb/enable-and-disable-cookies-website-preferences' src/content/pages/privacy.md && grep -q 'support.google.com/chrome/answer/95647?hl=en' src/content/pages/privacy.md && grep -q 'windows.microsoft.com/it-it/internet-explorer/delete-manage-cookies' src/content/pages/privacy.md && grep -q 'support.apple.com/kb/HT1677?viewlocale=it_IT&locale=it_IT' src/content/pages/privacy.md && grep -q 'help.opera.com/Mac/12.10/it/cookies.html' src/content/pages/privacy.md && grep -q 'policies.google.com/privacy' src/content/pages/privacy.md && grep -q 'www.cookiechoices.org' src/content/pages/privacy.md && grep -q 'www.garanteprivacy.it/cookie' src/content/pages/privacy.md` succeeds (all 8 links preserved)
    - `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn build` exits 0
    - `grep -c 'chiocciola' public/privacy/index.html` → 0 (obfuscation gone from rendered output)
    - `grep -o '<title[^>]*>[^<]*</title>' public/privacy/index.html` contains `Privacy e Cookie`
    - `git log -1 --format=%s` contains `05-03`
  </acceptance_criteria>
  <done>The privacy page is clean markdown (no raw HTML), all 8 links preserved, H4 fixed, email deobfuscated, Italian title rendered, and the build + rendered-source checks pass.</done>
  <reversibility rating="reversible">git revert restores the malformed markdown; content file only, no code.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seo component → react-helmet → every page | lang/hreflang/image-tags cross here; a malformed image string would leak into all pages |
| blog-list template → rendered /blog, /blog/2 | meta + pagination strings cross here; English leaks visible in page source |
| markdown → gatsby-transformer-remark → /privacy | raw HTML blocks cross here; unbalanced tags render as broken fragments |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11 | Tampering | seo.js — non-string image regresses to [object Object] | high | mitigate | typeof-string guard in the same commit; built-html grep asserts zero [object Object] site-wide |
| T-05-12 | Tampering | seo.js — lang/hreflang drift (en-US or alternates re-added) | medium | mitigate | acceptance greps assert `<html lang="it" />` and zero hrefLang in source + rendered output |
| T-05-13 | Spoofing | meta description injection — English starter copy leaks into rendered meta | medium | mitigate | acceptance greps assert the exact Italian strings and `! grep 'Stackrole base blog'` |
| T-05-14 | Tampering | test/source drift — assertions updated without the source change (or vice versa) | medium | mitigate | both files edited in ONE commit; targeted jest run gates it |
| T-05-15 | Tampering | privacy raw HTML leaves a stray tag → malformed page | medium | mitigate | acceptance greps assert zero `<a ` / `</p>` in the markdown; rendered-output greps check 'chiocciola' + title |
| T-05-16 | Info disclosure | deobfuscated email in rendered source (harvesting surface) | low | accept | deliberate tradeoff (contactability is the site's core value); recorded in RESEARCH security domain |
| T-05-SC | Tampering | supply chain — no new packages in this plan | — | n/a | zero package changes; no install commands |
</threat_model>

<verification>
- `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn build && yarn test src/templates/blog-list.test.js && yarn test` exits 0
- `grep -o '<html lang="[^"]*"' public/index.html` → `<html lang="it">`; `grep -c 'rel="alternate"' public/index.html` → 0 (SEOS-01)
- `grep -rl "Stackrole base blog page" public/ | wc -l` → 0; `grep -rl "Blog — Page" public/ | wc -l` → 0 (SEOS-02)
- `grep -o '<title[^>]*>[^<]*</title>' public/blog/index.html` → `Blog`; `public/blog/2/index.html` → `Blog — Pagina 2`
- `grep -c '</p>' public/privacy/index.html` balanced; `grep -c 'chiocciola' public/privacy/index.html` → 0 (SEOS-03)
- `grep -c "object Object" public/*/index.html` → 0 (IMAG-03 belt verified everywhere)
</verification>

<success_criteria>
- SEOS-01: every page renders `<html lang="it">` with zero hreflang alternates
- SEOS-02: Italian blog-list meta, Precedente/Successivo labels, Pagina non trovata/Grazie titles, no English starter copy in any rendered source
- SEOS-03: privacy page renders valid content from clean markdown
- IMAG-03 belt: Seo string guard prevents any future [object Object] across all pages
</success_criteria>

## Artifacts this phase produces

- `src/components/seo.js` — `lang="it"` on the `<html>` element, three hreflang `<link rel="alternate">` entries deleted, `imageUrl` typeof-string guard feeding `seo.image`
- `src/templates/blog-list.js` — Italian title (Blog / Blog — Pagina N) + Italian meta description, labels Precedente/Successivo
- `src/templates/blog-list.test.js` — "Precedente"/"Successivo" assertions (6 sites), dead `jest.mock("gatsby-image")` removed, stale comments updated
- `src/pages/404.js` — `<Seo title="Pagina non trovata" />`
- `src/pages/thanks.js` — `<Seo title="Grazie" />`
- `src/content/pages/privacy.md` — frontmatter `title: Privacy e Cookie`, raw-HTML blocks → 8 markdown links, `####` H4 fixed, email deobfuscated
- Git commits `feat(05-03): set lang=it, drop hreflang alternates, harden Seo image guard (SEOS-01, IMAG-03, D-11)`, `feat(05-03): Italian blog-list meta and pagination labels with test co-change (SEOS-02, D-15, D-16)`, `feat(05-03): Italian titles on 404 and thanks pages (SEOS-02, D-17)`, `feat(05-03): rebuild privacy page as clean markdown (SEOS-03, D-19)`
- `.planning/phases/05-image-pipeline-seo-fixes/05-03-SUMMARY.md` (created at plan completion)

<output>
Create `.planning/phases/05-image-pipeline-seo-fixes/05-03-SUMMARY.md` when done
</output>
</content>
