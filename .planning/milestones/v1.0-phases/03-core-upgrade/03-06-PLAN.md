---
phase: 03-core-upgrade
plan: 06
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
  - 03-04
  - 03-05
files_modified:
  - .nvmrc
  - package.json
  - README.md
autonomous: false
requirements:
  - UPGR-02
  - UPGR-07
user_setup: []

must_haves:
  truths:
    - "Node 24 is the enforced version: .nvmrc `24`, engines.node \"24.x\", README Node notes updated — in a SEPARATE commit AFTER the full 5.16.1 + dart-sass matrix is green under Node 20 (D-07/D-07b)"
    - "`nvm use 24 && yarn install && yarn build && yarn test` exits 0 — the Node 24 gate is green (D-07)"
    - "The first post-upgrade Netlify deploy runs with cleared cache (Netlify UI: Deploys → Clear cache and deploy site), the build log shows Node 24, and all pages on laryart.it render without build errors (D-15, ROADMAP success criterion 2)"
    - "The content editor can open /admin, log in, and save a post through Decap CMS (ROADMAP success criterion 3)"
    - "Site analytics keep recording visits via the vendored Matomo _paq snippet with disableCookies enabled (ROADMAP success criterion 4)"
  artifacts:
    - ".nvmrc with content `24`"
    - "package.json with engines.node \"24.x\""
    - "README.md with Node 24 notes (prerequisites, enforcement note, nvm alias default 24, Netlify Node version line)"
    - "Netlify deploy log showing Node 24 and a successful build (user checkpoint evidence)"
  key_links:
    - "nvm use 24 → .nvmrc → yarn install → engines.node 24.x + engine-strict → preinstall → scripts/check-node-version.js (reads .nvmrc dynamically, no code change)"
    - "Netlify deploy → .nvmrc (24) → yarn build → public/ → laryart.it"
    - "Netlify UI → Deploys → Clear cache and deploy site (D-15 manual step)"
---

<objective>
Bump the enforced Node version from 20 to 24 (LTS) — .nvmrc `20` → `24`, engines.node "20.x" → "24.x", README Node notes updated — as a SEPARATE commit that comes only AFTER the full 5.16.1 + dart-sass matrix is verified green under Node 20 (D-07/D-07b: never in the same commit as the dependency changes). Then surface the manual deploy checkpoint (D-15): the user performs the first post-upgrade Netlify deploy with cleared cache and verifies the live site.

Purpose: Align the enforced Node version with the owner's local dev environment (nvm default lts/* = Node 24) now that dart-sass removed the node-sass native-binding constraint, and prove the whole upgraded stack deploys cleanly to production.
Output: Node 24 enforced and green locally; first post-upgrade Netlify deploy (cache cleared) succeeds with Node 24 in the build log; live-site checks pass.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-upgrade/03-CONTEXT.md
@.planning/phases/03-core-upgrade/03-RESEARCH.md
@.planning/phases/03-core-upgrade/03-PATTERNS.md
@.planning/phases/02-foundation-cleanup/02-04-SUMMARY.md
@.nvmrc
@package.json
@scripts/check-node-version.js
@README.md
@netlify.toml
</context>

<tasks>

<task type="auto">
  <name>Node 24 bump: .nvmrc + engines.node + README Node notes (UPGR-02, D-07/D-07b)</name>
  <files>.nvmrc, package.json, README.md</files>
  <read_first>
    - .nvmrc (content `20`)
    - package.json (engines block lines 71-73: "node": "20.x")
    - scripts/check-node-version.js (reads .nvmrc dynamically — NO code change needed)
    - README.md (lines 19, 25, 28, 58: Node 20 references)
    - .planning/phases/03-core-upgrade/03-CONTEXT.md D-07 (Node 24 bump, owner decision), D-07b (separate commit, AFTER the 5.16.1 + dart-sass matrix is green under Node 20)
    - .planning/phases/03-core-upgrade/03-PATTERNS.md §.nvmrc and §README.md (exact edit patterns)
  </read_first>
  <action>
    Edit .nvmrc: replace `20` with `24` (single line, keep the format).

    Edit package.json: change engines.node from "20.x" to "24.x". Do NOT touch any dependency, script, or other field — this commit contains ONLY the Node version bump (D-07b).

    Edit README.md (Italian prose, keep style):
    - line 19: `Node.js 20 (`.nvmrc`)` → `Node.js 24 (`.nvmrc`)`
    - line 25: `Node.js 20 (con [nvm]...` → `Node.js 24 (con [nvm]...`
    - line 28: the enforcement note — `Node 20` → `Node 24` and `nvm alias default 20` → `nvm alias default 24`
    - line 58: `Versione Node: 20 (letta da `.nvmrc`)` → `Versione Node: 24 (letta da `.nvmrc`)`

    Do NOT touch netlify.toml (Phase 2 already set yarn build; .nvmrc resolves via nvm on Netlify). Do NOT touch scripts/check-node-version.js (reads .nvmrc dynamically).

    Commit atomically: `git commit -m "feat(03-06): bump enforced Node version to 24 (D-07)"` staging exactly .nvmrc, package.json, README.md.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && node --version && yarn install && yarn build && yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qx '24' .nvmrc` succeeds
    - `grep -q '"node": "24.x"' package.json` succeeds
    - `! grep -q 'Node.js 20' README.md` succeeds (zero stale Node 20 references)
    - `grep -q 'nvm alias default 24' README.md` succeeds
    - `source ~/.nvm/nvm.sh && nvm use 24 && yarn install && yarn build && yarn test` exits 0 (D-07 gate green)
    - `git log -1 --format=%s` contains `03-06`
  </acceptance_criteria>
  <done>Node 24 is the enforced version (.nvmrc + engines + README), the full Node 24 loop is green, and the bump is its own commit after the Node 20 matrix was verified.</done>
  <reversibility rating="reversible">git revert restores .nvmrc `20`, engines.node "20.x", and the README Node 20 notes; re-run the loop under Node 20 to confirm.</reversibility>
</task>

<task type="checkpoint">
  <name>First post-upgrade Netlify deploy with cleared cache + live-site verification (UPGR-07, D-15)</name>
  <files>none (manual user action in the Netlify UI)</files>
  <read_first>
    - .planning/phases/03-core-upgrade/03-CONTEXT.md D-15 (deploy MUST run with cleared cache — stale NODE_VERSION/cache history has broken builds before; manual user step, not automatable)
    - .planning/ROADMAP.md Phase 3 success criteria 2, 3, 4 (deploy succeeds, Decap CMS works, Matomo records)
  </read_first>
  <action>
    MANUAL USER CHECKPOINT — do not attempt to automate. Ask the user to:
    1. Push the current branch to GitHub (or merge to main) so Netlify picks up the upgraded stack.
    2. In the Netlify UI: Deploys → Clear cache and deploy site (D-15 — the first post-upgrade deploy MUST run with cleared cache).
    3. Verify the build log shows Node 24 and the build succeeds.
    4. Verify all pages on laryart.it render without build errors (home, blog, a post, laryart, privacy, contatti).
    5. Verify /admin opens, login works, and a test post can be saved through Decap CMS (ROADMAP success criterion 3).
    6. Verify Matomo keeps recording visits (ROADMAP success criterion 4) — e.g. check the Matomo dashboard for a fresh visit after browsing the site.
    7. Verify sitemap.xml is served and lists the site's pages (ROADMAP success criterion 5).
    Record the results (deploy log excerpt, page checks, CMS save, Matomo visit) in the plan summary.
  </action>
  <verify>
    <manual>User confirms: Netlify deploy succeeded with cleared cache, build log shows Node 24, all pages render, /admin login + post save works via Decap CMS, Matomo records visits, sitemap.xml is served.</manual>
  </verify>
  <acceptance_criteria>
    - Netlify deploy log shows Node 24 and a successful build (cache cleared per D-15)
    - All pages on laryart.it render without build errors
    - /admin opens, login works, and a post can be saved through Decap CMS
    - Matomo dashboard shows fresh visits after browsing the site (vendored _paq, disableCookies)
    - sitemap.xml is served and lists the site's pages
  </acceptance_criteria>
  <done>The first post-upgrade deploy (cache cleared) succeeded on Node 24, and the live site passes the CMS, analytics, and sitemap checks.</done>
  <reversibility rating="n/a">Manual verification step — nothing to revert; if the deploy fails, the fix path is a new plan (do not silently change scope).</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| local shell → yarn install/build/test | Node 24 crosses here; engines + engine-strict + pre* guard enforce it (check-node-version.js reads .nvmrc dynamically) |
| git push → Netlify build → laryart.it | the upgraded stack crosses here; the cleared-cache deploy (D-15) is the gate against stale NODE_VERSION/cache history |
| browser → laryart.it → Matomo | live analytics cross here; verified in the checkpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-15 | DoS | Node 24 bump breaking the build (e.g. a dependency incompatible with Node 24) | high | mitigate | D-07b ordering: bump only AFTER the 5.16.1 + dart-sass matrix is green under Node 20; full nvm use 24 + install + build + test gate before commit |
| T-03-16 | DoS | stale Netlify cache breaking the first post-upgrade deploy | high | mitigate | D-15 manual checkpoint: deploy MUST run with cleared cache (Netlify UI → Deploys → Clear cache and deploy site) |
| T-03-17 | DoS | live-site regressions (CMS, analytics, sitemap) undetected | medium | mitigate | Checkpoint acceptance criteria cover /admin login + post save, Matomo visits, sitemap.xml, and all-page rendering |
| T-03-SC | Tampering | supply-chain risk | low | accept | No new packages in this plan — only version-pin and doc edits |
</threat_model>

<verification>
- `source ~/.nvm/nvm.sh && nvm use 24 && yarn install && yarn build && yarn test` exits 0 (D-07 gate)
- .nvmrc `24`, engines.node "24.x", README Node 24 notes, zero stale Node 20 references
- `git log` shows the single 03-06 commit (separate from all dependency commits, D-07b)
- User checkpoint evidence: Netlify deploy log (Node 24, cache cleared), page checks, CMS save, Matomo visit, sitemap.xml
</verification>

<success_criteria>
- ROADMAP Phase 3 success criterion 2: the first post-upgrade Netlify deploy (cache cleared) succeeds and all pages on laryart.it render without build errors
- ROADMAP Phase 3 success criterion 3: content editor can open /admin, log in, and save a post through Decap CMS
- ROADMAP Phase 3 success criterion 4: analytics keep recording via the vendored Matomo _paq snippet with disableCookies enabled
- ROADMAP Phase 3 success criterion 5: sitemap.xml is generated by exactly one plugin and lists the site's pages
- The enforced Node version matches the owner's local dev environment (Node 24 LTS)
</success_criteria>

## Artifacts this phase produces

- `.nvmrc` — content `24`
- `package.json` — engines.node "24.x"
- `README.md` — Node 24 notes (prerequisites, enforcement note, nvm alias default 24, Netlify Node version line)
- Git commit `feat(03-06): bump enforced Node version to 24 (D-07)`
- Netlify deploy log showing Node 24 + successful build (user checkpoint evidence, recorded in the summary)
- `.planning/phases/03-core-upgrade/03-06-SUMMARY.md` (created at plan completion)

<output>
Create `.planning/phases/03-core-upgrade/03-06-SUMMARY.md` when done
</output>
