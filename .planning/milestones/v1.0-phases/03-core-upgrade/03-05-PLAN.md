---
phase: 03-core-upgrade
plan: 05
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - package.json
  - yarn.lock
  - gatsby-config.js
autonomous: true
requirements:
  - UPGR-06
user_setup: []

must_haves:
  truths:
    - "gatsby-plugin-advanced-sitemap ^2.1.0 is removed from package.json and gatsby-config.js; gatsby-plugin-sitemap 6.16.0 is the single sitemap generator (D-14)"
    - "`yarn clean && yarn build` produces exactly one sitemap.xml, generated by gatsby-plugin-sitemap, listing the site's pages"
    - "`yarn test` exits 0 under Node 20"
  artifacts:
    - "package.json without gatsby-plugin-advanced-sitemap"
    - "yarn.lock regenerated (advanced-sitemap tree gone)"
    - "gatsby-config.js without the gatsby-plugin-advanced-sitemap entry (line 84)"
  key_links:
    - "yarn build → gatsby-config.js → gatsby-plugin-sitemap (only sitemap plugin) → public/sitemap.xml"
    - "yarn install → yarn.lock → no advanced-sitemap tree"
---

<objective>
Remove gatsby-plugin-advanced-sitemap ^2.1.0 (unmaintained, duplicates the official plugin) from package.json and its string entry from gatsby-config.js (line 84), leaving gatsby-plugin-sitemap 6.16.0 as the single sitemap generator (D-14).

Purpose: Eliminate the redundant sitemap plugin so exactly one sitemap.xml is generated, and drop an unmaintained dependency.
Output: single sitemap pipeline; `yarn clean && yarn build && yarn test` green under Node 20; one commit.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-upgrade/03-CONTEXT.md
@.planning/phases/03-core-upgrade/03-RESEARCH.md
@.planning/phases/03-core-upgrade/03-PATTERNS.md
@.planning/codebase/CONCERNS.md
@package.json
@gatsby-config.js
</context>

<tasks>

<task type="auto">
  <name>Single sitemap: remove gatsby-plugin-advanced-sitemap, keep gatsby-plugin-sitemap (UPGR-06, D-14)</name>
  <files>package.json, yarn.lock, gatsby-config.js</files>
  <read_first>
    - package.json (dependencies line 34 gatsby-plugin-advanced-sitemap ^2.1.0; line 43 gatsby-plugin-sitemap 6.16.0 — KEEP)
    - gatsby-config.js (line 21 "gatsby-plugin-sitemap" — KEEP; line 84 `gatsby-plugin-advanced-sitemap` — REMOVE)
    - .planning/phases/03-core-upgrade/03-CONTEXT.md D-14 (remove advanced-sitemap, keep gatsby-plugin-sitemap as the single generator)
    - .planning/phases/03-core-upgrade/03-PATTERNS.md §gatsby-config.js (Form A string-shorthand removal pattern)
  </read_first>
  <action>
    Remove the plugin with yarn only (never npm): `yarn remove gatsby-plugin-advanced-sitemap`.

    SERIALIZATION NOTE (W-1): this plan, 03-03, and 03-04 all run yarn add/remove against the same package.json + yarn.lock in wave 2. The execute-phase orchestrator serializes plans with overlapping files_modified — do NOT run yarn commands in parallel with the other wave-2 plans. If you detect another wave-2 executor active (uncommitted package.json/yarn.lock changes), wait for it to finish before running yarn.

    Edit gatsby-config.js: delete the string entry `gatsby-plugin-advanced-sitemap` (line 84). This is the ONLY gatsby-config.js edit in this plan — line-specific and non-overlapping with the other wave-2 plans (03-03 renames the netlify-cms entry at line 83; 03-04 removes the matomo block at lines 22-30). Do NOT touch the `"gatsby-plugin-sitemap"` entry (line 21) — it is the single surviving sitemap generator (D-14). Do NOT reorder remaining plugins.

    Commit atomically: `git commit -m "feat(03-05): remove gatsby-plugin-advanced-sitemap, keep single sitemap plugin (UPGR-06)"` staging exactly package.json, yarn.lock, gatsby-config.js.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 20 >/dev/null && yarn install && yarn clean && yarn build && ls public/sitemap.xml && yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q 'gatsby-plugin-advanced-sitemap' package.json` succeeds (zero matches)
    - `! grep -q 'gatsby-plugin-advanced-sitemap' gatsby-config.js` succeeds (entry removed)
    - `grep -q '"gatsby-plugin-sitemap": "6.16.0"' package.json` succeeds (KEEP)
    - `grep -q '"gatsby-plugin-sitemap"' gatsby-config.js` succeeds (KEEP, line 21)
    - `source ~/.nvm/nvm.sh && nvm use 20 && yarn install && yarn clean && yarn build && yarn test` exits 0
    - `test -f public/sitemap.xml` succeeds after build (exactly one sitemap generated by the surviving plugin)
    - `git log -1 --format=%s` contains `03-05`
  </acceptance_criteria>
  <done>advanced-sitemap is gone from deps and config, gatsby-plugin-sitemap is the single sitemap generator, public/sitemap.xml exists after build, and the Node 20 loop is green in one commit.</done>
  <reversibility rating="reversible">git revert restores gatsby-plugin-advanced-sitemap and its config entry; yarn.lock reverts with it.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| yarn build → gatsby-config.js → public/sitemap.xml | the sitemap generation crosses here; two plugins writing the same output is the redundancy being removed |
| npm registry → yarn install | the unmaintained advanced-sitemap package is removed from the supply chain entirely |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13 | DoS | build break from removing a plugin still referenced in config | medium | mitigate | Both package.json and gatsby-config.js entries removed in the same commit; full clean+build+test gate under Node 20 |
| T-03-14 | DoS | sitemap.xml missing after removal (surviving plugin misconfigured) | medium | mitigate | Acceptance asserts public/sitemap.xml exists after build; gatsby-plugin-sitemap entry verified untouched |
| T-03-SC | Tampering | unmaintained dependency remaining in the supply chain | medium | mitigate | D-14 removes gatsby-plugin-advanced-sitemap entirely; yarn.lock regenerated without its tree |
</threat_model>

<verification>
- `source ~/.nvm/nvm.sh && nvm use 20 && yarn install && yarn clean && yarn build && yarn test` exits 0
- gatsby-plugin-advanced-sitemap absent from package.json, yarn.lock, and gatsby-config.js
- gatsby-plugin-sitemap 6.16.0 present in package.json and gatsby-config.js
- `public/sitemap.xml` exists after build
- `git log` shows the single 03-05 commit
</verification>

<success_criteria>
- ROADMAP Phase 3 success criterion 5 satisfied locally: sitemap.xml is generated by exactly one plugin (gatsby-plugin-sitemap) and lists the site's pages
- The unmaintained gatsby-plugin-advanced-sitemap is out of the dependency tree
- The Node 20 gate stays green
</success_criteria>

## Artifacts this phase produces

- `package.json` — gatsby-plugin-advanced-sitemap removed
- `yarn.lock` — regenerated (advanced-sitemap tree gone)
- `gatsby-config.js` — gatsby-plugin-advanced-sitemap entry (line 84) removed
- Git commit `feat(03-05): remove gatsby-plugin-advanced-sitemap, keep single sitemap plugin (UPGR-06)`
- `.planning/phases/03-core-upgrade/03-05-SUMMARY.md` (created at plan completion)

<output>
Create `.planning/phases/03-core-upgrade/03-05-SUMMARY.md` when done
</output>
