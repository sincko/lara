---
phase: 03-core-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - yarn.lock
autonomous: true
requirements:
  - UPGR-01
user_setup: []

must_haves:
  truths:
    - "Gatsby core and ALL gatsby-* plugins sit on the registry-verified 5.16.x lockstep matrix in ONE commit — gatsby 5.16.1 + every plugin at .16.0 (no plugin ships .16.1; the .16.1 patch only touched core)"
    - "`yarn clean && yarn build && yarn test` exits 0 under Node 20 on the new matrix — the D-03 gate is green before any other Phase 3 plan proceeds"
    - "Legacy `gatsby-image` (^3.11.0) and `gatsby-plugin-netlify-cms-paths` (^1.3.0) are untouched — D-02 and D-09 hold"
  artifacts:
    - "package.json with all 13 gatsby-* entries exact-pinned to the 5.16.x matrix"
    - "yarn.lock regenerated by yarn (never hand-edited) for the new matrix"
  key_links:
    - "yarn install → yarn.lock → gatsby 5.16.1 + 12 plugins at .16.0"
    - "yarn build → gatsby-config.js plugin registry → all plugins resolve on the new matrix"
    - "yarn test → jest suite (4 suites) → regression net stays green"
---

<objective>
Upgrade Gatsby core and every gatsby-* plugin to the exact 5.16.x lockstep matrix in ONE commit (D-01): gatsby 5.16.1, gatsby-plugin-manifest 5.16.0, gatsby-plugin-offline 6.16.0, gatsby-plugin-react-helmet 6.16.0, gatsby-plugin-sass 6.16.0, gatsby-plugin-sharp 5.16.0, gatsby-plugin-sitemap 6.16.0, gatsby-remark-images 7.16.0, gatsby-remark-prismjs 7.16.0, gatsby-remark-responsive-iframe 6.16.0, gatsby-source-filesystem 5.16.0, gatsby-transformer-remark 6.16.0, gatsby-transformer-sharp 5.16.0. No partial bumps, no other dependency changes in this commit.

Purpose: This is the tracer of the phase — the single atomic version bump that every other Phase 3 plan builds on. It proves the 5.16.1 matrix is green under Node 20 before dart-sass, Decap, Matomo, or sitemap changes touch anything.
Output: package.json + yarn.lock on the 5.16.x matrix; `yarn clean && yarn build && yarn test` green under Node 20; one commit.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-upgrade/03-CONTEXT.md
@.planning/phases/03-core-upgrade/03-RESEARCH.md
@.planning/phases/03-core-upgrade/03-PATTERNS.md
@.planning/phases/02-foundation-cleanup/02-04-SUMMARY.md
@.planning/phases/01-test-scaffolding-performance-baseline/01-SUMMARY.md
@package.json
@gatsby-config.js
</context>

<tasks>

<task type="tracer">
  <name>Lockstep matrix upgrade: gatsby 5.16.1 + all gatsby-* plugins at .16.0 in ONE commit (UPGR-01, D-01/D-02/D-03)</name>
  <files>package.json, yarn.lock</files>
  <read_first>
    - package.json (dependencies block lines 27-58: current 5.15.0/6.15.0 pins, gatsby-image ^3.11.0 line 33, gatsby-plugin-netlify-cms-paths ^1.3.0 line 38)
    - .planning/phases/03-core-upgrade/03-CONTEXT.md D-01 (registry-verified matrix: plugins top out at .16.0, only core ships .16.1), D-02 (gatsby-image untouched), D-03 (Node 20 gate)
    - .planning/phases/03-core-upgrade/03-RESEARCH.md §Pitfall 1 (.16.1 plugin versions DO NOT EXIST)
    - .planning/phases/03-core-upgrade/03-PATTERNS.md §package.json (exact-pin style for gatsby-* deps; yarn-only rule)
  </read_first>
  <action>
    Bump exactly these 13 entries in package.json dependencies, exact pins (no caret), matching the existing gatsby-* pin style:
    - gatsby: 5.15.0 → 5.16.1
    - gatsby-plugin-manifest: 5.15.0 → 5.16.0
    - gatsby-plugin-offline: 6.15.0 → 6.16.0
    - gatsby-plugin-react-helmet: 6.15.0 → 6.16.0
    - gatsby-plugin-sass: 6.15.0 → 6.16.0
    - gatsby-plugin-sharp: 5.15.0 → 5.16.0
    - gatsby-plugin-sitemap: 6.15.0 → 6.16.0
    - gatsby-remark-images: 7.15.0 → 7.16.0
    - gatsby-remark-prismjs: 7.15.0 → 7.16.0
    - gatsby-remark-responsive-iframe: 6.15.0 → 6.16.0
    - gatsby-source-filesystem: 5.15.0 → 5.16.0
    - gatsby-transformer-remark: 6.15.0 → 6.16.0
    - gatsby-transformer-sharp: 5.15.0 → 5.16.0

    Use yarn only (never npm — double-lockfile rule): `yarn add gatsby@5.16.1 gatsby-plugin-manifest@5.16.0 gatsby-plugin-offline@6.16.0 gatsby-plugin-react-helmet@6.16.0 gatsby-plugin-sass@6.16.0 gatsby-plugin-sharp@5.16.0 gatsby-plugin-sitemap@6.16.0 gatsby-remark-images@7.16.0 gatsby-remark-prismjs@7.16.0 gatsby-remark-responsive-iframe@6.16.0 gatsby-source-filesystem@5.16.0 gatsby-transformer-remark@6.16.0 gatsby-transformer-sharp@5.16.0` so yarn.lock updates atomically.

    Do NOT touch: gatsby-image (^3.11.0, D-02), gatsby-plugin-netlify-cms-paths (^1.3.0, D-09 KEEP), node-sass, netlify-cms-app, gatsby-plugin-netlify-cms, gatsby-plugin-matomo, gatsby-plugin-advanced-sitemap, all devDependencies, engines.node (stays "20.x" — the Node 24 bump is plan 03-06, D-07b), scripts block, .nvmrc, gatsby-config.js, any source file.

    Commit atomically: `git commit -m "feat(03-01): upgrade Gatsby to 5.16.1 lockstep matrix (UPGR-01)"` staging exactly package.json and yarn.lock.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 20 >/dev/null && node --version && yarn clean && yarn build && yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"gatsby": "5.16.1"' package.json` succeeds
    - `grep -q '"gatsby-plugin-manifest": "5.16.0"' package.json` succeeds (same exact-pin check for all 12 plugins: offline 6.16.0, react-helmet 6.16.0, sass 6.16.0, sharp 5.16.0, sitemap 6.16.0, remark-images 7.16.0, remark-prismjs 7.16.0, remark-responsive-iframe 6.16.0, source-filesystem 5.16.0, transformer-remark 6.16.0, transformer-sharp 5.16.0)
    - `! grep -q '"gatsby": "5.15.0"' package.json` and no 5.15.0/6.15.0 gatsby-* pin remains
    - `grep -q '"gatsby-image": "^3.11.0"' package.json` succeeds (D-02 untouched)
    - `grep -q '"gatsby-plugin-netlify-cms-paths": "^1.3.0"' package.json` succeeds (D-09 untouched)
    - `grep -q '"node": "20.x"' package.json` succeeds (engines untouched)
    - `source ~/.nvm/nvm.sh && nvm use 20 && yarn clean && yarn build && yarn test` exits 0 (D-03 gate green)
    - `git log -1 --format=%s` contains `03-01`
  </acceptance_criteria>
  <done>The 13-entry lockstep matrix is exact-pinned in package.json, yarn.lock is regenerated, the full Node 20 loop (clean + build + test) is green, and it is all in one commit.</done>
  <reversibility rating="reversible">git revert restores the 5.15.0 matrix; yarn.lock reverts with it. No source or config changes.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → yarn install | 13 new package versions cross here; exact pins + yarn.lock freeze what lands |
| yarn build → gatsby-config.js plugin registry | every plugin must resolve on the new matrix; a missing .16.x version would break the build here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | DoS | build break from a non-existent .16.1 plugin pin | high | mitigate | Registry-verified matrix (D-01): plugins pinned at .16.0, only core at 5.16.1; full clean+build+test gate before commit |
| T-03-02 | Tampering | partial bump leaving a mixed 5.15/5.16 matrix | high | mitigate | Single atomic commit staging only package.json + yarn.lock; acceptance grep asserts all 13 pins and zero 5.15.0/6.15.0 remnants |
| T-03-03 | DoS | legacy gatsby-image breaking under core 5.16.1 | medium | mitigate | D-02 keeps gatsby-image untouched; build + jest suite (4 suites) is the regression net; if it breaks, revert and re-plan before proceeding |
| T-03-SC | Tampering | supply-chain risk from new package versions | medium | mitigate | Exact pins, yarn.lock committed, no new packages added (only version bumps of already-trusted gatsby-* packages) |
</threat_model>

<verification>
- `source ~/.nvm/nvm.sh && nvm use 20 && yarn clean && yarn build && yarn test` exits 0 (D-03 gate)
- All 13 pins verified by grep; no 5.15.0/6.15.0 gatsby-* entry remains
- gatsby-image and gatsby-plugin-netlify-cms-paths untouched
- `git log` shows the single 03-01 commit
</verification>

<success_criteria>
- ROADMAP Phase 3 success criterion 1 satisfied locally: `gatsby clean && yarn build` passes on the exact 5.16.1 matrix, upgraded in lockstep in one commit
- The jest regression net (4 suites) stays green on the new matrix
- No other dependency, config, or source file changed — the phase's remaining plans start from a clean, verified base
</success_criteria>

## Artifacts this phase produces

- `package.json` — 13 gatsby-* entries exact-pinned to the 5.16.x lockstep matrix
- `yarn.lock` — regenerated lockfile for the new matrix
- Git commit `feat(03-01): upgrade Gatsby to 5.16.1 lockstep matrix (UPGR-01)`
- `.planning/phases/03-core-upgrade/03-01-SUMMARY.md` (created at plan completion)

<output>
Create `.planning/phases/03-core-upgrade/03-01-SUMMARY.md` when done
</output>
