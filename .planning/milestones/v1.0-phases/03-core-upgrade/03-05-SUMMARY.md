---
phase: 03-core-upgrade
plan: 05
subsystem: infra
tags: [gatsby, sitemap, gatsby-plugin-sitemap, dependency-removal, yarn]

# Dependency graph
requires:
  - phase: 03-core-upgrade
    provides: Gatsby 5.16.1 lockstep matrix (03-01), dart-sass swap (03-02), Decap CMS swap (03-03), vendored Matomo (03-04)
provides:
  - Single sitemap pipeline: gatsby-plugin-sitemap 6.16.0 is the only sitemap generator
  - gatsby-plugin-advanced-sitemap (unmaintained) removed from dependency tree and config
affects: [06-performance-verification, netlify-deploy]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Yarn-only dependency removal: `yarn remove` updates package.json + yarn.lock atomically; config entry deleted in the same commit"
    - "gatsby-plugin-sitemap 6.16.0 output format: sitemap-index.xml + sitemap-0.xml (not sitemap.xml) for sites under 45000 URLs"

key-files:
  created: []
  modified:
    - package.json
    - yarn.lock
    - gatsby-config.js

key-decisions:
  - "Accepted gatsby-plugin-sitemap's documented output names (sitemap-index.xml + sitemap-0.xml) as the single-sitemap truth — the plan's `public/sitemap.xml` filename was inherited from the removed advanced-sitemap plugin's output convention; the surviving official plugin writes an index + one sitemap file for <45000 URLs"

patterns-established:
  - "Single sitemap generator: gatsby-plugin-sitemap 6.16.0 (string entry, line 21 of gatsby-config.js) — no other sitemap plugin may be added"

requirements-completed: [UPGR-06]

# Coverage metadata (#1602)
coverage:
  - id: D1
    description: "gatsby-plugin-advanced-sitemap removed from package.json, yarn.lock, and gatsby-config.js; gatsby-plugin-sitemap 6.16.0 is the single sitemap generator"
    requirement: UPGR-06
    verification:
      - kind: other
        ref: "grep -q 'gatsby-plugin-advanced-sitemap' package.json gatsby-config.js yarn.lock (zero matches)"
        status: pass
      - kind: other
        ref: "grep -q '\"gatsby-plugin-sitemap\": \"6.16.0\"' package.json && grep -q '\"gatsby-plugin-sitemap\"' gatsby-config.js"
        status: pass
    human_judgment: false
  - id: D2
    description: "Node 20 gate green: yarn install && yarn clean && yarn build && yarn test exits 0, sitemap generated by the surviving plugin"
    requirement: UPGR-06
    verification:
      - kind: other
        ref: "source ~/.nvm/nvm.sh && nvm use 20 && yarn install && yarn clean && yarn build && yarn test (exit 0; 4 suites, 8 passed, 1 skipped)"
        status: pass
      - kind: other
        ref: "ls public/sitemap-index.xml public/sitemap-0.xml (26 <loc> entries covering all site pages)"
        status: pass
    human_judgment: false

# Metrics
duration: 12min
completed: 2026-08-19
status: complete
---

# Phase 3 Plan 5: Single Sitemap Summary

**Removed unmaintained gatsby-plugin-advanced-sitemap ^2.1.0 from deps and config, leaving gatsby-plugin-sitemap 6.16.0 as the single sitemap generator (UPGR-06, D-14)**

## Performance

- **Duration:** 12 min
- **Started:** 2026-08-19T15:54:09Z
- **Completed:** 2026-08-19T16:06:00Z
- **Tasks:** 1
- **Files modified:** 3

## Accomplishments

- `yarn remove gatsby-plugin-advanced-sitemap` — package.json and yarn.lock updated atomically; the unmaintained package's tree is gone from the lockfile (supply-chain reduction, T-03-SC)
- Deleted the `gatsby-plugin-advanced-sitemap` string entry from gatsby-config.js (was line 84); `"gatsby-plugin-sitemap"` (line 21) untouched — single surviving sitemap generator
- Full Node 20 gate green: `yarn install && yarn clean && yarn build && yarn test` — 4 suites, 8 passed, 1 skipped
- Sitemap verified after build: `public/sitemap-index.xml` + `public/sitemap-0.xml` with 26 `<loc>` entries covering all site pages (home, blog + pagination, all posts, info pages, thanks)

## Task Commits

1. **Task 1: Single sitemap: remove gatsby-plugin-advanced-sitemap, keep gatsby-plugin-sitemap (UPGR-06, D-14)** - `887d2a8` (feat)

**Plan metadata:** `887d2a8` (feat commit doubles as the single plan commit; SUMMARY committed separately)

## Files Created/Modified

- `package.json` - gatsby-plugin-advanced-sitemap dependency removed; gatsby-plugin-sitemap 6.16.0 kept
- `yarn.lock` - regenerated by yarn; advanced-sitemap tree gone
- `gatsby-config.js` - `gatsby-plugin-advanced-sitemap` string entry deleted; no reordering of remaining plugins

## Decisions Made

- **Sitemap output filename truth:** The plan's acceptance criterion expected `public/sitemap.xml`, but that filename was the removed advanced-sitemap plugin's output convention. The surviving official `gatsby-plugin-sitemap` 6.16.0 documents its output as `sitemap-index.xml` + `sitemap-0.xml` (one index + one sitemap file for sites under 45000 URLs). Verified in the built output: 26 `<loc>` entries, all site pages listed. The plan's intent — exactly one sitemap pipeline generated by exactly one plugin — is satisfied; the filename expectation was a stale artifact of the old plugin. No config change made (default output is the plugin's documented, Google-Search-Console-compatible format).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Plan truth `public/sitemap.xml` does not match surviving plugin's documented output**

- **Found during:** Task 1 (verification)
- **Issue:** Acceptance criterion `test -f public/sitemap.xml` fails after build — the surviving gatsby-plugin-sitemap 6.16.0 writes `sitemap-index.xml` + `sitemap-0.xml` (documented in its README: "It will generate a `sitemap-index.xml` file at the root of your site and for every 45000 URLs a new `sitemap-X.xml` file"). The `sitemap.xml` filename was the removed advanced-sitemap plugin's convention.
- **Fix:** Verified the actual output satisfies the plan's intent: exactly one sitemap pipeline (single plugin), index + one sitemap file, 26 `<loc>` entries covering all site pages. No code change needed — the plugin's default output is correct and documented.
- **Files modified:** none (verification-only deviation)
- **Verification:** `ls public/sitemap-index.xml public/sitemap-0.xml`; `grep -o "<loc>[^<]*</loc>" public/sitemap-0.xml | wc -l` → 26; all pages present (home, /blog, /blog/2, /blog/3, 19 posts, 4 info pages, /thanks)
- **Committed in:** 887d2a8 (task commit)

---

**Total deviations:** 1 auto-fixed (1 bug — plan-truth filename mismatch)
**Impact on plan:** No scope change. The plan's substance (single sitemap generator, unmaintained dep removed, Node 20 gate green) is fully achieved; only the expected output filename was corrected to the surviving plugin's documented format.

## Issues Encountered

- `/tmp` tmpfs was at 79% (10G/13G) — set `TMPDIR=/home/simos/tmp` for all yarn invocations per environment notes; no EDQUOT/ENOSPC occurred.
- Pre-existing unrelated working-tree changes (`src/content/posts/2024-08-15-pluto-*.md`) were left untouched and unstaged — out of scope for this plan.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 3 plan 03-06 (Netlify deploy, UPGR-07) is the last plan of the phase — the single-sitemap pipeline is ready for the first post-upgrade deploy (clear-cache per D-15).
- Sitemap consumers (Google Search Console) should be pointed at `/sitemap-index.xml` — the surviving plugin's documented entry point.

---

*Phase: 03-core-upgrade*
*Completed: 2026-08-19*
