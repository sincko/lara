---
phase: 03-core-upgrade
plan: 01
subsystem: infra
tags: [gatsby, yarn, dependency-upgrade, lockstep-matrix, node-20]

# Dependency graph
requires:
  - phase: 02-foundation-cleanup
    provides: Node enforcement guards (engines + engine-strict + check-node-version.js) that gate the D-03 verification loop
  - phase: 01-test-scaffolding-performance-baseline
    provides: jest regression net (4 suites) that must stay green through the upgrade
provides:
  - Gatsby 5.16.1 core + 12 gatsby-* plugins at .16.0 in one atomic commit (UPGR-01)
  - Verified green base (clean + build + test under Node 20) for all remaining Phase 3 plans
affects: [03-02 dart-sass swap, 03-03 Decap swap, 03-04 Matomo vendoring, 03-05 sitemap removal, 03-06 Node 24 bump]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Lockstep gatsby-* version matrix: core 5.16.1 + all plugins at .16.0 (no plugin ships .16.1)"
    - "Exact pins (no caret) for gatsby-* deps; yarn-only dependency operations"

key-files:
  created: []
  modified:
    - package.json
    - yarn.lock

key-decisions:
  - "Registry-verified matrix used: gatsby 5.16.1 + plugins at .16.0 — .16.1 plugin versions do not exist (Pitfall 1)"
  - "yarn.lock transitive churn (babel-preset-gatsby, create-gatsby, body-parser/qs) accepted as inherent to the core bump — no direct dependency changed"

patterns-established:
  - "D-03 gate: every Phase 3 commit verified with nvm use 20 && yarn clean && yarn build && yarn test"

requirements-completed: [UPGR-01]

# Coverage metadata (#1602) — one entry per shipped deliverable. Drives DETERMINISTIC UAT routing in verify-work.
coverage:
  - id: D1
    description: "Gatsby core and all 12 gatsby-* plugins upgraded to the exact 5.16.x lockstep matrix in one commit (UPGR-01)"
    requirement: UPGR-01
    verification:
      - kind: integration
        ref: "source ~/.nvm/nvm.sh && nvm use 20 && yarn clean && yarn build && yarn test (D-03 gate)"
        status: pass
      - kind: other
        ref: "grep exact-pin assertions: gatsby 5.16.1 + 12 plugins at .16.0, zero 5.15.0/6.15.0 remnants, gatsby-image ^3.11.0 and gatsby-plugin-netlify-cms-paths ^1.3.0 untouched, engines.node 20.x"
        status: pass
    human_judgment: false

# Metrics
duration: 14min
completed: 2026-08-19
status: complete
---

# Phase 3 Plan 1: Lockstep Matrix Upgrade Summary

**Gatsby 5.16.1 core + all 12 gatsby-* plugins at .16.0 in one atomic commit, verified green under Node 20 (clean + build + test)**

## Performance

- **Duration:** 14 min
- **Started:** 2026-08-19T15:16:27Z
- **Completed:** 2026-08-19T15:31:08Z
- **Tasks:** 1 (tracer)
- **Files modified:** 2 (package.json, yarn.lock)

## Accomplishments

- All 13 gatsby-* entries exact-pinned to the registry-verified 5.16.x matrix: gatsby 5.16.1, 12 plugins at .16.0 (manifest, offline, react-helmet, sass, sharp, sitemap, remark-images, remark-prismjs, remark-responsive-iframe, source-filesystem, transformer-remark, transformer-sharp)
- D-03 gate green: `yarn clean && yarn build && yarn test` exits 0 under Node 20.20.2 — build 29-30s, jest 4/4 suites (8 passed, 1 skipped, matching the Phase 1 baseline)
- D-02/D-09 hold: gatsby-image ^3.11.0 and gatsby-plugin-netlify-cms-paths ^1.3.0 untouched; engines.node stays "20.x" (Node 24 bump is plan 03-06, D-07b)
- Single atomic commit `9cdb0b5` staging exactly package.json + yarn.lock — no partial bumps, no other dependency changes

## Task Commits

Each task was committed atomically:

1. **Task 1 (tracer): Lockstep matrix upgrade** - `9cdb0b5` (feat)

**Plan metadata:** `docs(03-01): complete lockstep-matrix-upgrade plan` (committed after this file)

## Files Created/Modified

- `package.json` - 13 gatsby-* entries bumped 5.15.0/6.15.0/7.15.0 → 5.16.1/.16.0 (exact pins, no caret)
- `yarn.lock` - regenerated by yarn for the new matrix (never hand-edited)

## Decisions Made

- Used the registry-verified matrix (gatsby 5.16.1 + plugins at .16.0) — `.16.1` plugin versions do not exist (RESEARCH.md Pitfall 1, E404-verified)
- Accepted yarn.lock transitive churn (babel-preset-gatsby, create-gatsby, body-parser/qs, etc.) as inherent to the core bump — these are gatsby 5.16's own dependency-tree updates, not direct dependency changes; package.json diff is exactly the 13 pins

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] yarn install failed with EDQUOT (-122) on /tmp tmpfs quota exhaustion**
- **Found during:** Task 1 (yarn add of the 13-entry matrix)
- **Issue:** `yarn add` aborted with `error Error: Unknown system error -122, write` — the /tmp tmpfs user quota (10330M) was exhausted, so yarn's temp writes failed before any package change
- **Fix:** Redirected yarn's temp dir to `/home/simos/tmp` (166G free on /home) via `TMPDIR=/home/simos/tmp`; install then completed in 9.95s
- **Files modified:** none (environment-only workaround)
- **Verification:** yarn add completed; all 13 pins present in package.json; yarn.lock regenerated
- **Committed in:** 9cdb0b5 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Environment-only workaround; no scope change. The plan's action, verify, and commit discipline executed exactly as written.

## Issues Encountered

- Pre-existing working-tree noise (uncommitted pluto-1/pluto-2 content edits, a caniuse-lite yarn.lock drift, untracked .planning/config.json, AGENTS.md, graphify-out/) was present at spawn. The yarn.lock drift was restored to HEAD before the upgrade so the commit contains only the matrix change; the content edits and untracked files were left untouched (out of scope, not owned by this plan).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The 5.16.1 matrix is green under Node 20 — ready for plan 03-02 (dart-sass swap + mandatory font @import hoist)
- No blockers; the STATE.md concern "gatsby-plugin-netlify-cms vs Gatsby 5.16 install" did not materialize (build green with the plugin still installed)

---

*Phase: 03-core-upgrade*
*Completed: 2026-08-19*
