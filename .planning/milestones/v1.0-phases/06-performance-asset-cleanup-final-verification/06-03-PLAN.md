---
phase: 06-performance-asset-cleanup-final-verification
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - static/ (manifest.json, browserconfig.xml, favicon.ico, 24 legacy icon files deleted)
autonomous: true
requirements:
  - PERF-03
user_setup: []

must_haves:
  truths:
    - "The legacy PWA set is deleted from static/: manifest.json, browserconfig.xml, android-icon-{36,48,72,96,144,192}x*.png, apple-icon-{57,60,72,76,114,120,144,152,180}x*.png, apple-icon.png, apple-icon-precomposed.png, favicon-{16,32,96}x*.png, favicon.ico, ms-icon-{70,144,150,310}x*.png (D-11, 27 files verified present 2026-08-20)"
    - "gatsby-plugin-manifest config in gatsby-config.js is UNTOUCHED (name 'LaryArt by Lara', theme #ff1c65, icon static/assets/stackrole.png — D-12; the config diff is empty)"
    - "The build emits exactly one manifest: public/manifest.webmanifest — and public/ contains no manifest.json from the legacy set and no legacy icon files (D-13; UI-SPEC E3 empty covered)"
    - "The manifest lists the 8 generated icons/icon-* entries with start_url, display: standalone, theme_color #ff1c65 — the plugin's populated manifest state (UI-SPEC E3 populated covered, D-12)"
    - "gatsby-plugin-offline config is untouched and its default offline behavior is preserved — it depends on the plugin-generated manifest, which is still present (UI-SPEC E5 error covered)"
    - "Every page head still links a real icon: public/favicon-32x32.png emitted and referenced via rel=icon, plus the manifest.webmanifest link and 8 apple-touch-icon links (plugin-generated — zero gap for modern browsers; UI-SPEC E2 empty/loading/populated covered: the head tags always exist, the icons are build-emitted tiny PNGs with no async state, and the tab shows the same 32x32 PNG as today's set — same stackrole.png source)"
    - "The single intentional delta is accepted and documented: a direct request to /favicon.ico returns 404 — browsers silently ignore it (the tab icon comes from the linked PNG); no redirect, no .ico replacement (UI-SPEC Documented delta)"
    - "static/admin/ is untouched (the CMS config lives there — only the legacy icon/manifest files at static/ root are deleted)"
    - "assumption (unclassified probe PERF-03, surfaced not dropped): the plugin's generated icon set is visually identical to the legacy set (both derive from the same static/assets/stackrole.png source — D-12), so tab/home-screen icons render unchanged"
    - "assumption (probe PERF-03 idempotency/concurrency, surfaced not dropped): the deletion is a one-shot file removal, serialized after 06-02 by the wave structure — re-running the phase leaves nothing to delete (the globs match zero files on a clean tree); if interrupted mid-delete, git status shows the partial set and the remaining globs re-run to completion before the commit"
  artifacts:
    - "static/ root cleaned: the 27 legacy manifest/icon files gone (verified absent)"
    - "public/manifest.webmanifest + public/favicon-32x32.png + public/icons/icon-{48..512}x*.png (plugin-generated, from the build)"
  key_links:
    - "gatsby-plugin-manifest (gatsby-config.js:75-86, unchanged) → build → public/manifest.webmanifest + icons + head tags (rel=manifest, rel=icon, apple-touch-icon)"
    - "static/ legacy files (deleted) → zero references in the built head (the plugin supplies all links itself — gatsby-ssr)"
  prohibitions:
    - "gatsby-config.js manifest/offline plugin options must NOT change (D-12 — zero config diff)"
    - "No redirect or .ico replacement for the /favicon.ico 404 (accepted delta — a fix would defeat the payload cleanup)"
    - "static/admin/ must NOT be touched"
    - "No package installs in this plan"
---

<objective>
Delete the legacy PWA manifest + icon set from static/ (manifest.json, browserconfig.xml, favicon.ico, and the 24 legacy icon files) so exactly ONE manifest is served — the one gatsby-plugin-manifest generates into public/ — then prove manifest/icon continuity on the built output (public/manifest.webmanifest exists, no legacy files anywhere, icon head tags still reference generated files).

Purpose: the legacy set is served-but-unreferenced duplication (research-verified from the plugin source — the plugin emits its own manifest + icons + head links with the config untouched); deleting it is a pure payload reduction with zero visual delta. The manifest-continuity gate (D-13) is the proof.

Output: static/ root cleaned of the 27 legacy files, a fresh build with exactly one manifest, the continuity greps recorded, and a documented /favicon.ico delta.

Environment notes (apply to every verify step): node is NOT on PATH — prefix commands with `source ~/.nvm/nvm.sh && nvm use 24` (nvm has v20.20.2, v24.18.0, v24.19.0; the project enforces Node 24 via engines + engine-strict + scripts/check-node-version.js; .nvmrc is 24). /tmp is a tmpfs that fills with JIT artifacts — set TMPDIR=/home/simos/tmp for yarn/build commands. yarn 1.22 only, never npm.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-performance-asset-cleanup-final-verification/06-CONTEXT.md
@.planning/phases/06-performance-asset-cleanup-final-verification/06-RESEARCH.md
@.planning/phases/06-performance-asset-cleanup-final-verification/06-PATTERNS.md
@.planning/phases/06-performance-asset-cleanup-final-verification/06-UI-SPEC.md
@.planning/phases/06-performance-asset-cleanup-final-verification/06-VALIDATION.md
@gatsby-config.js
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Delete the legacy PWA manifest + icon set from static/ root (D-11) — the PERF-03 tracer</name>
  <files>static/manifest.json, static/browserconfig.xml, static/favicon.ico, static/android-icon-{36,48,72,96,144,192}x*.png, static/apple-icon-{57,60,72,76,114,120,144,152,180}x*.png, static/apple-icon.png, static/apple-icon-precomposed.png, static/favicon-{16,32,96}x*.png, static/ms-icon-{70,144,150,310}x*.png</files>
  <read_first>
    - The files being deleted: the static/ root listing (27 legacy files verified present 2026-08-20 — android-icon-*, apple-icon-*, ms-icon-*, favicon-*, favicon.ico, manifest.json, browserconfig.xml; the exact globs are in the PATTERNS §static/ legacy PWA deletion section)
    - gatsby-config.js lines 75-87 (the gatsby-plugin-manifest options block + gatsby-plugin-offline — UNTOUCHED, D-12)
    - .planning/phases/06-performance-asset-cleanup-final-verification/06-RESEARCH.md §PWA manifest continuity pattern (plugin emits manifest.webmanifest + favicon-32x32.png + icons/icon-*; head tags are plugin-injected) and Code Example D (the continuity greps)
    - .planning/phases/06-performance-asset-cleanup-final-verification/06-PATTERNS.md §static/ legacy PWA deletion (the exact delete set + verification greps)
    - .planning/phases/06-performance-asset-cleanup-final-verification/06-UI-SPEC.md §Favicon / PWA Continuity Contract (the Documented delta: /favicon.ico 404 — accepted, no redirect)
  </read_first>
  <action>
    Delete the 27 legacy files at static/ root only — manifest.json, browserconfig.xml, favicon.ico, and every file matching android-icon-*, apple-icon-*, ms-icon-*, favicon-{16,32,96}x*.png, apple-icon.png, apple-icon-precomposed.png. A single glob command is safe here (no referenced files match these patterns — the CMS admin config lives in static/admin/ which is NOT touched; the plugin config icon lives in static/assets/ which is NOT under these globs): `git rm static/manifest.json static/browserconfig.xml static/favicon.ico static/android-icon-*.png static/apple-icon-*.png static/apple-icon.png static/apple-icon-precomposed.png static/favicon-*.png static/ms-icon-*.png`.

    Then verify with git that the count matches BEFORE committing: `git diff --cached --name-only --diff-filter=D -- static/ | wc -l` — expect 27 deletions (or `git status --short static/ | grep -c '^D'` while the deletions are still staged). If any pattern did not match files, list the remaining static/ root files and confirm they are admin/ (the CMS dir) or assets/ — do NOT delete anything else.

    Do NOT touch gatsby-config.js (D-12 — zero diff on the manifest/offline block), do NOT touch static/admin/, do NOT add any redirect or icon replacement for the /favicon.ico delta (documented accepted).

    Commit atomically: `git commit -m "feat(06-03): delete legacy PWA manifest and icon set from static (PERF-03, D-11)"` staging exactly the deleted files.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && test "$(git diff --cached --name-only --diff-filter=D -- static/ | wc -l)" -eq 27 && test ! -f static/manifest.json && test ! -f static/favicon.ico && test ! -f static/browserconfig.xml && git diff --quiet HEAD -- gatsby-config.js</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --cached --name-only --diff-filter=D -- static/ | wc -l` → 27, OR `git status --short static/ | grep -cE '^ ?D'` → 27 while staged (the count gate runs BEFORE the commit — post-commit `git status` shows zero deletions, which is correct); alternatively `git log -1 --stat` shows the 27 deletions
    - `test ! -f static/manifest.json` and `test ! -f static/favicon.ico` and `test ! -f static/browserconfig.xml` succeed
    - `ls static/` shows only `admin/` and `assets/` remaining
    - `git diff --quiet HEAD -- gatsby-config.js` exits 0 (manifest/offline config untouched, D-12)
    - `git log -1 --format=%s` contains `06-03`
  </acceptance_criteria>
  <done>The 27 legacy files are deleted from static/ root, the manifest config is untouched, and the staged set matches the researched inventory exactly.</done>
  <reversibility rating="reversible">git history preserves all 27 files — `git checkout -- <path>` restores any of them.</reversibility>
</task>

<task type="auto">
  <name>Build + manifest-continuity gate: exactly one manifest, plugin icons present, head tags reference generated files (D-13, PERF-03 success criterion 3)</name>
  <files>public/ (build output — verification only; gatsby-config.js read-only reference)</files>
  <read_first>
    - .planning/phases/06-performance-asset-cleanup-final-verification/06-RESEARCH.md Code Example D (the continuity greps) and §PWA manifest continuity pattern
    - .planning/phases/06-performance-asset-cleanup-final-verification/06-UI-SPEC.md §Manifest-continuity gate (exactly one manifest.webmanifest, zero manifest.json in public/, zero legacy icons) and the Favicon continuity table (E2/E3 covered states)
    - .planning/phases/06-performance-asset-cleanup-final-verification/06-VALIDATION.md per-task map row 06-03-01
    - No source edits expected — build + grep only
  </read_first>
  <action>
    Run `source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn clean && TMPDIR=/home/simos/tmp yarn build`.

    Then run the continuity gate on the fresh build (record every output):
    1. `test -f public/manifest.webmanifest` — the single canonical manifest
    2. `test ! -f public/manifest.json` — zero legacy-named manifest in the build
    3. `grep -rl 'rel="manifest"' public/index.html` — the head links the generated manifest (href="/manifest.webmanifest")
    4. `grep -rl 'rel="icon"' public/index.html` — the head links the generated favicon-32x32.png (href="/favicon-32x32.png?...")
    5. `grep -o 'apple-touch-icon' public/index.html | wc -l` — ≥ 8 (the plugin's apple-touch-icon links)
    6. `ls "public/icons/" | grep -c "icon-"` — ≥ 8 (the manifest's icon set)
    7. `grep -c '"manifest.webmanifest"\|manifest.webmanifest' public/index.html` — ≥ 1 (the manifest link)
    8. Zero legacy file references in the built HTML: `grep -rc 'browserconfig.xml' public/ | grep -v ':0' | wc -l` → 0

    If any gate fails, root-cause in the plugin config (must NOT change) or the deleted-file set (restore a wrongly deleted file) and rebuild; commit any fix separately with `git commit -m "fix(06-03): PWA continuity gate fixes (PERF-03)"`. If all green, make NO commit. Record the /favicon.ico → 404 accepted delta in the plan SUMMARY (the Netlify log line is expected, not an error).
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && test -f public/manifest.webmanifest && test ! -f public/manifest.json && grep -q 'rel="manifest"' public/index.html && grep -q 'rel="icon"' public/index.html && test "$(grep -o 'apple-touch-icon' public/index.html | wc -l)" -ge 8</automated>
  </verify>
  <acceptance_criteria>
    - `test -f public/manifest.webmanifest` succeeds (exactly one manifest)
    - `test ! -f public/manifest.json` succeeds (no legacy-named manifest in the build)
    - `grep -q 'rel="manifest"' public/index.html` succeeds
    - `grep -q 'rel="icon"' public/index.html` succeeds (tab icon present — the E2 populated state)
    - `grep -o 'apple-touch-icon' public/index.html | wc -l` ≥ 8 (apple-touch continuity)
    - `ls public/icons/ | grep -c 'icon-'` ≥ 8 (the manifest's icon list)
    - `grep -rc 'browserconfig.xml' public/ | grep -v ':0' | wc -l` → 0 (no legacy references in the build)
    - `git diff --quiet HEAD -- gatsby-config.js` still exits 0 (D-12)
    - The SUMMARY records the /favicon.ico 404 delta as accepted
  </acceptance_criteria>
  <done>The build emits exactly one manifest (public/manifest.webmanifest), zero legacy manifest/icon artifacts, the icon links in the head reference generated files, the manifest lists ≥ 8 icons, and the /favicon.ico delta is documented.</done>
  <reversibility rating="reversible">verification-only task — no code changed on a green run; any fix commits atomically.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static/ root → built site | the deleted files cross here — the plugin must supply every icon link the pages need |
| plugin config → gatsby build | the manifest options cross here — a config change would break the D-12 contract |
| /favicon.ico requests → Netlify | the accepted 404 crosses here — legacy browsers silently ignore it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-09 | Tampering | gatsby-config.js manifest/offline options changed (D-12 contract broken) | medium | mitigate | zero-diff acceptance gate on gatsby-config.js; the continuity gate verifies the plugin output |
| T-06-10 | DoS | legacy delete leaves an icon gap (a head tag references a deleted file) | high | mitigate | the head-link greps (rel=manifest/rel=icon/apple-touch) + icons/ listing prove the plugin generates everything; research verified zero gap from a real build |
| T-06-11 | Info disclosure | wrong glob deletes files outside the legacy set (e.g. static/admin or assets) | high | mitigate | the exact git rm globs listed in the action; acceptance asserts only 27 deletions and that static/ shows only admin/ + assets/ |
| T-06-SC | Tampering | no package installs in this plan | n/a | n/a | zero install commands |

</threat_model>

<verification>
- `git status --short static/ | grep -cE '^ ?D'` → 27; static/ contains only admin/ + assets/
- `TMPDIR=/home/simos/tmp yarn clean && yarn build` exits 0; `test -f public/manifest.webmanifest`; `test ! -f public/manifest.json`
- `grep -q 'rel="manifest"' public/index.html`; `grep -q 'rel="icon"' public/index.html`; `grep -o 'apple-touch-icon' public/index.html | wc -l` ≥ 8; `ls public/icons/ | grep -c 'icon-'` ≥ 8
- `git diff --quiet HEAD -- gatsby-config.js` (D-12 zero diff)
- The /favicon.ico 404 delta documented as accepted in the SUMMARY
</verification>

<success_criteria>
- Exactly one manifest (public/manifest.webmanifest); zero legacy manifest/icons in static/ or public/
- Every head icon link and the manifest icon list point at plugin-generated files — no icon gap
- gatsby-config.js manifest/offline block byte-identical (D-12)
- The /favicon.ico 404 is the only delta, documented and accepted
</success_criteria>

## Artifacts this phase produces

- `static/. — 27 legacy files deleted (manifest.json, browserconfig.xml, favicon.ico, android-icon-{36,48,72,96,144,192}x*.png, apple-icon-{57,60,72,76,114,120,144,152,180}x*.png, apple-icon.png, apple-icon-precomposed.png, favicon-{16,32,96}x*.png, ms-icon-{70,144,150,310}x*.png)
- Commit: `feat(06-03): delete legacy PWA manifest and icon set from static/ (PERF-03, D-11)`
- Verification record: the continuity greps + the accepted /favicon.ico 404 delta note in the SUMMARY
- `.planning/phases/06-performance-asset-cleanup-final-verification/06-03-SUMMARY.md` (created at plan completion)

<output>
Create `.planning/phases/06-performance-asset-cleanup-final-verification/06-03-SUMMARY.md` when done
</output>
</content>
