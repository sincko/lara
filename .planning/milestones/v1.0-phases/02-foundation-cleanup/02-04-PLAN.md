---
phase: 02-foundation-cleanup
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - package.json
  - .yarnrc
  - scripts/check-node-version.js
  - scripts/clean-node-sass-vendor.js
  - README.md
autonomous: true
gap_closure: true
gap_ids:
  - G-02-1
  - G-02-2
requirements:
  - FNDT-02
user_setup: []

must_haves:
  truths:
    - "Under Node 24, `yarn install` exits non-zero with an explicit NODE_VERSION_MISMATCH / engine-incompatible error — the wrong Node version can no longer silently corrupt node_modules (G-02-1 root cause closed)"
    - "Under Node 20, `nvm use 20 && yarn install && yarn build && yarn test` exits 0 — the phase's real gate is green again (G-02-1, G-02-2 closed)"
    - "A poisoned (non-ELF) node-sass vendor binding is deleted automatically on every install — a 9-byte HTTP-error body cannot survive to break the build (G-02-2 root cause closed)"
    - "README documents the enforced Node 20 contract and the one-command recovery (`nvm use && yarn install`)"
  artifacts:
    - "package.json with `engines.node: \"20.x\"` and preinstall/prebuild/predevelop/postinstall scripts"
    - ".yarnrc with `engine-strict true`"
    - "scripts/check-node-version.js (exits 1 on Node-major mismatch with .nvmrc)"
    - "scripts/clean-node-sass-vendor.js (deletes non-ELF vendor bindings; exits 1 if no valid binary for the active ABI)"
    - "README.md updated with the enforcement note"
  key_links:
    - "yarn install → engine-strict → engines.node 20.x (hard block under Node 24)"
    - "yarn install/build/develop → pre* scripts → scripts/check-node-version.js → .nvmrc"
    - "yarn install → postinstall → scripts/clean-node-sass-vendor.js → node_modules/node-sass/vendor"
---

<objective>
Close UAT gaps G-02-1 and G-02-2 (both blockers, one root cause: node-sass 9.0.0 has no prebuilt binary for Node 24 ABI 137, and its install script silently wrote the GitHub 404 response body to `node_modules/node-sass/vendor/linux-x64-137/binding.node`). This plan makes the repo enforce its own Node contract so the wrong Node version can never silently corrupt installs/builds again, and adds a postinstall hygiene guard that deletes poisoned node-sass vendor bindings.

Purpose: Restore Phase 02's truth — "one Node version" — as an enforced invariant, not a convention. The durable node-sass→dart-sass replacement stays in Phase 3 (Core Upgrade, UPGR-02); this plan only makes Node 20 unambiguous and unblocks install/build under it. No Phase 3 scope is taken.
Output: engines + engine-strict enforcement, pre* guard script, postinstall vendor-cleanup script, README documentation; `nvm use 20 && yarn install && yarn build && yarn test` green; `yarn install` under Node 24 fails loudly with an explicit error.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundation-cleanup/02-UAT.md
@.planning/phases/02-foundation-cleanup/02-CONTEXT.md
@.planning/phases/02-foundation-cleanup/02-RESEARCH.md
@.planning/phases/02-foundation-cleanup/02-02-SUMMARY.md
@.planning/debug/node-sass-distutils-install-failure.md
@.planning/debug/node-sass-binding-file-too-short.md
@package.json
@.nvmrc
@README.md
</context>

<tasks>

<task type="tracer">
  <name>Node-version enforcement: engines + engine-strict + pre* guard (G-02-1, G-02-2 root cause)</name>
  <files>package.json, .yarnrc, scripts/check-node-version.js</files>
  <read_first>
    - package.json (current scripts block lines 14-22; `packageManager: "yarn@1.22.22"` line 66)
    - .nvmrc (content `20`)
    - .planning/phases/02-foundation-cleanup/02-UAT.md §Gaps (G-02-1, G-02-2 root_cause fields)
    - .planning/phases/02-foundation-cleanup/02-CONTEXT.md D-05 (Node 20 pinned because node-sass 9.0.0 ships prebuilt binaries only for ABI ≤115)
    - .planning/phases/02-foundation-cleanup/02-RESEARCH.md §Pitfall 2 (shell default Node 24 breaks node-sass)
  </read_first>
  <action>
    Create `scripts/check-node-version.js` (new `scripts/` directory at repo root). Spec: read `.nvmrc` via `path.resolve(__dirname, '..', '.nvmrc')`; if the file does not exist, exit 0 (nothing to enforce). Normalize the pinned value: strip a leading `v`, take the segment before the first `.` (handles `20`, `v20`, `20.20.2`). Compare against the major of `process.versions.node`. On mismatch, print to stderr a message containing the token `NODE_VERSION_MISMATCH` explaining that node-sass 9.0.0 has no prebuilt binary for the active Node (ABI constraint) and the fix is `nvm use && yarn install`, then `process.exit(1)`. On match, exit 0 silently. No dependencies, no network, ~15 lines.

    Edit package.json:
    - Add top-level `"engines": { "node": "20.x" }` (place after the `"packageManager"` line).
    - Add to `scripts`: `"preinstall": "node scripts/check-node-version.js"`, `"prebuild": "node scripts/check-node-version.js"`, `"predevelop": "node scripts/check-node-version.js"`.

    Create `.yarnrc` (new file, repo root) with exactly one line: `engine-strict true`.

    Do NOT touch node-sass, gatsby-plugin-sass, or any SCSS pipeline file — the dart-sass migration is Phase 3 (UPGR-02) scope, not this plan. Do NOT change `.nvmrc` (stays `20` per D-05). Do NOT add any new npm package.

    Commit atomically: `git commit -m "fix(02-04): enforce Node 20 via engines + engine-strict + pre* guard (G-02-1, G-02-2)"` staging exactly package.json, .yarnrc, scripts/check-node-version.js.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && yarn install > /tmp/opencode/02-04-engines.log 2>&1; test $? -ne 0 && grep -qE "NODE_VERSION_MISMATCH|incompatible" /tmp/opencode/02-04-engines.log && nvm use 20 >/dev/null && yarn install && yarn build && yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"node": "20.x"' package.json` succeeds
    - `grep -qx 'engine-strict true' .yarnrc` succeeds
    - `grep -q '"preinstall": "node scripts/check-node-version.js"' package.json` succeeds (same for prebuild and predevelop)
    - Under Node 24: `yarn install` exits non-zero and its output contains `NODE_VERSION_MISMATCH` or an engine-incompatible error (the guard fires before any package work — node_modules untouched)
    - Under Node 20: `nvm use 20 && yarn install && yarn build && yarn test` exits 0
    - `git log -1 --format=%s` contains `02-04`
  </acceptance_criteria>
  <done>package.json declares engines.node 20.x with preinstall/prebuild/predevelop guards, .yarnrc enforces engine-strict, scripts/check-node-version.js blocks non-20 Node with an explicit NODE_VERSION_MISMATCH error, and the full Node 20 loop is green in one commit.</done>
  <reversibility rating="reversible">git revert removes the engines field, .yarnrc, and guard wiring; no dependency changes.</reversibility>
</task>

<task type="auto">
  <name>node-sass vendor hygiene: postinstall cleanup of poisoned bindings (G-02-2)</name>
  <files>scripts/clean-node-sass-vendor.js, package.json</files>
  <read_first>
    - .planning/debug/node-sass-binding-file-too-short.md (evidence: 9-byte HTTP-error body at node_modules/node-sass/vendor/linux-x64-137/binding.node; install.js writes non-2xx response bodies to disk)
    - node_modules/node-sass/lib/extensions.js getBinaryPath (vendor/linux-x64-{abi}/binding.node preferred; build/Release/binding.node fallback)
  </read_first>
  <action>
    Create `scripts/clean-node-sass-vendor.js`. Spec: vendorDir = `process.env.NODE_SASS_VENDOR_DIR || path.resolve(__dirname, '..', 'node_modules', 'node-sass', 'vendor')`; if vendorDir does not exist, exit 0 (nothing to clean). For every subdirectory of vendorDir containing a `binding.node`: read the first 4 bytes; if they are not the ELF magic bytes `0x7f 0x45 0x4c 0x46`, unlink the file and log `removed corrupt binding: <path>`. After cleanup, if neither `vendor/linux-x64-${process.versions.modules}/binding.node` nor `path.resolve(vendorDir, '..', 'build', 'Release', 'binding.node')` exists, print to stderr a message containing the token `NODE_SASS_BINARY_MISSING` (explain: no valid node-sass binary for the active Node; run `nvm use && yarn install`) and exit 1. Otherwise exit 0. No dependencies, no network, ~25 lines.

    Edit package.json scripts: add `"postinstall": "node scripts/clean-node-sass-vendor.js"`.

    Commit atomically: `git commit -m "fix(02-04): postinstall guard deletes poisoned node-sass vendor bindings (G-02-2)"` staging scripts/clean-node-sass-vendor.js and package.json.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 20 >/dev/null && mkdir -p node_modules/node-sass/vendor/linux-x64-137 && printf 'Not Found' > node_modules/node-sass/vendor/linux-x64-137/binding.node && node scripts/clean-node-sass-vendor.js && test ! -f node_modules/node-sass/vendor/linux-x64-137/binding.node && test -f node_modules/node-sass/vendor/linux-x64-115/binding.node && rm -rf /tmp/opencode/fake-vendor && mkdir -p /tmp/opencode/fake-vendor && NODE_SASS_VENDOR_DIR=/tmp/opencode/fake-vendor node scripts/clean-node-sass-vendor.js > /tmp/opencode/02-04-missing.log 2>&1; test $? -ne 0 && grep -q NODE_SASS_BINARY_MISSING /tmp/opencode/02-04-missing.log && yarn install && yarn build && yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/clean-node-sass-vendor.js` exits 0 under Node 20
    - A simulated poisoned binding (9-byte non-ELF file) is deleted — `test ! -f node_modules/node-sass/vendor/linux-x64-137/binding.node` succeeds
    - The valid `node_modules/node-sass/vendor/linux-x64-115/binding.node` (3.4MB ELF) survives — `test -f` succeeds
    - With an empty vendor dir (NODE_SASS_VENDOR_DIR override), the script exits non-zero and prints `NODE_SASS_BINARY_MISSING`
    - `grep -q '"postinstall": "node scripts/clean-node-sass-vendor.js"' package.json` succeeds
    - `nvm use 20 && yarn install && yarn build && yarn test` exits 0 (postinstall runs during install and stays green)
  </acceptance_criteria>
  <done>The poisoned 137 binding is gone from node_modules, the cleanup script is wired as postinstall so any future non-ELF binding is deleted on install, and the full Node 20 loop is green.</done>
  <reversibility rating="reversible">git revert removes the script and the postinstall wiring; node_modules is regenerable.</reversibility>
</task>

<task type="auto">
  <name>README: document enforced Node 20 + recovery command (G-02-1 prevention)</name>
  <files>README.md</files>
  <read_first>
    - README.md §Sviluppo locale (lines 21-39: prerequisites list, yarn-only warning)
  </read_first>
  <action>
    Edit README.md §Sviluppo locale: after the `- Node.js 20 (con nvm: nvm use legge .nvmrc)` bullet, add an Italian note stating the project enforces Node 20 automatically — `yarn install`, `yarn build`, and `yarn develop` fail with an explicit error under any other version (engines + engine-strict + scripts/check-node-version.js) — and the recovery command is `nvm use && yarn install`. Optionally mention `nvm alias default 20` as a convenience for nvm users. Keep the existing yarn-only warning line unchanged. Do not introduce any starter-boilerplate token (no npm-lockfile mention, no English boilerplate).

    Commit: `git commit -m "docs(02-04): document enforced Node 20 and recovery command (G-02-1)"` staging README.md.
  </action>
  <verify>
    <automated>grep -q "nvm use && yarn install" README.md && grep -q "check-node-version" README.md && ! rg -iq 'stackrole|gatsby-starter-foundation|Deploy to Netlify|twitter-header|screenshot.png|package-lock.json|Google Analytics|UA-|pensive-engelbart' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README contains the recovery command `nvm use && yarn install`
    - README mentions the guard script (check-node-version)
    - Forbidden-token grep exits 1 (zero matches) — README stays clean Italian
  </acceptance_criteria>
  <done>README documents the enforced Node 20 contract and the one-command recovery, with zero starter tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell → yarn install/build | Node version crosses here; enforcement blocks non-20 Node before any package work |
| node-sass install.js → vendor/ | untrusted HTTP response body crosses here; postinstall cleanup deletes non-ELF files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | DoS | yarn install/build under Node 24 | high | mitigate | engines.node 20.x + engine-strict true + preinstall/prebuild/predevelop guard (scripts/check-node-version.js) — fails loudly before node-sass can corrupt node_modules |
| T-02-05 | Tampering | node-sass install.js writing non-2xx HTTP bodies to vendor/binding.node | medium | mitigate | postinstall scripts/clean-node-sass-vendor.js deletes any non-ELF binding on every install; NODE_SASS_BINARY_MISSING error if no valid binary remains |
| T-02-06 | Repudiation | silent corruption masking the real incompatibility | low | accept | guard + cleanup now fail loudly with distinct tokens (NODE_VERSION_MISMATCH, NODE_SASS_BINARY_MISSING); full dart-sass replacement lands in Phase 3 |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed — only two dependency-free repo scripts and config fields |
</threat_model>

<verification>
- `nvm use 20 && yarn install && yarn build && yarn test` exits 0 (the phase's real gate, green again)
- `yarn install` under Node 24 exits non-zero with NODE_VERSION_MISMATCH or engine-incompatible error
- Poisoned vendor binding deleted by postinstall; valid ABI-115 binding intact
- `git log` shows the three 02-04 commits
</verification>

<success_criteria>
- G-02-1 closed: install under Node 20 succeeds; install under Node 24 fails loudly with an explicit error (no silent node-gyp/distutils crash path)
- G-02-2 closed: build under Node 20 succeeds; poisoned non-ELF bindings are deleted automatically on install
- Phase 02 truth restored: one Node version, enforced — .nvmrc (20) is now backed by engines + engine-strict + guard scripts
- No Phase 3 scope taken: node-sass still in package.json; dart-sass migration untouched
</success_criteria>

<output>
Create `.planning/phases/02-foundation-cleanup/02-04-SUMMARY.md` when done
</output>
