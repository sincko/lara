---
phase: 02-foundation-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - netlify.toml
  - src/components/old-form.js
  - src/components/form-pulito.js
  - package.json
  - yarn.lock
autonomous: true
requirements:
  - FNDT-02
  - FNDT-03
  - FNDT-04
user_setup: []

must_haves:
  truths:
    - "D-04: `netlify.toml` has no `NODE_VERSION` key anywhere; the `[build.environment]` section is either deleted entirely or empty-valid TOML"
    - "D-05: `.nvmrc` content is `20` (unchanged — single source of truth)"
    - "D-06: `src/components/old-form.js` and `src/components/form-pulito.js` do not exist; `rg -n 'old-form|form-pulito' src/ gatsby-*.js` returns zero matches"
    - "D-07: `package.json` no longer lists any of: codemirror, seamless-immutable, gatsby-background-image, package-doctor (runtime group); redux, react-refresh, typescript, acorn (devDep group)"
    - "D-09: `netlify-cms-lib-widgets` removed from devDependencies; transitive survival via netlify-cms-app verified with `yarn why`"
    - "D-08/D-13: `prismjs` and `@testing-library/dom` remain in package.json (research-proven keepers — NOT in any removal group)"
    - "D-10: `nvm use 20 && yarn install && yarn build && yarn test` exits 0 after each removal group commit"
  artifacts:
    - "netlify.toml without NODE_VERSION (or without [build.environment] if emptied)"
    - "src/components/old-form.js deleted"
    - "src/components/form-pulito.js deleted"
    - "package.json with 9 dependencies removed across 2 logical groups"
  key_links:
    - ".nvmrc is the single Node version source → Netlify honors it per official docs (overrides UI setting)"
    - "Each `yarn remove` atomically updates package.json + yarn.lock so the single-lockfile state from Plan 01 stays consistent"
---

<objective>
Remove the stale Node pin and dead source components, then sweep the remaining unused dependencies in verified groups. This plan delivers FNDT-02 (unambiguous Node version), FNDT-03 (dead components gone), and the remainder of FNDT-04 (9 deps removed, each pre-verified with `yarn why`, post-verified with build+test, one logical group per commit per D-10/Pitfall 10).

Purpose: Plan 01 proved the single-lockfile yarn build path. This plan runs on that proven baseline to finish the repo-state cleanup: the Node version becomes unambiguous (`.nvmrc` only), the confusing dead form files are gone, and the manifest carries only deps the build actually uses.
Output: Three atomic commits (Node config + dead components; runtime dep group; devDep dep group), each verified green.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundation-cleanup/02-CONTEXT.md
@.planning/phases/02-foundation-cleanup/02-RESEARCH.md
@.planning/phases/02-foundation-cleanup/02-01-SUMMARY.md
@netlify.toml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Node config unambiguous + dead components deleted (D-04, D-05, D-06)</name>
  <files>netlify.toml, src/components/old-form.js, src/components/form-pulito.js</files>
  <read_first>
    - netlify.toml (after Plan 01: `[build] command = "yarn build"`; `[build.environment] NODE_VERSION = "10"` still present — this task deletes that)
    - .nvmrc (confirms content is `20` — this task does NOT modify it; D-05 keeps Node 20 for the milestone)
    - .planning/phases/02-foundation-cleanup/02-RESEARCH.md §"Code Examples" Example 1 (netlify.toml before→after) and Pitfall 4 (delete whole `[build.environment]` section to avoid invalid TOML)
    - src/components/old-form.js and src/components/form-pulito.js (confirm they are the dead files; the live form is src/components/formik.js — do NOT touch formik.js)
    - .planning/codebase/CONCERNS.md §"Dead components" (confirms form-pulito.js is syntactically invalid as a module; zero imports anywhere)
  </read_first>
  <action>
    Pre-verify dead components have zero references (D-06 requires grep before delete):
    - `nvm use 20` first.
    - `rg -n 'old-form|form-pulito' src/ gatsby-config.js gatsby-node.js gatsby-browser.js` — MUST return zero matches (research Example 3 verified this 2026-08-19; re-verify now in case of drift). If ANY match appears, STOP and report — do not delete.

    Delete the stale Node pin (D-04):
    - Edit netlify.toml: delete the entire `[build.environment]` section (lines 5-6: the section header and `NODE_VERSION = "10"`). Per Pitfall 4, deleting the whole section is correct because it contains only NODE_VERSION. The resulting file must have `[build]` (with `publish` and `command = "yarn build"`) then `[[plugins]]` — valid TOML with no empty section.
    - Do NOT modify `.nvmrc` (D-05: keep at Node 20; the Node 22 bump is deferred to Phase 3 after dart-sass replaces node-sass).

    Delete the dead components (D-06):
    - `git rm src/components/old-form.js src/components/form-pulito.js`

    Post-verify: `nvm use 20 && yarn build && yarn test` — green (no deps changed, so no `yarn install` needed, but run it anyway to be safe). Confirm `rg -n 'old-form|form-pulito' src/ gatsby-*.js` now exits 1 (no matches).

    Commit atomically: `git commit -m "chore(02-02): remove stale NODE_VERSION pin, delete dead form components (FNDT-02, FNDT-03, D-04..D-06)"`. Stage: netlify.toml, src/components/old-form.js (deletion), src/components/form-pulito.js (deletion).
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 20 && ! grep -q 'NODE_VERSION' netlify.toml && grep -xq '20' .nvmrc && test ! -f src/components/old-form.js && test ! -f src/components/form-pulito.js && ! rg -q 'old-form|form-pulito' src/ gatsby-config.js gatsby-node.js gatsby-browser.js && yarn install && yarn build && yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'NODE_VERSION' netlify.toml` fails (key gone)
    - `grep -xq '20' .nvmrc` succeeds (unchanged — single source of truth)
    - `test ! -f src/components/old-form.js` succeeds (file deleted)
    - `test ! -f src/components/form-pulito.js` succeeds (file deleted)
    - `rg -q 'old-form|form-pulito' src/ gatsby-config.js gatsby-node.js gatsby-browser.js` exits 1 (zero references)
    - `yarn build` exits 0 under Node 20
    - `yarn test` exits 0
    - netlify.toml parses as valid TOML (no empty `[build.environment]` dangling — the whole section is removed)
    - `git log -1 --format=%s` contains `02-02` and `FNDT-02`
  </acceptance_criteria>
  <done>`netlify.toml` has no NODE_VERSION (`.nvmrc` is the sole Node source), both dead form files are deleted with zero remaining references, and the build+test loop is green in one atomic commit.</done>
  <reversibility rating="reversible">git revert restores the files and the NODE_VERSION line.</reversibility>
</task>

<task type="auto">
  <name>Remove unused runtime dependencies — verified group (D-07 runtime subset, D-10)</name>
  <files>package.json, yarn.lock</files>
  <read_first>
    - package.json (confirm `codemirror` line 26, `seamless-immutable` line 56, `gatsby-background-image` line 30, `package-doctor` line 50 are in `dependencies`; confirm `prismjs` line 51 is ALSO in dependencies but MUST STAY — D-08 resolution: peer of gatsby-remark-prismjs, yarn 1 does not auto-install peers)
    - .planning/phases/02-foundation-cleanup/02-RESEARCH.md §"Packages to REMOVE" table (codemirror: direct-only, netlify-cms-app has own nested v5; seamless-immutable: direct-only; gatsby-background-image: direct-only; package-doctor: npm-audit hack remnant git a66d212) and Pitfall 1 (prismjs MUST NOT be removed — empirically proven build break)
    - gatsby-config.js (confirm none of codemirror/seamless-immutable/gatsby-background-image/package-doctor are referenced in the plugin list — research verified this but re-read to be certain)
  </read_first>
  <action>
    Pre-verify each package with `yarn why` BEFORE removal (D-10 — the entire risk profile of this phase is "delete something still needed"):
    - `nvm use 20` first.
    - `yarn why codemirror` — expect "specified in dependencies" only (netlify-cms-app keeps its own nested codemirror@5.65.20).
    - `yarn why seamless-immutable` — expect "specified in dependencies" only.
    - `yarn why gatsby-background-image` — expect "specified in dependencies" only.
    - `yarn why package-doctor` — expect "specified in dependencies" only.
    - If any `yarn why` shows a runtime consumer other than the direct entry, STOP and report — do not remove that package.

    Remove as one logical group (research Commit 3a):
    - `yarn remove codemirror seamless-immutable gatsby-background-image package-doctor` (atomic package.json + yarn.lock update; `yarn remove` not hand-editing per "Don't Hand-Roll").

    Post-verify (D-10): `nvm use 20 && yarn install && yarn build && yarn test` — all exit 0. `yarn build` green confirms gatsby-config.js plugin list still resolves (none of these were referenced). `yarn test` green confirms the regression net is intact.

    Commit atomically: `git commit -m "chore(02-02): remove unused runtime deps codemirror seamless-immutable gatsby-background-image package-doctor (FNDT-04, D-07, D-10)"`. Stage: package.json, yarn.lock.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 20 && ! grep -q '"codemirror"' package.json && ! grep -q '"seamless-immutable"' package.json && ! grep -q '"gatsby-background-image"' package.json && ! grep -q '"package-doctor"' package.json && grep -q '"prismjs"' package.json && yarn install && yarn build && yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"codemirror"' package.json` fails
    - `grep -q '"seamless-immutable"' package.json` fails
    - `grep -q '"gatsby-background-image"' package.json` fails
    - `grep -q '"package-doctor"' package.json` fails
    - `grep -q '"prismjs"' package.json` succeeds (KEEP — peer of gatsby-remark-prismjs, D-08)
    - `grep -q '"@testing-library/dom"' package.json` succeeds (KEEP — peer of RTL, not in this group)
    - `yarn install` exits 0
    - `yarn build` exits 0 under Node 20
    - `yarn test` exits 0
    - `git log -1 --format=%s` contains `02-02` and `FNDT-04`
  </acceptance_criteria>
  <done>Four unused runtime deps are gone from package.json, prismjs is confirmed still present, and build+test is green in one atomic commit.</done>
  <reversibility rating="reversible">git revert restores the four dependency entries.</reversibility>
</task>

<task type="auto">
  <name>Remove unused devDependencies — verified group (D-07 devDep subset, D-09, D-10)</name>
  <files>package.json, yarn.lock</files>
  <read_first>
    - package.json (confirm `redux` line 74, `react-refresh` line 73, `typescript` line 75, `acorn` line 65, `netlify-cms-lib-widgets` line 71 are in `devDependencies`; confirm `@testing-library/dom` line 62 is ALSO a devDep but MUST STAY — peer of @testing-library/react@16.3.2, Phase 1 hit this exact failure)
    - .planning/phases/02-foundation-cleanup/02-RESEARCH.md §"Packages to REMOVE" table (redux: gatsby keeps nested v4.2.1; react-refresh: gatsby keeps nested 0.14.2; typescript: no tsconfig.json, no .ts files; acorn: hoisted from gatsby#webpack, transitive stays; netlify-cms-lib-widgets: netlify-cms-app@2.15.72 depends on it ^1.8.1 transitively — D-09) and the @testing-library/dom row (KEEP — D-13/Phase 1 evidence)
    - .planning/phases/01-test-scaffolding-performance-baseline/01-01-SUMMARY.md §"Auto-fixed Issues" item 1 (confirms @testing-library/dom was added explicitly because yarn 1 does not auto-install RTL 16 peers — removing it breaks `yarn test`)
  </read_first>
  <action>
    Pre-verify each package with `yarn why` BEFORE removal (D-10):
    - `nvm use 20` first.
    - `yarn why redux` — expect direct devDep only (gatsby keeps nested redux@4.2.1).
    - `yarn why react-refresh` — expect direct devDep only (gatsby keeps nested 0.14.2).
    - `yarn why typescript` — expect direct devDep only (no tsconfig.json, no .ts files in repo).
    - `yarn why acorn` — expect hoisted from gatsby#webpack#acorn etc.; direct entry removable, transitive stays.
    - `yarn why netlify-cms-lib-widgets` — expect direct devDep; netlify-cms-app@2.15.72 keeps it transitively (`^1.8.1`). D-09: verify this transitive survives after removal.
    - `yarn why @testing-library/dom` — confirm it is a peer of @testing-library/react@16.3.2 (this is the check that proves it MUST STAY; do NOT remove it).
    - If any `yarn why` (for the 5 removal targets) shows a runtime consumer, STOP and report.

    Remove as one logical group (research Commit 3b):
    - `yarn remove --dev redux react-refresh typescript acorn netlify-cms-lib-widgets` (atomic package.json + yarn.lock update).

    Post-verify (D-10): `nvm use 20 && yarn install && yarn build && yarn test` — all exit 0. CRITICAL: `yarn test` green proves @testing-library/dom still satisfies the RTL peer (it was NOT removed). Additionally run `yarn why netlify-cms-lib-widgets` after removal to confirm the transitive copy via netlify-cms-app survives (D-09 verification — expect "info Found netlify-cms-lib-widgets" with netlify-cms-app as the reason).

    Commit atomically: `git commit -m "chore(02-02): remove unused devDeps redux react-refresh typescript acorn netlify-cms-lib-widgets (FNDT-04, D-07, D-09, D-10)"`. Stage: package.json, yarn.lock.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 20 && ! grep -q '"redux"' package.json && ! grep -q '"react-refresh"' package.json && ! grep -q '"typescript"' package.json && ! grep -q '"acorn"' package.json && ! grep -q '"netlify-cms-lib-widgets"' package.json && grep -q '"@testing-library/dom"' package.json && yarn install && yarn build && yarn test && yarn why netlify-cms-lib-widgets 2>&1 | grep -q 'netlify-cms-app'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"redux"' package.json` fails
    - `grep -q '"react-refresh"' package.json` fails
    - `grep -q '"typescript"' package.json` fails
    - `grep -q '"acorn"' package.json` fails
    - `grep -q '"netlify-cms-lib-widgets"' package.json` fails
    - `grep -q '"@testing-library/dom"' package.json` succeeds (KEEP — peer of RTL)
    - `grep -q '"prismjs"' package.json` succeeds (KEEP — peer of gatsby-remark-prismjs)
    - `yarn install` exits 0
    - `yarn build` exits 0 under Node 20
    - `yarn test` exits 0 (proves @testing-library/dom peer still satisfied)
    - `yarn why netlify-cms-lib-widgets` shows netlify-cms-app as a transitive consumer (D-09)
    - `git log -1 --format=%s` contains `02-02` and `FNDT-04`
  </acceptance_criteria>
  <done>Five unused devDeps are gone, @testing-library/dom and prismjs confirmed still present, netlify-cms-lib-widgets transitive copy confirmed surviving, and build+test is green in one atomic commit.</done>
  <reversibility rating="reversible">git revert restores the five devDependency entries.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo config → Netlify build image | `.nvmrc` + netlify.toml cross here; removing NODE_VERSION makes .nvmrc the sole Node source |
| manifest → dependency resolution | `yarn remove` rewrites package.json + yarn.lock atomically; hand-editing would create drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | DoS | Removing prismjs breaks build on first code block | high | mitigate | D-08 resolution: KEEP prismjs; this plan's acceptance criteria assert `"prismjs"` remains in package.json after every group |
| T-02-05 | DoS | Removing @testing-library/dom breaks `yarn test` | high | mitigate | Phase 1 evidence: yarn 1 no peer auto-install; this plan's acceptance criteria assert `"@testing-library/dom"` remains; `yarn test` green is the gate |
| T-02-06 | DoS | netlify.toml left in invalid TOML after NODE_VERSION deletion | medium | mitigate | Pitfall 4: delete the whole `[build.environment]` section (contains only NODE_VERSION); acceptance criteria grep for NODE_VERSION absence + build green |
| T-02-07 | DoS | Building under Node 24 → node-sass compile failure | high | mitigate | Every command opens with `nvm use 20` (Pitfall 2) |
| T-02-08 | Tampering | Dead component still imported somewhere (drift since research) | medium | mitigate | D-06: re-grep `rg -n 'old-form\|form-pulito' src/ gatsby-*.js` before delete; STOP if any match |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed; only verified removals (research Package Legitimacy Audit: all removal targets OK) |
</threat_model>

<verification>
- After all three commits: `nvm use 20 && yarn install && yarn build && yarn test` exits 0
- `! grep -q 'NODE_VERSION' netlify.toml`
- `rg -q 'old-form|form-pulito' src/ gatsby-*.js` exits 1 (no matches)
- `grep -q '"prismjs"' package.json` succeeds (keeper)
- `grep -q '"@testing-library/dom"' package.json` succeeds (keeper)
- None of the 9 removed packages appear in package.json
- `git log --oneline -3` shows three `02-02` commits (Node+components, runtime deps, devDeps)
</verification>

<success_criteria>
- FNDT-02 satisfied: netlify.toml has no NODE_VERSION; .nvmrc (20) is the single source of truth
- FNDT-03 satisfied: old-form.js and form-pulito.js deleted, zero references
- FNDT-04 satisfied (remainder): 9 unused deps removed (4 runtime + 5 devDep), each pre-verified with `yarn why`, post-verified with build+test; prismjs and @testing-library/dom confirmed kept
- D-04, D-05 (honor), D-06, D-07 (remainder), D-09, D-10 implemented
- Three atomic commits on disk
</success_criteria>

<output>
Create `.planning/phases/02-foundation-cleanup/02-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan produces no new symbols — it is a removal/config plan. Artifacts are state changes:
- **Config change:** `netlify.toml` `[build.environment]` section (containing `NODE_VERSION = "10"`) deleted; `.nvmrc` (content `20`) becomes the sole Node version source
- **Removed files:** `src/components/old-form.js`, `src/components/form-pulito.js` (git-tracked → deleted)
- **Manifest change (runtime group):** `package.json` `dependencies` entries `codemirror`, `seamless-immutable`, `gatsby-background-image`, `package-doctor` removed
- **Manifest change (devDep group):** `package.json` `devDependencies` entries `redux`, `react-refresh`, `typescript`, `acorn`, `netlify-cms-lib-widgets` removed
- **Confirmed keepers:** `prismjs` (dependencies), `@testing-library/dom` (devDependencies) — asserted present in every acceptance criteria
- **Proven state:** 9 unused deps gone, build+test green under Node 20, transitive netlify-cms-lib-widgets via netlify-cms-app confirmed surviving