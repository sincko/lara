---
phase: 01-test-scaffolding-performance-baseline
plan: 04
type: execute
wave: 2
depends_on: [01-03]
files_modified:
  - .planning/baseline/BASELINE.md
  - .planning/baseline/lighthouse/*.json
  - .planning/baseline/psi/*.json
autonomous: false
requirements: [FNDT-06]
user_setup: []

must_haves:
  truths:
    - "The full baseline (3 URLs × 3 runs × Lighthouse CLI; PSI with 429-tolerant fallback) is captured on the live site BEFORE any dependency or code change (ROADMAP success criterion 3)"
    - "BASELINE.md records median LCP/CLS/INP per URL per source, tool versions, capture dates, git commit SHA, and the canonical URL set (D-09)"
    - "Phase 6 can reproduce the identical measurement recipe from the stored artifacts"
  artifacts:
    - .planning/baseline/BASELINE.md
    - .planning/baseline/lighthouse/{home,blog,post-minnie}-{1,2,3}.json
    - .planning/baseline/psi/*.json (or per-run lighthouse-fallback markers when 429 persists)
  key_links:
    - capture-baseline.js ↔ the raw JSON artifacts (written at capture time)
    - median.js ↔ BASELINE.md (table comes from the script output)
    - BASELINE.md ↔ Phase 6 PERF-04 comparison

---

<objective>
Run the full FNDT-06 baseline capture on the live laryart.it site (3 canonical URLs × 3 runs, mobile, Lighthouse CLI + PSI-with-fallback), then write `BASELINE.md` with the median LCP/CLS/INP per URL per source plus methodology metadata.

Purpose: This is the expansion run over Plan 03's proven recipe. It produces the actual baseline artifact Phase 6 (PERF-04) compares against — captured before any dependency or code change.

Output: `.planning/baseline/` fully populated: raw JSONs + BASELINE.md with medians.

**Checkpoint note:** Plan 03's README records whether the PSI anonymous quota (HTTP 429 observed 2026-08-18) recovered. This plan's checkpoint gates the capture start so the owner can decide: provide a `PSI_API_KEY` (raises quota; env var only, never committed) or accept the documented Lighthouse-CLI fallback per metric (research Pitfall 9).
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-scaffolding-performance-baseline/01-RESEARCH.md
@.planning/phases/01-test-scaffolding-performance-baseline/01-CONTEXT.md
@.planning/phases/01-test-scaffolding-performance-baseline/01-VALIDATION.md
@.planning/baseline/README.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 1: Confirm baseline capture prerequisites — live site reachable, PSI quota status decided, Node 24 in use</name>
  <what-built>The baseline capture script from plan 03 (capture-baseline.js) — the PSI half needs a source decision before the full run starts.</what-built>
  <how-to-verify>
    Before running the full capture, verify on the shell: (1) `node --version` reports >= 22.19 (nvm default Node 24 — NOT .nvmrc Node 20); (2) `curl -sI https://laryart.it/` returns HTTP/1.1 200; (3) `ls .planning/baseline/lighthouse/home-1.json` exists (tracer proof from plan 03). Then decide the PSI source per the options below.
  </how-to-verify>
  <decision>
    How to source the PSI half of the baseline given the anonymous quota 429 (observed 2026-08-18): provide a free PSI_API_KEY, or use the documented Lighthouse-CLI fallback per metric?
  </decision>
  <context>
    D-08 locks "Lighthouse CLI (local) + PageSpeed Insights (live site), median of 3, mobile". PSI v5 works without a key for low volume, but the shared anonymous quota returned HTTP 429 on 2026-08-18 (verified live in research). The capture script (plan 03) implements retry/backoff and per-metric fallback to Lighthouse CLI against the live URL — baseline completeness is unaffected either way; the choice determines whether the `psi/` artifacts contain real PSI JSON or fallback markers. Also verify: `curl -I https://laryart.it/` returns 200, and the shell uses the nvm default Node 24 (`.nvmrc` pins 20 — `node --version` must be >= 22.19 for lighthouse 13).
  </decision>
  <options>
    <option id="psi-key">
      <name>Provide PSI_API_KEY</name>
      <pros>True PSI API data; unblocks the full D-08 "Lighthouse + PSI" pairing; Phase 6 re-verification reuses the same key path</pros>
      <cons>Requires a Google Cloud console visit (~2 min); the key must be exported in the shell env for the capture run</cons>
    </option>
    <option id="psi-fallback">
      <name>Accept Lighthouse-CLI fallback</name>
      <pros>No external signup; same lab metrics (LCP/CLS/INP, mobile) measured by the same lighthouse engine against the live URL; reproducible</pros>
      <cons>psi/ artifacts will carry "lighthouse-fallback" provenance markers instead of raw PSI responses — D-08's dual-source pairing becomes single-source with a documented fallback</cons>
    </option>
  </options>
  <resume-signal>Select psi-key (then export PSI_API_KEY=...) or psi-fallback — or describe a third option.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run the full 3-URL × 3-run × 2-source capture and write BASELINE.md</name>
  <files>
    - .planning/baseline/BASELINE.md
    - .planning/baseline/lighthouse/{home,blog,post-minnie}-{1,2,3}.json
    - .planning/baseline/psi/{home,blog,post-minnie}-{1,2,3}.json
  </files>
  <read_first>
    - .planning/baseline/capture-baseline.js (the recipe this task executes — do not modify its behavior here; the URLS constant already carries the verified post slug `/minnie` from plan 03 — no correction expected at execution)
    - .planning/baseline/median.js (the table builder this task's BASELINE.md consumes)
    - .planning/phases/01-test-scaffolding-performance-baseline/01-RESEARCH.md Pitfall 10 (record tool versions, dates, commit SHA — apples-to-apples for Phase 6)
  </read_first>
  <action>
    1. Ensure prerequisites from the checkpoint: shell Node >= 22.19 (`node --version`; if the shell resolves .nvmrc Node 20, run under the nvm default Node 24), live site 200 (`curl -sI https://laryart.it/`), and either `PSI_API_KEY` exported (psi-key choice) or the documented fallback accepted.
    2. Run `node .planning/baseline/capture-baseline.js` — full capture: 3 URLs × 3 runs × 2 sources ≈ 20+ min (lighthouse ~1 min/run + PSI ~10s/run + sleeps). Expect the script's per-run summary lines; PSI runs may 429 and fall back per the script's branch (markers recorded; never a hard failure).
    3. Run `node .planning/baseline/median.js` — must print the full table (3 URLs × 2 sources).
    4. Write `.planning/baseline/BASELINE.md` (Write tool; this is the canonical Phase 6 artifact):
       - Header: capture date, git commit SHA (`git rev-parse --short HEAD`), Node/chrome versions used
       - Table per URL: source × LCP ms / CLS / INP ms / perf score medians (paste the median.js output)
       - Method: "Lighthouse 13.4.1 CLI, --form-factor=mobile, default throttling, --only-categories=performance, median of 3 runs; PSI v5 strategy=mobile, median of 3 (or per-run fallback markers if 429)"
        - URL set with the post slug actually used (`/minnie/` — canonical URL, verified 301 → `/minnie/` at planning time; re-confirm with `curl -sI https://laryart.it/minnie/` returns 200 before writing), the redirect note (`/blog` → `/blog/` canonical), and the exact command lines to rerun for Phase 6
       - If PSI fell back: explicitly record which URLs/runs have fallback markers so Phase 6 compares like-for-like
    5. Commit the full `.planning/baseline/` (JSON + scripts + README + BASELINE.md) — `git add .planning/baseline && git commit -m "feat(phase-1): capture performance baseline (Lighthouse + PSI, median 3, mobile)"` (FNDT-06 storage; D-09)
  </action>
  <verify>
    <automated>node .planning/baseline/median.js</automated>
    <automated>test -f .planning/baseline/BASELINE.md && grep -q "laryart.it" .planning/baseline/BASELINE.md && grep -q "13.4.1" .planning/baseline/BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - 9 lighthouse JSONs + 9 psi JSONs (or fallback-marked psi artifacts) exist under .planning/baseline/, one set per URL
    - `node .planning/baseline/median.js` exits 0 and prints a full 3-URL table
    - BASELINE.md contains: the median table, the exact lighthouse version (13.4.1), the URL set with slugs, the mobile profile, dates, and the commit SHA
    - The baseline artifacts are committed to git
  </acceptance_criteria>
  <done>The baseline is captured, stored, documented, and committed — Phase 6 (PERF-04) has a like-for-like comparison target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → PSI API | 3 URLs hardcoded; optional PSI_API_KEY env (never committed/logged) |
| artifacts ↔ git | .planning/baseline/ committed with the full history as integrity record |
| capture run ↔ live site | measuring production laryart.it — read-only HTTP GETs + HEAD; no writes to the site |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-01 | Information disclosure | PSI_API_KEY in shell env / capture logs | high | mitigate | Key passed via env var only; script never prints it; BASELINE.md never records it; if the key leaks into the shell history, note it and rotate (documented in README) |
| T-4-02 | Tampering | BASELINE.md numbers | medium | mitigate | numbers in BASELINE.md come from `median.js` stdout (paste, don't retype); the raw JSONs committed alongside make the table auditable |
| T-4-03 | Spoofing | npx lighthouse@13.4.1 pin drift | low | accept | exact version pinned at npx time; no repo package entry; Phase 6 reruns the same pin |
| T-4-SC | Tampering | npx/lighthouse tarball | low | accept | registry-audited (research), no postinstall |
</threat_model>

<verification>
- `node .planning/baseline/median.js` exit 0
- `test -f .planning/baseline/BASELINE.md` + grep for URL + version (see verify)
- git status clean after the commit
</verification>

<success_criteria>
- [ ] 3 URLs × 3 runs × 2 sources captured (or documented fallback) in `.planning/baseline/`
- [ ] BASELINE.md: medians table + methodology + commit SHA
- [ ] `.planning/baseline/` committed
- [ ] ROADMAP Phase 1 success criterion 3 + 4 satisfied
</success_criteria>

## Artifacts this phase produces

**Files:**
- `.planning/baseline/BASELINE.md` — the comparison contract for Phase 6
- `.planning/baseline/lighthouse/{home,blog,post-minnie}-{1..3}.json` (raw LHR, full JSON)
- `.planning/baseline/psi/{home,blog,post-minnie}-{1..3}.json` (PSI responses or `lighthouse-fallback` markers)

**Contract keys:**
- `BASELINE.md` sections: `## Baseline` (table), `## Method`, `## URL set`, `## Re-run`
- env var `PSI_API_KEY` (opt-in, never committed)
</output>
