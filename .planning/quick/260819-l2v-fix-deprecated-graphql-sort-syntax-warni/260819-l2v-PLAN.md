---
phase: quick-260819-l2v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gatsby-node.js
  - src/components/blog-list-home.js
  - src/templates/blog-list.js
autonomous: true
requirements:
  - QUICK-260819-l2v
user_setup: []

must_haves:
  truths:
    - "yarn build and yarn develop emit zero 'Deprecated syntax of sort' warnings"
    - "yarn test stays green"
    - "Blog list ordering behavior is unchanged (DESC by frontmatter date)"
  artifacts:
    - gatsby-node.js
    - src/components/blog-list-home.js
    - src/templates/blog-list.js
  key_links:
    - "sort argument syntax in all three GraphQL queries matches Gatsby 5 nested sort syntax"
---

<objective>
Migrate three GraphQL queries from the deprecated Gatsby sort syntax
`sort: { order: DESC, fields: [frontmatter___date] }` to the Gatsby 5 nested
syntax `sort: { frontmatter: { date: DESC } }`, eliminating the
"Deprecated syntax of sort and/or aggregation field arguments" build warnings.

Purpose: Remove deprecation warnings from `yarn build` / `yarn develop` output.
Gatsby already auto-converts these queries at runtime, so this is a mechanical
syntax migration with identical behavior — no functional change.

Output: Three files with updated sort syntax; clean build logs; green tests.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Source files containing the deprecated syntax (exact lines):
#   gatsby-node.js:13
#   src/components/blog-list-home.js:31
#   src/templates/blog-list.js:12
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate sort syntax in all three GraphQL queries</name>
  <files>gatsby-node.js, src/components/blog-list-home.js, src/templates/blog-list.js</files>
  <action>
    In each of the three files, replace the deprecated sort argument
    `sort: { order: DESC, fields: [frontmatter___date] }` with the Gatsby 5
    nested syntax `sort: { frontmatter: { date: DESC } }`:

    1. gatsby-node.js line 13 — inside the createPages `allMarkdownRemark(...)` query.
    2. src/components/blog-list-home.js line 31 — inside the StaticQuery `allMarkdownRemark(...)` query.
    3. src/templates/blog-list.js line 12 — inside the `blogListQuery` page query.

    Change ONLY the sort argument. Do NOT touch:
    - `fluid`/`fixed` image resolvers (gatsby-plugin-image migration is Phase 5 scope)
    - the Browserslist caniuse-lite warning (separate concern)
    - any filter/limit/skip arguments or field selections

    Preserve exact indentation and surrounding query structure so the diff is
    minimal and reviewable.
  </action>
  <verify>
    <automated>grep -rn "fields: \[frontmatter___date\]" gatsby-node.js src/components/blog-list-home.js src/templates/blog-list.js; test $? -eq 1 && grep -rn "sort: { frontmatter: { date: DESC } }" gatsby-node.js src/components/blog-list-home.js src/templates/blog-list.js | wc -l | grep -q 3</automated>
  </verify>
  <done>
    All three files use `sort: { frontmatter: { date: DESC } }`; zero occurrences of
    the deprecated `fields: [frontmatter___date]` syntax remain in the repo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify clean build and green tests</name>
  <files>gatsby-node.js, src/components/blog-list-home.js, src/templates/blog-list.js</files>
  <action>
    Run the full verification suite to prove the migration is complete and
    behavior is unchanged:

    1. Build: `source ~/.nvm/nvm.sh && nvm use 20 && yarn build`
       (node is not on PATH in this environment — sourcing nvm first is required;
       fallback: /home/simos/.nvm/versions/node/v24.18.0/bin/node on PATH)
    2. Capture build output and confirm ZERO occurrences of
       "Deprecated syntax of sort" (the caniuse-lite Browserslist warning may
       still appear — that is expected and out of scope).
    3. Tests: `source ~/.nvm/nvm.sh && nvm use 20 && yarn test` — must stay green.

    If any "Deprecated syntax of sort" warning remains, grep the repo for
    `fields: [frontmatter___` and fix any missed occurrence, then re-run the build.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 20 && yarn build 2>&1 | tee /tmp/opencode/l2v-build.log; ! grep -q "Deprecated syntax of sort" /tmp/opencode/l2v-build.log && source ~/.nvm/nvm.sh && nvm use 20 && yarn test</automated>
  </verify>
  <done>
    `yarn build` completes with zero "Deprecated syntax of sort" warnings and
    `yarn test` passes with no new failures.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Mechanical GraphQL query syntax change; no new input surfaces, no runtime code paths added |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-l2v-01 | Tampering | GraphQL sort argument in 3 queries | low | mitigate | Syntax-only change; behavior verified identical via full `yarn build` + `yarn test` in Task 2 |
| T-l2v-SC | Tampering | npm/yarn installs | high | mitigate | No new packages installed in this plan; existing lockfile untouched |
</threat_model>

<verification>
- `grep -rn "fields: \[frontmatter___date\]"` across the repo returns nothing
- `yarn build` log contains zero "Deprecated syntax of sort" lines
- `yarn test` exits 0
</verification>

<success_criteria>
- All three queries use Gatsby 5 nested sort syntax
- Build and develop logs are free of the deprecated-sort warning
- Test suite remains green
</success_criteria>

<output>
Create `.planning/quick/260819-l2v-fix-deprecated-graphql-sort-syntax-warni/260819-l2v-SUMMARY.md` when done
</output>
