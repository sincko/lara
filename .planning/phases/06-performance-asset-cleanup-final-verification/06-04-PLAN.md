---
phase: 06-performance-asset-cleanup-final-verification
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - .planning/baseline/BASELINE.md
  - .planning/baseline/lighthouse/*.json
  - .planning/baseline/psi/*.json
  - baseline-tooling.test.js
autonomous: false
requirements:
  - PERF-04
user_setup: []

must_haves:
  truths:
    - "The final capture runs against the LIVE site (https://laryart.it) with the IDENTICAL Phase 1 recipe — capture-baseline.js + median.js reused as-is, Lighthouse 13.4.1 pin, --form-factor=mobile, default throttling, same 3-URL set (/, /blog/, /minnie/), median of 3 (D-14, D-17)"
    - "The capture runs ONLY after the owner deploys the full phase to Netlify — gated by a blocking manual checkpoint (D-16; Pitfall 4: a pre-deploy capture compares stale state and reports the baseline)"
    - "The comparison is median vs median per source (lighthouse vs lighthouse) against the BASELINE.md table — targets: LCP ≤ 2.5s, CLS ≤ 0.1 at the 75th percentile, all IMPROVED vs the Phase 1 baseline (LCP 3313.7 / 4750.71 / 3964.31, CLS 0.01/0.01/0) (D-15)"
    - "INP is reported n/a for static pages (timespan-only audit in LH 13.4.1 — documented, not a capture failure); the comparison is LCP + CLS + perf score vs baseline, with INP documented as n/a (D-15, BASELINE.md INP note)"
    - "PSI rows use the documented fallback when the anonymous quota 429s (provenance-only lighthouse-fallback markers; median.js never counts them as runs) — if a PSI_API_KEY is exported, real PSI rows are captured fresh (D-14, BASELINE.md PSI fallback note)"
    - "The full jest suite is green after the re-capture — baseline-tooling.test.js is co-changed to assert the NEW committed medians (the Phase-6 artifacts overwrite the Phase-1 artifacts in place, per the D-17 reuse rule — the old medians are preserved in git history and in the BASELINE.md comparison table)"
    - "The comparison table (final vs baseline, per URL per source) is recorded in the SUMMARY and in BASELINE.md — the improvement claim is backed by the printed medians, not asserted from memory"
    - "assumption (unclassified probe PERF-04, surfaced not dropped): the 75th-percentile threshold interpretation is the median-of-3 capture under the Phase 1 methodology (the only 75th-percentile instrument this recipe has — the baseline itself was captured the same way; D-15)"
    - "assumption (probe PERF-04 idempotency, surfaced not dropped): the capture overwrites the 18 artifacts in place — running it twice is a re-capture of the same 3-URL set, not an append; the committed artifact set is always the LAST capture, and the co-changed test follows it"
  artifacts:
    - ".planning/baseline/lighthouse/{home,blog,post-min}-{1,2,3}.json — re-captured Lighthouse LHRs (median-of-3)"
    - ".planning/baseline/psi/{home,blog,post-min}-{1,2,3}.json — real PSI responses or 429 fallback markers"
    - ".planning/baseline/BASELINE.md — a 'Final capture (Phase 6)' section: the new median table + comparison rows (the Phase-1 tables stay — they are the reference)"
    - "baseline-tooling.test.js — the three hardcoded-median assertions updated to the new committed values"
  key_links:
    - "owner deploy (manual) → live site final state → capture-baseline.js → lighthouse/psi artifacts → median.js → BASELINE.md comparison table → baseline-tooling.test.js co-change"
    - "capture-baseline.js URLs (hardcoded) ↔ the 3 canonical pages — no argv URL input (T-3-01 preserved)"
  prohibitions:
    - "The capture tooling must NOT be modified (D-17) — no version bump, no URL change, no new source; the co-change in this plan is limited to BASELINE.md + baseline-tooling.test.js"
    - "The Phase-1 baseline rows in BASELINE.md must NOT be deleted or rewritten — they are the comparison reference; only in-place overwrite of the artifact JSONs is allowed"
    - "A pre-deploy capture is FORBIDDEN — the manual checkpoint gates the run"
    - "No npm installs in this plan (lighthouse runs via the script's npx pin)"
---

<objective>
Final Core Web Vitals verification — PERF-04: after the owner deploys the full phase to Netlify (blocking manual checkpoint), re-run the identical Phase 1 capture recipe (capture-baseline.js + median.js, unchanged), compare the medians vs the Phase 1 baseline, co-change the test that hardcodes the committed baseline medians, and record the comparison table.

Purpose: this is the phase's closing verification — the numbers must come from the LIVE final site (a pre-deploy capture would compare a stale state, Pitfall 4), measured with the identical recipe (comparability, D-14), and compared honestly per source (lighthouse vs lighthouse; PSI 429 fallback honored).

Output: fresh capture artifacts, the median table, the comparison table recorded in BASELINE.md, baseline-tooling.test.js green against the final medians, and the SUMMARY with the verdict.

Environment notes (apply to every step): node is NOT on PATH — prefix commands with `source ~/.nvm/nvm.sh && nvm use 24` (nvm has v20.20.2, v24.18.0, v24.19.0; the project enforces Node 24 via engines + engine-strict + scripts/check-node-version.js; .nvmrc is 24). /tmp is a tmpfs — set TMPDIR=/home/simos/tmp for yarn commands. yarn 1.22 only. The capture needs ~20+ min (3 URLs × 3 runs × 2 sources + sleeps); PSI may 429 and fall back per the script.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/baseline/BASELINE.md
@.planning/baseline/capture-baseline.js
@.planning/baseline/median.js
@baseline-tooling.test.js
@.planning/phases/06-performance-asset-cleanup-final-verification/06-CONTEXT.md
@.planning/phases/06-performance-asset-cleanup-final-verification/06-RESEARCH.md
@.planning/phases/06-performance-asset-cleanup-final-verification/06-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: owner deploys the full phase to Netlify, confirms the live site, then approves the capture start (D-16)</name>
  <what-built>The complete Phase 6 output so far (06-01 fonts self-hosting, 06-02 asset dedup/deletion, 06-03 PWA manifest/icon cleanup) — the state that must be LIVE before the capture runs.</what-built>
  <how-to-verify>
    1. Deploy the current main branch to Netlify (the production build via the existing netlify.toml pipeline — `yarn build` on Netlify, publish public/).
    2. After the deploy finishes, confirm the live site is the new build: open https://laryart.it and check the page source for the absence of the Google Fonts CSS import lines in the style bundle (or confirm in the Netlify dashboard that the deploy completed with the latest commit).
    3. Confirm the shell can reach the site: `curl -sI https://laryart.it/ | head -1` returns HTTP/1.1 200 (a 301 to the canonical trailing-slash target also counts as reachable).
    4. Decide the PSI source: either export a PSI_API_KEY in the shell (raises the quota — real PSI rows) or accept the documented 429 fallback (the median.js runs_used=0 n/a rows, same as the Phase-1 baseline). The default is the accepted fallback.
    5. Reply "approved" (with or without the key decision), or describe what's blocking the deploy.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally noting the PSI key decision), or describe the blocker</resume-signal>
</task>

<task type="auto">
  <name>Run the identical capture + median, record the comparison table, co-change baseline-tooling.test.js (D-14/D-15/D-17)</name>
  <files>.planning/baseline/lighthouse/*.json, .planning/baseline/psi/*.json, .planning/baseline/BASELINE.md, baseline-tooling.test.js</files>
  <read_first>
    - The tooling (read-only — reused as-is, D-17): .planning/baseline/capture-baseline.js (hardcoded URL set, LH 13.4.1 pin, PSI 429 branch) and .planning/baseline/median.js (median rule, runs_used, WARNs)
    - .planning/baseline/BASELINE.md (the comparison reference — the Phase-1 medians: home 3313.7 / blog 4750.71 / post-min 3964.31, CLS 0.01/0.01/0; the Re-run section; the INP + PSI fallback notes)
    - baseline-tooling.test.js (the three assertions this task co-changes: "prints real lighthouse medians with runs_used=3" lines 30-42, the PSI fallback assertions lines 44-53, and "BASELINE.md records the version…" lines 95-101)
    - .planning/phases/06-performance-asset-cleanup-final-verification/06-VALIDATION.md per-task map row 06-04-01 (the manual capture)
    - .planning/phases/06-performance-asset-cleanup-final-verification/06-RESEARCH.md Pitfall 4 (pre-deploy trap) and Pitfall 5 (PSI 429 noise)
  </read_first>
  <action>
    Run under the nvm default Node 24 (the capture script requires >=22.19); export TMPDIR=/home/simos/tmp.

    1. Run the capture: `node .planning/baseline/capture-baseline.js` — ~20+ min. Expect per-run summary lines; PSI runs may 429 and fall back per the script's branch (provenance markers, never a hard failure). Do NOT abort on WARNs about INP or PSI.
    2. Run the median table: `node .planning/baseline/median.js` — the stdout is the new median table.
    3. Compare median vs median per source against the BASELINE.md Phase-1 rows. Record for each URL: LCP, CLS, perf score — against the targets LCP ≤ 2500 ms, CLS ≤ 0.1, and "improved vs baseline" per metric (all three URLs). If any metric exceeds a threshold OR fails to improve, PERF-04 is NOT met: record the failure in the SUMMARY, verify the phase-6 work (fonts/assets/manifest) is all deployed, and consider one remediation rebuild; the requirement stays open for the UAT gate. If a URL's median is missing (e.g. a lighthouse error artifact), record it and re-capture only that slug via the script's bounded mode: `node .planning/baseline/capture-baseline.js <slug> <source>`.
    4. Co-change baseline-tooling.test.js (test co-change discipline — the re-capture overwrites the committed artifacts in place, so the hardcoded medians must follow): update the "lighthouse" assertions (lines 30-42: the three LCP strings 3313.7/4750.71/3964.31 and the runs_used=3 checks) to the NEW median values, and the "BASELINE.md records" assertions (lines 95-101) to the same new values. The PSI-fallback assertions (lines 44-53: psi rows n/a, runs_used=0) stay as-is — the 429 markers persist.
    5. Update BASELINE.md: append a "## Final capture (Phase 6)" section — the new capture date + git SHA, the new median table, the comparison table per URL (final vs baseline, delta), verdict rows (met / not met for LCP/CLS/perf-score improvement), and the INP + PSI notes repeated if applicable. Do NOT delete or rewrite the Phase-1 baseline tables — they are the comparison reference.
    6. Commit atomically: `git commit -m "feat(06-04): capture final CWV and compare vs Phase-1 baseline (PERF-04)"` staging the capture artifacts, BASELINE.md, baseline-tooling.test.js — the co-change rides the same commit as the artifacts, so the suite is green at the commit.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn test && node .planning/baseline/median.js | grep -E '^lighthouse'</automated>
  </verify>
  <acceptance_criteria>
    - `node .planning/baseline/median.js` exits 0 and prints a lighthouse row per slug (runs_used=3 or a recorded WARN)
    - `TMPDIR=/home/simos/tmp yarn test` exits 0 (the co-changed baseline-tooling.test.js passes — new medians asserted)
    - `grep -q "Final capture" .planning/baseline/BASELINE.md` succeeds (the comparison section exists)
    - the SUMMARY contains the comparison table (final vs Phase-1, per URL) and the explicit met/not-met verdict for LCP ≤ 2.5s, CLS ≤ 0.1, and improved-vs-baseline
    - `git log -1 --format=%s` contains `06-04`
    - the INP n/a note and the PSI fallback outcome (key used or 429 markers) are recorded in the SUMMARY
  </acceptance_criteria>
  <done>The final capture ran against the LIVE deployed site, the medians are compared vs baseline per URL, BASELINE.md records the comparison, the co-changed test passes with the new medians, and the verdict (met / not met) is recorded.</done>
  <reversibility rating="reversible">capture artifacts + BASELINE.md + test — git revert restores the Phase-1 state; the comparison remains documented in history.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live site → capture script | the final site's CWV data crosses here — the capture must be of the DEPLOYED final state, not the pre-deploy state (D-16) |
| capture artifacts → jest | the new medians cross into baseline-tooling.test.js here — the co-change must match the committed artifacts exactly |
| BASELINE.md → comparison | the comparison reference crosses here — the Phase-1 rows must stay intact for like-for-like comparison |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-12 | Tampering | capture runs against the pre-deploy site → stale numbers reported as final | critical | mitigate | the blocking manual checkpoint (D-16) gates the capture; the plan forbids the pre-deploy run; the checkpoint's deploy-confirm + curl 200 are the gate |
| T-06-13 | Repudiation | fabricated/overlooked median in the comparison — claiming improvement without the print | high | mitigate | median.js stdout is the source (never hand-typed); the SUMMARY records the printed rows and the explicit per-metric verdict |
| T-06-14 | Tampering | baseline-tooling.test.js hardcoded medians drift from the committed artifacts | high | mitigate | the co-change is in the same commit as the capture artifacts (atomic); `yarn test` green gates it |
| T-06-15 | Info disclosure | PSI_API_KEY leaked in logs/artifacts | medium | mitigate | the script never prints/logs the key (D-17, baseline-tooling.test.js asserts it); key passed via env only |
| T-06-16 | DoS | capture interrupted → missing/partial runs, empty table | medium | mitigate | bounded-mode re-capture per slug; the commit happens only after the full capture completes |
| T-06-SC | Tampering | npx-based lighthouse dependency fetch | low | mitigate | the pinned version lives in the existing script (D-17); no new dependencies installed |

</threat_model>

<verification>
- `node .planning/baseline/capture-baseline.js` runs to completion; `node .planning/baseline/median.js` prints the table with the median per slug
- The comparison table rows (final vs baseline) are recorded in BASELINE.md
- `TMPDIR=/home/simos/tmp yarn test` exits 0 (the co-changed baseline-tooling.test.js passes)
- The SUMMARY records: the printed medians, the per-URL delta vs Phase-1, the verdict per target (LCP/CLS/perf-score), the INP n/a note, the PSI outcome
</verification>

<success_criteria>
- The capture is live-site, final-state, identical-recipe (D-14/D-16/D-17)
- The comparison table is recorded and the verdict is explicit per metric
- The suite is green with the co-changed test
- PERF-04 is met only if the recorded values show LCP ≤ 2.5s, CLS ≤ 0.1, all improved vs baseline — any shortfall is recorded, not hidden
</success_criteria>

## Artifacts this phase produces

- `.planning/baseline/lighthouse/home-{1,2,3}.json`, `blog-{1,2,3}.json`, `post-min-{1,2,3}.json` — re-captured Lighthouse LHRs (final)
- `.planning/baseline/psi/{home,blog,post-min}-{1,2,3}.json` — real PSI responses or the 429 fallback markers
- `.planning/baseline/BASELINE.md` — the "Final capture (Phase 6)" section: capture metadata + comparison table vs the Phase-1 rows
- `baseline-tooling.test.js` — the hardcoded-median assertions updated to the new committed medians
- Commit: `feat(06-04): capture final CWV and compare vs Phase-1 baseline (PERF-04)`
- `.planning/phases/06-performance-asset-cleanup-final-verification/06-04-SUMMARY.md` (created at plan completion)

<output>
Create `.planning/phases/06-performance-asset-cleanup-final-verification/06-04-SUMMARY.md` when done
</output>
</content>


