---
phase: 06-performance-asset-cleanup-final-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - scripts/asset-cleanup/check-unreferenced.js
  - static/assets/ (38 unreferenced files deleted, 7 dedup twins + trilli-2 pair deleted)
  - design/logo-bianco.xcf
  - design/logo-rosa.xcf
autonomous: true
requirements:
  - PERF-02
user_setup: []

must_haves:
  truths:
    - "The deletion list is computed by a committed script (scripts/asset-cleanup/check-unreferenced.js) that greps every static/assets/ basename against src/content/, src/, gatsby-config.js, static/admin/config.yml, and src/util/site.json — never a hand-maintained list (D-07; the script is the source of truth)"
    - "The dedup keeps the content-referenced extension per pair: farfalle.jpg, minnie.jpg, paperino.jpg, pluto-1.jpeg, pluto-2.jpeg, topolino.jpg, trilli.jpeg survive byte-identical; the 7 twins (farfalle.jpeg, minnie.jpeg, paperino.jpeg, pluto-1.jpg, pluto-2.jpg, topolino.jpeg, trilli.jpg) are deleted (D-06, PATTERNS table)"
    - "trilli-2.jpg + trilli-2.jpeg are both deleted (unreferenced pair); trilly.jpg is KEPT and resolvable (referenced by 2021-10-05-trilly.md — distinct file, name-collision trap avoided by exact-basename matching)"
    - "heart.png (src/util/site.json meta.image), stackrole.png (gatsby-plugin-manifest icon + CMS logo default), home-1.jpg (index.md featuredImage) are KEPT — the grep keeps every referenced basename (D-09)"
    - "The two .xcf files (logo-bianco.xcf, logo-rosa.xcf) are MOVED to design/ at repo root — not deleted, not served (D-08 default: move)"
    - "Every featuredImage: frontmatter path in src/content/posts/*.md still resolves after deletion — zero frontmatter edits (research-verified: all 7 dedup posts already reference the kept extension; no content changes per phase domain)"
    - "The build passes and the rendered public/ HTML contains every kept asset path — zero broken images on any page (success criterion 5, D-10 held-out grep)"
    - "Zero grep hits for any deleted basename anywhere in src/content/, src/, gatsby-config.js, static/admin/config.yml, src/util/site.json"
    - { statement: "UI-SPEC E4 error backstop: a deletion-script false-negative (a referenced file deleted) surfaces as a build-time sharp error or a broken asset URL — the held-out grep on built HTML plus the human visual pass in 06-04 catch it; git history is the restore path", verification: backstop }
    - "assumption (unclassified probe PERF-02, surfaced not dropped): visual parity holds because the kept twin is the file the site already renders today (A2, research — UI-SPEC E4 populated covered: byte-identical keepers render exactly as today, no query/output change); the phase deletes duplicates and unreferenced files only — it does NOT re-encode or optimize any kept image (research anti-pattern)"
    - "assumption (probe PERF-02 idempotency, surfaced not dropped): re-running the deletion script on the already-cleaned tree is a no-op — every remaining file is referenced (the script's second run reports zero deletable files)"
    - "assumption (probe PERF-02 concurrency, surfaced not dropped): the script and the build are serialized by the wave structure (06-02 wave 2 after 06-01 wave 1) — a --delete run is never interleaved with a build; if interrupted mid-delete, the script exits non-zero and git status shows the partial set, which is reconciled before the commit (the commit happens only after the post-delete scan reports zero deletable)"
  artifacts:
    - "scripts/asset-cleanup/check-unreferenced.js — committed reference-grep script (read-only operation by default; --delete flag performs the deletion)"
    - "static/assets/ — the 38 unreferenced + 9 dedup files removed; 23 referenced keepers intact"
    - "design/logo-bianco.xcf + design/logo-rosa.xcf — moved (new folder at repo root)"
  key_links:
    - "scripts/asset-cleanup/check-unreferenced.js → static/assets/ listing → grep against all reference roots → deletable set → git rm → build → rendered-HTML grep for every kept path"
    - "kept featuredImage paths → frontmatter → gatsby build → public/*/index.html asset URLs"
  prohibitions:
    - "Deletion must NOT be gated on byte-identity/cmp/hash — all 8 jpg/jpeg pairs are re-encoded DIFFERENT-content twins (research-verified, supersedes UI-SPEC); the reference grep is the gate"
    - "No kept image may be re-encoded, resized, or optimized (would change pixels and break the byte-parity contract)"
    - "No frontmatter/content edits in this phase (domain: no content changes; research verified zero needed)"
    - "The .xcf files must NOT be deleted (default is move; deletion only with an explicit owner decline)"
    - "No npm/yarn package installs in this plan"
---

<objective>
Deduplicate the 8 jpg/jpeg pairs, delete the unreferenced asset set, and move the GIMP logo sources out of the published payload — PERF-02: build the committed reference-grep script that computes the deletion list (never a hand-maintained list), apply the deletions, move the .xcf files to design/, and prove no page shows broken images (build + rendered-source grep, D-10).

Purpose: the phase's safety contract is script-computed reference — the grep is the only trustworthy inventory, and the build + grep gates catch any false negative. This plan makes the cleanup auditable and reversible.

Output: the reference script committed, the asset set lean (61 → 23 files in static/assets/), design/ populated, build green with every referenced asset path present in the rendered HTML, and a recorded verification.

Environment notes (apply to every verify step): node is NOT on PATH — prefix commands with `source ~/.nvm/nvm.sh && nvm use 24` (nvm has v20.20.2, v24.18.0, v24.19.0; the project enforces Node 24 via engines + engine-strict + scripts/check-node-version.js; .nvmrc is 24). /tmp is a tmpfs that fills with JIT artifacts — set TMPDIR=/home/simos/tmp for yarn/build commands. yarn 1.22 only, never npm.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-performance-asset-cleanup-final-verification/06-CONTEXT.md
@.planning/phases/06-performance-asset-cleanup-final-verification/06-RESEARCH.md
@.planning/phases/06-performance-asset-cleanup-final-verification/06-PATTERNS.md
@.planning/phases/06-performance-asset-cleanup-final-verification/06-UI-SPEC.md
@.planning/phases/06-performance-asset-cleanup-final-verification/06-VALIDATION.md
@scripts/asset-cleanup/check-unreferenced.js
@static/admin/config.yml
@src/util/site.json
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Write the reference-grep deletion script (scripts/asset-cleanup/check-unreferenced.js) — the source of truth (D-07)</name>
  <files>scripts/asset-cleanup/check-unreferenced.js</files>
  <read_first>
    - .planning/phases/06-performance-asset-cleanup-final-verification/06-RESEARCH.md Code Example B (the verified reference-grep logic — the shape this script implements) and Pitfall 3 (trilly vs trilli exact-basename rule)
    - .planning/phases/06-performance-asset-cleanup-final-verification/06-PATTERNS.md §static/assets/ deletions (the roots list, the walk pattern from phase2-foundation-cleanup.test.js lines 16-24, the deletion mechanics)
    - src/util/site.json (the meta.image reference — heart.png must be caught by the grep)
    - static/admin/config.yml (the CMS logo default at line 187 — stackrole.png must be caught)
    - AGENTS.md (no semicolons, double quotes, arrowParens avoid — repo style for the script)
  </read_first>
  <action>
    Create scripts/asset-cleanup/check-unreferenced.js — a Node 24 script with NO dependencies (plain fs/path), runnable as `node scripts/asset-cleanup/check-unreferenced.js [--delete]`:

    - Grep roots (exact list): "src/content", "src", "static/admin/config.yml", "gatsby-config.js", "src/util/site.json". For a file root, read its content; for a directory root, walk recursively and read every file whose name matches /\.(md|js|jsx|yml|yaml|json|scss)$/ (scss included — a future SCSS font face could reference an asset).
    - For every entry in static/assets/, compute isReferenced(f) as a substring match of the exact basename f in any root file's content.
    - Default (no flag): print the deletable set (one basename per line) plus a summary line "N deletable / M total" to stdout; exit 0. Never touch the filesystem without --delete.
    - With --delete: print the same summary, then delete the deletable files with fs.unlinkSync; then verify zero deletable remain and exit 0 (exit 1 if any unlink failed or the post-delete scan still finds deletable files).
    - Exact-basename matching only — no globs (a "trill*" glob would match the referenced trilly.jpg, breaking the 2021-10-05 post; the basename substring grep is exact by construction).
    - Project style: no semicolons, double quotes, 2-space indent. Prettier: run `npx prettier --write scripts/asset-cleanup/check-unreferenced.js` (or leave style to `yarn format`).

    Commit atomically: `git commit -m "feat(06-02): add reference-grep asset cleanup script (PERF-02, D-07)"` staging exactly scripts/asset-cleanup/check-unreferenced.js.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && node scripts/asset-cleanup/check-unreferenced.js | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/asset-cleanup/check-unreferenced.js` exits 0 and prints "referenced: 23" + "deletable: 38" on the summary line (research-reverified counts; record the actual output — if the count differs, investigate before deleting: the script is the source of truth, a delta must be explained in the SUMMARY)
    - `node scripts/asset-cleanup/check-unreferenced.js` (no flag) exits 0 and makes zero filesystem changes (compare `ls static/assets | wc -l` before and after → 61)
    - `node scripts/asset-cleanup/check-unreferenced.js` does NOT list "heart.png", "stackrole.png", "home-1.jpg", or "trilly.jpg" in the deletable set
    - the script contains the grep roots "src/content", "src", "static/admin/config.yml", "gatsby-config.js", "src/util/site.json"
    - `git log -1 --format=%s` contains `06-02`
  </acceptance_criteria>
  <done>The reference-grep script exists, is committed, reports the verified 23/38 split, and is provably read-only without the --delete flag.</done>
  <reversibility rating="reversible">new file only — deleting it restores the pre-plan state.</reversibility>
</task>

<task type="auto">
  <name>Apply the deletions + move the .xcf sources to design/, then verify the built site has zero broken asset paths (D-06/D-07/D-08/D-10)</name>
  <files>static/assets/ (deletions), design/logo-bianco.xcf, design/logo-rosa.xcf, scripts/asset-cleanup/check-unreferenced.js (read-only reference)</files>
  <read_first>
    - The file being modified: the static/assets/ directory listing itself (61 files — the deletion targets come from the script, never from this doc)
    - scripts/asset-cleanup/check-unreferenced.js (the just-committed source of truth — re-run it here)
    - src/content/posts/*.md featuredImage lines (verify-only reference — the 7 dedup posts + trilly post; ZERO edits)
    - .planning/phases/06-performance-asset-cleanup-final-verification/06-PATTERNS.md §static/assets/ deletions (the dedup pair table, the keep list, the xcf move, the git mechanics)
    - .planning/phases/06-performance-asset-cleanup-final-verification/06-UI-SPEC.md §Image / Asset Surface Contract (the dedup table + keep list + broken-image gate)
  </read_first>
  <action>
    1. Run the script in delete mode: `node scripts/asset-cleanup/check-unreferenced.js --delete` — deletes the 38 unreferenced files (the 24 numeric-ID Facebook exports, the two date-named JPEGs, the 7 dedup twins + trilli-2 pair, home-2.jpg, stackrole-spin-circle.png, the two .xcf sources, …). NOTE: the script deletes the .xcf files too — per D-08 they must be MOVED to design/, not deleted, so BEFORE running --delete, move them out first: `mkdir -p design && git mv static/assets/logo-bianco.xcf design/logo-bianco.xcf && git mv static/assets/logo-rosa.xcf design/logo-rosa.xcf`. After the move, re-run the script: the two .xcf basenames no longer exist in static/assets/, so the deletable set drops to 36.
    2. After --delete, run `git add -A` (git history preserves every deleted file — the reversibility contract; plain rm + add -A is the documented mechanics, no git mv needed for deletions).
    3. Manual no-op check: re-run `node scripts/asset-cleanup/check-unreferenced.js` — expect "deletable: 0" (idempotency — the probe's second-run assumption, now proven).
    4. Verify zero broken references: `source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build`, then for every kept asset basename still in static/assets/, `grep -rl "<basename>" public/ | wc -l` must be ≥ 1 across the built HTML/CSS (the D-10 rendered-source grep). Also confirm the specific keepers: heart.png, stackrole.png, home-1.jpg, trilly.jpg each appear in the built output.
    5. Verify zero content edits: `git status --short src/content` — must be empty (no frontmatter touched; research resolved zero needed).
    6. Commit atomically: `git commit -m "feat(06-02): dedup assets and delete unreferenced files; move logo sources to design (PERF-02, D-06/D-07/D-08)"` staging the deleted files, design/, and the moved xcf files. If the build surfaced a broken reference, fix the root cause BEFORE the commit and record it in the SUMMARY.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && test "$(node scripts/asset-cleanup/check-unreferenced.js | tail -1 | grep -o 'deletable: [0-9]*' | cut -d' ' -f2)" -eq 0 && test "$(ls static/assets | wc -l)" -eq 23 && test -f design/logo-bianco.xcf && test -f design/logo-rosa.xcf && ! git ls-files static/assets | grep -q 'trilli-2' && test "$(git status --short src/content | wc -l)" -eq 0 && TMPDIR=/home/simos/tmp yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `ls static/assets | wc -l` → 23 (the 61-file set minus 36 script deletions minus the 2 xcf moves)
    - `node scripts/asset-cleanup/check-unreferenced.js` reports "deletable: 0" on re-run (idempotent)
    - `test -f design/logo-bianco.xcf` and `test -f design/logo-rosa.xcf` both succeed (moved, not deleted)
    - `! git ls-files static/assets | grep -q 'trilli-2'` succeeds (both trilli-2 files gone)
    - `grep -rl 'trilly.jpg' src/content/` still matches 2021-10-05-trilly.md (the kept file survived; the exact-basename rule held)
    - `grep -q 'heart.png' src/util/site.json` still succeeds (keeper untouched)
    - `git status --short src/content | wc -l` → 0 (zero content/frontmatter edits)
    - `TMPDIR=/home/simos/tmp yarn build` exits 0, and the built public/ HTML contains every kept asset basename (the D-10 grep: for each of the 23 kept basenames, `grep -rl "<basename>" public/` returns ≥ 1 file)
    - `git log -1 --format=%s` contains `06-02`
  </acceptance_criteria>
  <done>The asset set is deduplicated and lean (23 files), the reference script reports zero deletable on re-run, the .xcf sources live in design/, the build passes with every kept asset path present in the rendered output, and zero content files changed.</done>
  <reversibility rating="reversible">git history preserves every deleted twin and unreferenced file; `git checkout -- <path>` restores any of them; design/ moves revert with git mv.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference-grep script → filesystem | the --delete run crosses here — the script's reference computation is the gate; a false negative deletes a referenced file |
| static/assets/ → gatsby build | deleted files cross into the build here; any remaining reference breaks sharp processing or renders a broken URL |
| git history → restore | the reversibility boundary — every deletion is recoverable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | deletion script — a referenced file deleted (false negative in the grep) | critical | mitigate | script-computed list from the 5 grep roots (D-07); the trilly/trilli exact-basename rule (Pitfall 3); build + rendered-HTML grep for every kept path (D-10); git history restore path; UI-SPEC E4 backstop covers the residual risk with the human visual pass in 06-04 |
| T-06-06 | Tampering | byte-identity gate misfires — all 8 pairs differ, a cmp gate would block the entire dedup or, worse, keep the wrong twin | medium | mitigate | the reference grep is the sole gate (research Pitfall 2); the kept file is untouched — no re-encode, no rename |
| T-06-07 | Tampering | the .xcf sources deleted instead of moved (D-08 default is move) | low | mitigate | the task moves them with git mv BEFORE the script --delete run; acceptance asserts design/ files exist |
| T-06-08 | DoS | deletion script run without --delete accidentally touching files | low | mitigate | read-only default is coded into the script contract (acceptance asserts dry-run leaves 61 files) |
| T-06-SC | Tampering | no package installs in this plan | n/a | n/a | zero install commands |

</threat_model>

<verification>
- `node scripts/asset-cleanup/check-unreferenced.js` → "referenced: 23" / "deletable: 0" (after cleanup; the before-run must show 38 deletable)
- `ls static/assets | wc -l` → 23; `test -f design/logo-bianco.xcf` and `test -f design/logo-rosa.xcf`
- `TMPDIR=/home/simos/tmp yarn build` exits 0 and every kept basename greps ≥ 1 in public/
- `git status --short src/content | wc -l` → 0 (no content edits)
- No broken images: the built-HTML grep is the automated gate; the human visual pass over /, /blog, /minnie/ runs in 06-04
</verification>

<success_criteria>
- static/assets/ holds exactly the 23 referenced files; all 8 dedup pairs resolved by keeping the content-referenced twin
- The two .xcf logo sources are in design/ and no longer served
- The build passes with every kept asset path present in the rendered output (zero broken images — success criterion 5)
- The deletion list is auditable: script-computed, committed, idempotent on re-run
</success_criteria>

## Artifacts this phase produces

- `scripts/asset-cleanup/check-unreferenced.js` — new committed Node script (reference-grep; --delete flag; no deps)
- `static/assets/` — 38 files deleted by the script (24 numeric-ID exports, 20200907_233102.jpg, IMG_20200906_223238_974.jpg, home-2.jpg, stackrole-spin-circle.png, the 7 dedup twins, trilli-2.jpg + trilli-2.jpeg); 23 keepers intact
- `design/logo-bianco.xcf` + `design/logo-rosa.xcf` — moved from static/assets/ (new folder at repo root)
- Commits: `feat(06-02): add reference-grep asset cleanup script (PERF-02, D-07)` and `feat(06-02): dedup assets and delete unreferenced files; move logo sources to design (PERF-02, D-06/D-07/D-08)`
- `.planning/phases/06-performance-asset-cleanup-final-verification/06-02-SUMMARY.md` (created at plan completion)

<output>
Create `.planning/phases/06-performance-asset-cleanup-final-verification/06-02-SUMMARY.md` when done
</output>
</content>
