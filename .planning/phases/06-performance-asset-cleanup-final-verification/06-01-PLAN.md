---
phase: 06-performance-asset-cleanup-final-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - yarn.lock
  - src/components/layout.js
  - src/assets/scss/style.scss
  - phase3-upgrade-matrix.test.js
autonomous: true
requirements:
  - PERF-01
user_setup: []

must_haves:
  truths:
    - "@fontsource/ubuntu@5.3.0 and @fontsource/parisienne@5.3.0 are pinned EXACTLY (no caret) in package.json dependencies (lockstep discipline, 03-CONTEXT D-01; versions registry-verified 2026-08-20)"
    - "The three @fontsource CSS imports live in src/components/layout.js adjacent to the existing style.scss import (the layout-entry path — the ONLY mechanism that builds; the SCSS @use variant is verified broken in the real Gatsby build, research correction superseding UI-SPEC)"
    - "The two Google-Fonts CSS import lines at the top of style.scss are removed; zero such imports remain anywhere in src/assets/scss/ (D-03, PERF-01 success criterion 1)"
    - "--font-family / --font-family-titles in _theme-variables.scss stay byte-identical; zero font-family name changes (D-02 — the fallback stacks are the browser-side failure contract)"
    - "Built output carries the self-hosted @font-face blocks with font-display: swap (inherent to @fontsource, D-04) and hashed woff2/woff files; zero references to the Google Fonts origin anywhere in public/ (built HTML + CSS)"
    - "phase3-upgrade-matrix.test.js UPGR-02 block is rewritten to the new contract (zero CSS-import lines in style.scss, three @fontsource import lines in layout.js, exact pins) and the full jest suite is green — test co-change discipline (VALIDATION.md Wave 0)"
    - "UI-SPEC E1 loading covered: font-display swap renders text in the fallback stack during load, identical to today's Google &display=swap behavior — no FOIT, no extra config"
    - "UI-SPEC E1 error covered: a failed face silently falls back per the --font-family cascade (\"Ubuntu\", sans-serif / \"Parisienne\", cursive) — self-hosting strictly reduces this risk (no third-party request)"
    - "UI-SPEC E1 populated covered: all family+weight combos used by the SCSS resolve — the built @font-face set is exactly Ubuntu 400 + Ubuntu 700 + Parisienne 400"
    - "assumption (unclassified probe PERF-01, surfaced not dropped): the layout-entry transport is the only workable mechanism in this repo (SCSS path proven broken in a real build — research Pitfall 1, A1); the plan ships it as the implementation, with the built-output gate as the empirical proof"
  artifacts:
    - "package.json — \"@fontsource/ubuntu\": \"5.3.0\" + \"@fontsource/parisienne\": \"5.3.0\" (exact pins)"
    - "yarn.lock — resolution entries for both @fontsource packages"
    - "src/components/layout.js — three @fontsource CSS imports + a // PERF-01: comment, adjacent to the style.scss import"
    - "src/assets/scss/style.scss — lines 7-8 removed; the @use block (lines 1-6) byte-identical"
    - "phase3-upgrade-matrix.test.js — UPGR-02 block rewritten (old lines 80-95 replaced)"
  key_links:
    - "yarn add @fontsource/ubuntu@5.3.0 @fontsource/parisienne@5.3.0 → package.json exact pins → layout.js imports → webpack css-loader → emitted CSS @font-face blocks → hashed woff2/woff files in public/"
    - "style.scss lines 7-8 removal → zero Google Fonts import in SCSS → built CSS/HTML with zero Google Fonts references"
  prohibitions:
    - "@fontsource must NOT be imported through SCSS (any @use/@import variant — verified broken: sass-loader can't resolve from src/assets/scss, css-loader rebases ./files/ urls; RESEARCH Pitfall 1)"
    - "gatsby-config.js must NOT change for fonts (the layout-entry path needs zero config, research-verified)"
    - "package.json must NOT gain a caret on the @fontsource pins"
    - "No font-family name or CSS variable value change anywhere (D-02)"
    - "No npm install — yarn 1.22 only (single-lockfile rule, FNDT-01)"
---

<objective>
Self-host the two site fonts via @fontsource packages — the PERF-01 tracer: install the exact-pinned packages with yarn, land the three CSS imports in the layout entry (the mechanism that is proven to build in this repo — the SCSS `@use` path FAILS the real build and is explicitly not an option), remove the two render-blocking Google-Fonts CSS import lines from style.scss, and co-change the UPGR-02 test block that asserts the old mechanism (currently RED from the phase-3 `@use` migration).

Purpose: prove the full font transport chain (install → layout import → webpack CSS output → built @font-face + woff2 files → test gate) on the single mechanism this phase will rely on, and fix the broken regression test in the same commit as the behavior change (test co-change discipline).

Output: @fontsource packages pinned, layout.js imports, style.scss cleaned, UPGR-02 green, build green with self-hosted font faces in the emitted CSS and zero Google Fonts references; one commit per task.

Environment notes (apply to every step): this repo is a Gatsby static site (no server) — no deployment needed in this plan. node is NOT on PATH — prefix commands with `source ~/.nvm/nvm.sh && nvm use 24` (nvm has v20.20.2, v24.18.0, v24.19.0; the project enforces Node 24 via engines + engine-strict + scripts/check-node-version.js; .nvmrc is 24). /tmp is a tmpfs that fills with JIT artifacts — set TMPDIR=/home/simos/tmp for yarn/build commands. yarn 1.22 only, never npm. `yarn add` triggers the preinstall node-version check — run under nvm use 24.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-performance-asset-cleanup-final-verification/06-CONTEXT.md
@.planning/phases/06-performance-asset-cleanup-final-verification/06-RESEARCH.md
@.planning/phases/06-performance-asset-cleanup-final-verification/06-PATTERNS.md
@.planning/phases/06-performance-asset-cleanup-final-verification/06-UI-SPEC.md
@.planning/phases/06-performance-asset-cleanup-final-verification/06-VALIDATION.md
@package.json
@src/components/layout.js
@src/assets/scss/style.scss
@phase3-upgrade-matrix.test.js
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>Install @fontsource exact pins, land the three layout.js imports, remove the style.scss Google-Fonts lines, rewrite UPGR-02 (PERF-01, D-01/D-03/D-05)</name>
  <files>package.json, yarn.lock, src/components/layout.js, src/assets/scss/style.scss, phase3-upgrade-matrix.test.js</files>
  <read_first>
    - The file being modified: src/components/layout.js (import block lines 1-10 — the insertion point is line 8, next to the existing style.scss import)
    - src/assets/scss/style.scss (lines 1-8 — the @use block that must stay byte-identical and the two lines to delete)
    - phase3-upgrade-matrix.test.js (the read-helper header lines 1-16 and the RED UPGR-02 block lines 56-101, in particular the old font-import assertion lines 80-95)
    - package.json (dependencies block lines 26-53 — the exact-pin matrix pattern; never a caret)
    - .planning/phases/06-performance-asset-cleanup-final-verification/06-RESEARCH.md (Standard Stack install line, Pitfall 1 — the SCSS path that fails, Code Example A — the verified layout-entry target)
    - .planning/phases/06-performance-asset-cleanup-final-verification/06-PATTERNS.md §layout.js, §style.scss, §phase3-upgrade-matrix.test.js (the exact insertion point, the exact test rewrite shape, the exact-pin rule)
    - .planning/phases/06-performance-asset-cleanup-final-verification/06-CONTEXT.md D-01 (self-host via @fontsource), D-03 (the CSS import lines are REMOVED entirely), D-04 (swap inherent), D-05 (the @use migration is the baseline)
  </read_first>
  <action>
    Install exactly: `source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn add @fontsource/ubuntu@5.3.0 @fontsource/parisienne@5.3.0` — yarn 1.22 only, never npm (single-lockfile rule; the preinstall check requires Node 24). Verify yarn wrote exact "5.3.0" pins without a caret — if a "^" appears, fix it by hand in package.json.

    src/components/layout.js: add three import statements above the existing style.scss import (line 8), each with a single preceding comment line `// PERF-01: self-hosted font (was: Google Fonts CSS import in style.scss)`. The imports: "@fontsource/ubuntu/400.css", "@fontsource/ubuntu/700.css", "@fontsource/parisienne/400.css". Component body untouched.

    src/assets/scss/style.scss: delete exactly the two Google-Fonts CSS import URL lines (lines 7-8). Keep the top `@use` block (lines 1-6) byte-identical. Zero other SCSS edits.

    phase3-upgrade-matrix.test.js: replace the UPGR-02 font assertion block (the "hoisted the Google Fonts imports..." test, lines 80-95) with the new contract test: (a) `expect(styleScss).not.toMatch(/@import url\(/)` — zero CSS-import lines in style.scss; (b) inside the test, read layout.js via the existing `read("src/components/layout.js")` helper and assert it contains the three import strings "@fontsource/ubuntu/400.css", "@fontsource/ubuntu/700.css", "@fontsource/parisienne/400.css"; (c) `expect(pkg.dependencies["@fontsource/ubuntu"]).toBe("5.3.0")` and the same for "@fontsource/parisienne". KEEP the last line of the old block (`expect(themeVars).not.toMatch(/@import/)`) — it still holds. Keep the rest of the UPGR-02 describe block and the whole file otherwise untouched.

    Commit atomically: `git commit -m "feat(06-01): self-host fonts via @fontsource layout entry (PERF-01)"` staging exactly package.json, yarn.lock, src/components/layout.js, src/assets/scss/style.scss, phase3-upgrade-matrix.test.js.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn install && grep -q '"@fontsource/ubuntu": "5.3.0"' package.json && grep -q '"@fontsource/parisienne": "5.3.0"' package.json && grep -q '@fontsource/ubuntu/400.css' src/components/layout.js && grep -q '@fontsource/ubuntu/700.css' src/components/layout.js && grep -q '@fontsource/parisienne/400.css' src/components/layout.js && ! grep -q '@import url(' src/assets/scss/style.scss && TMPDIR=/home/simos/tmp yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"@fontsource/ubuntu": "5.3.0"' package.json` succeeds and `grep -q '"@fontsource/ubuntu": "^5.3.0"' package.json` fails (exact pin)
    - `grep -q '@fontsource/ubuntu/400.css' src/components/layout.js` succeeds; same for the other two import strings
    - `grep -q '// PERF-01:' src/components/layout.js` succeeds (the tying comment, UI-SPEC checklist item 6)
    - `! grep -q '@import url(' src/assets/scss/style.scss` succeeds (grep gate — the two lines are gone)
    - `git diff --exit-code HEAD -- src/assets/scss/_theme-variables.scss` exits 0 (D-02 untouched)
    - `TMPDIR=/home/simos/tmp yarn test` exits 0 (the rewritten UPGR-02 block passes — the suite was RED before this task)
    - `git log -1 --format=%s` contains `06-01`
  </acceptance_criteria>
  <done>The packages are pinned exactly, the three imports are in layout.js with the PERF-01 comment, the two Google-Fonts CSS import lines are gone from style.scss, the UPGR-02 test asserts the new contract and the full suite passes.</done>
  <reversibility rating="reversible">git revert restores the old imports; the packages remain installed until removed — no runtime state.</reversibility>
</task>

<task type="auto">
  <name>Build + built-output parity gates: @font-face set, swap, zero Google Fonts in the built site (D-04/D-07, PERF-01 success criteria 1-2)</name>
  <files>public/ (build output — verification only), src/assets/scss/_theme-variables.scss (read-only reference)</files>
  <read_first>
    - .planning/phases/06-performance-asset-cleanup-final-verification/06-UI-SPEC.md "Built-output gate" paragraph and the "Font-parity proof checklist" (items 1-6) — the exact gates
    - .planning/phases/06-performance-asset-cleanup-final-verification/06-RESEARCH.md §What the build produces (14 @font-face blocks incl. unicode-range subsets, 22 hashed woff2/woff, zero Google Fonts references) and §Environment availability (Chrome not needed here — build only)
    - .planning/phases/06-performance-asset-cleanup-final-verification/06-VALIDATION.md per-task map row 06-01-02
    - No source edits expected — this task only builds and greps; if a gate fails, root-cause in the task-1 artifacts
  </read_first>
  <action>
    Run `source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn clean && TMPDIR=/home/simos/tmp yarn build` (clean first — the emitted CSS is a hashed bundle).

    Then run the parity gates against the fresh build (record every output):
    1. `grep -o "@font-face" public/*.css | wc -l` — expect ≥ 14 faces (research-verified 14 for Ubuntu 400/700 + Parisienne 400 with unicode-range subsetting; gate ≥ 14, record the exact count)
    2. `grep -oE "font-display: ?swap" public/*.css | wc -l` — expect ≥ 14 (swap on every face — D-04 inherent)
     Then run the parity gates against the fresh build (record every output):
    <!-- planner-discipline-allow: fonts.googleapis.com -->
    1. `grep -o "@font-face" public/*.css | wc -l` — expect ≥ 14 faces (research-verified 14 for Ubuntu 400/700 + Parisienne 400 with unicode-range subsetting; gate ≥ 14, record the exact count)
    2. `grep -oE "font-display: ?swap" public/*.css | wc -l` — expect ≥ 14 (swap on every face — D-04 inherent)
    3. `grep -rl "fonts.googleapis.com" public/ | wc -l` — must be 0 across the whole build (research verified: zero references; the resolver no longer contains the origin)
    4. `find public -name "*.woff2" | wc -l` — expect ≥ 1 (research: 22 hashed files; record the count)
    5. `grep -c "font-family: \"Ubuntu\"" public/*.css` and `grep -c "font-family: \"Parisienne\"" public/*.css` — the built faces cover exactly the two families (400 + 700 / 400 weights per the css2 request today)
    6. `git diff --exit-code HEAD -- src/assets/scss/_theme-variables.scss` — must exit 0 (font-family CSS vars byte-identical, D-02, UI-SPEC checklist item 1)

    If any gate fails, fix the root cause in the source file from task 1 (imports/removal), rebuild, and re-run; commit the fix separately with `git commit -m "fix(06-01): built-output font gate fixes (PERF-01)"`. If all green, make NO commit.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && test "$(grep -o '@font-face' public/*.css | wc -l)" -ge 14 && test "$(grep -rl 'fonts.googleapis.com' public/ | wc -l)" -eq 0 && test "$(find public -name '*.woff2' | wc -l)" -ge 1 && git diff --quiet HEAD -- src/assets/scss/_theme-variables.scss</automated>
  </verify>
  <acceptance_criteria>
    - `grep -o "@font-face" public/*.css | wc -l` ≥ 14
    - `grep -oE "font-display: ?swap" public/*.css | wc -l` ≥ 14
    - `grep -rl "fonts.googleapis.com" public/ | wc -l` → 0
    - `find public -name "*.woff2" | wc -l` ≥ 1
    - `git diff --quiet HEAD -- src/assets/scss/_theme-variables.scss` exits 0 (the font family CSS vars untouched — D-02)
    - `grep -q '// PERF-01:' src/components/layout.js` still succeeds (the comment ties the imports to the requirement)
    - `TMPDIR=/home/simos/tmp yarn test` still exits 0 after the rebuild
  </acceptance_criteria>
  <done>The built CSS contains the self-hosted faces (≥ 14 with swap), hashed woff2 files are emitted, zero Google Fonts references in the built output, the font-family variables are byte-identical, and the suite stays green.</done>
  <reversibility rating="reversible">verification-only task — no code changed on a green run; fixes land as atomic commits.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → yarn install | @fontsource packages enter the dependency tree here; yarn.lock freezes resolution |
| webpack css-loader → public CSS | the three imported CSS files cross into the emitted bundle here; a wrong import path breaks the build |
| style.scss → dart-sass | the removal of the two import lines crosses here; any reintroduction re-adds render-blocking third-party fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | package.json — @fontsource pinned wrong/non-exact version | high | mitigate | RESEARCH Package Legitimacy Audit (both OK, no postinstall); exact "5.3.0" greps in acceptance; yarn.lock is the resolution record |
| T-06-02 | Tampering | SCSS-import path re-attempted → sass-loader "Can't find stylesheet to import" build break | high | mitigate | the task action pins the layout-entry path only; a broken build is an immediate red gate (verify blocks on `yarn build`) |
| T-06-03 | Tampering | a Google-Fonts origin import reintroduced in SCSS/JS | medium | mitigate | zero-CSS-import grep on source (task 1) + zero-origin grep on the built output (task 2) |
| T-06-04 | DoS | @font-face misconfiguration (missing swap) → invisible text under slow font load | low | mitigate | @fontsource ships swap in every face (D-04); the built-CSS swap count gate asserts it |
| T-06-SC | Tampering | npm install instead of yarn → second lockfile | low | mitigate | project rule (FNDT-01 test asserts single lockfile); the task uses `yarn add` only |

</threat_model>

<verification>
- `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn install && yarn test && yarn build` exits 0
- `grep -o "@font-face" public/*.css | wc -l` ≥ 14; `grep -oE "font-display: ?swap" public/*.css | wc -l` ≥ 14
- `grep -rl "fonts.googleapis.com" public/ | wc -l` → 0; `find public -name "*.woff2" | wc -l` ≥ 1
- `git diff --quiet HEAD -- src/_theme-variables.scss` exits 0 (D-02)
- phase3-upgrade-matrix.test.js UPGR-02 green (the suite was RED at plan time)
</verification>

<success_criteria>
- The fonts self-host via @fontsource through the layout entry — the tracer proves install → import → build → built-output chain
- style.scss has zero CSS-import lines; no Google Fonts request remains in the built site (PERF-01 success criteria 1-2)
- The font contract is preserved: same families, same weights, same swap, byte-identical CSS variables (font-parity checklist 1-6)
- The jest suite is green, including the co-changed UPGR-02 block
</success_criteria>

## Artifacts this phase produces

- `package.json` — new exact-pinned dependencies "@fontsource/ubuntu": "5.3.0", "@fontsource/parisienne": "5.3.0"
- `yarn.lock` — resolution entries for both packages
- `src/components/layout.js` — three `@fontsource/*.css` imports + `// PERF-01:` comment lines, adjacent to the style.scss import
- `src/assets/scss/style.scss` — the two Google-Fonts CSS import lines removed (lines 7-8); @use block byte-identical
- `phase3-upgrade-matrix.test.js` — UPGR-02 block rewritten to assert the new font contract
- Commits: `feat(06-01): self-host fonts via @fontsource layout entry (PERF-01)` and optionally `fix(06-01): built-output font gate fixes (PERF-01)`
- `.planning/phases/06-performance-asset-cleanup-final-verification/06-01-SUMMARY.md` (created at plan completion)

<output>
Create `.planning/phases/06-performance-asset-cleanup-final-verification/06-01-SUMMARY.md` when done
</output>
</content>
