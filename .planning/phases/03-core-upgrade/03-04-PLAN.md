---
phase: 03-core-upgrade
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - package.json
  - yarn.lock
  - gatsby-config.js
  - gatsby-browser.js
autonomous: true
requirements:
  - UPGR-04
user_setup: []

must_haves:
  truths:
    - "gatsby-plugin-matomo ^0.17.0 and its gatsby-config.js block are removed; analytics run through a vendored `_paq` snippet in gatsby-browser.js (D-12/D-13)"
    - "The snippet keeps the same siteId \"4\" and matomoUrl \"https://matomo.duckdns.org/\" and pushes `disableCookies` BEFORE any trackPageView — cookie-less tracking, matching the privacy page's no-tracking-cookies claim (D-12)"
    - "The snippet is vendored in the repo — only matomo.js/matomo.php requests hit the Matomo server at runtime, no external script dependency at build time (D-13)"
    - "`yarn clean && yarn build && yarn test` exits 0 under Node 20"
  artifacts:
    - "package.json without gatsby-plugin-matomo"
    - "yarn.lock regenerated (matomo plugin tree gone)"
    - "gatsby-config.js without the matomo plugin block (lines 22-30)"
    - "gatsby-browser.js with the vendored _paq snippet + onRouteUpdate export (window-guarded)"
  key_links:
    - "route change → gatsby-browser.js onRouteUpdate → window._paq.push(trackPageView) → matomo.duckdns.org/matomo.php"
    - "page load → module-scope init → window._paq (disableCookies, setTrackerUrl, setSiteId, enableHeartBeatTimer) → matomo.js injected"
    - "yarn build → gatsby-config.js → no matomo plugin (removed)"
---

<objective>
Remove gatsby-plugin-matomo ^0.17.0 and its config block from gatsby-config.js (lines 22-30), and replace it with a vendored `_paq` snippet in gatsby-browser.js (D-12/D-13): module-scope init guarded by `typeof window !== "undefined"` (SSR/build safety), an `onRouteUpdate` export that tracks page views on route changes, the same siteId "4" and matomoUrl "https://matomo.duckdns.org/", and `disableCookies: true` (cookie-less tracking — the privacy page already claims no tracking cookies; this makes it true).

Purpose: Drop the unmaintained plugin and close the Matomo consent gap with cookie-less tracking, while keeping visit analytics working through a vendored snippet with no external build-time dependency.
Output: vendored _paq tracking in gatsby-browser.js, matomo plugin fully removed; `yarn clean && yarn build && yarn test` green under Node 20; one commit.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-upgrade/03-CONTEXT.md
@.planning/phases/03-core-upgrade/03-RESEARCH.md
@.planning/phases/03-core-upgrade/03-PATTERNS.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/CONVENTIONS.md
@package.json
@gatsby-config.js
@gatsby-browser.js
</context>

<tasks>

<task type="auto">
  <name>Vendored Matomo: remove gatsby-plugin-matomo + add _paq snippet with disableCookies (UPGR-04, D-12/D-13)</name>
  <files>package.json, yarn.lock, gatsby-config.js, gatsby-browser.js</files>
  <read_first>
    - package.json (dependencies line 36 gatsby-plugin-matomo ^0.17.0)
    - gatsby-config.js (lines 22-30: the matomo object block with siteId "4", matomoUrl "https://matomo.duckdns.org/", siteUrl "https://laryart.it", disableCookies false)
    - gatsby-browser.js (current file: only onServiceWorkerUpdateReady export)
    - .planning/phases/03-core-upgrade/03-CONTEXT.md D-12 (vendored snippet, same siteId/matomoUrl, disableCookies true), D-13 (vendored, not loaded from Matomo server)
    - .planning/phases/03-core-upgrade/03-RESEARCH.md §Code Examples (verified _paq snippet, lines 293-334)
    - .planning/phases/03-core-upgrade/03-PATTERNS.md §gatsby-browser.js (exact snippet + ordering constraint: disableCookies BEFORE trackPageView; setTimeout(…, 32) mirrors the old plugin's react-helmet title delay)
  </read_first>
  <action>
    Remove the plugin with yarn only (never npm): `yarn remove gatsby-plugin-matomo`.

    SERIALIZATION NOTE (W-1): this plan, 03-03, and 03-05 all run yarn add/remove against the same package.json + yarn.lock in wave 2. The execute-phase orchestrator serializes plans with overlapping files_modified — do NOT run yarn commands in parallel with the other wave-2 plans. If you detect another wave-2 executor active (uncommitted package.json/yarn.lock changes), wait for it to finish before running yarn.

    Edit gatsby-config.js: delete the entire matomo object block (lines 22-30, from `{` with `resolve: "gatsby-plugin-matomo"` through its closing `},`). This is the ONLY gatsby-config.js edit in this plan — line-specific and non-overlapping with the other wave-2 plans (03-03 renames the netlify-cms entry at line 83; 03-05 removes the advanced-sitemap entry at line 84). Do NOT reorder remaining plugins.

    Edit gatsby-browser.js: keep the existing onServiceWorkerUpdateReady export unchanged, and add the vendored _paq snippet:
    - const MATOMO_URL = "https://matomo.duckdns.org" and const MATOMO_SITE_ID = "4" (values migrated from the removed plugin block)
    - module-scope init guarded by `if (typeof window !== "undefined")`: window._paq = window._paq || []; push ["disableCookies"] FIRST (ordering constraint — config call before any trackPageView), then ["setTrackerUrl", `${MATOMO_URL}/matomo.php`], ["setSiteId", MATOMO_SITE_ID], ["enableHeartBeatTimer"]; then create the script element (g.type = "text/javascript", g.async = true, g.defer = true, g.src = `${MATOMO_URL}/matomo.js`) and insert it before the first script tag
    - export const onRouteUpdate = ({ location, prevLocation }) with a `typeof window === "undefined" || !window._paq` guard; build url from location.pathname + location.search + location.hash and prevUrl from prevLocation (null-safe); inside setTimeout(…, 32): push ["setReferrerUrl", prevUrl] if prevUrl, ["setCustomUrl", url], ["setDocumentTitle", document.title], ["trackPageView"], ["enableLinkTracking"]

    Style constraints (CONVENTIONS.md): no semicolons, double quotes, arrowParens "avoid" (the destructured object param keeps parens). Run `yarn format` after editing.

    Commit atomically: `git commit -m "feat(03-04): replace gatsby-plugin-matomo with vendored _paq snippet (UPGR-04)"` staging exactly package.json, yarn.lock, gatsby-config.js, gatsby-browser.js.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 20 >/dev/null && yarn install && yarn clean && yarn build && yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q 'gatsby-plugin-matomo' package.json` succeeds (zero matches)
    - `! grep -q 'gatsby-plugin-matomo' gatsby-config.js` succeeds (block removed)
    - `grep -q 'MATOMO_URL = "https://matomo.duckdns.org"' gatsby-browser.js` succeeds
    - `grep -q 'MATOMO_SITE_ID = "4"' gatsby-browser.js` succeeds
    - `grep -q '\["disableCookies"\]' gatsby-browser.js` succeeds and the disableCookies push appears BEFORE any trackPageView push in the file
    - `grep -q 'export const onRouteUpdate' gatsby-browser.js` succeeds
    - `grep -q 'typeof window === "undefined"' gatsby-browser.js` succeeds (SSR/build guard present)
    - `grep -q 'export const onServiceWorkerUpdateReady' gatsby-browser.js` succeeds (existing hook untouched)
    - `source ~/.nvm/nvm.sh && nvm use 20 && yarn install && yarn clean && yarn build && yarn test` exits 0
    - `git log -1 --format=%s` contains `03-04`
  </acceptance_criteria>
  <done>The matomo plugin is gone from deps and config, the vendored _paq snippet tracks page views cookie-less with the same siteId/matomoUrl, and the Node 20 loop is green in one commit.</done>
  <reversibility rating="reversible">git revert restores gatsby-plugin-matomo, its config block, and the previous gatsby-browser.js; yarn.lock reverts with it.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| browser → matomo.duckdns.org | matomo.js/matomo.php requests cross here at runtime; cookie-less (disableCookies) so no tracking cookie crosses back |
| gatsby-browser.js module scope → SSR/build | window/document access crosses here; the `typeof window !== "undefined"` guard prevents Node crashes |
| route change → onRouteUpdate → _paq | page-view events cross here; the !window._paq guard prevents errors when the snippet failed to load |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | DoS | build/SSR crash from window access in gatsby-browser.js module scope | high | mitigate | `typeof window !== "undefined"` guard on the init block; full clean+build+test gate under Node 20 |
| T-03-11 | Info disclosure | tracking cookies contradicting the privacy page's no-cookies claim | high | mitigate | disableCookies pushed BEFORE trackPageView (ordering constraint); acceptance grep asserts the order |
| T-03-12 | DoS | analytics silently stopping after plugin removal | medium | mitigate | Snippet mirrors the removed plugin's behavior (same siteId/matomoUrl, onRouteUpdate, 32ms title delay); live verification happens post-deploy in plan 03-06's checkpoint |
| T-03-SC | Tampering | external script dependency at build time | medium | mitigate | D-13: snippet is vendored in the repo; only runtime matomo.js/matomo.php requests hit the Matomo server |
</threat_model>

<verification>
- `source ~/.nvm/nvm.sh && nvm use 20 && yarn install && yarn clean && yarn build && yarn test` exits 0
- gatsby-plugin-matomo absent from package.json, yarn.lock, and gatsby-config.js
- gatsby-browser.js contains the vendored snippet with siteId "4", matomoUrl "https://matomo.duckdns.org/", disableCookies before trackPageView, window guards, and the untouched onServiceWorkerUpdateReady hook
- `git log` shows the single 03-04 commit
</verification>

<success_criteria>
- ROADMAP Phase 3 success criterion 4 groundwork: analytics keep recording via the vendored _paq snippet with disableCookies enabled (live verification happens post-deploy in plan 03-06's checkpoint)
- The privacy page's no-tracking-cookies claim is now true (cookie-less tracking)
- No external script dependency at build time (D-13)
- The Node 20 gate stays green
</success_criteria>

## Artifacts this phase produces

- `package.json` — gatsby-plugin-matomo removed
- `yarn.lock` — regenerated (matomo plugin tree gone)
- `gatsby-config.js` — matomo plugin block (lines 22-30) removed
- `gatsby-browser.js` — vendored _paq snippet + onRouteUpdate export with window guards
- Git commit `feat(03-04): replace gatsby-plugin-matomo with vendored _paq snippet (UPGR-04)`
- `.planning/phases/03-core-upgrade/03-04-SUMMARY.md` (created at plan completion)

<output>
Create `.planning/phases/03-core-upgrade/03-04-SUMMARY.md` when done
</output>
