---
phase: 03-core-upgrade
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - package.json
  - yarn.lock
  - gatsby-config.js
  - static/admin/config.yml
  - README.md
autonomous: true
requirements:
  - UPGR-03
user_setup: []

must_haves:
  truths:
    - "netlify-cms-app ^2.15.72 and gatsby-plugin-netlify-cms 6.22.0 are fully replaced by decap-cms-app 3.6.4 and gatsby-plugin-decap-cms 4.0.4 (D-08)"
    - "gatsby-plugin-netlify-cms-paths ^1.3.0 is KEPT — it is CMS-agnostic and actively processes the inline image in laryart.md (D-09)"
    - "static/admin/config.yml declares `branch: main` so CMS saves land on the repo's default branch (D-10)"
    - "README documents `npx decap-server` as the local CMS proxy command (D-11)"
    - "`yarn clean && yarn build && yarn test` exits 0 under Node 20 after the swap"
  artifacts:
    - "package.json with decap-cms-app 3.6.4 + gatsby-plugin-decap-cms 4.0.4, netlify-cms-app and gatsby-plugin-netlify-cms removed"
    - "yarn.lock regenerated (netlify-cms tree gone, decap tree in)"
    - "gatsby-config.js with the gatsby-plugin-netlify-cms entry renamed to gatsby-plugin-decap-cms"
    - "static/admin/config.yml with branch: main and the decap-server comment"
    - "README.md with the decap-server local-dev command"
  key_links:
    - "yarn build → gatsby-config.js → gatsby-plugin-decap-cms → static/admin/config.yml (branch: main)"
    - "yarn develop + npx decap-server → /admin/ local CMS editing"
    - "gatsby-plugin-netlify-cms-paths → static/admin/config.yml media_folder/public_folder (KEPT, D-09)"
---

<objective>
Replace the EOL netlify-cms stack with Decap CMS: netlify-cms-app ^2.15.72 → decap-cms-app 3.6.4 and gatsby-plugin-netlify-cms 6.22.0 → gatsby-plugin-decap-cms 4.0.4 (D-08), rename the plugin entry in gatsby-config.js, fix the CMS backend branch reference master → main in static/admin/config.yml (D-10), and update the README local-dev command to npx decap-server (D-11). gatsby-plugin-netlify-cms-paths stays (D-09 KEEP — CMS-agnostic, processes the laryart.md inline image).

Purpose: Move the content-editing stack to the maintained Decap fork so /admin keeps working after Netlify CMS went EOL, and make CMS saves land on the correct branch.
Output: Decap CMS wired end-to-end (deps + plugin + config + docs); `yarn clean && yarn build && yarn test` green under Node 20; one commit.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-upgrade/03-CONTEXT.md
@.planning/phases/03-core-upgrade/03-RESEARCH.md
@.planning/phases/03-core-upgrade/03-PATTERNS.md
@.planning/codebase/INTEGRATIONS.md
@package.json
@gatsby-config.js
@static/admin/config.yml
@README.md
</context>

<tasks>

<task type="auto">
  <name>Decap swap: deps + gatsby-config plugin rename + config.yml branch fix + README decap-server (UPGR-03, D-08/D-09/D-10/D-11)</name>
  <files>package.json, yarn.lock, gatsby-config.js, static/admin/config.yml, README.md</files>
  <read_first>
    - package.json (dependencies lines 37 gatsby-plugin-netlify-cms 6.22.0, 38 gatsby-plugin-netlify-cms-paths ^1.3.0, 50 netlify-cms-app ^2.15.72)
    - gatsby-config.js (line 83 `gatsby-plugin-netlify-cms` string entry; lines 6-11 netlifyCmsPaths const — KEEP)
    - static/admin/config.yml (line 3 branch: master; line 12 local_backend comment)
    - README.md (lines 43-50 Netlify CMS section; line 48 npx netlify-cms-proxy-server)
    - .planning/phases/03-core-upgrade/03-CONTEXT.md D-08/D-09/D-10/D-11
    - .planning/phases/03-core-upgrade/03-PATTERNS.md §gatsby-config.js, §static/admin/config.yml, §README.md
  </read_first>
  <action>
    Swap the CMS packages with yarn only (never npm): `yarn remove netlify-cms-app gatsby-plugin-netlify-cms && yarn add decap-cms-app@3.6.4 gatsby-plugin-decap-cms@4.0.4`.

    Edit gatsby-config.js: rename the string entry on line 83 from `gatsby-plugin-netlify-cms` to `gatsby-plugin-decap-cms`. This is the ONLY gatsby-config.js edit in this plan — line-specific and non-overlapping with the other wave-2 plans (03-04 removes the matomo block at lines 22-30; 03-05 removes the advanced-sitemap entry at line 84). Do NOT touch the netlifyCmsPaths const (lines 6-11) — gatsby-plugin-netlify-cms-paths stays (D-09 KEEP).

    Edit static/admin/config.yml: change line 3 `branch: master` to `branch: main` (D-10). Update the line 12 comment from `# run npx netlify-cms-proxy-server for local testing` to `# run npx decap-server for local testing` (D-11). Do NOT touch the collections schema or media_folder/public_folder (lines 14-15) — gatsby-plugin-netlify-cms-paths reads them.

    Edit README.md: in the Netlify CMS section (lines 43-50), update the local-dev command from `npx netlify-cms-proxy-server` to `npx decap-server` (D-11), and update the section heading/body to mention Decap CMS where natural (the maintained fork of Netlify CMS). Keep Italian prose. Do NOT make the Node-24 README edits here — those belong to plan 03-06 (D-07b).

    Commit atomically: `git commit -m "feat(03-03): replace Netlify CMS with Decap CMS (UPGR-03)"` staging exactly package.json, yarn.lock, gatsby-config.js, static/admin/config.yml, README.md.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 20 >/dev/null && yarn install && yarn clean && yarn build && yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"decap-cms-app": "3.6.4"' package.json` succeeds
    - `grep -q '"gatsby-plugin-decap-cms": "4.0.4"' package.json` succeeds
    - `! grep -q 'netlify-cms-app' package.json` succeeds (zero matches)
    - `! grep -q '"gatsby-plugin-netlify-cms"' package.json` succeeds (zero matches)
    - `grep -q '"gatsby-plugin-netlify-cms-paths": "^1.3.0"' package.json` succeeds (D-09 KEEP)
    - `grep -q 'gatsby-plugin-decap-cms' gatsby-config.js` succeeds and `! grep -q 'gatsby-plugin-netlify-cms"' gatsby-config.js` succeeds (plugin entry renamed; netlifyCmsPaths const untouched)
    - `grep -q 'branch: main' static/admin/config.yml` succeeds
    - `grep -q 'npx decap-server' static/admin/config.yml` succeeds (comment updated)
    - `grep -q 'npx decap-server' README.md` succeeds
    - `source ~/.nvm/nvm.sh && nvm use 20 && yarn install && yarn clean && yarn build && yarn test` exits 0
    - `git log -1 --format=%s` contains `03-03`
  </acceptance_criteria>
  <done>Decap CMS replaces Netlify CMS across deps, plugin registry, CMS config, and docs; netlify-cms-paths survives; the Node 20 loop is green in one commit.</done>
  <reversibility rating="reversible">git revert restores netlify-cms-app + gatsby-plugin-netlify-cms, the old plugin entry, branch: master, and the old README command; yarn.lock reverts with it.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| npm registry → yarn install | decap-cms-app 3.6.4 + gatsby-plugin-decap-cms 4.0.4 cross here; exact pins + yarn.lock |
| CMS editor → git-gateway → GitHub | CMS saves cross here; `branch: main` (D-10) ensures commits land on the repo's default branch |
| yarn build → gatsby-config.js → /admin bundle | the Decap plugin must bundle the admin UI; a wrong plugin name breaks the build here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | DoS | build break from the plugin rename | high | mitigate | Full clean+build+test gate under Node 20; acceptance greps assert the exact new plugin name and zero old-name remnants |
| T-03-08 | Tampering | CMS saves landing on the wrong branch (master vs main) | high | mitigate | D-10 branch fix in config.yml; acceptance grep asserts `branch: main` |
| T-03-09 | DoS | removing netlify-cms-paths and breaking the laryart.md inline image | medium | mitigate | D-09 KEEP verdict enforced: package.json and gatsby-config.js netlifyCmsPaths const untouched; acceptance greps assert both |
| T-03-SC | Tampering | supply-chain risk from the Decap packages | medium | mitigate | Exact pins (3.6.4 / 4.0.4) per D-08, yarn.lock committed, Decap is the community-maintained fork of Netlify CMS |
</threat_model>

<verification>
- `source ~/.nvm/nvm.sh && nvm use 20 && yarn install && yarn clean && yarn build && yarn test` exits 0
- decap-cms-app 3.6.4 + gatsby-plugin-decap-cms 4.0.4 present; netlify-cms-app and gatsby-plugin-netlify-cms absent
- gatsby-config.js plugin entry renamed; netlifyCmsPaths const intact
- config.yml branch: main + decap-server comment; README decap-server command
- `git log` shows the single 03-03 commit
</verification>

<success_criteria>
- ROADMAP Phase 3 success criterion 3 groundwork: /admin is served by Decap CMS (full editor login/save verification happens post-deploy in plan 03-06's checkpoint)
- The CMS config is Decap-compatible and saves land on the main branch
- gatsby-plugin-netlify-cms-paths keeps processing the laryart.md inline image (D-09)
- The Node 20 gate stays green
</success_criteria>

## Artifacts this phase produces

- `package.json` — decap-cms-app 3.6.4 + gatsby-plugin-decap-cms 4.0.4 in; netlify-cms-app + gatsby-plugin-netlify-cms out
- `yarn.lock` — regenerated (netlify-cms tree gone, decap tree in)
- `gatsby-config.js` — plugin entry renamed to gatsby-plugin-decap-cms (line 83 only)
- `static/admin/config.yml` — branch: main + decap-server comment
- `README.md` — npx decap-server local-dev command
- Git commit `feat(03-03): replace Netlify CMS with Decap CMS (UPGR-03)`
- `.planning/phases/03-core-upgrade/03-03-SUMMARY.md` (created at plan completion)

<output>
Create `.planning/phases/03-core-upgrade/03-03-SUMMARY.md` when done
</output>
