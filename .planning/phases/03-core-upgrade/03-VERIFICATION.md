---
phase: 03-core-upgrade
verified: 2026-08-19T17:34:42Z
status: passed
score: 12/12 must-haves verified
behavior_unverified: 0
overrides_applied: 2
overrides:
  - must_have: "Site analytics keep recording visits via the vendored Matomo _paq snippet with disableCookies enabled (gatsby-plugin-matomo removed)"
    reason: "Owner decision during the UPGR-07 checkpoint: matomo.duckdns.org proved unreachable from the browser (no-response on matomo.js), so the vendored _paq snippet could not track. Replaced with a vendored GA4 gtag snippet (measurement ID G-JFNK4HVQCC, anonymize_ip: true, same window-guard + onRouteUpdate pattern) in commit d370540, refined by f978563 (send_page_view: false dedup + absolute page_referrer). The requirement's intent — site analytics keep recording visits — is satisfied by GA4; the user confirmed live-site verification ('si funziona tutto')."
    accepted_by: "owner (Simone Foschi)"
    accepted_at: "2026-08-19T16:50:57Z"
  - must_have: "yarn clean && yarn build produces exactly one sitemap.xml, generated by gatsby-plugin-sitemap, listing the site's pages"
    reason: "The surviving gatsby-plugin-sitemap 6.16.0 writes sitemap-index.xml + sitemap-0.xml (its documented output format for sites under 45000 URLs), not sitemap.xml — that filename was the removed advanced-sitemap plugin's output convention. The plan's intent — exactly one sitemap pipeline generated by exactly one plugin, listing all site pages — is satisfied: 26 <loc> entries covering home, blog + pagination, all posts, info pages, and thanks. Documented as an auto-fixed deviation in 03-05-SUMMARY.md."
    accepted_by: "executor (documented deviation)"
    accepted_at: "2026-08-19T16:06:00Z"
---

# Phase 3: Core Upgrade Verification Report

**Phase Goal:** The site runs on Gatsby 5.16.1 (latest stable) with modern build tooling — dart-sass, Decap CMS, vendored Matomo, single sitemap
**Verified:** 2026-08-19T17:34:42Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Gatsby core and ALL gatsby-* plugins sit on the 5.16.x lockstep matrix in ONE commit (UPGR-01) | ✓ VERIFIED | `package.json` pins gatsby 5.16.1 + 12 plugins at .16.0 (exact, no caret); commit `9cdb0b5` staged exactly package.json + yarn.lock (269 insertions, 286 deletions); zero 5.15.0/6.15.0 remnants |
| 2 | `yarn clean && yarn build && yarn test` exits 0 on the new matrix (D-03 gate) | ✓ VERIFIED | Re-ran the full loop in this verification under Node 24.18.0: build exit 0 (27.7s), jest 4/4 suites (8 passed, 1 skipped). Node 20 gate was green at each plan commit (SUMMARY evidence + commit messages) |
| 3 | Legacy `gatsby-image` ^3.11.0 and `gatsby-plugin-netlify-cms-paths` ^1.3.0 untouched (D-02/D-09) | ✓ VERIFIED | Both present in `package.json` with original pins; `netlifyCmsPaths` const intact in `gatsby-config.js` (lines 6-11) |
| 4 | node-sass gone, `sass` ^1.30.0 (dart-sass) is the SCSS compiler with zero gatsby-config.js changes (D-04) | ✓ VERIFIED | `package.json` has `"sass": "^1.30.0"`, no node-sass; `gatsby-config.js` has bare `gatsby-plugin-sass` string entry (no `implementation` option); yarn.lock resolves sass@^1.30.0 |
| 5 | Google Fonts `@import url(...)` lines hoisted to top of style.scss, removed from `:root` (D-06) | ✓ VERIFIED | `style.scss` lines 1-2 are the two font imports; `_theme-variables.scss` `:root` has zero @imports; built CSS (`public/styles.*.css`) shows both imports at top level, column 1, before `:root{...}` |
| 6 | Phase 2 node-sass hygiene guards removed; Node enforcement guards KEPT (D-05) | ✓ VERIFIED | `scripts/` contains only `check-node-version.js`; no `postinstall` in package.json; `preinstall`/`prebuild`/`predevelop` present; `.yarnrc` has `engine-strict true` |
| 7 | netlify-cms-app + gatsby-plugin-netlify-cms fully replaced by decap-cms-app 3.6.4 + gatsby-plugin-decap-cms 4.0.4; branch: main; decap-server docs (UPGR-03) | ✓ VERIFIED | `package.json` has decap-cms-app 3.6.4 + gatsby-plugin-decap-cms 4.0.4, zero netlify-cms-app/gatsby-plugin-netlify-cms; `gatsby-config.js` entry renamed; `static/admin/config.yml` has `branch: main` + `npx decap-server` comment; README documents `npx decap-server` |
| 8 | gatsby-plugin-matomo removed; analytics run through a vendored snippet in gatsby-browser.js | ✓ VERIFIED (override) | Zero matomo references in package.json/yarn.lock/gatsby-config.js; `gatsby-browser.js` contains a vendored gtag snippet (GA4, owner decision — see overrides) with window guards + onRouteUpdate |
| 9 | Cookie-less / anonymized tracking matching the privacy page claim | ✓ PASSED (override) | GA4 `anonymize_ip: true` in the module-scope config; privacy.md updated in f978563 to accurately describe first-party cookies + last-octet IP truncation |
| 10 | gatsby-plugin-advanced-sitemap removed; gatsby-plugin-sitemap 6.16.0 is the single sitemap generator (UPGR-06) | ✓ VERIFIED (override) | Zero advanced-sitemap references in package.json/yarn.lock/gatsby-config.js; `"gatsby-plugin-sitemap": "6.16.0"` + config entry kept; build output `public/sitemap-index.xml` + `public/sitemap-0.xml` with 26 `<loc>` entries (filename deviation — see overrides) |
| 11 | Node 24 enforced (.nvmrc, engines.node, README) in a SEPARATE commit after the Node 20 matrix was green (D-07/D-07b) | ✓ VERIFIED | `.nvmrc` = `24`; `engines.node` = `"24.x"`; README Node 24 notes + `nvm alias default 24`; commit `293a776` staged exactly .nvmrc + package.json + README.md, ordered after all dependency commits; `check-node-version.js` and `netlify.toml` untouched in the phase |
| 12 | First post-upgrade Netlify deploy (cache cleared) succeeds on Node 24; /admin login + post save via Decap CMS; analytics recording; sitemap served (UPGR-07) | ✓ VERIFIED (user-confirmed) | Checkpoint closed in commit `100c634` — user confirmed: clear-cache deploy succeeded with Node 24 in the build log, all pages render, /admin login + post save via Decap CMS, GA4 visits recorded, sitemap-0.xml served. Local corroboration: `public/admin/` contains decap-cms-app.js + index.html; GA4 ID found in built JS |

**Score:** 12/12 truths verified (2 PASSED via override, 0 present-but-behavior-unverified)

### Documented Deviations (not gaps)

1. **Matomo → GA4 (owner decision, UPGR-07 checkpoint).** The phase goal text says "vendored Matomo", but matomo.duckdns.org proved unreachable from the browser during live verification. The owner chose GA4 (G-JFNK4HVQCC, vendored gtag snippet, `anonymize_ip: true`) in commit `d370540`, refined by `f978563` (CR-03 dedup fix: `send_page_view: false` so onRouteUpdate owns every pageview; CR-04 privacy disclosure accuracy). The requirement's intent — analytics keep recording visits — is satisfied and user-confirmed live. Recorded as an override, not a gap.
2. **Sitemap filename.** The surviving gatsby-plugin-sitemap writes `sitemap-index.xml` + `sitemap-0.xml` (documented output format), not `sitemap.xml`. Single-pipeline intent satisfied with 26 entries. Recorded as an override.

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `package.json` | 13 gatsby-* pins at 5.16.x; sass ^1.30.0; decap 3.6.4/4.0.4; no node-sass/matomo/advanced-sitemap/netlify-cms; engines 24.x | ✓ VERIFIED | All pins exact; resolutions pin for @mapbox/jsonlint-lines-primitives 2.0.2 present |
| `yarn.lock` | Regenerated; gatsby@5.16.1, sass@^1.30.0, decap trees in; node-sass/matomo/advanced-sitemap trees out | ✓ VERIFIED | Confirmed via lockfile greps |
| `gatsby-config.js` | matomo block + advanced-sitemap entry removed; decap-cms entry renamed; netlifyCmsPaths + sitemap kept | ✓ VERIFIED | Lines verified |
| `gatsby-browser.js` | Vendored analytics snippet + onRouteUpdate, window-guarded, onServiceWorkerUpdateReady intact | ✓ VERIFIED | GA4 snippet (override); guards present; CR-03 fix applied |
| `src/assets/scss/style.scss` | Font imports at top | ✓ VERIFIED | Lines 1-2 |
| `src/assets/scss/_theme-variables.scss` | `:root` without @imports | ✓ VERIFIED | Clean |
| `scripts/clean-node-sass-vendor.js` | Deleted | ✓ VERIFIED | `scripts/` has only check-node-version.js |
| `static/admin/config.yml` | branch: main + decap-server comment | ✓ VERIFIED | Verified |
| `.nvmrc` | `24` | ✓ VERIFIED | Single line `24` |
| `README.md` | dart-sass, Decap, GA4, Node 24, decap-server | ✓ VERIFIED | All present; zero stale Node 20 references |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| yarn install | yarn.lock | gatsby 5.16.1 + 12 plugins at .16.0 | ✓ WIRED | Lockfile greps confirm |
| yarn build | gatsby-config.js plugin registry | all plugins resolve on new matrix | ✓ WIRED | Full build re-ran green |
| yarn build | gatsby-plugin-sass | sass (dart-sass) auto-detected | ✓ WIRED | No `implementation` option; build green |
| yarn build | public CSS | font @imports at top level | ✓ WIRED | Built CSS verified |
| yarn build | gatsby-plugin-sitemap | public/sitemap-index.xml + sitemap-0.xml | ✓ WIRED | 26 `<loc>` entries |
| yarn build | gatsby-plugin-decap-cms | public/admin/ bundle | ✓ WIRED | decap-cms-app.js + index.html in built output |
| route change | gatsby-browser.js onRouteUpdate | gtag config with page_path/page_referrer | ✓ WIRED | Code verified; CR-03 dedup fix applied |
| nvm use 24 | .nvmrc → engines + engine-strict → preinstall | check-node-version.js | ✓ WIRED | Guard reads .nvmrc dynamically; loop green under Node 24 |
| Netlify deploy | .nvmrc (24) → yarn build → public/ | laryart.it | ✓ WIRED (user-confirmed) | Clear-cache deploy succeeded, Node 24 in build log |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| gatsby-browser.js (analytics) | `GA_MEASUREMENT_ID` / `location` | gtag.js runtime script + Gatsby route state | Yes — real measurement ID, real route data | ✓ FLOWING |
| public/sitemap-0.xml | `<loc>` entries | gatsby-plugin-sitemap from site.json siteUrl + created pages | Yes — 26 real URLs | ✓ FLOWING |
| public/admin/ | Decap CMS bundle | gatsby-plugin-decap-cms → decap-cms-app 3.6.4 | Yes — real admin bundle | ✓ FLOWING |
| public/styles.*.css | font @imports | style.scss → dart-sass | Yes — real top-level imports | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Full test suite green under Node 24 | `TMPDIR=/home/simos/tmp yarn test` | 4 suites passed, 8 passed / 1 skipped, exit 0 | ✓ PASS |
| Production build green under Node 24 | `TMPDIR=/home/simos/tmp yarn clean && yarn build` | exit 0, 27.7s | ✓ PASS |
| Font imports at top level in built CSS | `grep -n "@import" public/*.css \| grep -i fonts.googleapis` | Both imports at line 1, column 1, before `:root{...}` | ✓ PASS |
| Sitemap lists site pages | `grep -o "<loc>" public/sitemap-0.xml \| wc -l` | 26 entries (home, blog + pagination, 19 posts, 4 info pages, thanks) | ✓ PASS |
| GA4 snippet in built JS | `grep -l "G-JFNK4HVQCC" public/*.js` | Found in public/app-*.js | ✓ PASS |
| Decap admin bundle built | `ls public/admin/` | decap-cms-app.js, index.html, config.yml, netlify-identity.js | ✓ PASS |

### Probe Execution

No probes declared in any PLAN/SUMMARY and no `scripts/*/tests/probe-*.sh` files exist — Step 7c: SKIPPED (not applicable to this phase).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| UPGR-01 | 03-01 | Gatsby 5.16.1 + all gatsby-* plugins in lockstep, one commit | ✓ SATISFIED | 13 exact pins; commit 9cdb0b5 (package.json + yarn.lock only) |
| UPGR-02 | 03-02, 03-06 | node-sass → dart-sass; build passes locally and on Netlify | ✓ SATISFIED | sass ^1.30.0, no node-sass; Node 20 and Node 24 loops green; Netlify deploy user-confirmed |
| UPGR-03 | 03-03 | netlify-cms-app → decap-cms-app 3.6.4 + gatsby-plugin-decap-cms 4.0.4; /admin works | ✓ SATISFIED | Deps + config + docs verified; /admin login + post save user-confirmed live |
| UPGR-04 | 03-04 | gatsby-plugin-matomo → vendored snippet | ✓ SATISFIED (override) | Plugin removed; vendored GA4 snippet (owner decision — Matomo endpoint unreachable); visits user-confirmed |
| UPGR-06 | 03-05 | gatsby-plugin-advanced-sitemap removed | ✓ SATISFIED (override) | Zero references; single sitemap pipeline, 26 entries (sitemap-index.xml + sitemap-0.xml) |
| UPGR-07 | 03-06 | First post-upgrade Netlify deploy with cleared cache | ✓ SATISFIED | User-confirmed checkpoint (commit 100c634): Node 24 build log, all pages render, CMS save, GA4 visits, sitemap served |

**Orphaned requirements:** none. All 6 phase requirement IDs (UPGR-01, UPGR-02, UPGR-03, UPGR-04, UPGR-06, UPGR-07) are claimed by plans and satisfied. UPGR-05 correctly belongs to Phase 4.

**Bookkeeping note (info):** the REQUIREMENTS.md traceability table at the bottom still shows UPGR-02/UPGR-06/UPGR-07 as "Pending" while the checkbox list at the top shows them `[x]` — a documentation lag, not a code gap.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| src/assets/scss/style.scss | 220 | Stray `©` producing invalid CSS (CR-01) | ⚠️ Warning | Pre-existing (present in commit 9cdb0b5, the phase's first commit) — out of phase scope per 03-REVIEW.md |
| src/assets/scss/style.scss | 555-559 | Broken mobile pagination `padding: 50px 0 ul` (CR-02) | ⚠️ Warning | Pre-existing — out of phase scope |
| scripts/check-node-version.js | 21 | Stale node-sass error message (WR-01) | ⚠️ Warning | File untouched by phase; guard logic correct (reads .nvmrc dynamically) |
| AGENTS.md | 9, 14 | Stale node-sass/postinstall + netlify-cms-proxy-server docs (WR-02) | ⚠️ Warning | Out of phase scope (not in any plan's files_modified) |
| gatsby-browser.js | 36-44 | 32ms setTimeout title race (WR-03) | ⚠️ Warning | Minor; page_referrer already fixed to absolute URL in f978563 |
| static/admin/config.yml | 12 | `local_backend: true` committed in production config (WR-04) | ⚠️ Warning | Pre-existing pattern; Decap falls back to git-gateway |
| gatsby-browser.js | 4-24 | GA4 loads with no consent mechanism (WR-05) | ⚠️ Warning | Compliance risk under Italian cookie guidelines; owner decision documented |

No TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER markers in any phase-modified file. No blocker anti-patterns.

### Human Verification Required

None outstanding. The phase's manual checkpoint (UPGR-07, D-15) was already performed and confirmed by the user: Netlify clear-cache deploy succeeded on Node 24, all pages render, /admin login + post save via Decap CMS, GA4 visits recorded, sitemap-0.xml served ("si funziona tutto"). Recorded in 03-06-SUMMARY.md and commit 100c634.

### Gaps Summary

No gaps. All 12 must-have truths verified (2 via documented owner/executor overrides), all artifacts present and substantive, all key links wired, all 6 requirement IDs satisfied, no blocker anti-patterns, and the manual checkpoint user-confirmed. The phase goal is achieved: the site runs on Gatsby 5.16.1 with dart-sass, Decap CMS, a vendored analytics snippet (GA4 per owner decision), and a single sitemap pipeline.

---

_Verified: 2026-08-19T17:34:42Z_
_Verifier: the agent (gsd-verifier)_
