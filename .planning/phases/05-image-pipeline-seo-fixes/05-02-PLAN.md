---
phase: 05-image-pipeline-seo-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/templates/index-page.js
  - src/components/blog-list-home.js
  - src/templates/blog-list.js
  - src/components/post-card.js
  - package.json
  - yarn.lock
autonomous: true
requirements:
  - IMAG-01
  - IMAG-02
user_setup: []

must_haves:
  truths:
    - "index-page hero queries gatsbyImageData(layout: \"constrained\", quality: 80, breakpoints: [960, 1440], placeholder: DOMINANT_COLOR) and renders <GatsbyImage image={getImage(...)}> with className=\"featured-image\" and loading=\"eager\" — the only surface in the phase with an eager override and the only DOMINANT_COLOR surface (D-03, D-07)"
    - "The post-card query in BOTH blog-list.js and blog-list-home.js becomes gatsbyImageData(layout: \"constrained\", width: 540, height: 360, quality: 80, transformOptions: { fit: COVER, cropFocus: CENTER }, placeholder: BLURRED) — width/height replace the deprecated maxWidth/maxHeight, and transformOptions override the default saliency crop (cropFocus attention) to restore the legacy center crop; the two queries are byte-identical after the swap (IMAG-01, D-03)"
    - "post-card.js renders <GatsbyImage image={getImage(data.frontmatter.featuredImage.childImageSharp)} objectFit=\"cover\" objectPosition=\"50% 50%\" alt={title + \" - Featured image\"} className=\"featured-image\" /> inside the existing Link wrapper — className lands on the GatsbyImage wrapper, preserving .post-card .featured-image SCSS hooks without rewriting style.scss (D-04, UI-SPEC parity rule 4)"
    - "After the migration zero `gatsby-image` imports remain in src/ (only the blog-list.test.js jest.mock reference is left, and that is removed in 05-04) — grep -rn \"gatsby-image\" src/ returns only test-mock hits"
    - "gatsby-image ^3.11.0 is removed from package.json and yarn.lock (D-05) — only after every consumer migrated; yarn install && yarn build && yarn test exit 0"
    - "Blog cards render at 540x360 with center crop as before; adjacent cards in a row do not merge or overlap and order remains date-DESC stable (the sort is untouched)"
    - { statement: "An index-hero image processing failure surfaces as a Gatsby build error, not a runtime state — a held-out visual check on / after build confirms the hero renders", verification: backstop }
    - { statement: "A card image processing failure surfaces as a Gatsby build error, not a runtime state — a held-out visual check on /blog after build confirms cards render at the legacy center crop", verification: backstop }
  artifacts:
    - "src/templates/index-page.js — hero query + component migrated (DOMINANT_COLOR, eager)"
    - "src/components/blog-list-home.js — StaticQuery card query migrated (byte-identical to blog-list's)"
    - "src/templates/blog-list.js — pageQuery card query migrated"
    - "src/components/post-card.js — Img → GatsbyImage component swap"
    - "package.json — gatsby-image removed from dependencies"
    - "yarn.lock — gatsby-image resolution tree gone"
  key_links:
    - "blog-list.js pageQuery → PostCard component → GatsbyImage (query and component are in different files — both must swap in lockstep or the build breaks with a GraphQL error)"
    - "blog-list-home.js StaticQuery → PostCard → GatsbyImage (same lockstep requirement)"
    - "yarn remove gatsby-image → package.json + yarn.lock → grep-verified zero legacy imports in src/"
  prohibitions:
    - "loading=\"eager\" must NOT appear on post-card or blog-list surfaces (hero-only contract, D-07 parity rule 8)"
    - "DOMINANT_COLOR must NOT appear on any surface other than the index hero (D-07)"
    - "transformOptions must NOT be omitted from the card query (default cropFocus is attention — legacy center-crop parity requires fit: COVER, cropFocus: CENTER)"
    - "style.scss must NOT be rewritten — className=featured-image on the wrapper keeps the existing selectors matching (if .post-card .featured-image img breaks, restore with imgClassName, never SCSS edits)"
    - "gatsby-image must NOT be removed while any consumer still imports it — the three migration tasks run before the removal task in this plan"
---

<objective>
Wave-2 horizontal expansion of the image pipeline: migrate the remaining three surfaces — index-page hero (index-page.js), post-card component (post-card.js), and both card query sites (blog-list.js pageQuery + blog-list-home.js StaticQuery) — from gatsby-image to gatsby-plugin-image, then remove gatsby-image from the dependency tree (D-05).

Purpose: complete IMAG-01 (all five surfaces migrated; the blog-post tracer from 05-01 proved the pipeline) and IMAG-02 (hero placeholder contract: DOMINANT_COLOR + eager). The hero is the only DOMINANT_COLOR surface and the only eager surface; the card query requires the transformOptions center-crop override to preserve legacy crop parity.

Output: all four remaining query/component sites migrated, zero legacy gatsby-image imports in src/, gatsby-image removed from package.json + yarn.lock, `yarn build && yarn test` green, one commit per surface + one for the removal.

Environment notes (apply to every verify step in this plan): node is NOT on PATH — prefix commands with `source ~/.nvm/nvm.sh && nvm use 24` (nvm has v20.20.2, v24.18.0, v24.19.0; the project enforces Node 24 via engines + engine-strict + scripts/check-node-version.js; .nvmrc is 24). /tmp is a tmpfs that fills with JIT artifacts — set TMPDIR=/home/simos/tmp for yarn/build commands. yarn 1.22 only, never npm.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-image-pipeline-seo-fixes/05-CONTEXT.md
@.planning/phases/05-image-pipeline-seo-fixes/05-RESEARCH.md
@.planning/phases/05-image-pipeline-seo-fixes/05-PATTERNS.md
@.planning/phases/05-image-pipeline-seo-fixes/05-UI-SPEC.md
@.planning/phases/05-image-pipeline-seo-fixes/05-VALIDATION.md
@package.json
@src/templates/index-page.js
@src/components/post-card.js
@src/templates/blog-list.js
@src/components/blog-list-home.js
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Migrate index-page hero: DOMINANT_COLOR + eager (IMAG-01/02, D-03/D-07)</name>
  <files>src/templates/index-page.js</files>
  <read_first>
    - src/templates/index-page.js (import line 3, Image extraction lines 37-39, hero component block lines 59-67, query lines 18-24)
    - .planning/phases/05-image-pipeline-seo-fixes/05-PATTERNS.md §index-page.js (import swap, getImage-only — no getSrc; hero swap with loading="eager"; query args matrix)
    - .planning/phases/05-image-pipeline-seo-fixes/05-RESEARCH.md Code Example 2 (hero target), Pitfall 5 (eager on non-hero — apply nowhere else)
    - .planning/phases/05-image-pipeline-seo-fixes/05-UI-SPEC.md Image Surface Contract (index hero row: DOMINANT_COLOR + eager; parity rules 2, 7-8)
    - .planning/phases/05-image-pipeline-seo-fixes/05-CONTEXT.md D-02, D-03 (hero sizing breakpoints 960/1440), D-07 (DOMINANT_COLOR for the hero)
  </read_first>
  <action>
    Migrate src/templates/index-page.js in place:

    Import swap (line 3): replace `import Img from "gatsby-image"` with `import { GatsbyImage, getImage } from "gatsby-plugin-image"` (NO getSrc — this surface passes no image prop; `<Seo />` at line 42 is bare and stays bare).

    Data extraction (lines 37-39): replace `const Image = frontmatter.featuredImage ? frontmatter.featuredImage.childImageSharp.fluid : ""` with `const Image = getImage(frontmatter.featuredImage?.childImageSharp)`. Keep the guard shape.

    Hero component (lines 59-67): replace `<Img fluid={Image} alt={...} className="featured-image" />` with `<GatsbyImage image={Image} alt={frontmatter.title + " - Featured image"} className="featured-image" loading="eager" />`. Do NOT add objectFit/objectPosition — today's code has none and the defaults (cover / 50% 50%) match (parity). loading="eager" is this surface's D-07 contract (above-the-fold LCP) and appears NOWHERE else.

    Query (lines 18-24): replace the fluid block with gatsbyImageData(layout: "constrained", quality: 80, breakpoints: [960, 1440], placeholder: DOMINANT_COLOR). Drop the ...GatsbyImageSharpFluid fragment. The enum is uppercase-unquoted. Note: DOMINANT_COLOR is unique to this surface (D-07).

    Everything else (title/tagline/cta fields, Layout, BlogListHome import, the .home-banner grids wrapper) is verbatim.

    Prettier conventions: no semicolons, double quotes, arrowParens avoid, 2-space indent; run `yarn format` after editing.

    Commit atomically: `git commit -m "feat(05-02): migrate index hero to gatsby-plugin-image with dominant-color placeholder (IMAG-01, IMAG-02, D-07)"` staging exactly src/templates/index-page.js.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && grep -q "gatsby-image-wrapper" public/index.html && ! grep -q "gatsby-image-outer-wrapper" public/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'import { GatsbyImage, getImage } from "gatsby-plugin-image"' src/templates/index-page.js` succeeds
    - `! grep -q 'gatsby-image' src/templates/index-page.js` succeeds (legacy import gone)
    - `grep -q 'placeholder: DOMINANT_COLOR' src/templates/index-page.js` succeeds (hero-only placeholder, D-07)
    - `grep -q 'loading="eager"' src/templates/index-page.js` succeeds (hero-only eager)
    - `grep -q 'breakpoints: \[960, 1440\]' src/templates/index-page.js` succeeds
    - `! grep -q 'GatsbyImageSharpFluid' src/templates/index-page.js` succeeds
    - `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn build` exits 0
    - `grep -c "gatsby-image-outer-wrapper" public/index.html` → 0 (legacy class gone from the home page HTML)
    - `git log -1 --format=%s` contains `05-02`
  </acceptance_criteria>
  <done>The home page hero renders through GatsbyImage with a DOMINANT_COLOR placeholder, eager loading, the legacy wrapper class absent from the built home HTML, and the build green.</done>
  <reversibility rating="reversible">git revert restores the gatsby-image hero; a single commit covers the whole file.</reversibility>
</task>

<task type="auto">
  <name>Migrate card surface (post-card component + blog-list/blog-list-home queries) (IMAG-01, D-02/D-03/D-04)</name>
  <files>src/components/post-card.js, src/templates/blog-list.js, src/components/blog-list-home.js</files>
  <read_first>
    - src/components/post-card.js (import line 3, Img block lines 7-19 — the component used by BOTH lists)
    - src/templates/blog-list.js (query lines 25-32 — card query in the pageQuery)
    - src/components/blog-list-home.js (query lines 44-51 — byte-identical card query in the StaticQuery)
    - .planning/phases/05-image-pipeline-seo-fixes/05-PATTERNS.md §post-card.js (component swap with Link wrapper; query swap with width 540 / height 360 / fit COVER / cropFocus CENTER) and §blog-list.js query + §blog-list-home.js (byte-identical queries; pageContext lines 42, 62 for pagination are untouched)
    - .planning/phases/05-image-pipeline-seo-fixes/05-RESEARCH.md Code Example 3 (card target), Pitfall 2 (crop drift without the transformOptions override), Pitfall 6 (SSR registration already done in 05-01)
    - .planning/phases/05-image-pipeline-seo-fixes/05-UI-SPEC.md Image Surface Contract (post-card row: width/height + transformOptions args; parity rule 1 aspect 540x360, rule 4 class hooks)
    - .planning/phases/05-image-pipeline-seo-fixes/05-CONTEXT.md D-02 (post-card covers BOTH lists), D-03 (card sizing), D-04 (alt text)
  </read_first>
  <action>
    Migrate the three card-surface files in ONE change — the component swap and the two parent query swaps must land together or the build breaks (GatsbyImage receives a fluid object / the query shape no longer matches):

    post-card.js:
    - Import swap (line 3): `import { GatsbyImage, getImage } from "gatsby-plugin-image"` (no getSrc — cards pass no image prop).
    - Component swap (lines 9-15): inside the existing `{data.frontmatter.featuredImage ? (<Link to={...}> ... </Link>) : ""}` guard, replace `<Img fluid={data.frontmatter.featuredImage.childImageSharp.fluid} objectFit="cover" objectPosition="50% 50%" alt={data.frontmatter.title + " - Featured image"} className="featured-image" />` with `<GatsbyImage image={getImage(data.frontmatter.featuredImage.childImageSharp)} objectFit="cover" objectPosition="50% 50%" alt={data.frontmatter.title + " - Featured image"} className="featured-image" />`. Do NOT add loading (default lazy is the contract).

    blog-list.js query (lines 27-32) and blog-list-home.js query (lines 46-51) — byte-identical swap in both:
    - Replace the `fluid(maxWidth: 540, maxHeight: 360, quality: 80)` fragment block with: `gatsbyImageData(layout: "constrained", width: 540, height: 360, quality: 80, transformOptions: { fit: COVER, cropFocus: CENTER }, placeholder: BLURRED)`.
    - width/height (NOT maxWidth/maxHeight — deprecated); transformOptions: { fit: COVER, cropFocus: CENTER } (required — default cropFocus is attention/saliency and would shift the crop from the legacy center).
    - The rest of each query (sort, filter, limit/skip, id/excerpt/date/slug/title) is untouched.

    Keep the guard semantics: a post without featuredImage renders no image (existing conditional).

    Prettier conventions + `yarn format` on all three files.

    Commit atomically: `git commit -m "feat(05-02): migrate post cards to gatsby-plugin-image with center crop (IMAG-01, D-02)"` staging exactly src/components/post-card.js, src/templates/blog-list.js, src/components/blog-list-home.js.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && grep -q "gatsby-image-wrapper" public/blog/index.html && grep -q "gatsby-image-wrapper" public/index.html && ! grep -q "gatsby-image-outer-wrapper" public/blog/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'import { GatsbyImage, getImage } from "gatsby-plugin-image"' src/components/post-card.js` succeeds
    - `! grep -q 'gatsby-image' src/components/post-card.js src/templates/blog-list.js src/components/blog-list-home.js` succeeds (all three clean)
    - `grep -q 'transformOptions: { fit: COVER, cropFocus: CENTER }' src/templates/blog-list.js` succeeds
    - `grep -q 'transformOptions: { fit: COVER, cropFocus: CENTER }' src/components/blog-list-home.js` succeeds
    - `grep -q 'width: 540' src/templates/blog-list.js && grep -q 'height: 360' src/components/blog-list-home.js` succeeds
    - `! grep -q 'maxWidth\|maxHeight' src/templates/blog-list.js src/components/blog-list-home.js` succeeds (deprecated args gone)
    - `! grep -q 'GatsbyImageSharpFluid' src/` succeeds (fragments gone from all queries)
    - `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn build` exits 0
    - `grep -c "gatsby-image-outer-wrapper" public/blog/index.html` → 0 and `public/index.html` → 0
    - `git log -1 --format=%s` contains `05-02`
  </acceptance_criteria>
  <done>Cards render through GatsbyImage on both lists with the 540x360 center-crop query contract; both card queries byte-identical; no legacy fluid/maxWidth remnants in src/; build green.</done>
  <reversibility rating="reversible">git revert restores the three card files; no external state.</reversibility>
</task>

<task type="auto">
  <name>Remove gatsby-image from package.json + yarn.lock (IMAG-01, D-05)</name>
  <files>package.json, yarn.lock</files>
  <read_first>
    - package.json (line 31 — "gatsby-image": "^3.11.0" — the last legacy consumer in the tree, after the previous two tasks migrated every import in src/)
    - .planning/phases/05-image-pipeline-seo-fixes/05-CONTEXT.md D-05 (remove gatsby-image after migration)
    - .planning/phases/05-image-pipeline-seo-fixes/05-RESEARCH.md Package Legitimacy Audit (gatsby-image flagged SUS-deprecated — deprecation is the removal reason, no checkpoint needed) and State of the Art (unmaintained, blocks sharp 5.x lockstep)
    - AGENTS.md (yarn 1.22 only; postinstall node-sass guard — if install errors, run nvm use && yarn install)
  </read_first>
  <action>
    Remove the legacy image plugin with yarn only (never npm): `source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn remove gatsby-image`. This updates package.json (line 31 removed) and regenerates yarn.lock (gatsby-image tree + its internal deps gone) in one step.

    Do NOT touch: gatsby-plugin-image 3.16.0 (must stay), any other dependency, scripts, engines, resolutions.

    Commit atomically: `git commit -m "feat(05-02): remove gatsby-image after full migration (IMAG-01, D-05)"` staging exactly package.json and yarn.lock.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn install && ! grep -q '"gatsby-image"' package.json && ! grep -q 'gatsby-image' yarn.lock && TMPDIR=/home/simos/tmp yarn build && yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q 'gatsby-image' package.json` succeeds
    - `! grep -q 'gatsby-image' yarn.lock` succeeds
    - `grep -rn "from \"gatsby-image\"\|from 'gatsby-image'\|require(\"gatsby-image\")" src/` returns zero matches (no dangling imports — the three migrations removed all consumers)
    - `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn install && yarn build && yarn test` exits 0
    - `git log -1 --format=%s` contains `05-02`
  </acceptance_criteria>
  <done>gatsby-image is gone from the dependency tree and the lockfile; src/ has zero imports of it; the full install → build → test loop is green.</done>
  <reversibility rating="reversible">git revert restores package.json + yarn.lock; the removed package is re-resolvable from yarn.lock history. The removal is the culmination of the migration — the consumers were already swapped.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GraphQL resolver → templates | gatsbyImageData objects cross into index-page/blog-list/blog-list-home here; arg-name drift silently degrades output |
| blog-list/blog-list-home → PostCard | query + component in different files — a one-sided swap breaks the build with a GraphQL type error |
| npm registry → yarn install | gatsby-image leaves the tree here; yarn.lock regenerates |
| browser → cards/hero | rendered GatsbyImage markup crosses here; missing classes mean silent visual regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Tampering | card query — cropFocus defaults to attention (saliency); center-crop parity lost | medium | mitigate | transformOptions: { fit: COVER, cropFocus: CENTER } locked in both card queries; acceptance greps assert it |
| T-05-07 | Tampering | maxWidth/maxHeight deprecated args creep back | low | mitigate | acceptance greps assert `! maxWidth && ! maxHeight` in both card query files |
| T-05-08 | Tampering | hero eager/dominant-color contract leaks to cards (contrast/LCP intent) | medium | mitigate | acceptance greps pin eager to index-page.js only; DOMINANT_COLOR pinned to index-page.js only |
| T-05-09 | Denial of service | build failure from component/query swap mismatch across the three card files | high | block | the three files migrate in ONE commit; full build gate after the commit |
| T-05-10 | Tampering | dangling gatsby-image import after removal → build breaks mid-wave | critical | block | removal task runs LAST in the plan and is gated by the zero-import grep across src/ |
| T-05-SC | Tampering | supply chain — gatsby-image removal | low | mitigate | no new packages in this plan; removal audited in RESEARCH.md (deprecated-flag SUS, removal is the fix) |
</threat_model>

<verification>
- `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn install && yarn build && yarn test` exits 0
- `grep -rn "gatsby-image" src/` returns zero matches (success criterion 1: no gatsby-image imports remain)
- `grep -rl "gatsby-image-outer-wrapper" public/ | wc -l` → 0
- index page HTML shows DOMINANT_COLOR behavior (grep `data-placeholder-image` in public/index.html) and /blog shows the wrapper class
</verification>

<success_criteria>
- IMAG-01 complete: all five surfaces (blog-post, index-page, blog-list, post-card, blog-list-home) render via gatsby-plugin-image; zero gatsby-image imports and the package removed from the tree
- IMAG-02 hero contract: DOMINANT_COLOR + eager on the index hero only; BLURRED elsewhere; tracedSVG already removed in 05-01
- Card crop parity: transformOptions fit COVER / cropFocus CENTER on both list queries
- Full install → build → test loop green
</success_criteria>

## Artifacts this phase produces

- `src/templates/index-page.js` — import `{ GatsbyImage, getImage } from "gatsby-plugin-image"`, hero query `gatsbyImageData(layout: "constrained", quality: 80, breakpoints: [960, 1440], placeholder: DOMINANT_COLOR)`, `<GatsbyImage loading="eager" className="featured-image">`
- `src/components/post-card.js` — import `{ GatsbyImage, getImage }`, `<GatsbyImage image={getImage(...)} objectFit="cover" objectPosition="50% 50%" alt={title + " - Featured image"} className="featured-image">` inside the Link wrapper
- `src/templates/blog-list.js` — card query swapped to `gatsbyImageData(constrained, width: 540, height: 360, quality: 80, transformOptions: { fit: COVER, cropFocus: CENTER }, placeholder: BLURRED)`
- `src/components/blog-list-home.js` — identical card query swap in the StaticQuery
- `package.json` — `"gatsby-image": "^3.11.0"` removed (line 31)
- `yarn.lock` — gatsby-image resolution tree removed
- Git commits `feat(05-02): migrate index hero ... (IMAG-01, IMAG-02, D-07)`, `feat(05-02): migrate post cards to gatsby-plugin-image with center crop (IMAG-01, D-02)`, `feat(05-02): remove gatsby-image after full migration (IMAG-01, D-05)`
- `.planning/phases/05-image-pipeline-seo-fixes/05-02-SUMMARY.md` (created at plan completion)

<output>
Create `.planning/phases/05-image-pipeline-seo-fixes/05-02-SUMMARY.md` when done
</output>
</content>
