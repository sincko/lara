---
phase: 05-image-pipeline-seo-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - yarn.lock
  - gatsby-config.js
  - src/templates/blog-post.js
autonomous: true
requirements:
  - IMAG-01
  - IMAG-02
  - IMAG-03
user_setup: []

must_haves:
  truths:
    - "gatsby-plugin-image is installed at the EXACT pin 3.16.0 — NOT 5.16.0, which does not exist on npm (research-verified correction superseding D-01's claim; the .16 lockstep family is the plugin's internal deps: gatsby-core-utils 4.16.0 / gatsby-plugin-utils 4.16.0 / babel-plugin-remove-graphql-queries 5.16.0)"
    - "gatsby-plugin-image is registered as a string entry in the gatsby-config.js plugins array (SSR registration — without it GatsbyImage falls back to inline absolute-positioning styles and the shared .gatsby-image-wrapper CSS is missing)"
    - "tracedSVG: true is removed from the gatsby-remark-images options in gatsby-config.js (IMAG-02, D-06); sharp 5.16.0 already falls back to blurred output, so the removal is behaviorally neutral; all other gatsby-remark-images options (maxWidth 1024, showCaptions, linkImagesToOriginal, loading lazy) stay verbatim (D-08)"
    - "src/templates/blog-post.js queries featuredImage via gatsbyImageData(layout: \"constrained\", quality: 80, breakpoints: [350, 700, 1050, 1400], placeholder: BLURRED) — the srcSetBreakpoints legacy arg is replaced by breakpoints (research-verified resolver arg)"
    - "blog-post.js renders <GatsbyImage image={getImage(...)}> with alt={title + \" - Featured image\"}, className=\"featured-image\", objectFit=\"cover\", objectPosition=\"50% 50%\" — the Image ? <GatsbyImage> : \"\" guard is preserved verbatim, so a post without featuredImage renders no image element (empty state, parity D-04)"
    - "og:image is resolved via getSrc(frontmatter.featuredImage?.childImageSharp) and passed into <Seo image={...}> as a URL string only (IMAG-03, D-10) — never a processed-image object"
    - "rendered blog-post HTML shows a real og:image URL (https://laryart.it/static/...) and zero '[object Object]' occurrences on post pages after yarn build"
    - "When two images sit adjacent in the same list (ordered by date desc — existing sort unchanged), each renders in its own card box with no merging or overlap; output order is stable (same date : sort order is date DESC, unchanged from today)"
    - { statement: "A blog-post image processing failure surfaces as a Gatsby build error, not a runtime state — a held-out visual check on a post page after build confirms the happy path renders", verification: backstop }
  artifacts:
    - "package.json with \"gatsby-plugin-image\": \"3.16.0\" in dependencies (exact pin, no ^)"
    - "yarn.lock with the gatsby-plugin-image 3.16.0 resolution tree"
    - "gatsby-config.js — \"gatsby-plugin-image\" in the plugins array; gatsby-remark-images options without tracedSVG"
    - "src/templates/blog-post.js — migrated query + GatsbyImage component + getSrc-wired Seo"
  key_links:
    - "yarn add gatsby-plugin-image@3.16.0 → package.json pin → yarn.lock → gatsby-config.js plugins registration → GraphQL gatsbyImageData resolver (gatsby-plugin-sharp 5.16.0) → GatsbyImage SSR output"
    - "blog-post.js → getSrc(image) → <Seo image={string}> → react-helmet og:image/twitter:image/meta image in public/farfalla-blu/index.html"
  prohibitions:
    - "gatsby-plugin-image must NOT be pinned to any 5.x version (does not exist — yarn resolution fails)"
    - "maxWidth/maxHeight must NOT appear in any gatsbyImageData arg (deprecated — use width/height or breakpoints)"
    - "gatsby-image must NOT be removed from package.json yet — blog-list/index-page/post-card still import it until 05-02 migrates them (removing now breaks the build)"
    - "loading=\"eager\" must NOT be used on the blog-post surface (reserved for the index hero only, D-07 parity rule 8)"
    - "No SCSS rewrite — .blog-post .featured-image min-height 50vh rules stay untouched (05-03 holds the conditional absolute-fill fix)"
---

<objective>
Wave-1 tracer for the gatsby-plugin-image migration (IMAG-01/02/03 core): install gatsby-plugin-image@3.16.0 (registry-verified correction of D-01's non-existent "5.16.0"), register it in gatsby-config.js for SSR support, remove tracedSVG from the gatsby-remark-images options (IMAG-02), then migrate the FIRST surface — src/templates/blog-post.js — end-to-end: query swap (fluid → gatsbyImageData constrained/breakpoints/BLURRED), component swap (Img → GatsbyImage + getImage with guard), and the og:image fix (getSrc → string into <Seo image>) that kills the [object Object] bug on post pages (IMAG-03).

Purpose: prove the whole new image pipeline (install → config registration → GraphQL resolver → GatsbyImage render → built-HTML output) on one real page before the remaining three surfaces migrate in wave 2. The blog-post surface also carries the two riskiest changes of the phase — the gatsbyImageData arg shape and the getSrc og:image wiring — so a green build here de-risks 05-02.

Output: gatsby-plugin-image installed + configured, tracedSVG removed, blog-post.js migrated; `yarn build` green; post-page HTML shows the plugin's .gatsby-image-wrapper and a real og:image URL with zero [object Object]; one commit per task.

Environment notes (apply to every verify step in this plan): node is NOT on PATH — prefix commands with `source ~/.nvm/nvm.sh && nvm use 24` (nvm has v20.20.2, v24.18.0, v24.19.0; the project enforces Node 24 via engines + engine-strict + scripts/check-node-version.js; .nvmrc is 24). /tmp is a tmpfs that fills with JIT artifacts — set TMPDIR=/home/simos/tmp for yarn/build commands. yarn 1.22 only, never npm. If node-sass errors appear during install, run `nvm use && yarn install` (AGENTS.md postinstall guard) — do not reinstall node-sass.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-image-pipeline-seo-fixes/05-CONTEXT.md
@.planning/phases/05-image-pipeline-seo-fixes/05-RESEARCH.md
@.planning/phases/05-image-pipeline-seo-fixes/05-PATTERNS.md
@.planning/phases/05-image-pipeline-seo-fixes/05-UI-SPEC.md
@.planning/phases/05-image-pipeline-seo-fixes/05-VALIDATION.md
@package.json
@gatsby-config.js
@src/templates/blog-post.js
@src/components/seo.js
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Install gatsby-plugin-image@3.16.0, register in gatsby-config, remove tracedSVG (IMAG-01/02, D-01-corrected, D-06)</name>
  <files>package.json, yarn.lock, gatsby-config.js</files>
  <read_first>
    - package.json (dependencies lines 26-53: exact-pinned gatsby-* deps, alphabetized — gatsby-image ^3.11.0 at line 31 must STAY for now)
    - gatsby-config.js (plugins array lines 20-88; gatsby-remark-images options block lines 45-52 with tracedSVG: true at line 50; the gatsby-transformer-sharp/gatsby-plugin-sharp string entries at lines 36-37 — the registration placement model)
    - .planning/phases/05-image-pipeline-seo-fixes/05-RESEARCH.md (Headline finding — 3.16.0 NOT 5.16.0; Standard Stack install line; Pitfall 1; Pitfall 6)
    - .planning/phases/05-image-pipeline-seo-fixes/05-PATTERNS.md §package.json and §gatsby-config.js (exact-pin rule, plugin-string pattern, tracedSVG line removal)
    - .planning/phases/05-image-pipeline-seo-fixes/05-CONTEXT.md D-01 (locked decision + registry correction), D-06 (tracedSVG), D-08 (remark images untouched otherwise)
  </read_first>
  <action>
    Install exactly: `source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn add gatsby-plugin-image@3.16.0` — pin EXACT 3.16.0 (yarn add writes "3.16.0" without caret because the version is specified exactly; verify and hand-fix the caret if yarn writes "^3.16.0" — exact pins are the Phase-3 lockstep rule). Do NOT touch gatsby-image (still imported by 3 files until 05-02).

    Edit gatsby-config.js: add the string entry "gatsby-plugin-image" to the plugins array, placed with the sharp-pipeline strings (immediately after `gatsby-plugin-sharp` at line 37). Do not add options — the plugin has none.

    Edit the gatsby-remark-images options block (lines 46-52): delete exactly one line — `tracedSVG: true,` (line 50). Keep maxWidth: 1024, showCaptions: true, linkImagesToOriginal: false, loading: "lazy" verbatim (D-08). Do not touch gatsby-remark-responsive-iframe or gatsby-remark-prismjs blocks.

    Commit atomically: `git commit -m "feat(05-01): install gatsby-plugin-image 3.16.0 and drop tracedSVG (IMAG-01, IMAG-02)"` staging exactly package.json, yarn.lock, gatsby-config.js.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn install && grep -q '"gatsby-plugin-image": "3.16.0"' package.json && grep -q '"gatsby-plugin-image"' gatsby-config.js && ! grep -q 'tracedSVG' gatsby-config.js && TMPDIR=/home/simos/tmp yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"gatsby-plugin-image": "3.16.0"' package.json` succeeds (exact pin — grep for "5.16.0" must fail)
    - `grep -q '"gatsby-plugin-image"' gatsby-config.js` succeeds
    - `! grep -q 'tracedSVG' gatsby-config.js` succeeds
    - `grep -q 'maxWidth: 1024' gatsby-config.js` succeeds (remark options otherwise intact, D-08)
    - `grep -q '"gatsby-image": "^3.11.0"' package.json` still succeeds (removal is 05-02 task 3 — removing now breaks the other 3 consumers)
    - `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn install && yarn build` exits 0
    - `git log -1 --format=%s` contains `05-01`
  </acceptance_criteria>
  <done>The plugin is installed at the registry-verified 3.16.0, registered in gatsby-config, tracedSVG is gone, the remark block is otherwise untouched, and a full build passes.</done>
  <reversibility rating="reversible">git revert restores the pre-install package.json/gatsby-config; yarn.lock re-resolves; no runtime state.</reversibility>
</task>

<task type="tracer">
  <name>End-to-end blog-post migration: gatsbyImageData query, GatsbyImage render, getSrc og:image (IMAG-01/03, D-02/D-03/D-04/D-10)</name>
  <files>src/templates/blog-post.js</files>
  <read_first>
    The file being modified: src/templates/blog-post.js (import line 3, Image extraction lines 55-57, Seo block lines 67-74 with the buggy image={Image} at line 72, <Img> block lines 81-91, query lines 129-136)
    - .planning/phases/05-image-pipeline-seo-fixes/05-PATTERNS.md §blog-post.js (every line-level swap: import replacement, data extraction with getImage/getSrc, component with preserved guard, query args matrix)
    - .planning/phases/05-image-pipeline-seo-fixes/05-RESEARCH.md Pattern 1 (fluid → gatsbyImageData), Pattern 2 (GatsbyImage component swap with guard), Code Example 1 (full blog-post target), Pitfall 4 (getSrc, never the object), Pitfall 3 (banner fill — do NOT preemptively fix, held-out visual check gates it)
    - .planning/phases/05-image-pipeline-seo-fixes/05-UI-SPEC.md Image Surface Contract (blog-post row: target args, component props, preserved hooks; parity rules 1-6, 8)
    - .planning/phases/05-image-pipeline-seo-fixes/05-CONTEXT.md D-02 (migrate this surface), D-03 (sizing), D-04 (alt/objectFit/className carry over), D-10 (getSrc string)
  </read_first>
  <action>
    Migrate src/templates/blog-post.js to gatsby-plugin-image in place, per the research pattern — this is the phase tracer: one surface wired through install → query → component → built HTML, proven by the verify below.

    Import swap: replace `import Img from "gatsby-image"` (line 3) with `import { GatsbyImage, getImage, getSrc } from "gatsby-plugin-image"`.

    Data extraction (lines 55-57): replace the fluid access with `const Image = getImage(frontmatter.featuredImage?.childImageSharp)` and add `const imageSrc = getSrc(frontmatter.featuredImage?.childImageSharp)` (D-10). getImage/getSrc are null-safe — posts without featuredImage yield null; the existing `Image ? ... : ""` guard pattern must be preserved.

    Seo block (line 72): change `image={Image}` to `image={imageSrc}` — the URL string ONLY (the fluid object was stringified to "[object Object]" by seo.js's template interpolation; the getSrc call fixes IMAG-03). No other Seo props change.

    Component swap (lines 81-91): keep the guard — `{Image ? (<GatsbyImage image={Image} objectFit="cover" objectPosition="50% 50%" alt={frontmatter.title + " - Featured image"} className="featured-image" />) : ""}`. Do NOT add loading — GatsbyImage defaults to lazy and this surface stays lazy (hero eager is 05-02's index-page surface only). className lands on the wrapper — the existing SCSS (.blog-post .featured-image { min-height: 50vh }) keeps matching.

    Query swap (lines 129-136): replace the fluid(...) {...GatsbyImageSharpFluid ...GatsbyImageSharpFluidLimitPresentationSize} block with a single resolver call: gatsbyImageData(layout: "constrained", quality: 80, breakpoints: [350, 700, 1050, 1400], placeholder: BLURRED). Use the resolver arg breakpoints (NOT the legacy srcSetBreakpoints) and the placeholder GraphQL enum BLURRED (uppercase, unquoted). Drop both fluid fragments — constrained layout inherently limits upscale (LimitPresentationSize behavior is inherent). Do not touch the rest of the query (id, html, excerpt(pruneLength: 148), date(formatString), slug, title, description).

    Prettier conventions: no semicolons, double quotes, arrowParens avoid, 2-space indent. Run `yarn format` on the file after editing.

    Do NOT touch style.scss — the 50vh banner parity question (gap on landscape, clip on portrait) is a held-out visual check; the conditional absolute-fill CSS is documented in RESEARCH Pattern 3 but must NOT be applied preemptively.

    Commit atomically: `git commit -m "feat(05-01): migrate blog-post to gatsby-plugin-image with getSrc og:image (IMAG-01, IMAG-03, D-02)"` staging exactly src/templates/blog-post.js.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && grep -q "gatsby-image-wrapper" public/farfalla-blu/index.html && ! grep -q "object Object" public/farfalla-blu/index.html && grep -o 'property="og:image" content="[^"]*"' public/farfalla-blu/index.html | grep -q "^property=\"og:image\" content=\"https://laryart.it/static/"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'import { GatsbyImage, getImage, getSrc } from "gatsby-plugin-image"' src/templates/blog-post.js` succeeds
    - `! grep -q 'gatsby-image' src/templates/blog-post.js` succeeds (no legacy import remains in this file)
    - `grep -q 'const imageSrc = getSrc(frontmatter.featuredImage?.childImageSharp)' src/templates/blog-post.js` succeeds
    - `grep -q 'image={imageSrc}' src/templates/blog-post.js` succeeds (the URL-string contract, D-10)
    - `grep -q 'gatsbyImageData(layout: "constrained"' src/templates/blog-post.js` succeeds
    - `grep -q 'breakpoints: \[350, 700, 1050, 1400\]' src/templates/blog-post.js` succeeds (resolver arg, NOT srcSetBreakpoints)
    - `grep -q 'placeholder: BLURRED' src/templates/blog-post.js` succeeds
    - `grep -q 'className="featured-image"' src/templates/blog-post.js` succeeds (parity hook)
    - `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn build` exits 0
    - `grep -o 'property="og:image" content="[^"]*"' public/farfalla-blu/index.html` shows `https://laryart.it/static/...` with no "[object Object]" suffix
    - `grep -c "object Object" public/farfalla-blu/index.html` → 0
    - `git log --oneline --all | grep "05-01"` matches the commit
  </acceptance_criteria>
  <done>The blog-post page renders through GatsbyImage, the gatsbyImageData query args are the research-verified shape, the og:image meta contains a real absolute URL string and zero [object Object]; the full build passes and the commit is atomic.</done>
  <reversibility rating="reversible">git revert restores the gatsby-image version; the plugin remains installed but unused until 05-02 — no data migration involved.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → yarn install | gatsby-plugin-image 3.16.0 enters the dependency tree here; yarn.lock freezes resolution |
| gatsby-config → build | the plugin registration + tracedSVG removal cross here; a typo breaks SSR and placeholder output |
| GraphQL resolver → template | the gatsbyImageData object crosses from sharp to blog-post.js here; a wrong arg name (srcSetBreakpoints) silently produces degraded images |
| template → Seo → react-helmet | the og:image string crosses here; a non-string re-triggers the [object Object] leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | package.json — installing gatsby-plugin-image@5.16.0 (does not exist) | high | block | acceptance greps pin the exact "3.16.0" string and `grep "5.16.0"` must fail; research-verified registry fact supersedes CONTEXT's claim |
| T-05-02 | Tampering | gatsby-config plugins array — missing "gatsby-plugin-image" string → inline fallback styling | medium | mitigate | registration is in the same commit as the install; verify greps assert the string entry |
| T-05-03 | Tampering | gatsbyImageData — legacy srcSetBreakpoints/maxWidth arg names silently warn+work, deprecated API creep | medium | mitigate | research-verified arg names (breakpoints/width/height/placeholder enum) are pinned in acceptance greps |
| T-05-04 | Tampering | Seo image prop — fluid object passed again after the swap → [object Object] rebuild | critical | mitigate | getSrc at the call site (this task) + string guard in seo.js (05-05 task 1, D-11); verify greps the rendered og:image content |
| T-05-05 | Tampering | blog-post banner — constrained flow vs legacy absolute-fill under 50vh (gap/clip) | medium | mitigate | held-out visual check per UI-SPEC backstop E1; conditional absolute-fill CSS documented, NOT applied preemptively |
| T-05-SC | Tampering | npm install — gatsby-plugin-image legitimacy | low | mitigate | RESEARCH.md Package Legitimacy Audit: OK, official Gatsby monorepo, ~5.8 yr, 85k/wk downloads, no postinstall script — no checkpoint needed |
</threat_model>

<verification>
- `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn install && yarn build` exits 0 (full gate after each task)
- `grep -o 'property="og:image" content="[^"]*"' public/farfalla-blu/index.html` → `https://laryart.it/static/...` with no "[object Object]"
- `grep -c "object Object" public/farfalla-blu/index.html` → 0
- `grep -rl "gatsby-image-outer-wrapper" public/farfalla-blu/ | wc -l` → 0 (legacy class gone on this page)
- Zero `from "gatsby-image"` imports in blog-post.js
</verification>

<success_criteria>
- The blog-post surface renders through GatsbyImage with a real gatsbyImageData — the tracer proves the install → config → query → render → built-HTML pipeline works (IMAG-01 wave-1 slice)
- tracedSVG removed from config; placeholders BLURRED on this surface (IMAG-02)
- og:image on post pages is a real absolute URL via getSrc, [object Object] gone from the built HTML (IMAG-03)
- package.json pins gatsby-plugin-image@3.16.0 exactly; gatsby-image still installed (05-02 removes it after the remaining surfaces migrate)
</success_criteria>

## Artifacts this phase produces

- `package.json` — new dependency `"gatsby-plugin-image": "3.16.0"` (exact pin; gatsby-image still present until 05-02)
- `yarn.lock` — new resolution tree for gatsby-plugin-image 3.16.0 (+ gatsby-core-utils / gatsby-plugin-utils / babel-plugin-remove-graphql-queries .16 family)
- `gatsby-config.js` — `"gatsby-plugin-image"` string entry added to plugins (after gatsby-plugin-sharp); `tracedSVG: true` removed from the gatsby-remark-images options (all other options verbatim)
- `src/templates/blog-post.js` — new import `{ GatsbyImage, getImage, getSrc } from "gatsby-plugin-image"` (replaces `import Img from "gatsby-image"`); new `imageSrc` string derived from getSrc; `<Seo image={imageSrc}>`; `<GatsbyImage image={getImage(...)}>` render; query swapped to `gatsbyImageData(layout: "constrained", quality: 80, breakpoints: [350, 700, 1050, 1400], placeholder: BLURRED)`
- Git commits `feat(05-01): install gatsby-plugin-image 3.16.0 and drop tracedSVG (IMAG-01, IMAG-02)` and `feat(05-01): migrate blog-post to gatsby-plugin-image with getSrc og:image (IMAG-01, IMAG-03, D-02)`
- `.planning/phases/05-image-pipeline-seo-fixes/05-01-SUMMARY.md` (created at plan completion)

<output>
Create `.planning/phases/05-image-pipeline-seo-fixes/05-01-SUMMARY.md` when done
</output>
