---
phase: 05-image-pipeline-seo-fixes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/pages/privacy.md
  - src/assets/scss/style.scss
autonomous: true
gap_closure: true
gap_ids: [G-05-1a, G-05-1b]
requirements:
  - IMAG-01
  - SEOS-03
user_setup: []

must_haves:
  truths:
    - "Built /privacy HTML contains a SINGLE <a> element per external bare-URL link — no nested anchor (the remark-gfm autolink nesting is gone) — and all 10 target=\"_blank\" occurrences (8 external + wa.me + mailto) are preserved"
    - "The 3 bare-URL link texts (https://policies.google.com/privacy, https://www.cookiechoices.org/, http://www.garanteprivacy.it/cookie) render verbatim, visually unchanged, wrapped in an inline <span>"
    - "Compiled CSS in public/styles.*.css contains display:block on .post-card .featured-image (killing the inline-block descender gap)"
    - "The pre-existing .post-card .featured-image img { display: block; margin: 0 } rule is untouched — additive discipline honored"
    - "Full build passes and no phase-5 marker regresses (gatsby-image-outer-wrapper 0, [object Object] 0)"
  artifacts:
    - "src/content/pages/privacy.md — lines 67-69: bare-URL link text wrapped in <span> inside the existing raw <a> tags"
    - "src/assets/scss/style.scss — .post-card .featured-image gains display: block (additive, per the 05-04 banner precedent)"
  key_links:
    - "privacy.md raw <a> → remark-gfm autolink → rendered HTML: the span is the ONLY construct that breaks the bare-URL autolink match while staying rendering-transparent (verified via remark AST in 05-UAT root cause)"
    - "style.scss .post-card .featured-image → gatsby-plugin-image .gatsby-image-wrapper-constrained (display: inline-block from plugin CSS): display:block on the wrapper kills the descender gap — same pattern as the .blog-post banner fix (68d7cfe)"
---

<objective>
Close the two UAT gaps from 05-UAT: (1) the 3 privacy external links whose link text is a bare URL (policies.google.com/privacy, www.cookiechoices.org/, www.garanteprivacy.it/cookie) do not open in a new tab because remark-gfm's autolink wraps the bare-URL text between the raw `<a>` tags in a NESTED `<a>` without target="_blank" — browsers honor the inner anchor; (2) blog-list card images show a descender gap below the image because gatsby-plugin-image's `.gatsby-image-wrapper-constrained` is `display: inline-block` inside `.post-card .featured-image`.

Purpose: SEOS-03's "all 8 external links open in a new tab" truth and IMAG-01's visual parity truth are both violated; both root causes are empirically verified in 05-UAT with the fix proven via remark parse. These are targeted, minimal edits — no re-planning of the phase.

Output: privacy.md lines 67-69 span-wrapped, style.scss `.post-card .featured-image` + display:block, both verified in freshly built rendered output, and the plan SUMMARY.

Environment notes (apply to every verify step in this plan): node is NOT on PATH — prefix commands with `source ~/.nvm/nvm.sh && nvm use 24 >/dev/null` (.nvmrc is 24; engines enforce 24.x). /tmp is a tmpfs that fills with JIT artifacts — set TMPDIR=/home/simos/tmp for yarn/build commands. yarn 1.22 only, never npm. To inspect the built site: `TMPDIR=/home/simos/tmp yarn serve` (gatsby serve on port 9000) or open public/ files directly.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-image-pipeline-seo-fixes/05-UAT.md
@.planning/phases/05-image-pipeline-seo-fixes/05-CONTEXT.md
@.planning/phases/05-image-pipeline-seo-fixes/05-04-SUMMARY.md
@src/content/pages/privacy.md
@src/assets/scss/style.scss
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Fix G-05-1a: span-wrap the 3 bare-URL privacy link texts so remark-gfm stops nesting a target-less anchor (SEOS-03)</name>
  <files>src/content/pages/privacy.md</files>
  <read_first>
    - src/content/pages/privacy.md lines 67-69 (the three raw-HTML `<a>` blocks with bare-URL link text)
    - .planning/phases/05-image-pipeline-seo-fixes/05-UAT.md gap G-05-1a (root cause: remark-gfm autolink wraps bare URL text between raw tags in a NESTED `<a href=url>` WITHOUT target; verified fix: span breaks the match, yielding a single `<a>` node)
    - .planning/phases/05-image-pipeline-seo-fixes/05-04-SUMMARY.md "Delta 3" (the raw-HTML `<a target="_blank" rel="noopener noreferrer">` form is the established pattern — do not regress to markdown/kramdown)
  </read_first>
  <action>
    Per D-19/SEOS-03 and gap G-05-1a: in src/content/pages/privacy.md, edit ONLY the three raw-HTML anchor blocks at lines 67, 68, 69. Each currently has a bare URL as its text node:

    1. Line 67 — `<a href="https://policies.google.com/privacy" target="_blank" rel="noopener noreferrer">https://policies.google.com/privacy</a>` becomes `<a href="https://policies.google.com/privacy" target="_blank" rel="noopener noreferrer"><span>https://policies.google.com/privacy</span></a>`
    2. Line 68 — `<a href="https://www.cookiechoices.org/" target="_blank" rel="noopener noreferrer">https://www.cookiechoices.org/</a>` becomes `<a href="https://www.cookiechoices.org/" target="_blank" rel="noopener noreferrer"><span>https://www.cookiechoices.org/</span></a>`
    3. Line 69 — `<a href="http://www.garanteprivacy.it/cookie" target="_blank" rel="noopener noreferrer">http://www.garanteprivacy.it/cookie</a>` becomes `<a href="http://www.garanteprivacy.it/cookie" target="_blank" rel="noopener noreferrer"><span>http://www.garanteprivacy.it/cookie</span></a>`

    The <span> is an inline, rendering-transparent element — visible link text stays verbatim. It breaks the GFM autolink match on the bare-URL text so remark emits ONE `<a>` node carrying the existing target="_blank" rel="noopener noreferrer" (verified via remark parse in the UAT diagnosis).

    Do NOT touch anything else: the 5 named-text links (Firefox, Chrome, Internet Explorer, Safari, Opera) render correctly and must stay as-is; do not convert these raw anchors to markdown syntax; do not alter the hrefs, attributes, or surrounding prose.

    Rebuild and verify the rendered output (see verify), then commit atomically: `git commit -m "fix(05-05): span-wrap bare-URL privacy links to stop nested autolink anchor (G-05-1a, SEOS-03)"` staging src/content/pages/privacy.md.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && test "$(grep -c 'target="_blank"' public/privacy/index.html)" -eq 10 && test "$(grep -Ec '<a href="https://policies.google.com/privacy" target="_blank" rel="noopener noreferrer"><a |<a href="https://www.cookiechoices.org/" target="_blank" rel="noopener noreferrer"><a |<a href="http://www.garanteprivacy.it/cookie" target="_blank" rel="noopener noreferrer"><a ' public/privacy/index.html)" -eq 0 && grep -q '<span>https://policies.google.com/privacy</span>' public/privacy/index.html && grep -q '<span>https://www.cookiechoices.org/</span>' public/privacy/index.html && grep -q '<span>http://www.garanteprivacy.it/cookie</span>' public/privacy/index.html</automated>
  </verify>
  <done>Built /privacy HTML has exactly 10 target="_blank" occurrences, zero nested-anchor patterns for the 3 URLs, and each URL text present inside a span; the page renders 8 external links each opening in a new tab.</done>
  <reversibility rating="reversible">three line-level content edits; reverting restores the original anchors.</reversibility>
</task>

<task type="auto">
  <name>Fix G-05-1b: add display:block to .post-card .featured-image so the card image has no descender gap (IMAG-01)</name>
  <files>src/assets/scss/style.scss</files>
  <read_first>
    - src/assets/scss/style.scss lines 225-238 (the .post-card block: .featured-image { border-radius: 12px; ... } and its nested img rule)
    - src/assets/scss/style.scss lines 326-339 (the .blog-post .featured-image block — the banner precedent: display: block added at 68d7cfe)
    - .planning/phases/05-image-pipeline-seo-fixes/05-UAT.md gap G-05-1b (root cause: gatsby-plugin-image .gatsby-image-wrapper-constrained is display: inline-block → descender gap below the card image; verified fix: display:block on .post-card .featured-image, same pattern as the banner fix)
    - .planning/phases/05-image-pipeline-seo-fixes/05-04-SUMMARY.md "Delta 1" + key-decisions (additive-only SCSS discipline: add properties to existing rules, never rewrite selectors)
  </read_first>
  <action>
    Per IMAG-01 parity and gap G-05-1b: in src/assets/scss/style.scss, the `.post-card` block (line 225) contains `.featured-image { border-radius: 12px; ... }` (lines 232-238) with a nested `img { display: block; margin: 0 }` rule. Add `display: block;` to the `.featured-image` rule itself — exactly the additive pattern the banner fix used (68d7cfe): the property joins `border-radius` inside the existing selector.

    The result is `.post-card .featured-image { display: block; border-radius: 12px; ... }` — the block display kills the inline-block descender gap from the plugin's .gatsby-image-wrapper-constrained while the nested img rule stays untouched.

    Do NOT touch the nested `img { display: block; margin: 0 }` rule, do NOT rewrite any other selector, do NOT add new selectors. The mobile media-query variant (lines 266-268, border-radius override) stays as-is.

    Rebuild and verify the compiled CSS (see verify), then commit atomically: `git commit -m "fix(05-05): display:block on post-card featured-image wrapper kills descender gap (G-05-1b, IMAG-01)"` staging src/assets/scss/style.scss.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && grep -q 'post-card .featured-image{display:block' public/styles.*.css && grep -q 'blog-post .featured-image{display:block' public/styles.*.css && test "$(grep -rl 'gatsby-image-outer-wrapper' public/ | wc -l)" -eq 0 && test "$(grep -c 'object Object' public/*/index.html)" -eq 0</automated>
  </verify>
  <done>Compiled CSS in public/styles.*.css contains display:block on .post-card .featured-image; the banner rule is unaffected; the build passes with zero marker regressions (no gatsby-image-outer-wrapper, no [object Object]).</done>
  <reversibility rating="reversible">one additive property on an existing rule; removing it restores the prior state instantly.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| privacy.md raw HTML → remark-gfm render | the span-wrapped bare URLs cross here; a re-nested anchor means the fix regressed |
| rendered HTML → browser anchor resolution | the inner-vs-outer anchor ambiguity resolves here — this was the original failure |
| style.scss → dart-sass compile | the additive display:block crosses here; a syntax error breaks the whole build |
| built output → verification | both fixes are asserted on this boundary (rendered HTML + compiled CSS greps) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-20 | Tampering | remark-gfm autolink re-nests an anchor despite the span (fix ineffective) | medium | mitigate | verify greps assert zero nested-anchor patterns for all 3 URLs in built HTML — the same evidence class that diagnosed the root cause |
| T-05-21 | Tampering | span wrap alters visible link text or surrounding prose (accidental edit drift) | low | mitigate | action restricts edits to the 3 anchor text nodes; verify greps assert the verbatim URL text inside each span |
| T-05-22 | Denial of service | display:block on the card wrapper collapses or overflows the image layout | low | mitigate | one-property additive change per the proven banner precedent (68d7cfe); verify asserts compiled CSS contains both the new property and the untouched banner rule |
| T-05-23 | Repudiation | a fix committed without rendered-output evidence | low | accept | build-verified greps are the acceptance gate before each atomic commit; SUMMARY records exact command output |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | n/a | zero install commands in this plan — yarn build only |

</threat_model>

<verification>
- `source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build` exits 0 (both tasks)
- G-05-1a: `grep -c 'target="_blank"' public/privacy/index.html` → 10; nested-anchor pattern count for the 3 URLs → 0; span-wrapped URL text present ×3
- G-05-1b: `grep -q 'post-card .featured-image{display:block' public/styles.*.css`; banner rule still carries display:block
- No marker regression: gatsby-image-outer-wrapper count → 0; object Object count → 0
- Both fixes committed atomically (one commit per task)
</verification>

<success_criteria>
- All 8 external privacy links open in a new tab when clicked (single anchor per link in rendered HTML)
- Blog-list and home card images have no gap below them (wrapper is display:block in compiled CSS)
- The 5 named-text privacy links and the blog-post banner are untouched
- Full build green; zero phase-5 marker regressions
</success_criteria>

<output>
Create `.planning/phases/05-image-pipeline-seo-fixes/05-05-SUMMARY.md` when done
</output>
