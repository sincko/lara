---
phase: 05-image-pipeline-seo-fixes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/pages/privacy.md
  - gatsby-config.js
  - package.json
  - yarn.lock
  - src/assets/scss/style.scss
autonomous: false
gap_closure: true
gap_ids: [G-05-1a, G-05-1b]
requirements:
  - IMAG-01
  - SEOS-03
  - SEOS-02
user_setup: []

must_haves:
  truths:
    - "Built /privacy HTML has EXACTLY 8 external anchors, each carrying target=\"_blank\" rel=\"noopener noreferrer\", and NO anchor contains a nested <a> (python tag-walker max depth 1 — the nested-autolink failure mode is structurally impossible)"
    - "privacy.md contains ZERO raw <a> HTML — all 8 external links (5 named-text + 3 bare-URL) are markdown links `[text](url)`, texts preserved verbatim; the mailto email stays plain text (GFM autolink; gatsby-remark-external-links does not touch mailto: URLs)"
    - "gatsby-remark-external-links@0.0.4 is in package.json dependencies + yarn.lock and registered in gatsby-transformer-remark options.plugins with target: \"_blank\" and rel: \"noopener noreferrer\" — site-wide markdown external links gain target/_blank (intended, matches the site's existing raw-HTML link convention)"
    - "Compiled CSS in public/styles.*.css contains display:block on .post-card .featured-image (order-tolerant regex — dart-sass preserves source order, border-radius comes first)"
    - "The pre-existing .blog-post .featured-image{...display:block...} banner rule and .post-card .featured-image img { display: block; margin: 0 } are untouched — additive discipline honored"
    - "Full build + jest suite pass; zero phase-5 marker regressions (gatsby-image-outer-wrapper 0, [object Object] 0, target=\"_blank\" count on /privacy unchanged at 12)"
  artifacts:
    - "src/content/pages/privacy.md — all 8 external links converted to markdown links, raw <a> removed entirely (SEOS-03 clean-markdown intent restored)"
    - "gatsby-config.js — gatsby-remark-external-links entry inside gatsby-transformer-remark options.plugins"
    - "package.json + yarn.lock — gatsby-remark-external-links@0.0.4"
    - "src/assets/scss/style.scss — .post-card .featured-image gains display: block (additive, per the 05-04 banner precedent)"
  key_links:
    - "privacy.md markdown links → remark `link` AST nodes → gatsby-remark-external-links hProperties injection → rendered <a target=\"_blank\" rel=\"noopener noreferrer\">: the AST-level test proves `[URL](URL)` emits ONE link node (no nesting), unlike every span/entity/ZWSP variant which leaves the remark-gfm autolink nested anchor alive"
    - "style.scss .post-card .featured-image → gatsby-plugin-image .gatsby-image-wrapper-constrained (display: inline-block from plugin CSS): display:block on the wrapper kills the descender gap — same pattern as the .blog-post banner fix (68d7cfe)"
    - "package.json → yarn.lock → gatsby-config.js: the new dependency must land in the lockfile via yarn 1.22 (never npm — no second lockfile)"
---

<objective>
Close the two UAT gaps from 05-UAT with a corrected mechanism. G-05-1a: the previous span-wrap approach is REJECTED — an AST-level test on remark-parse@9.0.0 + remark-gfm@1.0.0 (gatsby-transformer-remark 6.16.0's exact deps) shows the span does NOT break the autolink: the AST still contains html(a) > html(span) > link(autolink) — the nested target-less anchor persists. The working form is markdown-style `[URL](URL)` (emits ONE link node) paired with gatsby-remark-external-links to carry target/_blank — converting ALL 8 privacy external links from raw HTML to markdown links, which also fully removes the last raw HTML from privacy.md (SEOS-03 intent). G-05-1b: add display:block to .post-card .featured-image, verified order-tolerantly.

Purpose: Both SEOS-03's "all 8 external links open in a new tab" truth and IMAG-01's visual parity truth remain violated. This revision replaces the empirically-dead span fix with the mechanism proven at the AST level and fixes the blind-green verify gates flagged by the checker (nested-anchor grep, dart-sass order, multi-file grep -c).

Output: gatsby-remark-external-links@0.0.4 installed (package.json + yarn.lock) and registered in gatsby-config.js; privacy.md converted to pure markdown (0 raw <a>); style.scss display:block on .post-card .featured-image; both fixes verified against a fresh build; the plan SUMMARY.

Environment notes (apply to every verify step in this plan): node is NOT on PATH — prefix commands with `source ~/.nvm/nvm.sh && nvm use 24 >/dev/null` (.nvmrc is 24; engines enforce 24.x). /tmp is a tmpfs that fills with JIT artifacts — set TMPDIR=/home/simos/tmp for yarn/build commands. yarn 1.22 only, never npm. To inspect the built site: `TMPDIR=/home/simos/tmp yarn serve` (gatsby serve on port 9000) or open public/ files directly.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-image-pipeline-seo-fixes/05-UAT.md
@.planning/phases/05-image-pipeline-seo-fixes/05-CONTEXT.md
@.planning/phases/05-image-pipeline-seo-fixes/05-04-SUMMARY.md
@src/content/pages/privacy.md
@src/assets/scss/style.scss
@gatsby-config.js
@package.json
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Package legitimacy gate: gatsby-remark-external-links@0.0.4 (before ANY install)</name>
  <what-built>Legitimacy audit evidence for the new dependency gatsby-remark-external-links@0.0.4, gathered with npm view (registry metadata) and npmjs.com/package review. The 05-RESEARCH.md Package Legitimacy Audit does NOT list this package, so per the package gate protocol it is [ASSUMED] — a blocking-human checkpoint is mandatory before the install in Task 2.</what-built>
  <how-to-verify>
    Run these and record the output in the SUMMARY:
    1. `npm view gatsby-remark-external-links versions --json` → must show exactly 0.0.1..0.0.4; latest is 0.0.4
    2. `npm view gatsby-remark-external-links@0.0.4 license deprecated time.created` → MIT, no "deprecated" flag, created 2017-09-29
    3. `npm view gatsby-remark-external-links@0.0.4 repository.homepage` → github.com/JLongley/gatsby-remark-external-links (canonical Gatsby remark plugin; a port of remark-external-links; actively used in the Gatsby ecosystem since 2017, stable API)
    4. `npm view gatsby-remark-external-links@0.0.4 dependencies` (babel-runtime, is-relative-url, unist-util-find, unist-util-visit) and `npm view <each-dep> scripts.postinstall` → NO postinstall scripts anywhere in the tree (postinstall-check discipline from 05-RESEARCH)
    5. Human confirm via https://www.npmjs.com/package/gatsby-remark-external-links that the package page matches the above (MIT license, version 0.0.4, no deprecation banner)
  </how-to-verify>
  <resume-signal>Type "approved" to permit the yarn add in task 2, or describe any concern</resume-signal>
</task>

<task type="auto">
  <name>Fix G-05-1a: markdown links + gatsby-remark-external-links for all 8 privacy external links (SEOS-03)</name>
  <files>package.json, yarn.lock, gatsby-config.js, src/content/pages/privacy.md</files>
  <read_first>
    - .planning/phases/05-image-pipeline-seo-fixes/05-UAT.md gap G-05-1a (CORRECTED root cause — span-wrap does NOT break the autolink; the UAT now records: AST test on remark-parse@9.0.0 + remark-gfm@1.0.0 shows the span keeps the nested autolink `link` node alive; the working mechanism is `[URL](URL)` markdown → ONE link node + gatsby-remark-external-links to carry target/_blank)
    - src/content/pages/privacy.md lines 34-38 (the 5 named-text raw-HTML anchors: Firefox, Chrome, Internet Explorer, Safari, Opera) and lines 67-69 (the 3 bare-URL raw-HTML anchors: policies.google.com/privacy, www.cookiechoices.org/, www.garanteprivacy.it/cookie) and line 71 (the plain-text email — MUST stay plain text, GFM autolink)
    - gatsby-config.js lines 39-70 (the gatsby-transformer-remark options.plugins array — the insertion point for the new plugin)
    - package.json (dependencies block — no gatsby-remark-external-links yet; yarn 1.22 lockfile discipline from AGENTS.md)
    - .planning/phases/05-image-pipeline-seo-fixes/05-04-SUMMARY.md "Delta 3" (documents the SUPERSEDED raw-HTML decision — read to avoid re-reverting; the new mechanism replaces it)
  </read_first>
  <action>
    Per D-19/SEOS-03 and the corrected gap G-05-1a:

    1. Install: `source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn add gatsby-remark-external-links@0.0.4` (yarn 1.22 — never npm; exact version 0.0.4). Only after the task-1 checkpoint approves the package.
    2. gatsby-config.js: inside the existing `gatsby-transformer-remark` options.plugins array (after the `gatsby-remark-images` object), add:
       `{ resolve: "gatsby-remark-external-links", options: { target: "_blank", rel: "noopener noreferrer" } }`
       This injects target/_blank + rel into EVERY external markdown link site-wide (remark link nodes with non-relative URLs) — intended behavior, consistent with the site's existing raw-HTML target convention.
    3. src/content/pages/privacy.md: convert ALL 8 raw-HTML anchors to markdown links, preserving link text verbatim (including the trailing periods of the Firefox and Opera texts):
       - Line 34: the raw-HTML anchor wrapping the text "Firefox, in inglese." (opening tag with href "https://support.mozilla.org/en-US/kb/enable-and-disable-cookies-website-preferences" plus target and rel attributes, the text node, and the closing tag) → `[Firefox, in inglese.](https://support.mozilla.org/en-US/kb/enable-and-disable-cookies-website-preferences)`
       - Line 35: `[Chrome, in inglese](https://support.google.com/chrome/answer/95647?hl=en)`
       - Line 36: `[Internet Explorer, in inglese](http://windows.microsoft.com/it-it/internet-explorer/delete-manage-cookies#ie=ie-11)`
       - Line 37: `[Safari, in inglese](http://support.apple.com/kb/HT1677?viewlocale=it_IT&locale=it_IT)`
       - Line 38: `[Opera, in inglese.](http://help.opera.com/Mac/12.10/it/cookies.html)`
       - Line 67: `[https://policies.google.com/privacy](https://policies.google.com/privacy)` — do NOT wrap the bare URL text in any span/entity/ZWSP; the markdown form itself emits ONE link node (AST-verified), the autolink is dead because the text is now inside a link node, and the plugin adds target/_blank
       - Line 68: `[https://www.cookiechoices.org/](https://www.cookiechoices.org/)`
       - Line 69: `[http://www.garanteprivacy.it/cookie](http://www.garanteprivacy.it/cookie)`
       Remove the raw anchor wrappers entirely — the opening tag (the anchor element with its href plus target and rel attributes) and the matching closing tag — leaving only the markdown link syntax. The mailto email on line 71 stays plain text — the GFM autolink renders it as a mailto link and the plugin does not touch mailto: URLs (is-relative-url treats mailto: as relative).
    4. Do NOT touch: the wa.me raw-HTML anchor in contatti.md (raw HTML, not a remark link node — the plugin only visits markdown link nodes), the React anchors in src/components/top-contacts.js / footer-links.js / blog-post.js, or any other file.
    5. Rebuild + verify structurally (see verify), then commit atomically: `git commit -m "fix(05-05): markdown links + external-links plugin restore target=_blank on 8 privacy links (G-05-1a, SEOS-03)"` staging package.json, yarn.lock, gatsby-config.js, src/content/pages/privacy.md.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && test "$(grep -o 'target="_blank"' public/privacy/index.html | wc -l)" -eq 12 && test "$(grep -c '<a ' src/content/pages/privacy.md)" -eq 0 && test "$(grep -c 'gatsby-remark-external-links' package.json)" -eq 1 && python3 - <<'PYEOF'
import re
html = open("public/privacy/index.html", encoding="utf-8").read()
html_u = html.replace("&amp;", "&")
# 1) flat anchors: no <a> inside an <a>
depth = maxd = 0
for m in re.finditer(r"<a\b|</a>", html):
    depth += 1 if m.group(0) == "<a" else -1
    maxd = max(maxd, depth)
assert maxd == 1, f"nested <a> depth {maxd}"
# 2) every external href carries target+rel
hrefs = [
    "https://support.mozilla.org/en-US/kb/enable-and-disable-cookies-website-preferences",
    "https://support.google.com/chrome/answer/95647?hl=en",
    "http://windows.microsoft.com/it-it/internet-explorer/delete-manage-cookies#ie=ie-11",
    "http://support.apple.com/kb/HT1677?viewlocale=it_IT&locale=it_IT",
    "http://help.opera.com/Mac/12.10/it/cookies.html",
    "https://policies.google.com/privacy",
    "https://www.cookiechoices.org/",
    "http://www.garanteprivacy.it/cookie",
]
for h in hrefs:
    assert html_u.count(h) >= 1, f"missing href {h}"
    i = html_u.index(h)
    seg = html_u[html_u.rfind("<a ", 0, i) : i]
    assert 'target="_blank"' in seg and 'rel="noopener noreferrer"' in seg, f"attrs missing on {h}"
# 3) bare-URL texts appear verbatim as the anchor text (href + text = 2 occurrences)
for b in ("https://policies.google.com/privacy", "https://www.cookiechoices.org/", "http://www.garanteprivacy.it/cookie"):
    assert html_u.count(b) >= 2, f"bare text {b} not rendered"
# 4) the mailto email still renders untouched (plugin must not rewrite it)
assert "mailto:s.foschi@protonmail.com" in html_u, "mailto email link missing"
print("OK: 8 flat external anchors with target/rel, 3 bare-URL texts verbatim, mailto intact")
PYEOF</automated>
  </verify>
  <done>package.json + yarn.lock contain gatsby-remark-external-links@0.0.4; gatsby-config.js registers it with target="_blank" rel="noopener noreferrer"; privacy.md has zero raw <a> HTML (8 markdown links, verbatim texts, email untouched); built /privacy HTML has exactly 12 target="_blank" (8 external + wa.me + facebook from top-contacts + Freepik + Flaticon from footer-links — measured baseline, unchanged by this fix) and python tag-walker confirms zero nested anchors.</done>
  <reversibility rating="reversible">one new dev-time plugin + content conversions; reverting the commit restores the raw-HTML form.</reversibility>
</task>

<task type="auto">
  <name>Fix G-05-1b: display:block on .post-card .featured-image with order-tolerant verify (IMAG-01)</name>
  <files>src/assets/scss/style.scss</files>
  <read_first>
    - src/assets/scss/style.scss lines 225-238 (the .post-card block: .featured-image { border-radius: 12px; ... } with nested img rule)
    - src/assets/scss/style.scss lines 326-344 (the .blog-post .featured-image banner block — the 05-04 precedent, carries display:block from 68d7cfe)
    - .planning/phases/05-04-SUMMARY.md "Delta 1" + key-decisions (additive-only SCSS discipline: add properties to existing rules, never rewrite selectors)
    - .planning/phases/05-image-pipeline-seo-fixes/05-UAT.md gap G-05-1b (root cause: gatsby-plugin-image .gatsby-image-wrapper-constrained is display: inline-block → descender gap; fix: display:block on the card wrapper, same pattern as the banner)
  </read_first>
  <action>
    Per IMAG-01 parity and gap G-05-1b: in src/assets/scss/style.scss, the `.post-card` block (line 225) contains `.featured-image { border-radius: 12px; ... }` (lines 232-238). Add `display: block;` to the existing `.featured-image` rule — exactly the additive pattern the banner fix used (68d7cfe): the property joins `border-radius` inside the existing selector. The block display kills the inline-block descender gap from the plugin's .gatsby-image-wrapper-constrained while the nested img rule stays untouched.

    Do NOT touch the nested `img { display: block; margin: 0 }` rule, do NOT rewrite any selector, do NOT add new selectors, do NOT touch the media-query variant (lines 266-268).

    Rebuild and verify the compiled CSS (see verify — note the grep must be order-tolerant: dart-sass keeps source order, so `display:block` comes AFTER `border-radius` in the compiled rule), then commit atomically: `git commit -m "fix(05-05): display:block on post-card featured-image kills descender gap (G-05-1b, IMAG-01)"` staging src/assets/scss/style.scss.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && grep -q 'post-card .featured-image{[^}]*display:block' public/styles.*.css && grep -q 'blog-post .featured-image{[^}]*display:block' public/styles.*.css && test "$(grep -rl 'gatsby-image-outer-wrapper' public/ | wc -l)" -eq 0 && test "$(grep -rl 'object Object' public --include='*.html' | wc -l)" -eq 0</automated>
  </verify>
  <done>Compiled CSS in public/styles.*.css contains display:block on .post-card .featured-image (order-tolerant match — the rule keeps border-radius first) and the banner rule still carries display:block; build passes with zero marker regressions (no gatsby-image-outer-wrapper, no [object Object] anywhere in public/).</done>
  <reversibility rating="reversible">one additive property on an existing rule; removing it restores the prior state.</reversibility>
</task>

<task type="auto">
  <name>Fix mobile post pagination: prev/next buttons keep desktop formatting under 991px (user request, WR-02)</name>
  <files>src/assets/scss/style.scss</files>
  <read_first>
    - src/assets/scss/style.scss lines 565-660 (the .pagination block: the `&.-post` desktop rules lines 584-633 and the `@media (max-width: 991px)` block lines ~634-652 which contains the malformed `padding: 50px 0 ul {` and the `&.-post ul { display: grid; justify-content: normal; ... }` override that restyles post prev/next as stacked gray blocks)
    - .planning/phases/05-image-pipeline-seo-fixes/05-REVIEW.md warning WR-02 (pre-existing broken mobile pagination media query — the malformed `padding: 50px 0 ul` compiles to nonsense declarations; the user explicitly requested: "i pulsanti prev e next dei singoli post, sul mobile vanno formattati esattamente come sul desktop")
    - src/templates/blog-post.js lines 9-50 (the Pagination component: ul with two li, each li contains a p with the title and the prev/next icon — the markup the -post flex layout targets)
  </read_first>
  <action>
    Per the user's explicit request and WR-02: the blog-post prev/next pagination (`.-post`) must render on mobile exactly as on desktop. In src/assets/scss/style.scss, inside the `@media (max-width: 991px)` block of `.pagination`:
    1. Remove the malformed statement `padding: 50px 0 ul {` (lines ~641-643) and its body — it compiles to garbage (`padding-display: flex`, etc.) and is the WR-02 root cause.
    2. Remove the `&.-post ul { display: grid; justify-content: normal; a { padding: 20px; border: 1px solid rgba(0, 0, 0, 0.1); display: block; margin-bottom: 10px; background: #fafafa; } }` override (lines ~645-651) — this is what restyles the post prev/next as stacked gray blocks on mobile. The desktop `-post` flex layout (from the parent rules) then applies at all widths, so the two prev/next buttons render side-by-side with the white text/icons exactly as on desktop.
    3. Keep the mobile `.num { display: none; }` (the blog-list page-number pills remain hidden on mobile — that is the existing intended blog-list behavior, NOT the post pagination).
    4. Do NOT touch the blog-list (non `-post`) `.pagination a` pill rules, the `.-post` desktop rules, or any other file.
    5. Rebuild and verify (see verify), then commit atomically: `git commit -m "fix(05-05): post prev/next pagination keeps desktop formatting on mobile (WR-02)"` staging src/assets/scss/style.scss.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && grep -q -- '-post ul{display:flex;justify-content:space-between}' public/styles.*.css && ! grep -q 'display:grid' public/styles.*.css && ! grep -q 'padding:50px 0 ul' public/styles.*.css && test "$(grep -rl 'object Object' public --include='*.html' | wc -l)" -eq 0</automated>
  </verify>
  <done>Compiled CSS in public/styles.*.css contains the `-post` flex layout WITHOUT the grid/gray-block override; the malformed `padding: 50px 0 ul` is gone; post prev/next buttons render side-by-side on mobile exactly as desktop (white text, icons, no gray boxes).</done>
  <reversibility rating="reversible">removing the two media-query statements restores prior behavior.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| privacy.md markdown links → gatsby-remark-external-links hProperties | the markdown link nodes cross here; a plugin misconfig (missing target) silently regresses the whole site's external links |
| remark `link` node → built HTML | the GFM autolink can still nest an anchor if any non-markdown construct wraps the URL text — the exact G-05-1a failure mode |
| style.scss → dart-sass compile | the additive display:block crosses here; a syntax error breaks the whole build |
| public/ → verification | both fixes are asserted on the built output (python tag-walker + compiled-CSS greps) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-20 | Tampering | gatsby-remark-external-links misregistered (wrong plugin array, options dropped) → anchors render WITHOUT target | medium | mitigate | structural python walker asserts target="_blank" rel="noopener noreferrer" on each of the 8 hrefs in built HTML — a no-op config fails the gate, not a blind green |
| T-05-21 | Tampering | markdown conversion drift (link text changed, trailing period moved, bare URL re-wrapped in span/entity/ZWSP) | medium | mitigate | markdown-level constraint: verify greps assert zero `<a ` in privacy.md AND each bare-URL text occurs twice in built HTML (href + text verbatim) |
| T-05-22 | Tampering | gatsby-remark-external-links rewrites mailto/relative links site-wide (e.g., the privacy email) | medium | mitigate | plugin's is-relative-url@2 treats mailto: as relative (no `://`); verify asserts the email link still renders; the [ASSUMED] package legitimacy is human-gated (task 1) |
| T-05-23 | Denial of service | display:block on the card wrapper collapses/overflows the image layout | low | mitigate | one-property additive change per the proven banner precedent; verify asserts compiled CSS contains both the new property and the intact banner rule (order-tolerant) |
| T-05-24 | Repudiation | fix committed without rendered-output evidence | low | accept | build-verified greps are the acceptance gate before each atomic commit; SUMMARY records exact command output |
| T-05-SC | Tampering | npm install of gatsby-remark-external-links@0.0.4 | high | mitigate | package legitimacy checkpoint (task 1, blocking-human) + registry verification of version/license/deprecated/postinstall before yarn add |

</threat_model>

<verification>
- `source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build` exits 0 (each task)
- G-05-1a: python tag-walker on public/privacy/index.html → max anchor depth 1, each of the 8 external hrefs carries target="_blank" rel="noopener noreferrer"; `grep -c '<a '` in privacy.md → 0; target="_blank" count on /privacy → 12 (unchanged baseline)
- G-05-1b: `grep -q 'post-card .featured-image{[^}]*display:block' public/styles.*.css` (order-tolerant); banner rule still carries display:block
- WR-02 (user request): compiled CSS has `-post ul{display:flex;justify-content:space-between}` with NO `display:grid` and NO malformed `padding:50px 0 ul`; post prev/next identical on mobile and desktop
- No marker regression: gatsby-image-outer-wrapper count → 0; [object Object] → 0 (via `grep -rl | wc -l`)
- All fixes committed atomically (one commit per task)
</verification>

<success_criteria>
- All 8 external privacy links open in a new tab when clicked (single anchor per link, flat structure, target + rel present)
- Blog-list and home card images have no gap below them (wrapper display:block in compiled CSS)
- privacy.md is pure markdown (zero raw HTML) — the 5 named-text links and 3 bare-URL links all render with verbatim text
- Site-wide markdown links (blog posts, etc.) now carry target="_blank" consistently — intended consequence
- Post prev/next buttons render identically on mobile and desktop (side-by-side flex, white text/icons, no stacked gray blocks)
- Full build + jest suite green; zero phase-5 marker regressions
</success_criteria>

<output>
Create `.planning/phases/05-image-pipeline-seo-fixes/05-05-SUMMARY.md` when done
</output>
