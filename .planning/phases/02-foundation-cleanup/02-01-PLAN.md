---
phase: 02-foundation-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package-lock.json
  - netlify.toml
  - .prettierignore
  - package.json
  - yarn.lock
autonomous: true
requirements:
  - FNDT-01
  - FNDT-04
user_setup: []

must_haves:
  truths:
    - "`package-lock.json` does not exist in the repo (git ls-files shows zero matches) and `git status` does not surface it after `yarn install`"
    - "`netlify.toml` build command is exactly `command = \"yarn build\"` (no `npm run build` remains)"
    - "`netlify.toml` has no `NODE_VERSION` key (deleted in Plan 02, NOT this plan — this plan only touches the command line)"
    - "`.prettierignore` contains no `package-lock.json` line"
    - "`package.json` dependencies no longer list `yarn` or `y18n`"
    - "`nvm use 20 && yarn install && yarn build && yarn test` exits 0 after the tracer commit"
    - "`prismjs` and `@testing-library/dom` remain in package.json (NOT removed — research-proven keepers)"
  artifacts:
    - "package-lock.json removed from git and filesystem"
    - "netlify.toml with `command = \"yarn build\"`"
    - ".prettierignore without the `package-lock.json` entry"
    - "package.json with `yarn` and `y18n` absent from dependencies"
  key_links:
    - "yarn.lock is the sole lockfile resolving all deps for `yarn install`"
    - "netlify.toml command invokes `yarn build` (not `npm run build`) so Netlify cannot regenerate package-lock.json"
---

<objective>
Tracer: prove the single-lockfile, yarn-only build path end-to-end. Delete `package-lock.json`, switch `netlify.toml` build command to `yarn build`, remove the stale `.prettierignore` line, and remove the two hack-remnant dependencies (`yarn`, `y18n`) that pair logically with lockfile consolidation (D-07 subset). Verify the full `nvm use 20 && yarn install && yarn build && yarn test` loop stays green — the riskiest architectural assertion of Phase 2 (does yarn alone resolve everything and keep the build green?).

Purpose: This is the production-quality vertical slice through every config layer Phase 2 touches (lockfile, build config, ignore config, manifest, lockfile resolution). Committing it first means every later removal runs on a proven single-lockfile baseline. Per D-02, the command switch MUST be in the same commit as the lockfile deletion (Pitfall 7: `npm run build` on Netlify would resurrect `package-lock.json`).
Output: One atomic commit consolidating the lockfile + build command + ignore cleanup + `yarn`/`y18n` removal; green build + tests.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundation-cleanup/02-CONTEXT.md
@.planning/phases/02-foundation-cleanup/02-RESEARCH.md
@.planning/phases/01-test-scaffolding-performance-baseline/01-01-SUMMARY.md
@netlify.toml
@.prettierignore
@package.json
</context>

<tasks>

<task type="tracer">
  <name>Tracer: single-lockfile consolidation + yarn build switch + hack-dep removal (D-01, D-02, D-03, D-07 subset)</name>
  <files>package-lock.json, netlify.toml, .prettierignore, package.json, yarn.lock</files>
  <read_first>
    - netlify.toml (current 9-line file: `command = "npm run build"`, `[build.environment] NODE_VERSION = "10"` — this task changes ONLY the command line; NODE_VERSION is Plan 02's scope, do NOT touch it here)
    - .prettierignore (current 4 lines: `.cache`, `package.json`, `package-lock.json`, `public`)
    - package.json (confirm `yarn` at line 58 and `y18n` at line 57 are in `dependencies`; confirm `packageManager: "yarn@1.22.22"` at line 77)
    - .planning/phases/02-foundation-cleanup/02-RESEARCH.md §"Recommended Commit Structure" Commit 1 and §"Pattern 2: Node-version pinning" (every command opens with `nvm use 20`)
    - .planning/phases/01-test-scaffolding-performance-baseline/01-01-SUMMARY.md (confirms `yarn add` without `--ignore-scripts` fails on this machine due to node-sass/node-gyp vs Node 24; `yarn remove` does not trigger postinstall rebuilds so no flag needed, but `yarn install` after removal is safe)
  </read_first>
  <action>
    Pre-verify the two removals with `yarn why` (D-10 discipline) BEFORE removing:
    - `nvm use 20` first (shell default is Node 24.18.0; node-sass ABI 115 prebuilt exists only for Node 20 — Pitfall 2).
    - `yarn why yarn` — expect "specified in dependencies" as the only reason (it is the npm-package-as-dep Netlify-build hack remnant, git a66d212; the package manager is the `yarn@1.22.22` `packageManager` field, NOT this dependency entry).
    - `yarn why y18n` — expect hoisted-from node-sass#sass-graph#yargs and jest#jest-cli#yargs; the direct entry is removable because transitive copies survive (research Package table).

    Then make the consolidation changes as ONE logical group (Pitfall 7: the command switch and lockfile deletion MUST be co-committed so Netlify cannot regenerate package-lock.json):
    - `git rm package-lock.json` (removes from index AND working tree).
    - Edit netlify.toml: change line 3 `command = "npm run build"` → `command = "yarn build"` (D-02). Do NOT delete `[build.environment]` or `NODE_VERSION` here — that is Plan 02 / D-04. Leave the rest of the file (publish, plugins) untouched.
    - Edit .prettierignore: delete the `package-lock.json` line (D-03). Keep `.cache`, `package.json`, `public`.
    - `yarn remove yarn y18n` (updates package.json AND yarn.lock atomically — research "Don't Hand-Roll": never hand-edit package.json for removals; `yarn remove` prevents manifest/lockfile drift). This removes both from `dependencies`.

    Post-verify (D-10): `nvm use 20 && yarn install && yarn build && yarn test` — all must exit 0. `yarn install` must NOT create a new `package-lock.json` (`git status` must show no untracked package-lock.json). `yarn build` green proves node-sass ABI 115 prebuilt loads under Node 20. `yarn test` green proves the Phase 1 regression net (formik, navigation, gatsby-node suites) is intact.

    Commit atomically: `git commit -m "fix(02-01): consolidate to single yarn lockfile, switch netlify build to yarn, remove yarn/y18n hack deps (FNDT-01, D-01..D-03, D-07 subset)"`. Stage exactly: the package-lock.json deletion, netlify.toml, .prettierignore, package.json, yarn.lock.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 20 && test ! -f package-lock.json && git ls-files --error-unmatch package-lock.json 2>&1 | grep -q "did not match" && grep -xq 'command = "yarn build"' netlify.toml && ! grep -qx 'package-lock.json' .prettierignore && ! grep -q '"yarn"' package.json && ! grep -q '"y18n"' package.json && yarn install && yarn build && yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f package-lock.json` succeeds (file gone from filesystem)
    - `git ls-files package-lock.json` returns empty (file gone from index)
    - `grep -xq 'command = "yarn build"' netlify.toml` succeeds (exact line match)
    - `! grep -qx 'package-lock.json' .prettierignore` succeeds (line removed)
    - `grep -q '"yarn"' package.json` fails (yarn dep removed) — note: the `packageManager: "yarn@1.22.22"` line is a different key and MUST remain
    - `grep -q '"y18n"' package.json` fails (y18n dep removed)
    - `grep -q '"prismjs"' package.json` succeeds (keeper — NOT removed)
    - `grep -q '"@testing-library/dom"' package.json` succeeds (keeper — NOT removed)
    - `grep -q 'NODE_VERSION = "10"' netlify.toml` succeeds (still present — Plan 02 removes it; this plan must NOT touch it)
    - `yarn install` exits 0 and `git status` shows no untracked `package-lock.json`
    - `yarn build` exits 0 under Node 20 (node-sass prebuilt ABI 115 loads)
    - `yarn test` exits 0 (Phase 1 regression net green)
  </acceptance_criteria>
  <done>`package-lock.json` is gone from git and filesystem, `netlify.toml` builds with `yarn build`, `.prettierignore` has no `package-lock.json` line, `yarn` and `y18n` are absent from package.json dependencies, and `nvm use 20 && yarn install && yarn build && yarn test` is green in a single atomic commit.</done>
  <reversibility rating="reversible">git revert restores package-lock.json, the npm command, the .prettierignore line, and the two deps.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → build pipeline | `yarn install` resolution crosses here; single lockfile removes drift ambiguity |
| repo → Netlify build image | `netlify.toml` command crosses here; `yarn build` prevents npm from regenerating package-lock.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | package-lock.json resurrection via `npm run build` on Netlify | high | mitigate | D-02 same-commit command switch to `yarn build` (this task); `git status` check confirms no regen locally |
| T-02-02 | DoS | Building under Node 24 (shell default) → node-sass source-compile failure | high | mitigate | Every command opens with `nvm use 20` (Pitfall 2); node-sass ABI 115 prebuilt verified on disk |
| T-02-03 | Tampering | Manifest/lockfile drift from hand-editing package.json | medium | mitigate | Use `yarn remove` (atomic package.json + yarn.lock rewrite) — research "Don't Hand-Roll" |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed this phase; only removals of verified direct-only deps (research Package Legitimacy Audit: all OK) |
</threat_model>

<verification>
- `nvm use 20 && yarn install && yarn build && yarn test` exits 0 (full loop, the phase's real gate)
- `git ls-files package-lock.json` empty
- `git status` shows no untracked `package-lock.json` after install
- `grep -xq 'command = "yarn build"' netlify.toml`
- `git log -1 --format=%s` contains `02-01` and `FNDT-01`
</verification>

<success_criteria>
- FNDT-01 satisfied: `package-lock.json` deleted, `yarn.lock` is the only lockfile, `yarn install` resolves clean, `yarn build` passes
- D-01, D-02, D-03 implemented; D-07 subset (`yarn`, `y18n`) implemented
- prismjs and @testing-library/dom confirmed still present (keepers)
- NODE_VERSION untouched (deferred to Plan 02)
- One atomic commit on disk
</success_criteria>

<output>
Create `.planning/phases/02-foundation-cleanup/02-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan produces no new symbols — it is a removal/consolidation plan. Artifacts are state changes:
- **Removed file:** `package-lock.json` (git-tracked → deleted)
- **Config change:** `netlify.toml` `[build] command` value: `"npm run build"` → `"yarn build"`
- **Config change:** `.prettierignore` line `package-lock.json` removed (3 lines remain: `.cache`, `package.json`, `public`)
- **Manifest change:** `package.json` `dependencies` entries `yarn` and `y18n` removed; `yarn.lock` corresponding resolution entries pruned
- **Proven baseline:** single-lockfile yarn-only build path verified green under Node 20 (the contract every Plan 02/03 task builds on)