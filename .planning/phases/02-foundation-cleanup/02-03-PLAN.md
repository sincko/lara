---
phase: 02-foundation-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/util/site.json
  - README.md
autonomous: true
requirements:
  - SEOS-04
user_setup: []

must_haves:
  truths:
    - "D-11: `src/util/site.json` has no `ga` key anywhere; `node -e \"require('./src/util/site.json')\"` succeeds (valid JSON)"
    - "`src/util/site.json` `meta` object is intact (title, titleTemplate, description, siteUrl, image, twitterUsername unchanged)"
    - "D-12: `README.md` contains zero matches for the forbidden starter tokens: `stackrole`, `Stackrole`, `gatsby-starter-foundation`, `Deploy to Netlify`, `twitter-header`, `screenshot.png`, `package-lock.json`, `Google Analytics`, `UA-`, `pensive-engelbart-b7e7bb`"
    - "D-12: `README.md` is written in Italian and describes laryart.it accurately (Gatsby + Netlify CMS, content structure, local dev commands `yarn install`/`yarn develop`/`yarn build`, Node 20 requirement, deployment on Netlify)"
    - "`nvm use 20 && yarn build && yarn test` exits 0 (site.json edit did not break gatsby-config.js which reads `settings.meta`)"
  artifacts:
    - "src/util/site.json without the ga field (10-line file → 9-line file, valid JSON)"
    - "README.md rewritten in Italian for laryart.it (no starter boilerplate)"
  key_links:
    - "gatsby-config.js reads `settings.meta` from site.json — the `ga` field is not consumed anywhere (grep-verified), so removal is metadata-only with no build impact"
    - "README documents yarn-only workflow (no npm commands) so future contributors cannot resurrect package-lock.json"
---

<objective>
Remove the starter remnants: the dead `ga` placeholder in `src/util/site.json` (D-11) and the Stackrole starter README boilerplate, replaced with an accurate Italian README for laryart.it (D-12). This plan delivers SEOS-04.

Purpose: The repo's user-facing documentation and metadata still describe a generic starter (`gatsby-starter-foundation`, deploy buttons, Google Analytics placeholder, missing screenshot/twitter-header assets). After this plan, the repo describes the actual site — an Italian artisan's Gatsby + Netlify CMS portfolio/blog deployed at laryart.it — and carries no misleading config.
Output: One atomic commit with the cleaned site.json and rewritten README.md.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundation-cleanup/02-CONTEXT.md
@.planning/phases/02-foundation-cleanup/02-RESEARCH.md
@.planning/phases/02-foundation-cleanup/02-01-SUMMARY.md
@src/util/site.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Remove ga placeholder from site.json (D-11)</name>
  <files>src/util/site.json</files>
  <read_first>
    - src/util/site.json (current 11-line file: `meta` object with title/titleTemplate/description/siteUrl/image/twitterUsername, then a top-level `"ga": "UA-XXXXXXXXX-X"` at line 10)
    - gatsby-config.js (confirm it reads `settings.meta` from site.json — NOT `settings.ga`; research verified no consumer of `ga` exists anywhere via grep)
    - .planning/phases/02-foundation-cleanup/02-RESEARCH.md §"Code Examples" Example 4 (ga verification: `rg -n '"ga"|\.ga\b' src/ gatsby-config.js gatsby-node.js gatsby-browser.js static/` → only site.json:10 defines it) and Pitfall 5 context
    - static/admin/config.yml (research notes the CMS settings collection does not expose `ga` — safe to remove)
  </read_first>
  <action>
    Pre-verify the `ga` field has no consumer (D-11 requires confirmation):
    - `nvm use 20` first.
    - `rg -n '"ga"|\.ga\b' src/ gatsby-config.js gatsby-node.js gatsby-browser.js static/` — expect ONLY `src/util/site.json:10` as a match (the definition). If any OTHER file references `ga` or `.ga`, STOP and report — do not remove.

    Remove the ga field:
    - Edit src/util/site.json: delete line 10 (`  "ga": "UA-XXXXXXXXX-X"`) AND the comma at the end of line 8 (the `twitterUsername` line) so the JSON remains valid. The `meta` object must close cleanly with `"twitterUsername": "@simooooone"` (no trailing comma) then `}` then `}`. Do NOT touch any field inside `meta`.

    Post-verify:
    - `node -e "const s=require('./src/util/site.json'); console.log(s.meta.siteUrl)"` — must print `https://laryart.it` (proves valid JSON + meta intact).
    - `node -e "const s=require('./src/util/site.json'); console.log(s.ga)"` — must print `undefined` (ga field gone).
    - `rg -n '"ga"' src/util/site.json` — must exit 1 (no matches).
    - `nvm use 20 && yarn build && yarn test` — green (proves gatsby-config.js `settings.meta` consumer still resolves).

    Do NOT commit yet — commit together with the README rewrite in the next task (one atomic commit for SEOS-04, D-11 + D-12).
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 20 && ! rg -q '"ga"' src/util/site.json && node -e "const s=require('./src/util/site.json'); if(s.meta.siteUrl!=='https://laryart.it')process.exit(1); if(s.ga!==undefined)process.exit(1); console.log('OK')" && yarn build && yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `rg -q '"ga"' src/util/site.json` exits 1 (ga key gone)
    - `node -e "require('./src/util/site.json')"` succeeds (valid JSON)
    - `node -e "const s=require('./src/util/site.json'); process.exit(s.meta.siteUrl==='https://laryart.it'?0:1)"` exits 0 (meta.siteUrl intact)
    - `node -e "const s=require('./src/util/site.json'); process.exit(s.meta.title.includes('LaryArt')?0:1)"` exits 0 (meta.title intact)
    - `node -e "const s=require('./src/util/site.json'); process.exit(s.ga===undefined?0:1)"` exits 0 (ga undefined)
    - `yarn build` exits 0 under Node 20
    - `yarn test` exits 0
  </acceptance_criteria>
  <done>`ga` field is gone from site.json, the JSON is valid, `meta` is fully intact, and build+test is green. (Commit deferred to the README task — single SEOS-04 commit.)</done>
  <reversibility rating="reversible">git revert restores the ga line.</reversibility>
</task>

<task type="auto">
  <name>Rewrite README.md in Italian for laryart.it, remove all starter boilerplate (D-12)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current 160 lines — identify ALL starter boilerplate to remove: Netlify badge for `pensive-engelbart-b7e7bb` line 1, `Foundation`/`Stackrole` heading lines 3-5, Deploy to Netlify buttons lines 7+34, twitter link to stackrole line 9, screenshot.png reference line 11, Stackrole features list lines 13-27, twitter-header.jpg line 28, Quick Deploy section lines 30-36, Further Instructions list lines 38-47, Access to Netlify CMS lines 49-56, Editing content lines 58-62, Customing Site details line 64-66 (mentions Google Analytics), Custom domain lines 68-70, Install Locally lines 72-99 (uses `gatsby new` + `gatsby develop`, mentions package-lock.json at line 114+134), Folder Structure lines 101-138, Learning Gatsby lines 140-146, Thank you from Stackrole lines 148-160)
    - src/util/site.json `meta` (after Task 1: title, description, siteUrl `https://laryart.it`, twitterUsername `@simooooone` — use these as factual basis for the README)
    - .nvmrc (content `20` — document Node 20 requirement)
    - package.json `scripts` (build/develop/start/serve/clean/format/test — document the yarn commands) and `packageManager: "yarn@1.22.22"`
    - src/content/pages/ and src/content/posts/ directory listing (confirm the content structure: pages index/laryart/privacy/contatti, ~19 blog posts — describe in README)
    - .planning/phases/02-foundation-cleanup/02-RESEARCH.md §Pitfall 5 (forbidden-token grep list) and §"User Constraints" D-12 (Italian, accurate, yarn-only, Node 20, Netlify deployment)
    - .planning/codebase/STACK.md (factual stack: Gatsby 5.15, React 18, Netlify CMS, node-sass, Formik, Matomo)
  </read_first>
  <action>
    Write a new README.md entirely in Italian describing laryart.it. The README MUST:
    - Title: `# LaryArt` (or `# LaryArt — decoupage ed oggetti d'arte fatti a mano`) — NOT `Foundation`, NOT `Stackrole`.
    - Describe the site: il sito di Lara (artigianato, decoupage, oggetti d'arte fatti a mano), costruito con Gatsby e Netlify CMS, deployato su Netlify all'indirizzo https://laryart.it.
    - Tech stack section: Gatsby 5.15, React 18, Netlify CMS (admin su /admin/), node-sass, Formik, Matomo (analytics). Mention Node 20 (`.nvmrc`) and yarn 1.22.22 (`packageManager`).
    - Content structure section: `src/content/posts/` (blog posts in markdown con frontmatter), `src/content/pages/` (pagine: index, laryart, privacy, contatti).
    - Local development section: prerequisites (Node 20 via nvm, yarn 1.22); commands `yarn install`, `yarn develop` (dev server su http://localhost:8000), `yarn build`, `yarn test`, `yarn format`, `yarn clean`. Use ONLY yarn commands — NEVER document `npm install` or `npm run build` (Pitfall 3: npm regenerates package-lock.json).
    - CMS section: breve nota su /admin/ per la gestione contenuti via Netlify CMS + git-gateway; per uso locale `npx netlify-cms-proxy-server` + `yarn develop`.
    - Deployment section: Netlify (build command `yarn build`, publish directory `public`, Node 20 da `.nvmrc`).
    - NO Netlify status badge for `pensive-engelbart-b7e7bb` (that is a different site).
    - NO Deploy to Netlify button, NO Stackrole links, NO twitter-header.jpg, NO screenshot.png, NO `gatsby-starter-foundation`, NO `package-lock.json` mention, NO Google Analytics mention, NO `UA-` placeholder.

    Post-verify (Pitfall 5 forbidden-token grep — ALL must return zero matches):
    - `rg -i 'stackrole|gatsby-starter-foundation|Deploy to Netlify|twitter-header|screenshot\.png|package-lock\.json|Google Analytics|UA-|pensive-engelbart' README.md` — must exit 1 (no matches).
    - Confirm the README mentions yarn commands: `rg -q 'yarn install' README.md` and `rg -q 'yarn develop' README.md` and `rg -q 'yarn build' README.md` — all must succeed.
    - Confirm Italian: `rg -q 'Node 20' README.md` or `rg -q 'Node.js 20' README.md` (Node 20 requirement documented).

    Commit atomically with the site.json change from Task 1: `git commit -m "docs(02-03): remove ga placeholder, rewrite README in Italian for laryart.it (SEOS-04, D-11, D-12)"`. Stage: src/util/site.json, README.md.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 20 && ! rg -iq 'stackrole|gatsby-starter-foundation|Deploy to Netlify|twitter-header|screenshot\.png|package-lock\.json|Google Analytics|UA-|pensive-engelbart' README.md && rg -q 'yarn install' README.md && rg -q 'yarn develop' README.md && rg -q 'yarn build' README.md && rg -q '20' README.md && ! rg -q '"ga"' src/util/site.json && node -e "require('./src/util/site.json')" && yarn build && yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `rg -iq 'stackrole|gatsby-starter-foundation|Deploy to Netlify|twitter-header|screenshot\.png|package-lock\.json|Google Analytics|UA-|pensive-engelbart' README.md` exits 1 (zero forbidden tokens)
    - `rg -q 'yarn install' README.md` succeeds (yarn workflow documented)
    - `rg -q 'yarn develop' README.md` succeeds
    - `rg -q 'yarn build' README.md` succeeds
    - `rg -q 'laryart.it' README.md` succeeds (site URL documented)
    - `rg -iq 'npm install|npm run build' README.md` exits 1 (no npm commands documented — Pitfall 3 prevention)
    - `! rg -q '"ga"' src/util/site.json` (ga still gone from Task 1)
    - `node -e "require('./src/util/site.json')"` succeeds (JSON still valid)
    - `yarn build` exits 0 under Node 20
    - `yarn test` exits 0
    - `git log -1 --format=%s` contains `02-03` and `SEOS-04`
  </acceptance_criteria>
  <done>README.md is rewritten in Italian for laryart.it with zero starter boilerplate (forbidden-token grep clean), yarn-only workflow documented, Node 20 requirement stated, and the ga field is gone from site.json — all in one atomic commit with build+test green.</done>
  <reversibility rating="reversible">git revert restores the original README and the ga line.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo metadata → build config | `src/util/site.json` is consumed by `gatsby-config.js` (`settings.meta`); the `ga` field crosses this boundary but has no consumer (grep-verified) |
| repo docs → contributor workflow | README documents the package manager workflow; documenting npm would enable package-lock.json resurrection (Pitfall 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | DoS | site.json edit breaks gatsby-config.js JSON parse | medium | mitigate | `node -e "require('./src/util/site.json')"` validation + `yarn build` green in acceptance criteria; edit only removes the ga line + fixes the trailing comma |
| T-02-10 | Tampering | README still documents npm commands → package-lock.json resurrection | medium | mitigate | Pitfall 5 forbidden-token grep includes `package-lock.json`; acceptance criteria assert `rg -iq 'npm install|npm run build' README.md` exits 1 |
| T-02-11 | DoS | Building under Node 24 → node-sass compile failure | high | mitigate | Every verify command opens with `nvm use 20` (Pitfall 2) |
</threat_model>

<verification>
- `! rg -q '"ga"' src/util/site.json`
- `node -e "require('./src/util/site.json')"` succeeds
- `rg -iq 'stackrole|gatsby-starter-foundation|Deploy to Netlify|twitter-header|screenshot\.png|package-lock\.json|Google Analytics|UA-|pensive-engelbart' README.md` exits 1
- `rg -q 'yarn install' README.md` and `rg -q 'yarn develop' README.md` and `rg -q 'yarn build' README.md` all succeed
- `nvm use 20 && yarn build && yarn test` exits 0
- `git log -1 --format=%s` contains `02-03` and `SEOS-04`
</verification>

<success_criteria>
- SEOS-04 satisfied: ga placeholder removed from site.json, README rewritten in Italian for laryart.it with zero starter boilerplate
- D-11, D-12 implemented
- Forbidden-token grep clean (Pitfall 5)
- Yarn-only workflow documented (no npm commands)
- One atomic commit on disk
</success_criteria>

<output>
Create `.planning/phases/02-foundation-cleanup/02-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan produces no new code symbols. Artifacts are state changes:
- **Metadata change:** `src/util/site.json` top-level `"ga": "UA-XXXXXXXXX-X"` field removed; `meta` object (title, titleTemplate, description, siteUrl, image, twitterUsername) unchanged
- **Documentation artifact:** `README.md` — full Italian rewrite for laryart.it (title, site description, tech stack, content structure, local dev commands, CMS usage, Netlify deployment); zero starter boilerplate tokens remain
- **Proven guard:** forbidden-token grep on README returns zero matches (Pitfall 5 discipline); yarn-only workflow documented to prevent package-lock.json resurrection