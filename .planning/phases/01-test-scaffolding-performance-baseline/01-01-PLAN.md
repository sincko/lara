---
phase: 01-test-scaffolding-performance-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - yarn.lock
  - jest.config.js
  - jest-preprocess.js
  - jest.setup.js
  - loadershim.js
  - __mocks__/gatsby.js
  - __mocks__/file-mock.js
  - src/components/formik.test.js
autonomous: true
requirements: [FNDT-05]
user_setup: []

must_haves:
  truths:
    - "yarn test exits 0 and runs a real jest suite (starter placeholder replaced) — success criterion 1 of ROADMAP Phase 1"
    - "Formik validation errors for empty required fields surface through the UI via TextFieldConError (at least one passing assertion)"
    - "The form submit failure path (D-05) is covered by a preserved red test marked it.skip with an explicit FNDT-05 → FORM-04 comment so the suite stays green"
    - "emailjs-com is mocked before FormikContact is imported (D-07); the hardcoded emailjs key never executes or leaks into test output"
  artifacts:
    - package.json (test script + 8 pinned devDependencies)
    - jest.config.js, jest-preprocess.js, jest.setup.js, loadershim.js
    - __mocks__/gatsby.js, __mocks__/file-mock.js
    - src/components/formik.test.js
  key_links:
    - package.json test script → jest CLI (must exit 0, not the starter placeholder)
    - jest-preprocess.js (babel-preset-gatsby) ↔ all transformed test sources
    - __mocks__/gatsby.js ↔ every gatsby import in tested components (Link, graphql, useStaticQuery)
    - jest.setup.js jest-dom matchers ↔ formik assertions (toBeInTheDocument, toHaveAttribute)

---

<objective>
Replace the failing starter `test` placeholder with a real Jest 29 + @testing-library/react regression net, and prove the whole pipeline end-to-end with the formik contact form validation suite — the thinnest vertical slice through every layer this phase touches.

Purpose: This is the tracer slice for the test half of Phase 1. It proves deps-install → babel transform → module mocks → jsdom environment → RTL render → jest-dom matchers → `yarn test` exit 0 before any further test files are written (FNDT-05, ROADMAP success criteria 1 and 2).

Output: Working jest scaffold + `src/components/formik.test.js` (validation suite green + D-05 failure-path regression test preserved as skipped).

**D-01 deviation note:** gatsby-plugin-jest is UNPUBLISHED from npm (E404 verified 2026-08-18). The locked mechanism is swapped for the official Gatsby manual-mock pattern (`__mocks__/gatsby.js` + babel-jest + babel-preset-gatsby) — D-01's intent (automatic Link/graphql/useStaticQuery mocks) is fully preserved.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-scaffolding-performance-baseline/01-RESEARCH.md
@.planning/phases/01-test-scaffolding-performance-baseline/01-CONTEXT.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Wire the jest pipeline end-to-end — install pinned devDeps, official Gatsby scaffold, and a green formik validation test</name>
  <files>
    - package.json (test script + devDependencies via yarn add)
    - yarn.lock
    - jest.config.js
    - jest-preprocess.js
    - jest.setup.js
    - loadershim.js
    - __mocks__/gatsby.js
    - __mocks__/file-mock.js
    - src/components/formik.test.js
  </files>
  <read_first>
    - package.json (current scripts + deps; the `test` script at line 21 is the placeholder being replaced)
    - src/components/formik.js (only default export FormikContact; emailjs.init() at line 8; TextFieldConError errorText = meta.error && meta.touched; yup schema lines 25-28)
    - .planning/phases/01-test-scaffolding-performance-baseline/01-RESEARCH.md — Architecture Patterns (Pattern 1 official config + Pattern 3 behavioral form tests), Standard Stack table, Common Pitfalls 2, 3, 5, 6, 7
    - .prettierrc (semi: false, arrowParens: "avoid")
  </read_first>
  <action>
    1. Install the EXACT pinned devDependency matrix from the research Standard Stack (D-01/D-02 mechanism, versions verified on the registry):
       `yarn add --dev jest@29.7.0 babel-jest@29.7.0 jest-environment-jsdom@29.7.0 @testing-library/react@16.3.2 @testing-library/jest-dom@6.6.3 identity-obj-proxy@3.0.0 babel-preset-gatsby@3.16.0`
       Do NOT install gatsby-plugin-jest (unpublished, E404 — the D-01 mechanism swap is this plan's reason for the manual `__mocks__/gatsby.js`). Do NOT install @testing-library/jest-dom@7.x (vitest peer).
    2. package.json: replace the `"test": "echo \"Write tests! -> https://gatsby.dev/unit-testing\" && exit 1"` script (line 21) with `"test": "jest --watch=false"` — D-03. No other script changes.
    3. Create repo-root scaffold files, copied faithfully from the official Gatsby unit-testing guide (research Pattern 1 — write these exact semantics):
       - jest.config.js: transform `"^.+\\.jsx?$": "<rootDir>/jest-preprocess.js"`; moduleNameMapper — SCSS/CSS/styl/less/sass → `identity-obj-proxy`, jpg/jpeg/png/gif/eot/otf/webp/svg/ttf/woff/woff2/mp4/webm/wav/mp3/m4a/aac/oga → `<rootDir>/__mocks__/file-mock.js`; testPathIgnorePatterns `["node_modules", "\\.cache", "<rootDir>.*/public"]`; transformIgnorePatterns `["node_modules/(?!(gatsby|gatsby-script|gatsby-link)/)"]`; globals `{ __PATH_PREFIX__: "" }`; testEnvironmentOptions `{ url: "http://localhost" }`; setupFiles `["<rootDir>/loadershim.js"]`; setupFilesAfterEnv `["<rootDir>/jest.setup.js"]`
       - jest-preprocess.js: `module.exports = require("babel-jest").default.createTransformer({ presets: ["babel-preset-gatsby"] })`
       - loadershim.js: `global.___loader = { enqueue: jest.fn() }`
       - jest.setup.js: `import "@testing-library/jest-dom"` — the v6 root entry, NOT the v5 `extend-expect` path (Pitfall 7); plus the standard window.matchMedia mock guarded by `if (!window.matchMedia)` as insurance against MUI v4 probing (Pitfall 5)
       - __mocks__/gatsby.js: `const gatsby = jest.requireActual("gatsby")` re-exported, with `graphql: jest.fn()`, `Link: jest.fn().mockImplementation(({ to, ...rest }) => React.createElement("a", { href: to, ...rest }))`, `useStaticQuery: jest.fn()` — covers every gatsby import in this repo (Layout/blog-list/navigation/formik; no Slice usage in the repo — do not add Slice)
       - __mocks__/file-mock.js: `module.exports = "test-file-stub"`
    4. All scaffold + test files MUST follow CONVENTIONS.md formatting: no semicolons, double quotes, `arrowParens: "avoid"` — run `yarn format` after writing; do not copy the official example files' semicolon style (Pitfall 6).
    5. Write `src/components/formik.test.js`:
       - Line 1 docblock `/** @jest-environment jsdom */`
       - `jest.mock("emailjs-com", () => ({ init: jest.fn(), sendForm: jest.fn() }))` declared BEFORE the `import FormikContact from "./formik"` line — D-07 (babel-jest hoists jest.mock anyway; keep it visually first)
       - Suite "FormikContact validation": render `<FormikContact />`, fireEvent.click on the submit button (accessible name "Invia"), then `await screen.findByText(...)` for BOTH required-field error messages. Per research assumption A6, yup 1.7.1 default messages are English ("nome is a required field", "email is a required field") — assert the ACTUAL strings observed in the first run (trial run first, then fix assertions to the real messages; record the actual strings in a test comment). One passing assertion minimum.
       - Never reference, print, or assert the emailjs key string from formik.js:8 in any assertion or log (security: tests mock the module so the real key never executes).
    6. Run `yarn test src/components/formik.test.js` — the suite must pass. Run `yarn test` — must exit 0.
  </action>
  <verify>
    <automated>yarn test src/components/formik.test.js</automated>
    <automated>yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `yarn test` exits 0 (no longer the starter placeholder exit 1)
    - `node -e "const p=require('./package.json'); if(p.scripts.test!=='jest --watch=false') process.exit(1); for(const d of ['jest','babel-jest','jest-environment-jsdom','@testing-library/react','@testing-library/jest-dom','identity-obj-proxy','babel-preset-gatsby']) if(!p.devDependencies[d]) process.exit(1)"` exits 0
    - jest.config.js contains: the transform key `^.+.jsx?$`, moduleNameMapper entries for `identity-obj-proxy` and `<rootDir>/__mocks__/file-mock.js`, transformIgnorePatterns containing `node_modules/(?!(gatsby|gatsby-script|gatsby-link)/)`, setupFilesAfterEnv containing `<rootDir>/jest.setup.js`
    - __mocks__/gatsby.js exports Link (rendering `<a href={to}>`), graphql (jest.fn), useStaticQuery (jest.fn)
    - jest.setup.js imports `@testing-library/jest-dom` (root path, not extend-expect)
    - src/components/formik.test.js passes with at least 1 passing test (validation); the exact yup message strings asserted in the test are the ones actually rendered by the suite (verified by the passing run)
    - No .test.js file in the repo uses semicolons (Pitfall 6): `rg -l ';' src/components/formik.test.js` finds nothing
  </acceptance_criteria>
  <done>The jest pipeline runs a green formik validation test through `yarn test` (exit 0) — install, config, mocks, transform, jsdom, RTL, jest-dom all proven end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve the D-05 failure-path regression test as a skipped red test for Phase 4 (FORM-04)</name>
  <files>
    - src/components/formik.test.js
  </files>
  <read_first>
    - src/components/formik.js (the bug: `document.location.assign("/thanks")` at line 51 runs unconditionally after the emailjs promise chain — this task's test proves the bug exists)
    - .planning/ROADMAP.md Phase 4 FORM-04 ("form no longer reports success when email send fails")
    - .planning/phases/01-test-scaffolding-performance-baseline/01-VALIDATION.md (Manual-Only Verifications row: "it.skip with FNDT-05→FORM-04 comment; un-skip in Phase 4")
  </read_first>
  <action>
    1. Extend src/components/formik.test.js with a new suite describing the submit failure path (D-05):
       - `it.skip("does NOT navigate to /thanks when emailjs.sendForm rejects — regression net for FORM-04")`
       - Inside: `emailjs.sendForm.mockRejectedValue({ text: "network error" })`; stub the location first with `delete window.location` then `Object.defineProperty(window, "location", { value: { assign: jest.fn() }, writable: true })` (A7 fallback); fill the nome + email fields (getByLabelText("Nome") / ("Email")) with valid values; fireEvent.click("Invia"); `await waitFor(() => expect(emailjs.sendForm).toHaveBeenCalledTimes(1))`; then `expect(assign).not.toHaveBeenCalled()` — the RED assertion that fails against current buggy formik.js line 51.
       - This assertion is the proof for D-05: current code calls `document.location.assign("/thanks")` unconditionally, so `expect(assign).not.toHaveBeenCalled()` FAILS by design. That failure is the Phase 4 regression target — do not weaken or remove it; do not mock it to pass.
       - Add a comment above the `it.skip`: `// FNDT-05 → FORM-04 regression net: unskip when Phase 4 fixes the false-success bug. Fails on the current formik.js by design.`
       - Mark the test `it.skip` so `yarn test` exits 0 while the assertion text is preserved (research finding 5 / VALIDATION.md).
    2. Run `yarn test src/components/formik.test.js` — must exit 0 and report exactly 1 skipped test. Run full `yarn test` — exit 0.
  </action>
  <verify>
    <automated>yarn test src/components/formik.test.js</automated>
    <automated>yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `yarn test` exits 0 and the jest output for formik.test.js shows `1 skipped`
    - src/components/formik.test.js contains the string `it.skip(` exactly once and a comment containing the string `FORM-04`
    - The skipped test body contains `expect(assign).not.toHaveBeenCalled()` with NO jest-escape hatch (no `expect(...).anything()`, no try/catch swallowing the assertion)
  </acceptance_criteria>
  <done>The false-success failure path is covered as a skipped red test; `yarn test` still exits 0; Phase 4 (FORM-04) has a one-commit un-skip target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine ↔ npm registry | `yarn add --dev` pulls 8 packages; integrity relies on registry + audit |
| test runner (jsdom) ↔ production source | jest transforms and executes src/components/formik.js inside the test process — including the hardcoded emailjs key at formik.js:8 if not mocked |
| test output ↔ terminal/CI logs | console output from tests must not leak the hardcoded emailjs key |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-SC | Tampering | npm install (8 devDeps) | high | mitigate | RESEARCH.md Package Legitimacy Audit verified all 8 pinned packages (npm registry + official docs cross-check, 2026-08-18): all approved/OK, no postinstall scripts. No [SUS]/[ASSUMED] in the install set → no install checkpoint. Pinned EXACT versions; deviating (e.g. jest-dom v7) halts the task and requires a human-verify gate. |
| T-1-02 | Information disclosure | emailjs key (formik.js:8) in test process | medium | mitigate | `jest.mock("emailjs-com", ...)` before FormikContact import (D-07) so `init("user_...")` never executes; test code never prints, greps, or asserts the key string (acceptance criteria 6 enforces via no hardcoded key in the test file) |
| T-1-03 | Tampering | jest.config.js moduleNameMapper | low | accept | Local repo config with no user-supplied input; git history is the integrity record |
</threat_model>

<verification>
- After Task 1: `yarn test src/components/formik.test.js` green, `yarn test` green.
- After Task 2: `yarn test` green with the D-05 test reported as 1 skipped.
- Phase gate for this plan: `yarn test` exits 0 (FNDT-05 success criterion 1).
</verification>

<success_criteria>
- [ ] `yarn test` exits 0 with the formik suite running (at least 1 real assertion)
- [ ] `src/components/formik.test.js` covers both validation (Task 1) and the D-05 failure path (Task 2, skipped)
- [ ] Jest scaffold files exist and match the official Gatsby guide semantics
- [ ] No production code (src/ other than test files) modified — `git status` shows only package.json/yarn.lock/test files changed
</success_criteria>

## Artifacts this phase produces

**Files:**
- `jest.config.js`, `jest-preprocess.js`, `jest.setup.js`, `loadershim.js` (repo root)
- `__mocks__/gatsby.js`, `__mocks__/file-mock.js`
- `src/components/formik.test.js`
- `package.json` — `scripts.test = "jest --watch=false"`; 7 new devDependencies (jest, babel-jest, jest-environment-jsdom, @testing-library/react, @testing-library/jest-dom, identity-obj-proxy, babel-preset-gatsby)

**Test suites / exported symbols:**
- `describe("FormikContact form")` — validation suite (Task 1)
- `describe("submit failure path")` — skipped D-05 regression (Task 2), containing the stub `assign` jest.fn() and the assertion `expect(assign).not.toHaveBeenCalled()`

**Config keys introduced:**
- `transform`, `moduleNameMapper` (SCSS→identity-obj-proxy, images→file-mock), `testPathIgnorePatterns`, `transformIgnorePatterns`, `globals.__PATH_PREFIX__`, `setupFiles`, `setupFilesAfterEnv` in jest.config.js
</output>
