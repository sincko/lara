---
phase: 01-test-scaffolding-performance-baseline
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/templates/blog-list.test.js
  - src/components/navigation.test.js
  - gatsby-node.test.js
autonomous: true
requirements: [FNDT-05]
user_setup: []

must_haves:
  truths:
    - "Pagination math (isFirst/isLast/prevPage/nextPage, blog-list.js lines 80-84) is verified by passing assertions for pages 1, 2, 3 of 3"
    - "Navigation handleToggleClick flips the is-active class on the menu trigger (navigation.js lines 38-42, 55-57)"
    - "gatsby-node createPages is tested in the node environment with injected mocked graphql/actions/reporter — post pages with prev/next context, /blog pagination at 9/page, and panicOnBuild on GraphQL errors"
    - "`yarn test` exits 0 with all suites green"
  artifacts:
    - src/templates/blog-list.test.js
    - src/components/navigation.test.js
    - gatsby-node.test.js
  key_links:
    - blog-list.test.js ↔ __mocks__/gatsby.js Link mock (asserted hrefs) + gatsby-image mock
    - gatsby-node.test.js ↔ real gatsby-node.js createPages (component resolution via path.resolve, GraphQL error branch)
    - All three suites ↔ `yarn test` exit code (FNDT-05 success criterion 1)

---

<objective>
Expand the regression net from the proven jest pipeline: pagination math (blog-list), navigation state toggle, and page creation (gatsby-node) — completing FNDT-05's four priority targets with at least one passing assertion each (D-04, D-06).

Purpose: These are horizontal expansion tasks building out from Plan 01's proven vertical slice. Each adds a covered area a real user (or Phase 3's upgrade, or Phase 4's fix) can rely on.

Output: Three new test suites, all green under `yarn test`.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-scaffolding-performance-baseline/01-RESEARCH.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-test-scaffolding-performance-baseline/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test blog-list pagination math — pages 1, 2, 3 of 3 via rendered Link hrefs</name>
  <files>
    - src/templates/blog-list.test.js
  </files>
  <read_first>
    - src/templates/blog-list.js (BlogIndex lines 75-84: isFirst/isLast/prevPage/nextPage math; Pagination component lines 39-74 renders Links with rel="prev"/rel="next" and className "is-active num" on the current page; blogSlug is "/blog/"; BlogIndex lines 100-105 render Layout + Seo unconditionally — both must be mocked per the action)
    - src/components/layout.js (line 23 `useStaticQuery(query)`, line 24 destructures `site.siteMetadata` — with the root `__mocks__/gatsby.js` `useStaticQuery: jest.fn()` returning undefined this throws TypeError unless Layout is mocked)
    - src/components/seo.js (line 4 imports `useLocation` from `@reach/router` — absent from yarn.lock; mock it in the test)
    - src/components/post-card.js (imports `Img` from gatsby-image and `Link` from gatsby — must be mocked so the tree stays light)
    - .planning/phases/01-test-scaffolding-performance-baseline/01-RESEARCH.md Code Example 4 (pagination test pattern) + Pitfall 2 (SCSS/images)
  </read_first>
  <action>
    1. Create `src/templates/blog-list.test.js`:
       - Line 1 docblock `/** @jest-environment jsdom */`
       - Mock at file top BEFORE imports:
         `jest.mock("gatsby-image", () => ({ __esModule: true, Img: () => <img alt="" /> }))` (the `__esModule: true` flag is REQUIRED — babel-jest interop otherwise reads the component off `.default` and renders nothing, silently)
         `jest.mock("../components/post-card", () => props => <div>{props.data.frontmatter.title}</div>)`
         `jest.mock("@reach/router", () => ({ useLocation: () => ({ pathname: "/blog" }) }))` — seo.js line 4 imports `useLocation` from `@reach/router`, which exists only as the webpack alias `@gatsbyjs/reach-router` in yarn.lock; under Jest the bare import fails resolution without this mock.
         `jest.mock("../components/layout", () => ({ __esModule: true, default: ({ children }) => <div>{children}</div> }))` and `jest.mock("../components/seo", () => ({ __esModule: true, default: () => null }))` — Layout (layout.js lines 22-24) calls `useStaticQuery(query)` and then destructures `site.siteMetadata` from its return; the root `__mocks__/gatsby.js` `useStaticQuery` is `jest.fn()` returning undefined → `site.siteMetadata` throws TypeError. Layout/Seo are NOT pagination-math targets, so mocking them out is required to keep the tree renderable; the root `__mocks__/gatsby.js` still mocks Link/graphql/useStaticQuery.
         - Because Layout is mocked, the SCSS import (`layout.js` line 8) never loads in this suite — but the moduleNameMapper identity-obj-proxy from plan 01 covers it anyway.
       - Render `BlogIndex` with stub data `{ allMarkdownRemark: { edges: [] } }` (posts filter drops all — assert on the Pagination block, not on posts) + pageContext, and assert on the rendered Pagination output (the mocked Layout renders it directly).
       - Test cases (asserting the exact math of blog-list.js lines 80-84, blogSlug "/blog/"):
         - pageContext `{ currentPage: 1, numPages: 3 }`: `queryByText("Previous")` is null (isFirst); Next Link href is "/blog/2" (nextPage); the number "1" Link has className containing "is-active"
         - pageContext `{ currentPage: 2, numPages: 3 }`: Previous Link href "/blog/" (prevPage = blogSlug when currentPage-1 === 1); Next Link href "/blog/3"; number "2" is-active
         - pageContext `{ currentPage: 3, numPages: 3 }`: Previous href "/blog/2"; Next absent (isLast); number "3" is-active
       - Assert Link hrefs with `expect(...closest("a")).toHaveAttribute("href", ...)` on the "Previous"/"Next" text nodes; assert page numbers via `screen.getAllByText("1")` disambiguation — use `within()` on the `.pagination` container from `@testing-library/react`.
       - Follow CONVENTIONS.md: no semicolons, double quotes.
    2. Run `yarn test src/templates/blog-list.test.js` until green; then `yarn test` (full suite) must exit 0.
  </action>
  <verify>
    <automated>yarn test src/templates/blog-list.test.js</automated>
    <automated>yarn test</automated>
  </verify>
  <acceptance_criteria>
    - src/templates/blog-list.test.js passes with at least 3 passing tests covering pages 1, 2, and 3 of 3
    - The file contains the 5 jest.mock calls named above (gatsby-image, post-card, @reach/router, layout, seo) BEFORE the BlogIndex import, and the Layout/Seo mocks return renderable JSX/null rather than hitting the real components
    - The file asserts hrefs "/blog/2" and "/blog/" (prevPage special case) and absence of Next on the last page — visible in the test source as literal strings
    - `yarn test` exits 0
  </acceptance_criteria>
  <done>Pagination math is locked in by passing tests for all three pagination positions.</done>

</task>

<task type="auto">
  <name>Task 2: Test navigation handleToggleClick state toggle</name>
  <files>
    - src/components/navigation.test.js
  </files>
  <read_first>
    - src/components/navigation.js (class component; constructor binds handleToggleClick; handleToggleClick lines 38-42 flips showMenu; button className "menu-trigger" + " is-active" when showMenu, lines 55-57)
    - .planning/research phase 01-RESEARCH.md Code Pattern 4 (navigation toggle test)
  </read_first>
  <action>
    1. Create `src/components/navigation.test.js`:
       - Line 1 docblock `/** @jest-environment jsdom */`
       - render `<Navigation />`; `const trigger = screen.getByRole("button")`
       - Case 1: initially `trigger.className` does NOT contain "is-active" (matches the "menu-trigger" class)
       - Case 2: `fireEvent.click(trigger)` then className contains "is-active"
       - Case 3: click again then className does not contain "is-active" (toggle flips back)
       - `@material-ui` is not imported by navigation.js — no extra mock needed; the root `__mocks__/gatsby.js` handles Link; react-icons render fine.
    2. Run `yarn test src/components/navigation.test.js` green, then `yarn test` — exit 0.
  </action>
  <verify>
    <automated>yarn test src/components/navigation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - At least 3 passing assertions (initial off, click on, click off) in src/components/navigation.test.js
    - `yarn test` exits 0
  </acceptance_criteria>
  <done>Navigation toggle behavior is covered and green.</done>
</task>

<task type="auto">
  <name>Task 3: Test gatsby-node createPages in a node environment — post pages, pagination, panicOnBuild</name>
  <files>
    - gatsby-node.test.js
  </files>
  <read_first>
    - gatsby-node.js (exports.createPages at lines 4-80: graphql mock injection, result.errors → reporter.panicOnBuild("Error while running GraphQL query."), post pages with previous/next context from edge ordering, blog pagination with postsPerPage 9 and numPages Math.ceil; exports.onCreateNode at line 82 — NOT required to be tested here, only createPages per D-04)
    - .planning/research 01-RESEARCH.md Code Example 3 (gatsby-node test with node env + injected mocks) + Architectural Responsibility Map (node env, not jsdom — Pitfall 8)
    - .planning/codebase/CONVENTIONS.md (module design: exports.createPages signature)
  </read_first>
  <action>
    1. Create `gatsby-node.test.js` at REPO ROOT (next to gatsby-node.js, per research structure):
       - Line 1 docblock `/** @jest-environment node */` (Pitfall 8 — never jsdom for Node-API code)
       - `const { createPages } = require("../gatsby-node")` — require, not import (this file lives at root)
       - Use a deterministic inline fixture of 10 posts (NOT real content — research A3): nodes with id "1".."10", frontmatter.slug "/post-N", frontmatter.template "blog-post", title.
       - Case 1 "creates 10 post pages plus /blog paginated pages with prev/next context": `actions = { createPage: jest.fn() }`, `graphql = jest.fn().mockResolvedValue({ data: { allMarkdownRemark: { edges: fixture } } })`, `reporter = { panicOnBuild: jest.fn() }`; await createPages({ actions, graphql, reporter }); then `paths` (from createPage.mock.calls) contains "/post-1".."/post-10", contains "/blog" (page 1), "/blog/2", "/blog/3" (Math.ceil(10/9) === 2, so numPages 2 → /blog, /blog/2); `expect(reporter.panicOnBuild).not.toHaveBeenCalled()`.
        - Case 2 (context wiring): the createPage call for path "/post-1" has context `{ id, previous, next }` with previous.node === fixture[1].node (index 0 → the NEXT edge's node, per gatsby-node.js lines 39-42) and next null — the exact ordering semantics of gatsby-node.js lines 39-55.
       - Case 3 (error path): `graphql` resolves `{ errors: [{ message: "boom" }] }` → expect `reporter.panicOnBuild` toHaveBeenCalledWith("Error while running GraphQL query.") and createPage not called.
       - Never assert on graphql query contents (anti-pattern: unit-testing graphql tagged templates — build-time panicOnBuild covers queries).
    2. Run `yarn test gatsby-node.test.js` green; `yarn test` — exit 0.
  </action>
  <verify>
    <automated>yarn test gatsby-node.test.js</automated>
  </verify>
  <acceptance_criteria>
    - At least 3 passing tests in gatsby-node.test.js (post pages + pagination, prev/next context, panicOnBuild)
    - Paths asserted include "/blog" and "/blog/2" for a 10-post fixture; panicOnBuild asserted for the errors fixture
    - `yarn test` exits 0 (full suite)
  </acceptance_criteria>
  <done>Page creation logic is covered under node env with injected mocks; the suite's green gate holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner (jsdom) ↔ production source | jest executes src/templates/blog-list.js and src/components/navigation.js — imports of gatsby-image (post-card) and MUI-free paths only; no emailjs-like secrets here |
| test runner (node) ↔ gatsby-node.js | createPages runs with INJECTED graphql/actions/reporter — the real Gatsby runtime is never invoked (no build-time GraphQL execution, no fs side effects) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering | gatsby-node.test.js graphql mock | medium | mitigate | Mock resolves fixture edges ONLY; the test never executes a real GraphQL query against the filesystem (no data-source exposure); panicOnBuild test asserts the error branch |
| T-2-02 | Information disclosure | gatsby-node.js file-system access | low | accept | createPages with injected mocks touches no real paths (blogList path.resolve only describes the component path, never opened); fs access requires the real Gatsby runtime — absent in tests |
| T-2-SC | Tampering | node_modules integrity | low | accept | No new installs in this plan (all deps from plan 01); yarn.lock records integrity for existing devDeps |
</threat_model>

<verification>
- `yarn test` exits 0 with 3 suites green (blog-list, navigation, gatsby-node)
- FNDT-05 success criteria: at least one passing assertion per covered area
</verification>

<success_criteria>
- [ ] src/templates/blog-list.test.js, src/components/navigation.test.js, gatsby-node.test.js all exist and pass
- [ ] `yarn test` exits 0
- [ ] No production code modified (`git status` shows only test files)
</success_criteria>

## Artifacts this phase produces

**Files:**
- `src/templates/blog-list.test.js` (3 pagination-position cases)
- `src/components/navigation.test.js` (toggle on/off)
- `gatsby-node.test.js` (3 cases: 10-post pagination + prev/next wiring, panicOnBuild)

**Test suites:**
- `describe("Pagination")` in blog-list.test.js
- `describe("Navigation toggle")` in navigation.test.js
- `describe("createPages")` in gatsby-node.test.js (case names: "creates post pages + /blog pagination", "wires prev/next context", "panics on GraphQL errors")
</output>
