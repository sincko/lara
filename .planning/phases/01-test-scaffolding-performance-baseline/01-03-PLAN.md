---
phase: 01-test-scaffolding-performance-baseline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/baseline/capture-baseline.js
  - .planning/baseline/median.js
  - .planning/baseline/README.md
autonomous: true
requirements: [FNDT-06]
user_setup: []

must_haves:
  truths:
    - "D-08: Baseline captured with Lighthouse CLI (local) + PSI (live site), median of 3 runs, mobile profile, for LCP, CLS, INP"
    - "A reproducible capture script runs Lighthouse 13.4.1 CLI against a hardcoded laryart.it URL set (3 URLs × 3 runs, mobile, --only-categories=performance), writing raw JSON to .planning/baseline/lighthouse/"
    - "The script tolerates PSI 429s with retry/backoff and falls back to Lighthouse-CLI-against-live-URL per metric — the site's baseline is never blocked by the shared quota"
    - "A median extraction script reads the 3 raw JSONs per URL and emits median LCP/CLS/INP (per source), reproducible for Phase 6"
    - "Methodology metadata (versions, dates, commit SHA, URL set, mobile profile) is recorded next to the data (D-09)"
  artifacts:
    - .planning/baseline/capture-baseline.js
    - .planning/baseline/median.js
    - .planning/baseline/README.md
  key_links:
    - capture-baseline.js ↔ npx lighthouse@13.4.1 CLI (exact pin for Phase 6 reproducibility)
    - median.js ↔ the 3 raw JSON LHR files per URL (reads them, computes medians)
    - README.md ↔ BASELINE.md methodology (Phase 6 consumes both)

---

<objective>
Build the performance-baseline capture tooling (FNDT-06, D-08/D-09): a Lighthouse CLI capture script proven end-to-end on the home URL, a median extraction script, and a methodology README — before the full 3-URL × 3-run capture in Plan 04.

Purpose: This is the tracer slice for the baseline half of Phase 1. It proves the measurement recipe (exact lighthouse pin, mobile, JSON output, median math) on ONE URL before spending ~20 minutes on the full capture. It also encodes the PSI-429 fallback (research Pitfall 9 / finding 4) and hardcodes the URL set so no user input ever reaches the CLI (security).

Output: Reusable capture + median tooling in `.planning/baseline/`.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-scaffolding-performance-baseline/01-RESEARCH.md
@.planning/phases/01-test-scaffolding-performance-baseline/01-CONTEXT.md
@.planning/phases/01-test-scaffolding-performance-baseline/01-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Capture script for the one-URL tracer run — Lighthouse CLI against https://laryart.it/, proven end-to-end</name>
  <files>
    - .planning/baseline/capture-baseline.js
    - .planning/baseline/lighthouse/home-1.json (generated artifact)
  </files>
  <read_first>
    - .planning/research 01-RESEARCH.md Code Example 5 (capture loops) + Pitfall 9 (PSI 429 fallback) + Environment Availability table (Node 24 for LH 13; Chrome 151 auto-detected)
    - .planning/phases/01-test-scaffolding-performance-baseline/01-CONTEXT.md decisions D-08, D-09
  </read_first>
  <action>
    1. Create `.planning/baseline/capture-baseline.js` — a node script (repo has node 24; no new deps; use built-in `child_process.execFileSync`/`spawnSync`):
        - `const URLS = ["https://laryart.it/", "https://laryart.it/blog/", "https://laryart.it/minnie/"]` — hardcoded constants; NEVER read URLs from argv/stdin (T: untrusted input). Canonical set resolved from research Open Question 2: home, /blog/ canonical trailing slash per the 301, one post — the minnie post's canonical URL `/minnie/` (frontmatter `slug: /minnie` + 301 → `/minnie/`, verified live at planning time; the trailing slash is required so the plan's own 200-check passes). At execution, re-verify the live URLs return 200 (curl -sI) and record the final set in README.md.
       - `const RUNS = 3`, `const SOURCES = ["lighthouse", "psi"]`
        - Helper `slugify(url)` → "home" / "blog" / "post-minnie" (mirror the research bash slug convention; the post slug is `/minnie`, not the filename date prefix)
       - Lighthouse runner: for each URL × 3 runs: `npx -y lighthouse@13.4.1 <url> --only-categories=performance --form-factor=mobile --output=json --output-path=<lighthouse/<slug>-<run>.json> --quiet`, capturing exit code and output; on failure, retry once after 10s, then record `{ error: ... }` into the JSON path so the run count is explicit in the artifact set. Sleep 5s between runs (CDN settle, per research).
       - PSI runner (same loop): `curl -s "https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=<enc>&strategy=mobile&category=performance"` with `python3 -c 'urllib.parse.quote'` for encoding (or encodeURIComponent-equivalent in the script); tolerate HTTP 429 (body contains "RESOURCE_EXHAUSTED" or HTTP code 429) with up to 3 retries and backoff (10s/30s/60s); if still 429, fall back to a Lighthouse-CLI run against the live URL for that run and mark the artifact `{ source: "lighthouse-fallback", psi_quota: "429" }`; honor `process.env.PSI_API_KEY` if present (append `&key=`; NEVER print the key, never commit it)
       - `--quiet` on lighthouse to keep output minimal; console.log a per-run summary line (slug, source, exit ok/failed)
       - Must be run with the nvm default Node 24 (NOT `nvm use` 20 — .nvmrc pins 20; LH 13 needs >=22.19): document this in a comment at the top of the script
    2. Prove the tracer slice: run `node .planning/baseline/capture-baseline.js` — it must complete at least the first Lighthouse run for `https://laryart.it/` and produce `.planning/baseline/lighthouse/home-1.json` containing `audits["largest-contentful-paint"].numericValue`, `audits["cumulative-layout-shift"].numericValue`, `audits["interaction-to-next-paint"].numericValue` (verify via node -e reading the file; PSI runs may 429 at this stage — that is expected and handled)
    3. Do NOT run the full 3-URL × 3-run × 2-source capture in this task — that's Plan 04's job.
  </action>
  <verify>
    <automated>test -f .planning/baseline/lighthouse/home-1.json && node -e "const r=require('./.planning/baseline/lighthouse/home-1.json'); for(const a of ['largest-contentful-paint','cumulative-layout-shift','interaction-to-next-paint']) if(typeof r.audits[a].numericValue!=='number') process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/baseline/capture-baseline.js` exists, contains the literal URLS list with all 3 laryart.it URLs, the `--form-factor=mobile` flag, and a PSI-429 retry/backoff branch reading `process.env.PSI_API_KEY`
    - Running it produced `.planning/baseline/lighthouse/home-1.json` whose audits contain numeric LCP/CLS/INP numericValues (verified by the command above)
    - Script reads no stdin/argv URLs (only the hardcoded URLS constant)
  </acceptance_criteria>
  <done>The Lighthouse capture pipeline is proven on the home URL end-to-end with raw JSON artifact; PSI fallback is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Median extraction script + methodology README for Phase 6 reproducibility</name>
  <files>
    - .planning/baseline/median.js
    - .planning/baseline/README.md
  </files>
  <read_first>
    - .planning/research 01-RESEARCH.md Code Example 5 (median extraction) + Pitfall 10 (baseline variance — pin version, record metadata)
  </read_first>
  <action>
    1. Create `.planning/baseline/median.js` — plain node script (no new deps):
       - For each source dir (`lighthouse/`, `psi/`), for each slug, read the 3 JSON runs (skip files containing an `error` marker), sort by metric value, take the middle: median for `largest-contentful-paint.numericValue` (ms), `cumulative-layout-shift.numericValue`, `interaction-to-next-paint.numericValue`, and `categories.performance.score` (×100, rounded)
       - Print a TSV table: `source  slug  lcp_ms  cls  inp_ms  perf_score  runs_used`
        - Runs rule: 3 valid runs → median of 3 (the target); 2 valid runs → median of the 2, print `WARN runs_used=2`; 1 valid run → print the single value with `WARN runs_used=1` (single-run WARN exits 0 — Plan 03's own tracer artifacts are exactly this; the strict ≥2 rule applies ONLY to Plan 04's full capture, where all 3 runs are expected to exist). Fewer than 1 valid run for a slug (zero JSONs, or all carrying an `error` marker) → exit non-zero with a clear message naming the slug.
    2. Create `.planning/baseline/README.md` — methodology contract for Phase 6:
       - Capture command (exact: `node .planning/baseline/capture-baseline.js`), median command, URL set, mobile profile, median-of-3 rule, exact tool versions (lighthouse 13.4.1 via npx; PSI v5), capture date(s), git commit SHA, one-line methodology statement ("Lighthouse 13.4.1 CLI, mobile, default throttling, median of 3; PSI v5, strategy=mobile, median of 3"), and a note: if PSI 429s, the fallback per-metric source is recorded (which URLs got which source in the capture run log)
       - Leave the `BASELINE.md` path as the summary the next plan writes
    3. Run `node .planning/baseline/median.js` — must print the table from the JSONs produced in Task 1.
  </action>
  <verify>
    <automated>node .planning/baseline/median.js</automated>
  </verify>
  <acceptance_criteria>
     - `median.js` reads from the same paths the capture script writes (lighthouse/, psi/), computes median-of-3 per metric, and exits 0 on the current artifacts (a single home run prints runs_used=1 with a WARN — the strict ≥2 gate lives in Plan 04, not here)
    - README.md exists with: the command line (capture + median), the 3-URL set, version strings (lighthouse 13.4.1), the mobile profile, and a "Phase 6 must rerun with identical flags" note
  </acceptance_criteria>
  <done>Median math is scripted; Phase 6 can rerun the identical measurement recipe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → npx lighthouse | execSync runs the CLI with hardcoded URLs only; no user input; Node 24 (not .nvmrc Node 20 — LH 13 engines >=22.19) |
| script → PSI API | curl to the Google API with the site URL; optional `PSI_API_KEY` env var — never logged/committed |
| artifacts ↔ git | raw JSON + markdown committed to .planning/baseline/ — git history is the integrity record |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | Tampering | URL input → lighthouse/curl | medium | mitigate | URLs are hardcoded constants (3 laryart.it URLs); the script never accepts argv/stdin (acceptance criteria) |
| T-3-02 | Information disclosure | PSI_API_KEY in process env / logs | high | mitigate | Script reads `process.env.PSI_API_KEY` only; NEVER console.log/echoes it; never writes it to artifacts; not committed (capture path .planning/baseline/ is committed, but the key stays in the shell env) |
| T-3-03 | Spoofing | npx lighthouse@13.4.1 exact pin | medium | mitigate | npx with `@13.4.1` exact version; package not added to package.json; version pinned in README for Phase 6 reproducibility |
| T-3-SC | Tampering | npx fetch of lighthouse tarball | low | accept | npx pins the exact published tarball; no postinstall; registry-verified package (research audit) |
</threat_model>

<verification>
- Task 1: artifact JSON + median command on home URL
- Task 2: `node .planning/baseline/median.js` exit 0, README present
</verification>

<success_criteria>
- [ ] capture-baseline.js proven on the home URL (raw JSON with LCP/CLS/INP)
- [ ] median.js produces a table from real artifacts
- [ ] README.md records methodology (version, URL set, mobile, median-of-3, commit SHA)
- [ ] No production code touched

## Artifacts this phase produces

**Files:**
- `.planning/baseline/capture-baseline.js` (node, no deps)
- `.planning/baseline/median.js`
- `.planning/baseline/README.md`
- `.planning/baseline/lighthouse/home-1.json` (proof artifact; full capture overwrites)

**Script symbols:**
- `URLS` (array of 3 laryart.it URLs)
- `RUNS = 3`
- `slugify(url)`
- `runLighthouse(url, slug, run)`
- `runPSI(url, slug, run)` (retry/backoff on 429; `PSI_API_KEY` env; falls back to lighthouse)
- `median.js`: `computeMedians(sourceDir, slug)` → prints table

**Contract keys:**
- config key `PSI_API_KEY` (env), `--form-factor=mobile`, `--only-categories=performance`, `--output=json`
</output>
