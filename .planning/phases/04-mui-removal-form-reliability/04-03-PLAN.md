---
phase: 04-mui-removal-form-reliability
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - package.json
  - yarn.lock
  - src/components/formik.js
  - .env.example
  - .gitignore
autonomous: true
requirements:
  - FORM-03
  - UPGR-05
user_setup: []

must_haves:
  truths:
    - "emailjs-com ^3.2.0 is replaced by @emailjs/browser 4.4.1 (pinned exact, verified latest) — the import stays default-style: `import emailjs from \"@emailjs/browser\"` (D-07, Pitfall 5)"
    - "No hardcoded credentials remain in src/ — the public key, service ID, and template ID come from GATSBY_EMAILJS_PUBLIC_KEY / GATSBY_EMAILJS_SERVICE_ID / GATSBY_EMAILJS_TEMPLATE_ID (D-08); the module-scope init is guarded with `if (process.env.GATSBY_EMAILJS_PUBLIC_KEY)` and uses the v4 object form `emailjs.init({ publicKey: ... })` (Pitfall 3)"
    - ".env.example is committed with the D-09 values as placeholders (user_06xz85hi92oABMZqCIUu7 / service_q3997uk / template_m6tzcmm) — the .gitignore `.env*` rule (line 57) is neutralized with a `!.env.example` negation rule (Pitfall 1, verified via git check-ignore)"
    - "GATSBY_* vars are build-time inlined by Gatsby's DefinePlugin — no runtime lookup; the owner must set the Netlify env vars before the first post-phase deploy (manual checkpoint, 04-04's task)"
    - "yarn install && yarn build && yarn test exit 0; emailjs-com absent from package.json and yarn.lock"
  artifacts:
    - "package.json with @emailjs/browser 4.4.1 in dependencies, emailjs-com removed"
    - "yarn.lock regenerated (emailjs-com tree gone, @emailjs/browser tree in)"
    - "src/components/formik.js with the @emailjs/browser import, guarded env-var init, and env-var sendForm args"
    - ".env.example (NEW) with the three GATSBY_EMAILJS_* placeholders"
    - ".gitignore with the !.env.example negation rule"
  key_links:
    - "yarn build → Gatsby DefinePlugin → process.env.GATSBY_EMAILJS_* inlined into the client bundle → formik.js init/sendForm"
    - "formik.js onSubmit → emailjs.sendForm(GATSBY_EMAILJS_SERVICE_ID, GATSBY_EMAILJS_TEMPLATE_ID, \"#contact_form\") → api.emailjs.com"
    - "git add .env.example → .gitignore !.env.example negation → tracked file"
---

<objective>
EmailJS swap + env vars (FORM-03, UPGR-05): replace emailjs-com ^3.2.0 with @emailjs/browser 4.4.1 in package.json, move the hardcoded EmailJS credentials out of src/components/formik.js into GATSBY_EMAILJS_PUBLIC_KEY / GATSBY_EMAILJS_SERVICE_ID / GATSBY_EMAILJS_TEMPLATE_ID env vars with a guarded v4 object-form init, and commit .env.example with the D-09 values as placeholders — including the .gitignore `!.env.example` negation rule that makes the commit possible (Pitfall 1).

Serialization note: this plan and 04-04 both touch src/components/formik.js. The execute-phase orchestrator serializes overlapping plans automatically — 04-03 runs BEFORE 04-04 (04-04 depends_on 04-03). This plan must NOT restructure the onSubmit promise chain (the false-success fix is 04-04's job, D-11); it only swaps the SDK and the credential source.

Purpose: Remove the deprecated emailjs-com package (UPGR-05) and eliminate the hardcoded public key from the client source (FORM-03) — the key moves to build-time-inlined GATSBY_* vars, with .env.example documenting the contract for the owner's Netlify setup.

Output: @emailjs/browser 4.4.1 driving the form via env vars; .env.example tracked in git; no hardcoded creds in src/; install + build + test green; one commit per task.

Environment notes (apply to every verify step in this plan): node is NOT on PATH — prefix commands with `source ~/.nvm/nvm.sh && nvm use 24` (nvm has v20.20.2, v24.18.0, v24.19.0; the project enforces Node 24 via engines + engine-strict + scripts/check-node-version.js). /tmp is a tmpfs that fills with JIT artifacts — set `TMPDIR=/home/simos/tmp` for yarn/build commands. yarn 1.22 only, never npm.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mui-removal-form-reliability/04-CONTEXT.md
@.planning/phases/04-mui-removal-form-reliability/04-RESEARCH.md
@.planning/phases/04-mui-removal-form-reliability/04-PATTERNS.md
@.planning/phases/04-mui-removal-form-reliability/04-UI-SPEC.md
@.planning/phases/04-mui-removal-form-reliability/04-VALIDATION.md
@package.json
@.gitignore
@src/components/formik.js
</context>

<tasks>

<task type="auto">
  <name>Swap emailjs-com → @emailjs/browser 4.4.1 (UPGR-05, D-07)</name>
  <files>package.json, yarn.lock</files>
  <read_first>
    - package.json (dependencies line 30: "emailjs-com": "^3.2.0")
    - .planning/phases/04-mui-removal-form-reliability/04-RESEARCH.md §Standard Stack (@emailjs/browser 4.4.1 verified latest, zero deps, no postinstall; install commands), §State of the Art (v3→v4 breaking change: init object form)
    - .planning/phases/04-mui-removal-form-reliability/04-CONTEXT.md D-07 (replace emailjs-com ^3.2.0 with @emailjs/browser v4; import changes from "emailjs-com" to "@emailjs/browser")
    - .planning/phases/04-mui-removal-form-reliability/04-PATTERNS.md §package.json (deps block edit pattern; pin exact 4.4.1 per the project's mixed pin/caret style)
    - AGENTS.md (yarn 1.22 only, Node 24 enforced)
  </read_first>
  <action>
    Swap the SDK with yarn only (never npm): `yarn add @emailjs/browser@4.4.1 && yarn remove emailjs-com`. This updates package.json dependencies and regenerates yarn.lock in one step.

    Do NOT touch: formik, yup, react-icons, sass, any other dependency, scripts, engines.

    Commit atomically: `git commit -m "feat(04-03): replace emailjs-com with @emailjs/browser 4.4.1 (UPGR-05, D-07)"` staging exactly package.json and yarn.lock.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn install && grep -q '"@emailjs/browser": "4.4.1"' package.json && ! grep -q 'emailjs-com' package.json && ! grep -q 'emailjs-com' yarn.lock</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"@emailjs/browser": "4.4.1"' package.json` succeeds
    - `! grep -q 'emailjs-com' package.json` succeeds (zero matches)
    - `! grep -q 'emailjs-com' yarn.lock` succeeds (zero matches)
    - `grep -q '@emailjs/browser' yarn.lock` succeeds
    - `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn install` exits 0
    - `git log -1 --format=%s` contains `04-03`
  </acceptance_criteria>
  <done>@emailjs/browser 4.4.1 is the email SDK; emailjs-com is gone from the dependency tree; install is green under Node 24.</done>
  <reversibility rating="reversible">git revert restores emailjs-com and the lockfile; `yarn install` re-resolves the old tree.</reversibility>
</task>

<task type="auto">
  <name>Move formik.js to @emailjs/browser + guarded GATSBY_* env-var init (FORM-03, D-08)</name>
  <files>src/components/formik.js</files>
  <read_first>
    - src/components/formik.js (the 04-01 rewrite: line 5 `import emailjs from "emailjs-com"`, line 8 `emailjs.init("user_06xz85hi92oABMZqCIUu7")`, onSubmit lines with sendForm("service_q3997uk", "template_m6tzcmm", "#contact_form"))
    - .planning/phases/04-mui-removal-form-reliability/04-RESEARCH.md Pattern 2 (guarded module-scope init with the v4 object form), Pitfall 2 (GATSBY_* inlined at build time), Pitfall 3 (undefined vars → sendForm rejects; guard the init)
    - .planning/phases/04-mui-removal-form-reliability/04-CONTEXT.md D-08 (GATSBY_EMAILJS_PUBLIC_KEY / GATSBY_EMAILJS_SERVICE_ID / GATSBY_EMAILJS_TEMPLATE_ID; no hardcoded creds), D-09 (values to migrate)
    - .planning/phases/04-mui-removal-form-reliability/04-PATTERNS.md §formik.js (target import block with the guarded init)
  </read_first>
  <action>
    Edit src/components/formik.js:

    Import: change `import emailjs from "emailjs-com"` to `import emailjs from "@emailjs/browser"` (D-07 — default-import style kept, Pitfall 5).

    Init: replace the hardcoded `emailjs.init("user_06xz85hi92oABMZqCIUu7")` (and its `//https://dashboard.emailjs.com/admin` comment) with the guarded v4 object-form init:
    `if (process.env.GATSBY_EMAILJS_PUBLIC_KEY) { emailjs.init({ publicKey: process.env.GATSBY_EMAILJS_PUBLIC_KEY }) }`
    The guard prevents init with undefined when env vars are absent (tests, local dev without .env) — Pitfall 3. The v4 breaking change is the object form { publicKey }, NOT the v3 string (RESEARCH.md §State of the Art).

    sendForm args: change `emailjs.sendForm("service_q3997uk", "template_m6tzcmm", "#contact_form")` to `emailjs.sendForm(process.env.GATSBY_EMAILJS_SERVICE_ID, process.env.GATSBY_EMAILJS_TEMPLATE_ID, "#contact_form")`.

    Do NOT restructure the onSubmit promise chain — the unconditional resetForm/setSubmitting/assign after the chain stays as-is; the false-success fix is 04-04's job (D-11). Do NOT touch the Form JSX, validationSchema, or the plain-element fields from 04-01.

    Prettier style: no semicolons, double quotes, arrowParens avoid. Run `yarn format` on the file after editing.

    Commit atomically: `git commit -m "feat(04-03): move EmailJS creds to GATSBY_* env vars with guarded init (FORM-03, D-08)"` staging exactly src/components/formik.js.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'import emailjs from "@emailjs/browser"' src/components/formik.js` succeeds
    - `! grep -q 'emailjs-com' src/components/formik.js` succeeds (zero matches)
    - `! grep -q 'user_06xz85hi92oABMZqCIUu7' src/components/formik.js` succeeds (no hardcoded key, FORM-03)
    - `! grep -q 'service_q3997uk' src/components/formik.js` succeeds (no hardcoded service id)
    - `! grep -q 'template_m6tzcmm' src/components/formik.js` succeeds (no hardcoded template id)
    - `grep -q 'if (process.env.GATSBY_EMAILJS_PUBLIC_KEY)' src/components/formik.js` succeeds (guarded init, Pitfall 3)
    - `grep -q 'emailjs.init({ publicKey: process.env.GATSBY_EMAILJS_PUBLIC_KEY })' src/components/formik.js` succeeds (v4 object form)
    - `grep -q 'process.env.GATSBY_EMAILJS_SERVICE_ID' src/components/formik.js` succeeds
    - `grep -q 'process.env.GATSBY_EMAILJS_TEMPLATE_ID' src/components/formik.js` succeeds
    - `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn build && yarn test` exits 0
    - `git log -1 --format=%s` contains `04-03`
  </acceptance_criteria>
  <done>The form reads EmailJS credentials exclusively from GATSBY_* env vars with a guarded v4 init; no hardcoded creds remain in src/; build + test green.</done>
  <reversibility rating="reversible">git revert restores the hardcoded init and emailjs-com import; pure component edit with no runtime state.</reversibility>
</task>

<task type="auto">
  <name>Commit .env.example with GATSBY_EMAILJS_* placeholders + .gitignore negation rule (FORM-03, D-08/D-09, Pitfall 1)</name>
  <files>.env.example, .gitignore</files>
  <read_first>
    - .gitignore (lines 56-57: "# dotenv environment variable files" + `.env*` — matches .env.example, verified via git check-ignore)
    - .planning/phases/04-mui-removal-form-reliability/04-RESEARCH.md Pitfall 1 (.env.example silently gitignored; the durable fix is the !.env.example negation rule)
    - .planning/phases/04-mui-removal-form-reliability/04-PATTERNS.md §.env.example (content pattern with D-09 values) and §.gitignore (negation rule after line 57)
    - .planning/phases/04-mui-removal-form-reliability/04-CONTEXT.md D-08 (commit .env.example with current values as placeholders), D-09 (user_06xz85hi92oABMZqCIUu7 / service_q3997uk / template_m6tzcmm)
  </read_first>
  <action>
    Create .env.example with exactly these three lines (plus a short comment header in Italian or English noting the values are exposed client-side via the GATSBY_ prefix and must be copied to .env.development locally and set in the Netlify UI for production):
    GATSBY_EMAILJS_PUBLIC_KEY=user_06xz85hi92oABMZqCIUu7
    GATSBY_EMAILJS_SERVICE_ID=service_q3997uk
    GATSBY_EMAILJS_TEMPLATE_ID=template_m6tzcmm

    Edit .gitignore: after the `.env*` line (line 57), add the negation rule `!.env.example` (Pitfall 1 — the durable fix; without it `git add .env.example` silently fails).

    Verify the negation works BEFORE committing: `git check-ignore -v .env.example` must exit 1 (not ignored).

    Commit atomically: `git commit -m "feat(04-03): add .env.example with GATSBY_EMAILJS_* placeholders (FORM-03, D-08)"` staging exactly .env.example and .gitignore.
  </action>
  <verify>
    <automated>git check-ignore -v .env.example; test $? -eq 1 && git ls-files .env.example | grep -q .env.example && grep -q 'GATSBY_EMAILJS_PUBLIC_KEY' .env.example && grep -q 'GATSBY_EMAILJS_SERVICE_ID' .env.example && grep -q 'GATSBY_EMAILJS_TEMPLATE_ID' .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore -v .env.example` exits 1 (file NOT ignored after the negation rule)
    - `git ls-files .env.example` lists the file (tracked in git)
    - `grep -q 'GATSBY_EMAILJS_PUBLIC_KEY=user_06xz85hi92oABMZqCIUu7' .env.example` succeeds
    - `grep -q 'GATSBY_EMAILJS_SERVICE_ID=service_q3997uk' .env.example` succeeds
    - `grep -q 'GATSBY_EMAILJS_TEMPLATE_ID=template_m6tzcmm' .env.example` succeeds
    - `grep -q '!.env.example' .gitignore` succeeds
    - `git log -1 --format=%s` contains `04-03`
  </acceptance_criteria>
  <done>.env.example is committed with the D-09 placeholders and the .gitignore negation rule makes it trackable; the env-var contract is documented for the owner's Netlify setup.</done>
  <reversibility rating="reversible">git revert removes the file and the negation rule; no runtime state involved.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| npm registry → yarn install | @emailjs/browser 4.4.1 crosses here; pinned exact, yarn.lock committed |
| Netlify UI env vars → gatsby build → DefinePlugin → client bundle | GATSBY_EMAILJS_* values cross here; inlined at build time, frozen until the next deploy (Pitfall 2) |
| browser → api.emailjs.com | the public key + FormData cross here; the key is client-exposed by design (EmailJS FAQ) |
| git → .env.example | the placeholder file crosses here; the .env* rule would silently block it without the negation (Pitfall 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Info disclosure | hardcoded EmailJS creds remaining in the client bundle | high | block | Acceptance greps assert zero matches for user_06xz85hi92oABMZqCIUu7 / service_q3997uk / template_m6tzcmm in src/; the public key is client-exposed by design, but only via the GATSBY_ mechanism |
| T-04-09 | DoS | undefined env vars at build → sendForm rejects on every submit (Pitfall 3) | medium | mitigate | Guarded init `if (process.env.GATSBY_EMAILJS_PUBLIC_KEY)`; graceful degradation via the inline error (04-04); the real fix is the owner's Netlify checkpoint (04-04 manual task) |
| T-04-10 | Tampering | .env.example silently gitignored — FORM-03's committed-file criterion fails invisibly (Pitfall 1) | medium | mitigate | !.env.example negation rule added in the same commit; `git check-ignore -v .env.example` exit 1 and `git ls-files .env.example` asserted |
| T-04-11 | Tampering | mock shape mismatch after the SDK swap — emailjs.sendForm is not a function in tests (Pitfall 5) | medium | mitigate | Default-import style kept (D-07); 04-02 already renamed the jest.mock to @emailjs/browser with the matching factory |
| T-04-SC | Tampering | supply-chain risk from @emailjs/browser | low | mitigate | Official EmailJS SDK, zero deps, no postinstall (verified in RESEARCH.md §Package Legitimacy Audit); pinned 4.4.1; yarn.lock committed |
</threat_model>

<verification>
- `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn install && yarn build && yarn test` exits 0
- `grep -rn "user_06xz85hi92oABMZqCIUu7" src/` returns zero matches (FORM-03 proof)
- `! grep -q 'emailjs-com' package.json` and `! grep -q 'emailjs-com' yarn.lock` succeed
- `grep -q '"@emailjs/browser": "4.4.1"' package.json` succeeds
- `git check-ignore -v .env.example` exits 1; `git ls-files .env.example` lists the file
- `git log` shows the three 04-03 commits
</verification>

<success_criteria>
- No hardcoded emailjs key remains in source; the form works via GATSBY_* env vars, and .env.example is committed (ROADMAP Phase 4 criterion 3)
- The emailjs-com → @emailjs/browser v4 swap is complete with the v4 object-form init (UPGR-05, D-07)
- The .gitignore trap is closed — .env.example is trackable and tracked (Pitfall 1)
- The full install → build → test loop is green under Node 24
</success_criteria>

## Artifacts this phase produces

- `package.json` — @emailjs/browser 4.4.1 in, emailjs-com out
- `yarn.lock` — regenerated (emailjs-com tree gone, @emailjs/browser tree in)
- `src/components/formik.js` — @emailjs/browser import, guarded GATSBY_EMAILJS_PUBLIC_KEY init (v4 object form), env-var sendForm args
- `.env.example` — NEW: GATSBY_EMAILJS_PUBLIC_KEY / GATSBY_EMAILJS_SERVICE_ID / GATSBY_EMAILJS_TEMPLATE_ID with D-09 placeholders
- `.gitignore` — !.env.example negation rule after the .env* line
- Git commits `feat(04-03): replace emailjs-com with @emailjs/browser 4.4.1 (UPGR-05, D-07)`, `feat(04-03): move EmailJS creds to GATSBY_* env vars with guarded init (FORM-03, D-08)`, and `feat(04-03): add .env.example with GATSBY_EMAILJS_* placeholders (FORM-03, D-08)`
- `.planning/phases/04-mui-removal-form-reliability/04-03-SUMMARY.md` (created at plan completion)

<output>
Create `.planning/phases/04-mui-removal-form-reliability/04-03-SUMMARY.md` when done
</output>
