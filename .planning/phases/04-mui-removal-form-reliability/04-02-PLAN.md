---
phase: 04-mui-removal-form-reliability
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/components/formik.test.js
autonomous: true
requirements:
  - FORM-02
user_setup: []

must_haves:
  truths:
    - "The jest.mock module name changes from emailjs-com to @emailjs/browser (D-10) — the factory keeps returning the default-export object { init: jest.fn(), sendForm: jest.fn() } to match `import emailjs from \"@emailjs/browser\"` (Pitfall 5)"
    - "The Mui-error class assertions become error class assertions on the helper text elements (D-06) — the class name `error` is locked (Pitfall 7) and must be asserted on the helper <p> elements (screen.getByText(\"Nome richiesto\") / screen.getByText(\"Email richiesta\")), exactly as the current test targets them"
    - "The test's intent survives: validation errors surface through the UI — static helper texts always visible, flipping to the error class when invalid (D-05, UI-SPEC Copywriting Contract)"
    - "The it.skip FORM-04 regression test stays skipped in this plan — unskipping is 04-04's job (D-12), because the false-success fix lands there"
    - "yarn test src/components/formik.test.js exits 0"
  artifacts:
    - "src/components/formik.test.js with jest.mock(\"@emailjs/browser\") and toHaveClass(\"error\") assertions"
  key_links:
    - "yarn test → jest → formik.test.js → jest.mock(\"@emailjs/browser\") → FormikContact (formik.js) → helper <p> error class"
    - "formik.js TextFieldConError → className={hasError ? \"helper error\" : \"helper\"} → test asserts toHaveClass(\"error\")"
---

<objective>
Formik UX preservation (FORM-02): update src/components/formik.test.js to match the plain-CSS form from 04-01 — rename the jest.mock module from emailjs-com to @emailjs/browser (D-10) and change the Mui-error class assertions to the new plain-CSS error class (D-06). The test's intent (validation errors surface through the UI) stays; only the MUI-specific identifiers change.

Serialization note: this plan and 04-04 both touch src/components/formik.test.js. The execute-phase orchestrator serializes overlapping plans automatically — 04-02 runs BEFORE 04-04 (04-04 depends_on 04-02). This plan must NOT unskip the it.skip regression test; 04-04 owns that (D-12).

Purpose: Keep the regression net honest after the MUI removal — the test must assert the new error class on the helper text elements, proving the Formik validation UX survived the plain-element rewrite (ROADMAP Phase 4 criterion 2).

Output: formik.test.js green against the 04-01 form; one commit.

Environment notes (apply to every verify step in this plan): node is NOT on PATH — prefix commands with `source ~/.nvm/nvm.sh && nvm use 24` (nvm has v20.20.2, v24.18.0, v24.19.0; the project enforces Node 24 via engines + engine-strict + scripts/check-node-version.js). /tmp is a tmpfs that fills with JIT artifacts — set `TMPDIR=/home/simos/tmp` for yarn/build commands. yarn 1.22 only, never npm.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mui-removal-form-reliability/04-CONTEXT.md
@.planning/phases/04-mui-removal-form-reliability/04-RESEARCH.md
@.planning/phases/04-mui-removal-form-reliability/04-PATTERNS.md
@.planning/phases/04-mui-removal-form-reliability/04-UI-SPEC.md
@.planning/phases/04-mui-removal-form-reliability/04-VALIDATION.md
@src/components/formik.test.js
@src/components/formik.js
@jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Update formik.test.js: mock module rename + error class assertion (FORM-02, D-06/D-10)</name>
  <files>src/components/formik.test.js</files>
  <read_first>
    - src/components/formik.test.js (lines 5-10: jest.mock("emailjs-com") block; lines 21-24: Mui-error assertions; lines 28-53: the it.skip FORM-04 regression test)
    - src/components/formik.js (the 04-01 rewrite: TextFieldConError renders <p className={hasError ? "helper error" : "helper"}>)
    - .planning/phases/04-mui-removal-form-reliability/04-PATTERNS.md §formik.test.js (mock block update, error-class assertion update, test to unskip — NOT this plan)
    - .planning/phases/04-mui-removal-form-reliability/04-RESEARCH.md Pitfall 5 (mock shape must match default-import style), Pitfall 7 (class name `error` locked, assert on the helper text element), Example 3 (target test file)
    - .planning/phases/04-mui-removal-form-reliability/04-CONTEXT.md D-06 (Mui-error → new plain-CSS error class), D-10 (jest.mock("emailjs-com") → jest.mock("@emailjs/browser")), D-12 (unskip is 04-04's job)
  </read_first>
  <action>
    Edit src/components/formik.test.js:

    Mock block (lines 5-10): change `jest.mock("emailjs-com", () => ({ init: jest.fn(), sendForm: jest.fn() }))` to `jest.mock("@emailjs/browser", () => ({ init: jest.fn(), sendForm: jest.fn() }))`. Keep the factory returning the default-export object — it matches `import emailjs from "@emailjs/browser"` (Pitfall 5). Keep the comment above it (init still runs at module load, now guarded by 04-03's env-var init).

    Error-class assertions (lines 21-24): change `await waitFor(() => expect(nomeHelper).toHaveClass("Mui-error"))` to `await waitFor(() => expect(nomeHelper).toHaveClass("error"))` and `expect(emailHelper).toHaveClass("Mui-error")` to `expect(emailHelper).toHaveClass("error")`. Keep asserting on the helper text elements (screen.getByText("Nome richiesto") / screen.getByText("Email richiesta")) — the implementation puts the error class on the helper <p> (Pitfall 7). Update the explanatory comment (lines 17-20) to reference the error class instead of Mui-error.

    Do NOT touch the it.skip regression test (lines 28-53) — it stays skipped in this plan; 04-04 unskips it after the false-success fix (D-12). Do NOT touch the jsdom environment header (line 1).

    Prettier style: no semicolons, double quotes, arrowParens avoid. Run `yarn format` on the file after editing.

    Commit atomically: `git commit -m "test(04-02): update formik test for plain-CSS error class and @emailjs/browser mock (FORM-02, D-06/D-10)"` staging exactly src/components/formik.test.js.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn test src/components/formik.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'jest.mock("@emailjs/browser"' src/components/formik.test.js` succeeds
    - `! grep -q 'emailjs-com' src/components/formik.test.js` succeeds (zero matches)
    - `grep -q 'toHaveClass("error")' src/components/formik.test.js` succeeds (both assertions)
    - `! grep -q 'Mui-error' src/components/formik.test.js` succeeds (zero matches)
    - `grep -q 'it.skip' src/components/formik.test.js` succeeds (regression test STILL skipped — 04-04 unskips it, D-12)
    - `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn test src/components/formik.test.js` exits 0
    - `git log -1 --format=%s` contains `04-02`
  </acceptance_criteria>
  <done>The test asserts the plain-CSS error class on the helper texts and mocks @emailjs/browser; the FORM-04 regression test remains skipped for 04-04; the suite is green.</done>
  <reversibility rating="reversible">git revert restores the Mui-error assertions and the emailjs-com mock; pure test-file edit with no runtime state.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| jest → formik.test.js → jest.mock("@emailjs/browser") | the mock factory crosses here; a named-import mismatch breaks the mock (Pitfall 5) |
| formik.js render → helper <p> class → test assertion | the error class crosses here; a class-name mismatch between implementation and test fails the suite (Pitfall 7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | mock shape mismatch after the module rename — emailjs.sendForm is not a function in tests | medium | mitigate | Factory keeps returning the default-export object { init, sendForm }; acceptance grep asserts the exact jest.mock("@emailjs/browser" line |
| T-04-06 | Tampering | error-class assertion ambiguity — test asserts "error" but implementation uses a different class or puts it on the input only | medium | mitigate | Class name `error` locked (Pitfall 7); assertions target the helper text elements exactly as the current test does |
| T-04-07 | Tampering | premature unskip of the FORM-04 regression test — it would fail against the still-buggy onSubmit | high | block | Acceptance grep asserts `it.skip` is still present; unskipping is 04-04's task (D-12) |
</threat_model>

<verification>
- `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn test src/components/formik.test.js` exits 0
- `grep -q 'jest.mock("@emailjs/browser"' src/components/formik.test.js` succeeds
- `! grep -q 'Mui-error' src/components/formik.test.js` succeeds
- `grep -q 'toHaveClass("error")' src/components/formik.test.js` succeeds
- `grep -q 'it.skip' src/components/formik.test.js` succeeds (D-12 deferred to 04-04)
- `git log` shows the 04-02 commit
</verification>

<success_criteria>
- The Formik validation UX is proven preserved: the test asserts the error class on the helper texts of the plain-CSS form (ROADMAP Phase 4 criterion 2)
- The mock matches the @emailjs/browser default-import style (D-10, Pitfall 5)
- The FORM-04 regression net stays skipped and intact for 04-04 (D-12)
</success_criteria>

## Artifacts this phase produces

- `src/components/formik.test.js` — jest.mock("@emailjs/browser") + toHaveClass("error") assertions; it.skip regression test untouched
- Git commit `test(04-02): update formik test for plain-CSS error class and @emailjs/browser mock (FORM-02, D-06/D-10)`
- `.planning/phases/04-mui-removal-form-reliability/04-02-SUMMARY.md` (created at plan completion)

<output>
Create `.planning/phases/04-mui-removal-form-reliability/04-02-SUMMARY.md` when done
</output>
