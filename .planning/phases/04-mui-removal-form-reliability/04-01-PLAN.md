---
phase: 04-mui-removal-form-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - yarn.lock
  - src/components/formik.js
  - src/components/top-contacts.js
  - src/assets/scss/style.scss
autonomous: true
requirements:
  - FORM-01
user_setup: []

must_haves:
  truths:
    - "@material-ui/core ^4.12.4 and @material-ui/icons ^4.11.3 are removed from package.json and yarn.lock — the only MUI consumers were formik.js (TextField, Button) and top-contacts.js (FacebookIcon, WhatsAppIcon) (D-01)"
    - "formik.js renders plain <input>/<textarea>/<button> elements styled ONLY with existing theme variables from _theme-variables.scss — no new colors (D-02); same fields (nome, email, cellulare, messaggio), same placeholders, same layout, same 'Invia' button (D-04)"
    - "top-contacts.js uses react-icons ri set: RiWhatsappLine and RiFacebookBoxLine at 24px via style={{ fontSize: \"24px\" }}, no inline color — color inheritance from .icons-top a stays (D-03)"
    - "The .contact_form SCSS block (style.scss:434-464) is extended with .input/.helper/.submit/.send-error rules implementing the UI-SPEC Visual Delta Register TARGET column; the nested .textarea textarea selector is flattened (Pitfall 6) and the commented-out MUI label rule is deleted"
    - "Formik + yup stay exactly as-is (validationSchema: email + nome required) — this plan only swaps the render targets, the error-display rewrite is plan 04-02's test update (D-05)"
    - "yarn install && yarn build && yarn test --testPathIgnorePatterns=formik.test.js exit 0 (formik.test.js is EXPECTED RED until 04-02 updates the Mui-error assertion) and zero @material-ui imports remain in src/"
  artifacts:
    - "package.json without @material-ui/core and @material-ui/icons in dependencies"
    - "yarn.lock regenerated (MUI tree gone)"
    - "src/components/formik.js rewritten with plain elements (TextFieldConError → useField + <input> + helper <p>; Field as=\"textarea\" for messaggio; <button type=\"submit\" className=\"submit\">Invia</button>)"
    - "src/components/top-contacts.js with react-icons ri imports"
    - "src/assets/scss/style.scss with the extended .contact_form block"
  key_links:
    - "yarn build → gatsby-plugin-sass → sass (dart-sass) → style.scss → .contact_form .input/.helper/.submit/.send-error"
    - "contatti.js template → FormikContact (formik.js) → plain <input>/<textarea>/<button>"
    - "layout → TopContacts (top-contacts.js) → react-icons/ri RiWhatsappLine/RiFacebookBoxLine"
---

<objective>
MUI removal tracer (FORM-01): rewrite src/components/formik.js with plain <input>/<textarea>/<button> elements styled via the existing theme variables, swap the MUI icons in src/components/top-contacts.js for react-icons (RiWhatsappLine/RiFacebookBoxLine), THEN remove @material-ui/core ^4.12.4 and @material-ui/icons ^4.11.3 from package.json (task order: component rewrites first so no dangling imports exist when the deps leave), and extend the .contact_form SCSS block in style.scss to implement the UI-SPEC Visual Delta Register TARGET column. This is the wave-1 tracer: it proves the whole MUI-free pipeline (install → build → test) before waves 2's test/env/false-success work builds on it.

Purpose: Remove the EOL MUI v4 dependency surface without a visual redesign (D-04) — the form keeps its fields, placeholders, layout, and "Invia" button, and the Formik/yup validation wiring stays untouched (D-05). The error-display class assertion update is NOT this plan (04-02 owns formik.test.js) — formik.test.js is EXPECTED RED after the form rewrite (it still asserts Mui-error) and is excluded from this plan's test gates via --testPathIgnorePatterns=formik.test.js; 04-02 turns it green. The emailjs swap is NOT this plan (04-03 owns it) — formik.js here keeps the current emailjs-com import and hardcoded init so this plan's diff is purely the MUI removal.

Output: MUI-free contact form and top contacts rendering with plain SCSS; yarn install && yarn build && yarn test --testPathIgnorePatterns=formik.test.js green; zero @material-ui imports in src/; one commit per task.

Environment notes (apply to every verify step in this plan): node is NOT on PATH — prefix commands with `source ~/.nvm/nvm.sh && nvm use 24` (nvm has v20.20.2, v24.18.0, v24.19.0; the project enforces Node 24 via engines + engine-strict + scripts/check-node-version.js). /tmp is a tmpfs that fills with JIT artifacts — set `TMPDIR=/home/simos/tmp` for yarn/build commands. yarn 1.22 only, never npm.
</objective>

<execution_context>
@/home/simos/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/simos/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mui-removal-form-reliability/04-CONTEXT.md
@.planning/phases/04-mui-removal-form-reliability/04-RESEARCH.md
@.planning/phases/04-mui-removal-form-reliability/04-PATTERNS.md
@.planning/phases/04-mui-removal-form-reliability/04-UI-SPEC.md
@.planning/phases/04-mui-removal-form-reliability/04-VALIDATION.md
@package.json
@src/components/formik.js
@src/components/top-contacts.js
@src/components/navigation.js
@src/pages/thanks.js
@src/assets/scss/style.scss
@src/assets/scss/_theme-variables.scss
</context>

<tasks>

<task type="auto">
  <name>Rewrite formik.js with plain input/textarea/button per UI-SPEC (FORM-01, D-02/D-04/D-05)</name>
  <files>src/components/formik.js</files>
  <read_first>
    - src/components/formik.js (current MUI version: TextFieldConError lines 10-23, onSubmit lines 39-52, Form JSX lines 56-122)
    - .planning/phases/04-mui-removal-form-reliability/04-PATTERNS.md §formik.js (target import block, target TextFieldConError, field items to preserve, button + send-error)
    - .planning/phases/04-mui-removal-form-reliability/04-RESEARCH.md Pattern 1 (plain-element Formik field with preserved error UX), Example 1 (full target form)
    - .planning/phases/04-mui-removal-form-reliability/04-UI-SPEC.md Copywriting Contract (locked copy: "Invia", "Nome richiesto", "Email richiesta", placeholders, aria-labels) and Visual Delta Register (TARGET column)
    - .planning/phases/04-mui-removal-form-reliability/04-CONTEXT.md D-02 (plain elements, theme variables only), D-04 (no visual redesign), D-05 (Formik + yup exactly as-is)
  </read_first>
  <action>
    Rewrite src/components/formik.js in place:

    Imports: delete `import { TextField, Button } from "@material-ui/core"`. Keep `import React from "react"`, `import { Formik, Form, Field, useField } from "formik"`, `import * as yup from "yup"`, and `import emailjs from "emailjs-com"` with the module-scope `emailjs.init("user_06xz85hi92oABMZqCIUu7")` EXACTLY as they are — the emailjs swap and env-var move belong to 04-03, not this plan. Keep the `//https://dashboard.emailjs.com/admin` comment line.

    TextFieldConError: keep the useField + meta.error/meta.touched logic; replace the `Field as={TextField}` render with a plain `<input {...field} {...props} placeholder={placeholder} className={hasError ? "input error" : "input"} />` followed by a helper paragraph `<p className={hasError ? "helper error" : "helper"}>{helperText}</p>`, where `hasError = !!(meta.error && meta.touched)`. The class name `error` is locked (RESEARCH.md Pitfall 7) and must land on BOTH the input and the helper <p>. The static helperText props ("Nome richiesto"/"Email richiesta") render always-visible — this preserves the current observable behavior (the yup message never rendered verbatim because {...props} spread after helperText).

    Fields (same names, placeholders, aria-labels — test selectors depend on them):
    - nome: `<TextFieldConError type="text" name="nome" placeholder="Nome" helperText="Nome richiesto" aria-label="Nome" />` inside `<div className="item material">` (drop the MUI-only label prop)
    - email: `<TextFieldConError type="text" name="email" placeholder="Email" helperText="Email richiesta" aria-label="Email" />` inside `<div className="item">`
    - cellulare: `<Field aria-label="Cellulare" type="text" name="cellulare" placeholder="Cellulare" />` (plain Field, default as="input")
    - messaggio: `<Field as="textarea" rows="5" className="textarea" name="messaggio" aria-label="Scrivi qui il motivo per cui mi contatti" placeholder="Scrivi qui il motivo per cui mi contatti" />` (as="textarea" replaces multiline as={TextField})

    Button: replace `<Button type="submit" variant="contained" color="primary">Invia</Button>` with `<button type="submit" className="submit">Invia</button>` inside the existing `<div className="item text-align-right">`.

    Keep verbatim (D-13): the `<Form>` attributes (id="contact_form", data-netlify="true", className="contact_form", name="contact_form", form-name="contact_form", method="POST", action="/thanks", onSubmit={props.handleSubmit}), the honeypot `<p className="hidden">` block, the validationSchema, initialValues, and the current onSubmit body (emailjs.sendForm("service_q3997uk", "template_m6tzcmm", "#contact_form") with the unconditional resetForm/setSubmitting/assign — the false-success fix is 04-04's job, not this plan's). Delete the commented-out <pre> debug block.

    Prettier style: no semicolons, double quotes, arrowParens avoid, 2-space indent. Run `yarn format` on the file after editing.

    Commit atomically: `git commit -m "feat(04-01): rewrite contact form with plain elements (FORM-01, D-02)"` staging exactly src/components/formik.js.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && yarn test --testPathIgnorePatterns=formik.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q '@material-ui' src/components/formik.js` succeeds (zero matches)
    - `grep -q 'className={hasError ? "input error" : "input"}' src/components/formik.js` succeeds
    - `grep -q 'className={hasError ? "helper error" : "helper"}' src/components/formik.js` succeeds
    - `grep -q '<button type="submit" className="submit">' src/components/formik.js` succeeds
    - `grep -q 'as="textarea"' src/components/formik.js` succeeds
    - `grep -q 'data-netlify="true"' src/components/formik.js` succeeds (D-13 preserved)
    - `grep -q 'name="bot-field"' src/components/formik.js` succeeds (honeypot preserved)
    - `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn build && yarn test --testPathIgnorePatterns=formik.test.js` exits 0 (formik.test.js is EXPECTED RED until 04-02 updates the Mui-error assertion — excluded here)
    - `git log -1 --format=%s` contains `04-01`
  </acceptance_criteria>
  <done>The contact form renders with plain input/textarea/button elements, Formik + yup wiring untouched, all locked copy and attributes preserved, and the build + non-formik test loop is green.</done>
  <reversibility rating="reversible">git revert restores the MUI version of formik.js; the file is a pure component rewrite with no external state.</reversibility>
</task>

<task type="auto">
  <name>Swap top-contacts.js MUI icons for react-icons ri set (FORM-01, D-03)</name>
  <files>src/components/top-contacts.js</files>
  <read_first>
    - src/components/top-contacts.js (current: FacebookIcon/WhatsAppIcon from @material-ui/icons with width/height attributes)
    - src/components/navigation.js (line 3: `import { RiMenu3Line, RiCloseLine } from "react-icons/ri"` — the exact ri-set import style)
    - src/pages/thanks.js (lines 17-22: react-icons sizing via style={{ fontSize: "128px" }} — react-icons ignores width/height attributes)
    - .planning/phases/04-mui-removal-form-reliability/04-PATTERNS.md §top-contacts.js (import pattern, icon sizing pattern, structure to preserve)
    - .planning/phases/04-mui-removal-form-reliability/04-UI-SPEC.md Visual Delta Register (icons row: RiFacebookBoxLine/RiWhatsappLine, 24px, same color inheritance from .icons-top a)
  </read_first>
  <action>
    Rewrite src/components/top-contacts.js:

    Replace the two MUI imports with `import { RiWhatsappLine, RiFacebookBoxLine } from "react-icons/ri"` (D-03).

    Replace `<WhatsAppIcon width="24" height="24" />` with `<RiWhatsappLine style={{ fontSize: "24px" }} />` and `<FacebookIcon width="24" height="24" />` with `<RiFacebookBoxLine style={{ fontSize: "24px" }} />`. Do NOT set color inline — the .icons-top a rules (style.scss:54-64) own the color and hover.

    Keep verbatim: the `<div className="icons-top">` wrapper, both `<a>` hrefs (https://wa.me/393356785620 and https://www.facebook.com/larenlarylara), target="_blank" rel="noopener noreferrer" (D-04).

    Commit atomically: `git commit -m "feat(04-01): replace MUI icons with react-icons in top-contacts (FORM-01, D-03)"` staging exactly src/components/top-contacts.js.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && yarn test --testPathIgnorePatterns=formik.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q '@material-ui' src/components/top-contacts.js` succeeds (zero matches)
    - `grep -q 'import { RiWhatsappLine, RiFacebookBoxLine } from "react-icons/ri"' src/components/top-contacts.js` succeeds
    - `grep -q 'style={{ fontSize: "24px" }}' src/components/top-contacts.js` succeeds (both icons)
    - `grep -q 'https://wa.me/393356785620' src/components/top-contacts.js` succeeds (href preserved)
    - `grep -q 'https://www.facebook.com/larenlarylara' src/components/top-contacts.js` succeeds (href preserved)
    - `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn build && yarn test --testPathIgnorePatterns=formik.test.js` exits 0
    - `git log -1 --format=%s` contains `04-01`
  </acceptance_criteria>
  <done>Top contacts render react-icons ri icons at 24px with inherited color; no MUI import remains; build + non-formik test loop green.</done>
  <reversibility rating="reversible">git revert restores the MUI icon imports; react-icons is already a dependency so no lockfile change is involved.</reversibility>
</task>

<task type="auto">
  <name>Remove @material-ui/core + @material-ui/icons from package.json (FORM-01, D-01)</name>
  <files>package.json, yarn.lock</files>
  <read_first>
    - package.json (dependencies lines 27-28: "@material-ui/core": "^4.12.4", "@material-ui/icons": "^4.11.3")
    - .planning/phases/04-mui-removal-form-reliability/04-CONTEXT.md D-01 (remove both packages; only consumers are formik.js and top-contacts.js — BOTH already rewritten by tasks 1-2, so no dangling imports remain)
    - .planning/phases/04-mui-removal-form-reliability/04-PATTERNS.md §package.json (deps block edit pattern; Phase 3 node-sass → sass precedent)
    - AGENTS.md (yarn 1.22 only, Node 24 enforced, postinstall node-sass guard)
  </read_first>
  <action>
    Remove the two MUI packages with yarn only (never npm): `yarn remove @material-ui/core @material-ui/icons`. This updates package.json dependencies and regenerates yarn.lock in one step.

    Do NOT touch: emailjs-com (04-03 owns the @emailjs/browser swap), formik, yup, react-icons, sass, any other dependency, scripts, engines.

    Commit atomically: `git commit -m "feat(04-01): remove @material-ui/core and @material-ui/icons (FORM-01, D-01)"` staging exactly package.json and yarn.lock.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn install && ! grep -q '"@material-ui' package.json && ! grep -q '@material-ui' yarn.lock && yarn build && yarn test --testPathIgnorePatterns=formik.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q '"@material-ui' package.json` succeeds (zero matches)
    - `! grep -q '@material-ui' yarn.lock` succeeds (zero matches)
    - `grep -rn "@material-ui" src/` returns zero matches (no dangling imports — tasks 1-2 rewrote both consumers)
    - `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn install && yarn build && yarn test --testPathIgnorePatterns=formik.test.js` exits 0
    - `git log -1 --format=%s` contains `04-01`
  </acceptance_criteria>
  <done>MUI core + icons are gone from the dependency tree; yarn.lock no longer resolves them; install + build + non-formik test loop green under Node 24.</done>
  <reversibility rating="reversible">git revert restores both deps and the lockfile; `yarn install` re-resolves the MUI tree.</reversibility>
</task>

<task type="auto">
  <name>Extend .contact_form SCSS with input/textarea/button/error styles (FORM-01, UI-SPEC TARGET)</name>
  <files>src/assets/scss/style.scss</files>
  <read_first>
    - src/assets/scss/style.scss (lines 434-464: current .contact_form block — margin-top, label, .item, .item.material, .hidden, nested .textarea textarea)
    - src/assets/scss/style.scss (lines 341-360: .button-pink pattern — the submit button's design source)
    - src/assets/scss/_theme-variables.scss (lines 7-31: --primary-color #ff1c65, --button-alternate-color #ff0060, --button-color #ffffff, --label-text rgba(0,0,0,0.5), --input-focus-border #83aaac, --font-family)
    - .planning/phases/04-mui-removal-form-reliability/04-UI-SPEC.md Visual Delta Register (TARGET column: button bg --button-alternate-color, 12px radius, box-border inputs, focus --input-focus-border, error --primary-color) and Typography (input 16px/1.5, helper 12px/1.5, button 14px/500/1 + uppercase + letter-spacing 0.03em)
    - .planning/phases/04-mui-removal-form-reliability/04-RESEARCH.md Example 4 (target SCSS block) and Pitfall 6 (nested .textarea textarea selector must be flattened)
  </read_first>
  <action>
    Extend the existing .contact_form block in src/assets/scss/style.scss (lines 434-464) in place. Preserve the existing rules (margin-top: 36px, label color, .item padding-bottom: 16px, .item.material padding: 0, .hidden display: none). Delete the commented-out MUI label rule (lines 437-439).

    Flatten the textarea rule (Pitfall 6): replace the nested `.textarea { width: 100%; textarea { max-width: 100%; width: 100%; } }` with a direct `textarea { width: 100%; max-width: 100%; min-height: 120px; }` rule (the plain <textarea className="textarea"> IS the textarea — the nested selector would silently stop matching).

    Add these rules inside .contact_form, using ONLY the existing theme variables (D-02, no new colors):
    - `.input`: width 100%, padding 8px, border 1px solid rgba(0,0,0,0.1), border-radius 4px, font-family var(--font-family), font-size 16px, line-height 1.5; &:focus { outline: none; border-color: var(--input-focus-border); }; &.error { border-color: var(--primary-color); }
    - `textarea`: width 100%, max-width 100%, min-height 120px, padding 8px, border 1px solid rgba(0,0,0,0.1), border-radius 4px, font-family var(--font-family), font-size 16px, line-height 1.5; &:focus { outline: none; border-color: var(--input-focus-border); }
    - `.helper`: margin 4px 0 0, font-size 12px, line-height 1.5, color var(--label-text); &.error { color: var(--primary-color); }
    - `.submit`: padding 8px 24px, background-color var(--button-alternate-color), color var(--button-color), border 1px solid var(--button-alternate-color), border-radius 12px, font-family var(--font-family), font-size 14px, font-weight 500, line-height 1, text-transform uppercase, letter-spacing 0.03em, cursor pointer, transition background 0.3s linear; &:hover { background-color: var(--button-color); color: var(--button-alternate-color); }; &:disabled { opacity: 0.7; cursor: default; }
    - `.send-error`: margin-top 16px, color var(--primary-color)

    These implement the UI-SPEC Visual Delta Register TARGET column exactly: button background #ff0060 (--button-alternate-color), 12px radius no shadow, hover inversion per .button-pink, box-border inputs with --input-focus-border focus, error color #ff1c65 (--primary-color), helper color --label-text, disabled state opacity 0.7.

    SCSS conventions: nested selectors, kebab-case classes, var(--x) custom properties. Run `yarn format` on the file after editing.

    Commit atomically: `git commit -m "feat(04-01): extend .contact_form SCSS for plain form elements (FORM-01, UI-SPEC)"` staging exactly src/assets/scss/style.scss.
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && nvm use 24 >/dev/null && TMPDIR=/home/simos/tmp yarn build && grep -q "button-alternate-color" public/*.css && yarn test --testPathIgnorePatterns=formik.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '\.input' src/assets/scss/style.scss` succeeds
    - `grep -q '\.helper' src/assets/scss/style.scss` succeeds
    - `grep -q '\.submit' src/assets/scss/style.scss` succeeds
    - `grep -q '\.send-error' src/assets/scss/style.scss` succeeds
    - `grep -q 'var(--button-alternate-color)' src/assets/scss/style.scss` succeeds (button bg from theme)
    - `grep -q 'var(--input-focus-border)' src/assets/scss/style.scss` succeeds (focus border from theme)
    - `grep -q 'var(--primary-color)' src/assets/scss/style.scss` succeeds (error states from theme)
    - `! grep -q '\.textarea textarea' src/assets/scss/style.scss` succeeds (nested selector flattened, Pitfall 6)
    - `! grep -q 'MuiInput' src/assets/scss/style.scss` succeeds (commented MUI rule deleted)
    - `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn build && yarn test --testPathIgnorePatterns=formik.test.js` exits 0
    - `git log -1 --format=%s` contains `04-01`
  </acceptance_criteria>
  <done>The .contact_form block styles the plain form per the UI-SPEC TARGET column using only existing theme variables; the textarea width trap is closed; build + non-formik test loop green.</done>
  <reversibility rating="reversible">git revert restores the old .contact_form block; pure SCSS text edit with no runtime state.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| npm registry → yarn install | @material-ui packages leave the tree here; yarn.lock freezes the post-removal resolution |
| SCSS source → dart-sass compile → public/*.css | the new .input/.helper/.submit/.send-error rules cross here; a compile error breaks the build |
| browser → rendered form | the plain elements cross here; a missing class or flattened selector means silent visual regression (Pitfall 6) |
| browser → react-icons bundle | RiWhatsappLine/RiFacebookBoxLine cross here; a wrong import name breaks the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | DoS | build failure from a dangling @material-ui import after dependency removal | high | block | Full install+build+test gate after every task; `grep -rn "@material-ui" src/` must return zero before the plan is done |
| T-04-02 | Tampering | silent visual regression — nested .textarea textarea selector stops matching the plain textarea (Pitfall 6) | medium | mitigate | Flatten the rule in the same commit as the form rewrite; acceptance grep asserts the nested selector is gone |
| T-04-03 | Tampering | error class lands on the input but not the helper <p> — validation signal lost (Pitfall 7) | medium | mitigate | Class name `error` locked on BOTH elements; 04-02's test asserts on the helper text element |
| T-04-04 | Info disclosure | new colors introduced outside the theme variables (D-02 violation) | low | mitigate | Acceptance greps assert only var(--primary-color)/var(--button-alternate-color)/var(--button-color)/var(--label-text)/var(--input-focus-border)/var(--font-family) are used |
| T-04-SC | Tampering | supply-chain risk from dependency changes | low | mitigate | Only removals in this plan (no new packages); yarn.lock committed; react-icons already installed and audited in RESEARCH.md |
</threat_model>

<verification>
- `source ~/.nvm/nvm.sh && nvm use 24 && TMPDIR=/home/simos/tmp yarn install && yarn build && yarn test --testPathIgnorePatterns=formik.test.js` exits 0 (formik.test.js excluded — expected red until 04-02)
- `grep -rn "@material-ui" src/` returns zero matches (FORM-01 proof)
- `! grep -q '"@material-ui' package.json` and `! grep -q '@material-ui' yarn.lock` succeed
- formik.js renders plain input/textarea/button with locked copy and attributes (data-netlify, honeypot, aria-labels)
- top-contacts.js imports RiWhatsappLine/RiFacebookBoxLine from react-icons/ri at 24px
- style.scss .contact_form implements the UI-SPEC TARGET column with theme variables only; nested textarea selector flattened
- `git log` shows the four 04-01 commits
</verification>

<success_criteria>
- The contact form and top contacts render without MUI — plain elements styled with existing SCSS theme variables (ROADMAP Phase 4 criterion 1)
- The form keeps its fields, placeholders, layout, and "Invia" button — no visual redesign (D-04)
- Formik + yup wiring is untouched — the validation UX rewrite is 04-02's test update, not this plan (D-05)
- The full install → build → non-formik test loop is green under Node 24, proving the MUI-free pipeline for waves 2
</success_criteria>

## Artifacts this phase produces

- `package.json` — @material-ui/core and @material-ui/icons removed from dependencies
- `yarn.lock` — regenerated (MUI tree gone)
- `src/components/formik.js` — plain input/textarea/button rewrite (TextFieldConError → useField + <input> + helper <p>; Field as="textarea"; <button className="submit">Invia</button>)
- `src/components/top-contacts.js` — react-icons ri swap (RiWhatsappLine/RiFacebookBoxLine, 24px)
- `src/assets/scss/style.scss` — .contact_form extended with .input/.helper/.submit/.send-error per UI-SPEC TARGET
- Git commits `feat(04-01): remove @material-ui/core and @material-ui/icons (FORM-01, D-01)`, `feat(04-01): rewrite contact form with plain elements (FORM-01, D-02)`, `feat(04-01): replace MUI icons with react-icons in top-contacts (FORM-01, D-03)`, and `feat(04-01): extend .contact_form SCSS for plain form elements (FORM-01, UI-SPEC)`
- `.planning/phases/04-mui-removal-form-reliability/04-01-SUMMARY.md` (created at plan completion)

<output>
Create `.planning/phases/04-mui-removal-form-reliability/04-01-SUMMARY.md` when done
</output>
